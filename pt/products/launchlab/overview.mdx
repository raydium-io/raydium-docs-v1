---
title: "Visão geral do LaunchLab"
description: "Como funciona o mecanismo de lançamento com curva de ligação do Raydium de ponta a ponta: negociação pré-graduação contra uma curva determinística, o limite de financiamento que dispara a graduação e a migração automática para um pool CPMM padrão."
---

<Info>
  **Esta página foi traduzida automaticamente por IA. A versão em inglês é a fonte oficial.**

  [Ver versão em inglês →](/products/launchlab/overview)
</Info>

## O problema que o LaunchLab resolve

Antes do LaunchLab, lançar um novo token no Raydium exigia que o criador alimentasse um pool AMM com ambos os lados do par antecipadamente — ou seja, a equipe tinha que fornecer a liquidez do lado da cotação (SOL ou USDC) do próprio bolso. Isso favorecia projetos bem financiados e restringia o acesso ao mecanismo de lançamento ao capital inicial.

O LaunchLab substitui essa etapa inicial por uma **curva de ligação**: o token é implantado contra uma curva precificada em um mint de cotação (tipicamente SOL ou USDC). Os compradores adquirem o token enviando cotação para a curva, que atomicamente cunha ou libera unidades do token base a um preço determinado pela fórmula da curva e pela oferta atual. Nenhuma liquidez pré-alimentada necessária.

Quando a curva coleta cotação suficiente para corresponder à fórmula de liquidez de um pool AMM real, ela **se gradua**: o programa cria um pool CPMM na mainnet alimentado com a reserva base e a reserva de cotação da curva, e a partir desse ponto a negociação passa para o AMM.

## Ciclo de vida

```
             ┌───────────────────────┐
creator  ──▶ │  Initialize (Launch)  │   ▸ mints the base token, funds the curve vaults,
             │                        │     sets curve params and graduation threshold
             └───────────┬────────────┘
                         │
                         ▼
             ┌───────────────────────┐       repeated N times
             │   Buy  /  Sell        │ ◀─── traders interact with the curve
             └───────────┬────────────┘
                         │ curve quote_reserve
                         │ crosses threshold
                         ▼
             ┌───────────────────────┐   ▸ snapshots curve state
             │      Graduate         │   ▸ deploys CPMM pool seeded with curve reserves
             │                        │   ▸ locks / burns remaining base supply per policy
             └───────────┬────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │   CPMM pool live      │   ▸ standard CPMM behavior going forward
             └───────────────────────┘
```

Cada lançamento passa por essa sequência exatamente uma vez. `Buy` e `Sell` são as instruções voltadas para o usuário na fase intermediária. Após o limite ser ultrapassado, a carteira de migração armazenada em `GlobalConfig` assina `MigrateToC pswap`.

## Duas partes fixas

Um estado do LaunchLab tem duas contas distinguidas:

- **O mint base** — o token sendo lançado. Sua autoridade de cunhagem é mantida pelo programa LaunchLab até a graduação; pós-graduação, ela é revogada.
- **O mint de cotação** — a garantia. Sempre um mint mainstream (SOL / USDC / RAY). A configuração de lançamento escolhe um em `Initialize`; não pode ser alterado.

Mais dois cofres:

- **`base_vault`** — mantém a porção da oferta base que foi pré-cunhada para a curva mas ainda não foi vendida. Diminui conforme os usuários compram.
- **`quote_vault`** — acumula a cotação paga pelos compradores. Aumenta conforme os usuários compram. Este é o saldo que é verificado contra o limite de graduação.

## Modelo de precificação

O LaunchLab suporta múltiplas fórmulas de curva (veja [`bonding-curve`](/pt/products/launchlab/bonding-curve)). A mais comum é uma **curva de ligação quadrática** análoga à linhagem Pump.fun / Curve.fi / Bancor:

```
price(s) = k × (s / S_max)²     (or similar — exact formula is curve-config dependent)
```

onde `s` é a quantidade de base já vendida aos usuários e `S_max` é a oferta máxima da curva. O preço sobe monotonicamente a cada compra e cai a cada venda. Como o programa calcula o custo integrado ao AMM exatamente, uma compra de qualquer tamanho retorna a quantidade integrada correta; não há slippage por negociação além da convexidade natural da curva.

## Graduação

Um lançamento se gradua quando `quote_vault.balance ≥ graduation_threshold`. O limite é definido em `Initialize` e é tipicamente escolhido de forma que na graduação o preço implícito da curva corresponda ao preço que o pool AMM abrirá com as reservas coletadas. Concretamente:

```
threshold ≈ S_graduate × price(S_graduate) × f
```

onde `S_graduate` é a quantidade base já vendida, `price(S_graduate)` é o preço marginal da curva naquele ponto, e `f` é um pequeno fator para contabilizar a linha de taxa (1–2%).

Na graduação:

1. O programa captura `(base_vault_remaining, quote_vault)`.
2. Ele chama CPMM `CreatePool` CPI com essas duas reservas, cunhando o LP inicial para uma autoridade de propriedade do programa (geralmente queimada / bloqueada por política).
3. Ele revoga a autoridade de cunhagem do mint base (para que nenhum token base adicional possa ser cunhado).
4. `LaunchState.status` muda para `Graduated`.

Pós-graduação, `Buy` e `Sell` são rejeitados. A negociação continua no pool CPMM resultante, que é indistinguível de qualquer outro pool CPMM do Raydium.

## Taxas

Durante a fase da curva, cada `Buy` e `Sell` incorre em uma taxa dividida entre:

- **Lado LP da curva** — aumenta o `k` implícito da curva, o que beneficia compradores posteriores (preço mais apertado).
- **Protocolo** — acumula para o administrador do LaunchLab, coletado via `CollectFees`.
- **Criador** — opcional, configurável em `Initialize`. Alguns lançamentos direcionam uma parte para o criador como um fluxo de receita contínuo.

As taxas padrão são documentadas em [`bonding-curve`](/pt/products/launchlab/bonding-curve). A divisão exata é armazenada em `LaunchState.fees` e pode diferir por lançamento.

As taxas pós-graduação seguem a configuração CPMM com a qual o pool foi criado (tipicamente `AmmConfig[0]`, o nível de 0,25%).

## Quem mantém o LP após a graduação?

O LaunchLab usa as três escalas armazenadas da plataforma para produzir dois destinos efetivos de LP:

- **Bloqueio de plataforma** — para migrações executadas após a atualização de 2026-08-17, `platform_scale + creator_scale` é bloqueado através do LP-Lock e uma Fee Key vai para `platform_nft_wallet` quando essa participação é diferente de zero. Antes da atualização, `creator_scale` produzia uma Fee Key de criador separada.
- **Queima** — `burn_scale` é queimado permanentemente e não tem Fee Key.

As escalas vivem em `PlatformConfig` e devem somar `1_000_000`. A atualização altera apenas a execução de migração futura. Ela não altera Fee Keys de criador criadas por migrações anteriores.

## Invariantes importantes

- **O mint base é livre de inflação após a graduação.** Sua `mint_authority` é revogada; `freeze_authority` nunca foi definida.
- **Todos os novos lançamentos são apenas CPMM.** `Initialize`, `InitializeV2` e `InitializeWithToken2022` exigem `migrate_type = CPSWAP`. O estado legado existente ainda pode usar `MigrateToAmm`.
- **O estado da curva é monotônico em uma direção.** `base_sold` apenas sobe durante Buy, `quote_vault` apenas sobe durante Buy (cai durante Sell — o que simetricamente reduz `base_sold`). O programa nunca deixa a curva ficar negativa.
- **A graduação é uma porta de mão única.** Mesmo que a negociação pós-graduação empurre o preço do pool AMM de volta abaixo do preço de graduação, o lançamento não reverte para a curva.

## Quando usar o LaunchLab

<Note>
  Esta página descreve apenas a mecânica do protocolo. Nada aqui constitui aconselhamento financeiro, legal ou de investimento. Lançamentos de tokens carregam risco financeiro significativo. Consulte profissionais apropriados antes de lançar um token que envolva arrecadação pública de fundos.
</Note>

- Você está lançando um novo token sem mercado anterior.
- Você quer que o mercado determine o preço CPMM de abertura em vez de pré-declarar.
- Você quer permitir que qualquer pessoa — incluindo a própria equipe — compre aos mesmos preços determinados pela curva, em vez de pré-alocar para insiders com desconto.

**Não** use o LaunchLab para:

- Tokens existentes com mercados estabelecidos (use `CreatePool` no CPMM diretamente).
- Lançamentos onde você precisa de controle preciso sobre o preço AMM de abertura (você pode aproximar com configuração cuidadosa da curva, mas o mecanismo ainda é orientado pela curva).
- Tokens que exigem extensões Token-2022 que o LaunchLab não permite (o programa de lançamento rejeita extensões como `TransferHook` e `PermanentDelegate` mesmo no caminho Token-2022).

## Conteúdo do capítulo

- [`bonding-curve`](/pt/products/launchlab/bonding-curve) — a fórmula da curva, matemática de custo e receitas, derivação do limite de graduação.
- [`accounts`](/pt/products/launchlab/accounts) — `LaunchConfig`, `LaunchState`, cofres, PDAs de autoridade.
- [`instructions`](/pt/products/launchlab/instructions) — `Initialize`, `Buy`, `Sell`, `Graduate`, `CollectFees`, `SetParams`.
- [`code-demos`](/pt/products/launchlab/code-demos) — exemplos TypeScript de ponta a ponta.

## Para onde ir a seguir

- [`products/cpmm/overview`](/pt/products/cpmm/overview) — o que acontece após um lançamento se graduar.
- [`user-flows/create-cpmm-pool`](/pt/user-flows/create-cpmm-pool) — caminho alternativo para tokens que não precisam de uma curva.
- [`reference/program-addresses`](/pt/reference/program-addresses) — ID do programa LaunchLab.

Fontes:

- [Módulo `LaunchLab` do Raydium SDK v2](https://github.com/raydium-io/raydium-sdk-V2) (IDL sob `src/raydium/launchpad/`).
- O código-fonte do programa LaunchLab não é atualmente publicado como um repositório independente. O IDL agrupado com o SDK acima é a interface canônica.
