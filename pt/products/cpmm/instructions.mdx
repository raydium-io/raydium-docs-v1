---
title: "Instruções CPMM"
description: "Todas as instruções do programa CPMM, com seus argumentos, lista de contas, pré/pós-condições e os erros que podem retornar."
---

<Info>
  **Esta página foi traduzida automaticamente por IA. A versão em inglês é a fonte oficial.**

  [Ver versão em inglês →](/products/cpmm/instructions)
</Info>

## Resumo de instruções

| Nome do discriminador | Quem assina | O que faz |
|-------|-----------|--------------|
| `Initialize` | criador do pool | Cria um novo pool CPMM a partir de dois mints e uma `AmmConfig`. Sem permissão; qualquer um pode chamar. Codifica `enable_creator_fee = false` no novo pool. A conta `pool_state` pode ser tanto o PDA canônico quanto uma chave aleatória nova (veja contas de Initialize). |
| `InitializeWithPermission` | pagador + detentor de um PDA `Permission` | Variante com permissão de `Initialize`. O chamador (`payer`) deve possuir um PDA `Permission` derivado de sua própria chave pública. Usado por plataformas que precisam de criação de pool controlada (ex: graduações do LaunchLab). Permite que o chamador fixe `creator_fee_on` (`BothToken` / `OnlyToken0` / `OnlyToken1`) e força `enable_creator_fee = true` no novo pool. O campo `creator` em `pool_state` é definido para uma conta `creator` passada separadamente, não o pagador. Mesma flexibilidade PDA-canônico-ou-chave-aleatória para `pool_state` como em `Initialize`. |
| `Deposit` | LP | Adiciona liquidez em ambos os tokens; recebe tokens LP. |
| `Withdraw` | LP | Queima tokens LP; recebe ambos os tokens subjacentes pro-rata. |
| `SwapBaseInput` | trocador | Swap de entrada exata (`amount_in` entra, ≥ `minimum_amount_out` sai). |
| `SwapBaseOutput` | trocador | Swap de saída exata (≤ `maximum_amount_in` entra, `amount_out` sai). |
| `CollectProtocolFee` | `protocol_owner` (de `AmmConfig`) | Coleta taxas de protocolo acumuladas dos vaults. |
| `CollectFundFee` | `fund_owner` (de `AmmConfig`) | Coleta taxas de fundo acumuladas dos vaults. |
| `CollectCreatorFee` | `pool_creator` | Coleta taxas de criador acumuladas (se a taxa de criador foi habilitada). |
| `CollectCreatorFeePermissionless` | qualquer pagador | Coleta todas as taxas de criador acumuladas para as contas de token canônicas do `pool_creator`. O pagador pode criar ATAs de destinatário ausentes, mas não pode redirecionar a transferência. |
| `UpdatePoolStatus` | `admin` | Pausa / retoma operações específicas em um pool via máscara de bits. |
| `UpdateAmmConfig` | `admin` | Altera taxas ou o proprietário de protocolo/fundo em uma `AmmConfig`. |
| `CreateAmmConfig` | `admin` | Cria um novo nível de taxa (nova conta `AmmConfig`). |
| `CreatePermissionPda` | `admin` ou criador de PDA de permissão delegado | Cria um PDA `Permission` que permite que uma autoridade específica chame `InitializeWithPermission`. |
| `ClosePermissionPda` | `admin` | Revoga um PDA `Permission` emitido anteriormente. |

Máscara de status: o `status` de cada pool é um `u8` onde bit 0 = depósito desabilitado, bit 1 = saque desabilitado, bit 2 = swap desabilitado (`PoolStatusBitIndex { Deposit, Withdraw, Swap }` no programa). Um bit limpo significa que a operação é permitida; um bit definido significa que está pausada. `UpdatePoolStatus` recebe um `u8` bruto e sobrescreve o valor existente.

As próximas seções detalham cada uma. A ordem das contas segue o IDL do CPMM; o SDK e o cliente Rust em `raydium-cp-swap/programs/cp-swap/src/instructions` correspondem a esta ordem.

## `Initialize`

Cria um novo pool CPMM.

**Argumentos**

```
init_amount_0: u64
init_amount_1: u64
open_time:     u64   // Unix timestamp; swaps rejeitados antes disso
```

**Contas** (W = gravável, S = assinante)

| # | Nome | W | S | Notas |
|---|------|---|---|-------|
| 1 | `creator` | W | S | Paga aluguel; registrado como `pool_state.pool_creator`. |
| 2 | `amm_config` |   |   | O nível de taxa escolhido. |
| 3 | `authority` |   |   | PDA de autoridade global do CPMM. |
| 4 | `pool_state` | W | S* | `init`ializado aqui. **Ou** o PDA canônico `["pool", amm_config, token_0_mint, token_1_mint]` **ou** uma chave aleatória nova — quando não é o PDA canônico, o programa requer que `pool_state` assine (`require_eq!(pool_account_info.is_signer, true)`). O caminho de chave aleatória permite que um criador evite tentativas de front-running no PDA canônico. Os PDAs a jusante (`lp_mint`, `vaults`, `observation_state`) são derivados de `pool_state.key()` de qualquer forma. |
| 5 | `token_0_mint` |   |   | Ordenado: `token_0_mint < token_1_mint`. |
| 6 | `token_1_mint` |   |   | |
| 7 | `lp_mint` | W |   | `init`ializado aqui. Autoridade definida como `authority`. |
| 8 | `creator_token_0` | W |   | ATA de origem para `init_amount_0`. |
| 9 | `creator_token_1` | W |   | ATA de origem para `init_amount_1`. |
| 10 | `creator_lp_token` | W |   | Destino para LP (criado se ausente). |
| 11 | `token_0_vault` | W |   | `init`ializado aqui. Possuído por `authority`. |
| 12 | `token_1_vault` | W |   | |
| 13 | `create_pool_fee` | W |   | ATA de destino para a `create_pool_fee` paga pelo criador. |
| 14 | `observation_state` | W |   | `init`ializado aqui. |
| 15 | `token_program` |   |   | SPL Token (para LP mint). |
| 16 | `token_0_program` |   |   | SPL Token ou Token-2022. |
| 17 | `token_1_program` |   |   | SPL Token ou Token-2022. |
| 18 | `associated_token_program` |   |   | |
| 19 | `system_program` |   |   | |
| 20 | `rent` |   |   | |

`*` `pool_state` assina apenas no caminho de chave aleatória; o caminho PDA canônico executa sem `pool_state` assinando.

**Pré-condições**

- Mints são ordenados (`token_0_mint < token_1_mint` por ordem de bytes).
- Nenhum mint usa uma extensão fora da lista de permissões do CPMM (`TransferFeeConfig`, `MetadataPointer`, `TokenMetadata`, `InterestBearingConfig`, `ScaledUiAmount`) — veja [`products/cpmm/accounts`](/pt/products/cpmm/accounts#vaults-and-token-2022). Uma pequena lista de permissões por mint dentro do programa contorna a verificação para integração caso a caso.
- `creator` tem pelo menos `init_amount_0` e `init_amount_1` nos respectivos ATAs.
- `amm_config.disable_create_pool == false`.

**Pós-condições**

- `pool_state` existe com `lp_supply = sqrt(init_amount_0 * init_amount_1) − LOCKED_LP`.
- O iniciador de LP de `LOCKED_LP` (`100` lamports de token LP) é permanentemente bloqueado no pool — `pool_state.lp_supply` registra `liquidity − 100` enquanto `100` unidades de LP permanecem fora de circulação, impedindo que o pool seja completamente drenado e evitando divisão por zero.
- `observation_state` é inicializado; `observation_index = 0` e `pool_id = pool_state.key()`.
- `create_pool_fee` lamports são transferidos do criador para o receptor e sincronizados como SOL nativo (é um ATA wSOL).
- A máscara de status do pool é `0` (depósito / saque / swap todos habilitados).
- `enable_creator_fee = false` e `creator_fee_on = BothToken`. `Initialize` **não** suporta habilitar a taxa de criador — esse caminho é `InitializeWithPermission`.
- `open_time` é aumentado para `block_timestamp + 1` se o chamador passou um valor `<= block_timestamp`. Swaps são rejeitados antes de `open_time`; depósitos e saques funcionam imediatamente.

**Erros comuns** (lista completa em [`reference/error-codes`](/pt/reference/error-codes))

- `InvalidInput` — mints desordenados ou mints idênticos.
- `NotSupportMint` — extensão Token-2022 bloqueada.
- `ExceededSlippage` — raramente; se `init_amount_0/1` resultarem em zero LP devido a incompatibilidade de decimais.

## `Deposit`

Adiciona liquidez em ambos os tokens proporcionalmente ao pool.

**Argumentos**

```
lp_token_amount:   u64   // quantos tokens LP cunhar para o LP
maximum_token_0:   u64
maximum_token_1:   u64
```

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `owner` |   | S |
| 2 | `authority` |   |   |
| 3 | `pool_state` | W |   |
| 4 | `owner_lp_token` | W |   |
| 5 | `token_0_account` | W |   |
| 6 | `token_1_account` | W |   |
| 7 | `token_0_vault` | W |   |
| 8 | `token_1_vault` | W |   |
| 9 | `token_program` |   |   |
| 10 | `token_program_2022` |   |   |
| 11 | `vault_0_mint` |   |   |
| 12 | `vault_1_mint` |   |   |
| 13 | `lp_mint` | W |   |

**Matemática**

```
needed_token_0 = ceil(lp_token_amount * vault_0 / lp_supply)
needed_token_1 = ceil(lp_token_amount * vault_1 / lp_supply)
require(needed_token_0 <= maximum_token_0, "ExceededSlippage")
require(needed_token_1 <= maximum_token_1, "ExceededSlippage")
```

Sem mudança na proporcionalidade de `k` — ambos os vaults e `lp_supply` escalam pelo mesmo fator.

**Pós-condições**

- `lp_supply += lp_token_amount`.
- `vault_0 += needed_token_0` (líquido de qualquer taxa de transferência Token-2022 na entrada).
- `vault_1 += needed_token_1` (líquido de qualquer taxa de transferência Token-2022 na entrada).

**Erros comuns** — `ExceededSlippage`, `ZeroTradingTokens`, `InvalidStatus` se depósito está pausado.

## `Withdraw`

Queima tokens LP e recebe ambos os tokens subjacentes pro-rata.

**Argumentos**

```
lp_token_amount:   u64
minimum_token_0:   u64
minimum_token_1:   u64
```

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `owner` |   | S |
| 2 | `authority` |   |   |
| 3 | `pool_state` | W |   |
| 4 | `owner_lp_token` | W |   |
| 5 | `token_0_account` | W |   |
| 6 | `token_1_account` | W |   |
| 7 | `token_0_vault` | W |   |
| 8 | `token_1_vault` | W |   |
| 9 | `token_program` |   |   |
| 10 | `token_program_2022` |   |   |
| 11 | `vault_0_mint` |   |   |
| 12 | `vault_1_mint` |   |   |
| 13 | `lp_mint` | W |   |

(Idêntico a `Deposit`; `lp_mint` é gravável porque os tokens LP são queimados.)

**Matemática**

```
out_token_0 = floor(lp_token_amount * vault_0 / lp_supply)
out_token_1 = floor(lp_token_amount * vault_1 / lp_supply)
require(out_token_0 >= minimum_token_0, "ExceededSlippage")
require(out_token_1 >= minimum_token_1, "ExceededSlippage")
```

**Pós-condições**

- `lp_supply -= lp_token_amount`.
- Os vaults enviam `out_token_0` / `out_token_1` (bruto; o usuário recebe líquido de qualquer taxa de transferência Token-2022).

## `SwapBaseInput`

Swap de entrada exata.

**Argumentos**

```
amount_in:            u64
minimum_amount_out:   u64
```

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `payer` |   | S |
| 2 | `authority` |   |   |
| 3 | `amm_config` |   |   |
| 4 | `pool_state` | W |   |
| 5 | `input_token_account` | W |   |
| 6 | `output_token_account` | W |   |
| 7 | `input_vault` | W |   |
| 8 | `output_vault` | W |   |
| 9 | `input_token_program` |   |   |
| 10 | `output_token_program` |   |   |
| 11 | `input_token_mint` |   |   |
| 12 | `output_token_mint` |   |   |
| 13 | `observation_state` | W |   |

A ordem **entrada → saída** é pela direção do usuário, não pelo `token_0 / token_1` canônico do pool. O programa descobre qual vault é qual combinando mints.

**Matemática** — veja [`products/cpmm/math`](/pt/products/cpmm/math).

**Pré-condições**

- `open_time <= now`.
- `pool_status` permite swap.
- Nenhum mint pausado ou congelado para esta autoridade.
- `amount_in > 0`.

**Erros comuns**

- `ExceededSlippage` — `amount_out < minimum_amount_out`.
- `ZeroTradingTokens` — a negociação arredonda para zero.
- `NotApproved` — pool está pausado para swaps via `UpdatePoolStatus`.
- `InvalidInput` — mints não correspondem a nenhum dos mints do vault do pool.

## `SwapBaseOutput`

Swap de saída exata.

**Argumentos**

```
max_amount_in:  u64
amount_out:     u64
```

**Contas** — mesmas que `SwapBaseInput`.

**Matemática** — curva inversa com teto, veja [`products/cpmm/math`](/pt/products/cpmm/math).

**Erros comuns** — `ExceededSlippage` (`gross_in > max_amount_in`), `ZeroTradingTokens`, `InvalidInput`, `NotApproved`.

## `CollectProtocolFee`

Coleta taxas de protocolo acumuladas dos vaults para o destino do protocolo.

**Argumentos** — nenhum.

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `owner` |   | S | Deve corresponder a `amm_config.protocol_owner`. |
| 2 | `authority` |   |   |
| 3 | `pool_state` | W |   |
| 4 | `amm_config` |   |   |
| 5 | `token_0_vault` | W |   |
| 6 | `token_1_vault` | W |   |
| 7 | `vault_0_mint` |   |   |
| 8 | `vault_1_mint` |   |   |
| 9 | `recipient_token_0_account` | W |   |
| 10 | `recipient_token_1_account` | W |   |
| 11 | `token_program` |   |   |
| 12 | `token_program_2022` |   |   |

**Efeito**

```
transfer pool_state.protocol_fees_token0 from vault_0 to recipient_0
transfer pool_state.protocol_fees_token1 from vault_1 to recipient_1
pool_state.protocol_fees_token0 = 0
pool_state.protocol_fees_token1 = 0
```

Sem mudança nos saldos efetivos da curva (taxas acumuladas já foram excluídas).

**Erro comum** — `NotApproved` se o assinante não for `protocol_owner`.

## `CollectFundFee`

Mesma forma que `CollectProtocolFee` mas assinado por `fund_owner` e zerando os contadores `fund_fees_*`.

## `CollectCreatorFee`

Assinado por `pool_state.pool_creator`. Transfere os saldos completos de `creator_fees_token_0` e `creator_fees_token_1` para as contas de token do criador, depois zera ambos os contadores. Retorna `NoFeeCollect` quando ambos os contadores são zero.

## `CollectCreatorFeePermissionless`

Qualquer um pode disparar a coleta de taxa de criador. A instrução sempre envia os saldos acumulados completos para as contas de token associadas canônicas possuídas por `pool_state.pool_creator`; o chamador não pode escolher outro criador ou destino. Se qualquer ATA estiver ausente, o pagador financia sua criação.

O `CollectCreatorFee` original permanece chamável, então clientes existentes permanecem compatíveis.

**Argumentos** — nenhum.

**Contas**

| # | Nome | W | S | Notas |
|---|------|---|---|-------|
| 1 | `payer` | W | S | Qualquer assinante; financia ATAs de criador ausentes. |
| 2 | `creator` |   |   | Deve ser igual a `pool_state.pool_creator`; não assina. |
| 3 | `authority` |   |   | PDA de autoridade global de vault do CPMM. |
| 4 | `pool_state` | W |   | Mantém os contadores de taxa de criador acumulados. |
| 5 | `token_0_vault` | W |   | Deve corresponder ao vault de token 0 do pool. |
| 6 | `token_1_vault` | W |   | Deve corresponder ao vault de token 1 do pool. |
| 7 | `vault_0_mint` |   |   | Deve corresponder a `token_0_vault.mint`. |
| 8 | `vault_1_mint` |   |   | Deve corresponder a `token_1_vault.mint`. |
| 9 | `creator_token_0` | W |   | ATA canônico do criador para `vault_0_mint`; criado se ausente. |
| 10 | `creator_token_1` | W |   | ATA canônico do criador para `vault_1_mint`; criado se ausente. |
| 11 | `token_0_program` |   |   | SPL Token ou Token-2022, correspondendo ao token 0. |
| 12 | `token_1_program` |   |   | SPL Token ou Token-2022, correspondendo ao token 1. |
| 13 | `associated_token_program` |   |   | Cria um ATA de destinatário ausente. |
| 14 | `system_program` |   |   | Financia criação de conta a partir de `payer`. |

**Efeito**

- Transfere todo `creator_fees_token_0` e `creator_fees_token_1` dos vaults do pool para os ATAs do criador.
- Zera ambos os contadores de taxa de criador e atualiza `pool_state.recent_epoch`.
- Retorna `NoFeeCollect` quando ambos os contadores são zero.

## `UpdatePoolStatus`

Pausa ou retoma operações individuais em um pool. O campo `status` é uma máscara de bits:

| Bit | Flag | Efeito quando definido |
|-----|------|-----------------|
| 0 | `DEPOSIT_DISABLED` | `Deposit` rejeita com `NotApproved`. |
| 1 | `WITHDRAW_DISABLED` | `Withdraw` rejeita. |
| 2 | `SWAP_DISABLED` | `SwapBaseInput` / `SwapBaseOutput` rejeitam. |

**Argumentos**

```
status: u8      // nova máscara de bits
```

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `authority` |   | S | Deve corresponder à chave de admin no programa CPMM. |
| 2 | `pool_state` | W |   |

A chave de admin é a autoridade de upgrade no programa CPMM — na prática, o multisig da Raydium. Veja [`security/admin-and-multisig`](/pt/security/admin-and-multisig).

## `CreateAmmConfig`

Cria um novo nível de taxa.

**Argumentos**

```
index:             u16
trade_fee_rate:    u64
protocol_fee_rate: u64
fund_fee_rate:     u64
create_pool_fee:   u64
```

**Contas**

| # | Nome | W | S |
|---|------|---|---|
| 1 | `owner` | W | S | Admin. |
| 2 | `amm_config` | W |   | `init`ializado aqui. |
| 3 | `system_program` |   |   |

**Pré-condições**

- Nenhuma `AmmConfig` existente com o mesmo `index`.
- `protocol_fee_rate + fund_fee_rate <= FEE_RATE_DENOMINATOR_VALUE`.

## `UpdateAmmConfig`

Altera taxas ou propriedade em uma `AmmConfig` existente. Recebe um `param: u8` (discriminador para qual campo atualizar) e um `value: u64`. A semântica de valor por param está na fonte; comumente:

- `param = 0` → `trade_fee_rate`
- `param = 1` → `protocol_fee_rate`
- `param = 2` → `fund_fee_rate`
- `param = 3` → `new_protocol_owner` (passa bytes `Pubkey` como reinterpretação)
- `param = 4` → `new_fund_owner`
- `param = 5` → `create_pool_fee`
- `param = 6` → `disable_create_pool`

As mudanças são assinadas pelo admin e afetam **cada pool vinculado a esta `AmmConfig`** no próximo swap. Sem migração; pools simplesmente leem os novos valores.

## Matriz de mudança de estado

| Instrução | `lp_supply` | Saldos de vault | Campos de taxa acumulada | `observation` |
|-------------|------------|---------------|-------------------|---------------|
| `Initialize` | + init LP | + `init_amount_{0,1}` | 0 | init |
| `InitializeWithPermission` | + init LP | + `init_amount_{0,1}` | 0 | init |
| `Deposit` | + | + ambos | — | — |
| `Withdraw` | − | − ambos | — | — |
| `SwapBaseInput` | — | + entrada, − saída | + `trade_fee` dividido em protocolo/fundo; + `creator_fee` se habilitado | + (se intervalo decorrido) |
| `SwapBaseOutput` | — | + entrada, − saída | + `trade_fee` dividido em protocolo/fundo; + `creator_fee` se habilitado | + (se intervalo decorrido) |
| `CollectProtocolFee` | — | − (por buckets de protocolo) | `protocol_*` → 0 | — |
| `CollectFundFee` | — | − (por buckets de fundo) | `fund_*` → 0 | — |
| `CollectCreatorFee` | — | − (por buckets de criador) | `creator_*` → 0 | — |
| `CollectCreatorFeePermissionless` | — | − (por buckets de criador) | `creator_*` → 0 | — |
| `UpdatePoolStatus` | — | — | — | — |

## Próximos passos

- [`products/cpmm/code-demos`](/pt/products/cpmm/code-demos) — exemplos TypeScript executáveis para o acima.
- [`reference/error-codes`](/pt/reference/error-codes) — a tabela de erros Anchor completa.
- [`products/cpmm/fees`](/pt/products/cpmm/fees) — o modelo de acúmulo de taxa que as instruções `Collect*Fee` drenam.

Fontes:

- [`raydium-io/raydium-cp-swap` — instructions](https://github.com/raydium-io/raydium-cp-swap/tree/master/programs/cp-swap/src/instructions)
- [Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`](https://github.com/raydium-io/raydium-sdk-V2)
