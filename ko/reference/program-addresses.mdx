---
title: "프로그램 주소"
description: "Solana mainnet-beta 및 devnet의 정규 Raydium 프로그램 ID 및 공유 PDA. 문서의 다른 모든 페이지는 주소를 하드코딩하는 대신 여기로 링크합니다."
---

<Info>
  **이 페이지는 AI 자동 번역입니다. 모든 내용은 영문판을 기준으로 합니다.**

  [영문판 보기 →](/reference/program-addresses)
</Info>

<Info>
  **신뢰할 수 있는 정보 출처.** 이 페이지는 프로그램 주소를 명시적으로 나열하는 문서의 유일한 장소입니다. 다른 페이지는 여기로 링크합니다. ID가 변경되면 **여기에만** 업데이트하면 사이트의 모든 참조가 일관성 있게 유지됩니다.

  실제 거래에 서명하기 전에 항상 라이브 API(`https://api-v3.raydium.io/main/info`)에서 값을 교차 확인하세요.
</Info>

## Mainnet-beta

<Tip>
  경험상 규칙: 실제 프로그램 ID가 아래 표와 일치하지 않으면 **거래에 서명하지 마세요.** 프로그램 ID 불일치는 Solana에서 자금을 잃는 가장 쉬운 방법입니다.
</Tip>

### 온체인 프로그램

| 프로그램 | 프로그램 ID | 소스 |
|---------|------------|--------|
| **AMM v4** (하이브리드 AMM + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (표준 AMM) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (집중 유동성) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (StableSwap 스타일 곡선) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | 공개되지 않음 |
| **Farm v3** (레거시 RAY 스테이킹) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | 공개되지 않음 |
| **Farm v5** (레거시 생태계 팜) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | 공개되지 않음 |
| **Farm v6** (현재 생태계 팜) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | 공개되지 않음 |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | 공개되지 않음 |
| **AMM 라우팅** (Raydium의 온체인 라우터) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | 공개되지 않음 |
| **번 & 어닝 / LP 락** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | 공개되지 않음 |

참고:

- AMM v4 및 Farm v3/v5는 **기존 풀 및 포지션을 유지하기 위해** 보관됩니다. Raydium UI 및 SDK는 새 풀 생성 및 새 생태계 팜을 각각 CPMM, CLMM 및 Farm v6으로 라우팅합니다 — [`protocol-overview/versions-and-migration`](/ko/protocol-overview/versions-and-migration) 참조.
- 위의 Farm v3 ID는 \$RAY 단일 자산 스테이킹 프로그램으로도 사용됩니다. LP 스테이킹의 경우 동작이 Farm v3과 동일합니다.
- **Stable AMM**은 AMM 라우팅 프로그램이 AMM v4, CPMM 및 CLMM과 함께 대상으로 지정할 수 있는 별도의 프로그램입니다. 유동성은 다른 세 프로그램에 비해 얇으며 SDK는 이에 대한 일급 API를 노출하지 않습니다. 이를 통해 라우팅하는 통합자는 일반적으로 라우터를 통해 그렇게 합니다.
- **소스 코드 가용성.** 위의 온체인 프로그램 중 `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM) 및 `raydium-clmm` (CLMM)만 [`github.com/raydium-io`](https://github.com/raydium-io)의 공개 소스 저장소와 함께 제공됩니다. Stable AMM, LaunchLab, AMM 라우팅, 번 & 어닝 / LP 락 및 팜 프로그램은 공개되지 않습니다 — 라이브 API, 온체인 바이트코드 및 [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl)의 게시된 IDL에 대해 확인하세요.

### 공유 관리자 권한

모든 Raydium 프로그램 (AMM v4 포함)은 관리자 경로에 대한 명령 수준 접근 제어를 위해 단일 하드코딩된 관리자 `Pubkey`를 공유합니다. AMM v4에서는 `config_feature::amm_owner::ID` (gating `SetParams`, `CreateConfigAccount` 및 `UpdateConfigAccount`, 기본 `pnl_owner`로 사용)이고 Anchor 프로그램 (CLMM, CPMM, LaunchLab, Lock)에서는 `admin::ID`입니다 — `CreateAmmConfig` 또는 `UpdatePoolStatus`와 같은 경로를 gating합니다:

| 클러스터 | 관리자 |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

계정 수준 운영 권한 (예: CPMM/CLMM `AmmConfig`의 `protocol_owner`, `fund_owner` 또는 LaunchLab `GlobalConfig`의 마이그레이션 지갑)은 온체인에 저장되며 프로그램 관리자와 다를 수 있습니다. 높은 위험도의 거래를 보내기 전에 관련 구성 계정에서 직접 읽으세요.

### CPMM 권한 PDA 생성자 권한

`CreatePermissionPda`는 위의 공유 프로그램 관리자 또는 전용 소유자를 허용합니다. 2026-09 CPMM 업그레이드 이후 `ClosePermissionPda`는 동일한 두 서명자를 허용합니다. 그 전에는 관리자 전용이었습니다.

| 클러스터 | 전용 `CreatePermissionPda` 소유자 |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### 지원 민트 레지스트리 권한

`CreateSupportMintAssociated` 및 `CloseSupportMintAssociated`는 공유 프로그램 관리자 또는 아래의 프로그램 전용 소유자를 허용합니다. 민트를 등록하면 해당 프로그램의 Token-2022 확장 허용 목록을 통과합니다 — [`reference/token-2022-support`](/ko/reference/token-2022-support#the-one-bypass-the-per-mint-registry) 참조. CPMM과 CLMM은 각각 자신의 레지스트리 PDA와 자신의 권한을 유지합니다. 어느 키도 다른 프로그램에서 작동하지 않습니다.

| 프로그램 | 클러스터 | 전용 지원 민트 소유자 |
|---------|---------|------------------------------|
| CPMM | mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| CPMM | devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |
| CLMM | mainnet-beta | `RayVyjyJQz9vAi126A4sGexKnSU1XeZaHTRcM1mZMPY` |
| CLMM | devnet | `rayf3nEbb3bnfN6RDGFpqPbjc5uUa3tRUzu6UVYrRx5` |

### CPMM 수수료 소유자 지갑

2026-09 CPMM 업그레이드 이후 `CreateAmmConfig`는 관리자 서명자의 키를 복사하는 대신 이러한 고정 키를 새 구성의 `protocol_owner` 및 `fund_owner`에 씁니다. `CollectProtocolFee` 및 `CollectFundFee`는 구성에 저장된 키 **또는** 공유 프로그램 관리자 (`|| crate::admin::ID`)를 허용하므로 관리자는 허용된 서명자로 남아 있습니다 — 변경된 것은 기본적으로 수익이 가는 위치뿐입니다.

| 클러스터 | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  이것은 프로그램이 새 구성에 **쓰는** 값이지, 기존 구성에 대한 설명이 아닙니다. 업그레이드 전에 생성된 `AmmConfig` 계정은 저장된 모든 것을 유지합니다. 수집 거래를 구축하기 전에 항상 계정에서 `protocol_owner` / `fund_owner`를 읽으세요.
</Warning>

### 프로토콜 수수료 수집 지갑

각 프로그램의 프로토콜 측 거래 수수료 공유가 누적되는 위치입니다. 이것은 일반적인 토큰 보유 지갑이므로 단일 주소는 혼합된 SPL 민트 세트를 전달합니다 (SOL/USDC 풀의 wSOL 및 USDC, SOL/RAY 풀의 wSOL 및 RAY 등).

| 프로그램 | 수집 주소 | 저장 위치 |
|---------|--------------------|-----------|
| CLMM | `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416` | 모든 21개 구성의 `AmmConfig.owner` |
| CPMM | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `AmmConfig.protocol_owner` ([CPMM 수수료 소유자 지갑](#cpmm-수수료-소유자-지갑) 참조) |
| AMM v4 | `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF` | 단일 구성 PDA의 `AmmConfig.pnl_owner` |

Stable AMM의 프로토콜 공유는 `out_put.need_take_pnl_*`로 인 볼트에 누적되며 상시 지갑에 앉아 있는 대신 `WithdrawPnl`에 의해 스윕됩니다. CPMM의 경우 수집 거래를 구축하기 전에 특정 `AmmConfig`에서 `protocol_owner`를 읽으세요 — 2026-09 업그레이드 전에 생성된 구성은 다른 키를 보유할 수 있습니다.

### 초과 램포트 수집 지갑

2026-09-09에 업그레이드된 세 프로그램 각각은 [SIMD-0437 렌트 감소](/ko/solana-fundamentals/rent-and-reclaimable-rent)에 따라 프로그램 제어 계정의 렌트 면제 최소값 이상으로 보유된 램포트를 반환하는 관리자 명령을 얻었습니다. 각 프로그램은 자신의 대상 지갑을 하드코딩하며, 각각은 전용 키입니다 — 공유 프로그램 관리자가 아닙니다.

| 프로그램 | 명령 | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM과 LaunchLab은 추가로 공유 프로그램 관리자를 서명자로 허용합니다. AMM v4는 **오직** 위의 지갑만 허용합니다 — 관리자 폴백이 없습니다. AMM v4는 또한 `testnet` 기능 아래에서 별도의 테스트넷 키 (`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`)를 컴파일합니다.

이러한 명령은 램포트만 이동합니다. 토큰 잔액을 건드리거나, 계정을 닫거나, 풀 상태를 변경하지 않습니다. [`products/amm-v4/instructions`](/ko/products/amm-v4/instructions#withdrawexcesslamports), [`products/cpmm/instructions`](/ko/products/cpmm/instructions#collectexcesslamports) 및 [`products/launchlab/instructions`](/ko/products/launchlab/instructions#collectexcesslamports) 참조.

### LaunchLab 위임된 구성 생성자

`CreateConfig`는 공유 프로그램 관리자 또는 아래의 위임된 권한을 허용합니다. 위임된 역할은 새 `GlobalConfig`를 생성할 수 있습니다. 기존 계정에 대한 관리자 전용 `UpdateConfig` 경로를 호출할 수 없습니다.

| 클러스터 | 위임된 `CreateConfig` 권한 |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### CLMM 제한된 발급자 동결 권한

CLMM 풀의 두 볼트 민트 중 하나가 아래의 동결 권한 중 하나를 가지면 V2 경로를 통해 열린 새 포지션은 포지션 NFT 토큰 계정이 동결됩니다. 목록은 CLMM 프로그램의 `frozen_position_nft_authorities::IDS`이며, 단일 항목은 CLMM의 제거된 Superstate 감지도 일치한 발급자 권한입니다. 이것은 **일치 키로만 사용되는 기본 자산 발급자 권한**입니다. CLMM 풀 PDA (발급자 키가 아님)는 포지션 NFT 민트의 동결 권한이 됩니다.

| 클러스터 | 제한된 발급자 동결 권한 |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

목록은 CLMM 프로그램에 하드코딩됩니다. 권한을 추가하거나 제거하려면 프로그램 업그레이드가 필요합니다.

### 공유 구성 / PDA 규칙

여러 Raydium 프로그램은 공개 키가 안정적이고 공개 API에 나열된 **구성 계정**을 노출합니다. 하드코딩보다 API 조회를 선호하세요:

```
# CPMM 수수료 구성 (배열 {id, index, tradeFeeRate, ...} 반환)
GET https://api-v3.raydium.io/main/cpmm-config

# CLMM 수수료 구성
GET https://api-v3.raydium.io/main/clmm-config
```

### 풀 생성 수수료 수신자

CPMM과 AMM v4 모두 풀 생성 수수료 대상을 **정확한 주소로** 검증하므로 테스트 중인 클러스터에 이미 존재해야 합니다. CLMM은 별도의 풀 생성 수수료가 없습니다.

| 프로그램 | 상수 | mainnet-beta | devnet |
|---------|----------|--------------|--------|
| CPMM | `create_pool_fee_reveiver::ID` (오타는 소스에 있음) | `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` | `3oE58BKVt8KuYkGxx8zBojugnymWmBiyafWgMrnb6eYy` |
| AMM v4 | `config_feature::create_pool_fee_address::ID` | `7YttLkHDoNj9wyDur5pM1ejNaAvT9X4eqaYcHQqtj2G5` | `9y8ENuuZ3b19quffx9hQvRVygG5ky6snHfRvGpuSfeJy` |

AMM v4는 또한 `testnet` 기능 아래에서 `3TRTX4dXUpp2eqxi3tvQDFYUV7SdDJjcPE3Y4mbtftaX`를 컴파일합니다. CPMM은 Anchor의 `ConstraintAddress` (`2012`)로 불일치를 거부합니다. AMM v4는 `InvalidFee`로 거부합니다.

### CLMM 리미트 오더 키퍼

`SettleLimitOrder` 및 `CloseLimitOrder`는 오더의 자신의 `owner` 또는 아래의 프로그램 전체 키퍼 키를 허용합니다. 키퍼는 오더를 열거나, 증가하거나, 감소할 수 없으며, 출력은 항상 오더 소유자의 계정에 도달합니다 — [`security/admin-and-multisig`](/ko/security/admin-and-multisig) 참조.

| 클러스터 | `limit_order_admin` |
|---------|---------------------|
| mainnet-beta | `Ray8HHtixhL9zvnokMyELCVGp622PDPJj96zcVC9RWp` |
| devnet | `DRaypkxM96mjYYnqMmuYjsSL3stHxLf1CvYuTcqxUaav` |

참고용 기본 CPMM `AmmConfig` 인덱스 `0` (표준 0.25% 풀) 수수료 매개변수:

| 필드 | 값 | 의미 |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 거래량의 0.25% |
| `protocol_fee_rate` | `120000` | 거래 수수료의 12% (거래량이 아님) 프로토콜로 라우팅됨 |
| `fund_fee_rate` | `40000` | 거래 수수료의 4% 펀드 멀티시그로 라우팅됨 |
| `creator_fee_rate` | `500` | 거래량의 0.05% 풀 생성자에게 (선택 사항) |

분할이 어떻게 구성되는지는 [`products/cpmm/fees`](/ko/products/cpmm/fees) 참조, 교차 제품 매트릭스는 [`reference/fee-comparison`](/ko/reference/fee-comparison) 참조.

### PDA 시드

아래의 시드는 **정규**이며 SDK와 온체인 CPI 모두에서 사용됩니다. 항상 PDA를 계산하세요. 파생된 주소를 하드코딩하지 마세요.

```ts
// CPMM — 모든 시드는 명시되지 않은 한 정적 ASCII 문자열입니다.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint`는 해싱 전에 **공개 키 바이트 순서로 정렬됩니다** (token0 < token1). 이를 잘못하면 존재하지 않는 풀에 대한 유효한 PDA가 생성됩니다.

동등한 CLMM 시드는 동일한 스타일을 따릅니다. [`products/clmm/accounts`](/ko/products/clmm/accounts) 참조.

## Devnet

| 프로그램 | 프로그램 ID |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM 라우팅** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **번 & 어닝 / LP 락** | `DRay25Usp3YJAi7beckgpGUC7mGJ2cR1AVPxhYfwVCUX` |
| **Farm v3 / v5 / v6** | devnet에 대해 안정적으로 게시되지 않음 — 사용 전에 라이브 API (`https://api-v3-devnet.raydium.io/main/info`)를 통해 확인하세요. |

Devnet REST API 기본: `https://api-v3-devnet.raydium.io/` (mainnet과 동일한 경로 형태).

## 업그레이드 권한

mainnet-beta의 모든 Raydium 프로그램은 BPF 업그레이드 가능 로더 아래에 배포되며 **동일한** 업그레이드 권한 — 프로그램 업그레이드 Squads 멀티시그의 볼트를 가집니다:

| | 값 |
|---|---|
| 업그레이드 권한 (모든 프로그램) | `FytDrVzDybM1TwFQPGb8qaxZR7dBCzNeqT3vtQsceZQK` |
| Squads 멀티시그 구성 계정 | `tr8rgazUrZzgdkfc6Q622nVJHMMzh29trdBE2uBHb4u` |
| Squads 프로그램 | `SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf` (v4) |

2026-09-09에 AMM v4, CPMM, CLMM, Stable AMM, Farm v6, LaunchLab, AMM 라우팅 및 락 — 8개 모두에 대해 온체인에서 확인됨. 모두 해당 권한을 보고하며 어느 것도 불변이 아닙니다. 높은 위험도의 배포 전에 `solana program show <PROGRAM_ID> -u mainnet-beta`로 다시 확인하세요. [`security/admin-and-multisig`](/ko/security/admin-and-multisig)에서 멀티시그의 임계값 및 서명 관행을 참조하세요.

## 프로그램 수준 구성 계정

| 프로그램 | 구성 계정 | 내용 |
|---------|----------------|----------|
| AMM v4 | `9DCxsMizn3H1hprZ7xWe6LDzeUeZBksYFpBWBtSf1PQX` | 시드 `["amm_config_account_seed"]`의 단일 `AmmConfig` PDA. `pnl_owner` = `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF`, `cancel_owner` = `RAYdmrNjdRSu7ErPv9s2BqDtRoFa1QECn7UJYaC2tR3` 및 `create_pool_fee` = `150000000` (0.15 SOL)을 보유합니다. |
| CPMM | 21개 `AmmConfig` 계정 (인덱스 0–20) | 인덱스 0은 `D4FPEruKEHrG5TenZ2mpDGEfu1iUvTiqBxvpU8HLBvC2`입니다. 계층 세트는 [`reference/fee-comparison`](/ko/reference/fee-comparison) 참조. |
| CLMM | 21개 `AmmConfig` 계정 (인덱스 0–20) | 인덱스 1 (0.25%, 틱 간격 60)은 `E64NGkDLLCdQ2yFNPcavaKptrEgmiQaNykUuLC1Qgwyp`입니다. 모든 21개에서 `owner` = `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416`, `fund_owner` = `FundHfY8oo8J9KYGyfXFFuQCHe7Z1VBNmsj84eMcdYs4`. |

모든 값은 2026-09-09에 mainnet-beta에서 읽음. 이러한 계정의 수수료 비율은 관리자 변경 가능합니다 — 캐시하는 대신 라이브로 읽으세요.

## 온체인에서 주소를 확인하는 방법

1. **Solana Explorer.** 주소를 [explorer.solana.com](https://explorer.solana.com)에 붙여넣고 `Program`으로 표시되고 현재 업그레이드 권한이 있는지 확인하세요. Mainnet-beta는 Raydium의 업그레이드 권한으로 서명된 배포를 표시해야 합니다.
2. **CLI.** `solana program show <PROGRAM_ID> -u mainnet-beta`를 사용하여 배포 슬롯, BPF 로더, 업그레이드 권한 및 데이터 길이를 검사하세요. 이것을 실행 설명서에 기록하세요.
3. **IDL 첨부.** `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet`은 **CLMM, LaunchLab 및 Lock**에서 작동합니다. 이들은 여전히 프로그램 자체가 소유한 레거시 `anchor:idl` 계정을 유지합니다. CPMM에서는 **작동하지 않습니다**: CPMM의 온체인 IDL은 2026-09 업그레이드에서 Program Metadata 프로그램 (`ProgM6JCCvbYkfKqJYHePx4xxSUSqJp7rh8Lyv7nk7S`)으로 이동했으므로 대신 거기서 가져오세요. AMM v4, Stable AMM 및 Farm은 온체인 IDL이 없습니다. IDL은 [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)에 미러링됩니다 — 온체인 IDL을 저장소와 비교하고 신뢰하기 전에 차이를 확인하세요.
4. **구성/관리자 권한.** CPMM/CLMM 구성 계정의 경우 `owner`/`protocol_owner` 필드를 읽고 [`security/admin-and-multisig`](/ko/security/admin-and-multisig)에 게시된 현재 Raydium 멀티시그와 일치하는지 확인하세요.

위의 네 가지 확인 중 하나라도 이 페이지와 불일치하면 이 페이지를 잘못된 것으로 취급하고 새 코드를 주소에 대해 작성하기 전에 문제를 열어보세요.

## 이 페이지 업데이트

- 프로그램 ID 변경은 안전 관련입니다. 프로그램 ID 변경을 배포하지 마세요. (a) Raydium 공지로 링크, (b) [`reference/changelog`](/ko/reference/changelog)에 항목 추가, (c) 문서에 대한 링크 확인을 실행하여 페이지가 여전히 이전 값을 참조하지 않는지 확인하지 않고.
- 사용 중단은 삭제되는 대신 상태 메모와 함께 표에 남아 있습니다 — 기존 풀은 여전히 이전 프로그램을 통해 해결됩니다.

소스:

- 라이브 API: `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
