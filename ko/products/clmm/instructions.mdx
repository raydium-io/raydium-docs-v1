---
title: "CLMM 명령어"
description: "완전한 CLMM 명령어 참조: 풀 생명주기, 포지션 관리, 스왑, 보상 스트림."
---

<Info>
  **이 페이지는 AI 자동 번역입니다. 모든 내용은 영문판을 기준으로 합니다.**

  [영문판 보기 →](/products/clmm/instructions)
</Info>

이 페이지는 [`products/clmm/accounts`](/ko/products/clmm/accounts) (계정이 무엇인지)와 [`products/clmm/math`](/ko/products/clmm/math) (수학이 무엇인지)와 함께 사용됩니다. 인자와 계정 순서에 대해 권위 있는 참조이며, 구체적인 바이트 레이아웃은 IDL에서 나옵니다.

## 명령어 목록

| 그룹 | 명령어 | 설명 |
|-------|-------------|-------|
| Admin | `CreateAmmConfig` | 새로운 수수료 계층을 정의합니다. |
| Admin | `UpdateAmmConfig` | 기존 계층의 요금을 변경합니다. |
| Admin | `UpdatePoolStatus` | 풀의 작업을 일시 중지/재개합니다. |
| Admin | `CreateSupportMintAssociated` | CLMM 풀에서 사용할 Token-2022 민트 확장 구성을 허용 목록에 추가합니다. |
| Admin | `CreateOperationAccount` | 프로그램 수준의 작업 계정을 초기화합니다 (일회성). |
| Admin | `UpdateOperationAccount` | 작업 계정 허용 목록을 수정합니다. |
| Admin | `CreateDynamicFeeConfig` | u16 인덱스 아래에 재사용 가능한 동적 수수료 매개변수 집합을 만듭니다. |
| Admin | `UpdateDynamicFeeConfig` | 기존 `DynamicFeeConfig`를 수정합니다. 이미 스냅샷한 풀은 영향을 받지 않습니다. |
| Admin | `CreatePermissionPda` | `CreatePermissionedPool`을 호출할 권리를 권한에 부여하여 `Permission` PDA를 초기화합니다. |
| Admin | `ClosePermissionPda` | 이전에 부여된 권한을 `Permission` PDA를 닫아 취소합니다. 임차료는 호출자에게 환불됩니다. |
| Pool | `CreatePool` | `AmmConfig`에 바인딩된 CLMM 풀을 초기화합니다. 표준, FromInput 수수료 경로. `CreateCustomizablePool`과 공존합니다. |
| Pool | `CreateCustomizablePool` | 새 풀에 권장됩니다. `CreatePool`과 동일한 효과에 `collect_fee_on`과 선택적 `enable_dynamic_fee` 플래그를 추가합니다. |
| Pool | `CreatePermissionedPool` | 이미 풀이 있는 쌍에 대해 **추가** 풀을 만들고, 클라이언트가 제공한 `seed_index`를 풀 PDA에 포함합니다. 지불자는 `Permission` PDA를 보유해야 합니다. |
| Position | `OpenPosition` / `OpenPositionV2` / `OpenPositionWithToken22Nft` | 포지션 NFT를 발행합니다. `OpenPositionV2`는 V1을 대체합니다 (비트맵 확장 슬롯이 있는 새로운 계정 레이아웃). `OpenPositionWithToken22Nft`는 포지션 NFT를 SPL Token 대신 Token-2022로 발행합니다. 새 코드는 V2 또는 Token-2022 변형을 사용해야 합니다. |
| Position | `IncreaseLiquidity` / `IncreaseLiquidityV2` | 기존 포지션에 유동성을 추가합니다. |
| Position | `DecreaseLiquidity` / `DecreaseLiquidityV2` | 유동성을 제거하고 미지급 수수료를 수집합니다. |
| Position | `ClosePosition` | NFT를 소각하고 `PersonalPositionState`를 닫습니다. |
| Position | `CloseProtocolPosition` | 레거시 `ProtocolPositionState` PDA에 대한 관리자 전용 정리입니다. 현재 프로그램은 더 이상 `ProtocolPositionState`를 생성하거나 읽지 않습니다. 이 명령어는 오래된 프로그램 버전에서 생성한 계정의 임차료를 회수하기 위해서만 존재합니다. |
| Swap | `Swap` / `SwapV2` | 상수 유동성 스왑입니다. 두 변형 모두 동적 수수료, 단측 수수료 라우팅, 지정가 주문 매칭을 적용합니다. 유일한 차이는 `SwapV2`가 Token-2022 민트를 허용한다는 것입니다 (V1 변형은 두 금고 모두 클래식 SPL Token이어야 함). |
| Swap | `SwapRouterBaseIn` | 라우터에서 사용하는 다중 홉입니다. |
| Limit order | `OpenLimitOrder` | 특정 틱에서 판매 주문을 배치합니다. 미체결 토큰은 틱에 남아 있으며, 가격이 지나갈 때 매칭 엔진이 채웁니다. |
| Limit order | `IncreaseLimitOrder` | 기존 공개 주문에 추가합니다. |
| Limit order | `DecreaseLimitOrder` | 공개 주문을 줄이거나 취소합니다. 미체결 나머지와 이미 정산된 출력을 지불합니다. |
| Limit order | `SettleLimitOrder` | 채워진 출력 토큰을 주문 소유자에게 푸시합니다. 소유자 또는 운영 키퍼가 호출할 수 있습니다. |
| Limit order | `CloseLimitOrder` | 완전히 소비된 주문 계정을 닫습니다. 임차료는 항상 주문의 `owner`에게 반환됩니다. 소유자 또는 키퍼가 호출할 수 있습니다. |
| Fees | `CollectProtocolFee` | 프로토콜 수수료의 관리자 정리입니다. |
| Fees | `CollectFundFee` | 펀드 수수료의 관리자 정리입니다. |
| Rewards | `InitializeReward` | 풀에 새로운 보상 스트림을 연결합니다. |
| Rewards | `SetRewardParams` | 기존 보상의 배출 속도/종료를 변경합니다. |
| Rewards | `UpdateRewardInfos` | 보상 성장을 현재 시간으로 정산합니다 (모든 스왑/포지션 변경으로 호출됨). |
| Rewards | `TransferRewardOwner` | 보상 스트림을 설정하거나 충전할 수 있는 권한을 이전합니다. |
| Rewards | `CollectRemainingRewards` | 보상 스트림의 `end_time` 이후, 할당되지 않은 토큰을 펀더에게 다시 정리합니다. |
| Utility | `InitTickArray` | 틱 배열 계정을 초기화합니다 (`OpenPosition`과 함께 번들로 제공되는 경우가 많음). |

대부분의 관리자 전용 명령어 (`CreateAmmConfig`, `UpdateAmmConfig`, `UpdatePoolStatus`, `CreateSupportMintAssociated`, `CreateOperationAccount`, `UpdateOperationAccount`, `CloseProtocolPosition`)는 프로그램의 하드코딩된 `admin` 공개 키로 제어됩니다. `CreatePermissionPda` / `ClosePermissionPda`는 `admin` 공개 키 **또는** 전용 `permission_pda_admin` 키를 허용합니다. 보상 스트림 관리자 명령어 (`TransferRewardOwner`, `CollectRemainingRewards`)는 프로그램 관리자가 아닌 보상 펀더로 제어됩니다.

V2 접미사는 "금고/NFT에서 Token-2022를 지원하며, 비트맵 확장 슬롯이 필요함"을 의미합니다. SDK는 새 풀에 대해 기본적으로 V2를 선택합니다.

## `CreatePool`

**인자**

```
sqrt_price_x64: u128           // 초기 가격
open_time:      u64            // 이 시간 이전의 스왑은 거부됨
```

**계정 (축약)**

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| 1 | `pool_creator` | W | S | |
| 2 | `amm_config` |   |   | 선택된 수수료 계층. |
| 3 | `pool_state` | W |   | 여기서 `init`. |
| 4 | `token_mint_0` |   |   | 정렬됨. |
| 5 | `token_mint_1` |   |   | |
| 6 | `token_vault_0` | W |   | 여기서 `init`, 풀 권한 PDA가 소유. |
| 7 | `token_vault_1` | W |   | |
| 8 | `observation_state` | W |   | 여기서 `init`. |
| 9 | `tick_array_bitmap_extension` | W |   | 여기서 `init` (V2). |
| 10 | `token_program` |   |   | |
| 11 | `token_program_2022` |   |   | |
| 12 | `system_program`, `rent` |   |   | |

**전제 조건**

- `token_mint_0 < token_mint_1` (바이트 순서).
- `amm_config.disable_create_pool == false`.
- 민트는 Token-2022 확장 허용 목록에 의해 거부되지 않습니다.

**사후 조건**

- `pool_state.sqrt_price_x64 = sqrt_price_x64`, `tick_current = floor(log_{1.0001}(price))`.
- `pool_state.liquidity = 0` (아직 포지션 없음).
- `pool_state.fee_on = FromInput` (레거시 기본값).
- `pool_state.dynamic_fee_info`는 0으로 설정됨 (동적 수수료 비활성화).

## `CreateCustomizablePool`

새 풀에 권장됩니다. `CreatePool`과 동일한 효과에 풀별 수수료 수집 모드와 선택적 동적 수수료 옵트인을 추가합니다.

**인자**

```rust
pub struct CreateCustomizableParams {
    pub sqrt_price_x64:    u128,
    pub collect_fee_on:    CollectFeeOn,   // FromInput | Token0Only | Token1Only
    pub enable_dynamic_fee: bool,
}
```

**계정 (축약)** — `CreatePool`과 동일하며, `enable_dynamic_fee = true`일 때:

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| N | `dynamic_fee_config` |   |   | 스냅샷할 공유 구성. 이미 존재해야 합니다. |

**전제 조건** — `CreatePool`과 동일합니다. `enable_dynamic_fee = false`이면 `dynamic_fee_config`는 무시됩니다.

**사후 조건**

- `pool_state.fee_on`이 선택된 `CollectFeeOn` 변형으로 설정됩니다.
- 동적 수수료가 활성화된 경우: `pool_state.dynamic_fee_info`는 제공된 `DynamicFeeConfig`에서 초기화됩니다 (5개의 보정 매개변수 복사됨, 상태 필드는 0으로 설정됨).
- 그렇지 않으면: `pool_state.dynamic_fee_info`는 0으로 설정됨 (= 이 풀에 대해 동적 수수료가 영구적으로 비활성화됨).

`fee_on`과 동적 수수료 활성화 비트는 **풀 생성 시에만** 설정됩니다. 제자리 업그레이드는 없습니다. 레거시 `CreatePool`을 통해 생성된 풀은 소급하여 동적 수수료 또는 단측 수수료를 얻을 수 없습니다. 새 배포는 이 명령어를 기본값으로 사용해야 합니다.

## `CreatePermissionedPool`

`CreatePool`과 `CreateCustomizablePool` 모두 `["pool", amm_config, token_mint_0, token_mint_1]`에서 풀 PDA를 파생하므로, `(config, mint0, mint1)` 트리플당 정확히 **하나의** 정규 풀 주소가 있습니다. 같은 시드에서 두 번째 `init`은 실패합니다. `CreatePermissionedPool`은 클라이언트가 제공한 `seed_index: u16`을 풀 PDA 시드에 포함하여 이 제한을 해제하므로, **같은 쌍과 수수료 계층에 대해 여러 풀**을 만들 수 있습니다. 각각 자신의 주소에서. 임의의 풀 주소는 특권 기능이므로, 지불자는 이를 승인하는 `Permission` PDA를 보유해야 합니다.

풀에 대한 다른 모든 것은 `CreateCustomizablePool`과 동일합니다. 동일한 `CreateCustomizableParams`를 사용하며 단측 수수료와 동적 수수료 옵트인을 지원합니다.

**인자**

```rust
customizable_params: CreateCustomizableParams   // CreateCustomizablePool과 동일한 구조체
seed_index:          u16                          // 0이 아니어야 함. 풀 PDA 시드에 포함됨
```

**계정 (축약)** — `CreateCustomizablePool`과 동일하며, 앞에:

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| 1 | `payer` | W | S | 임차료를 지불합니다. 아래 `permission` PDA의 권한이어야 합니다. |
| 2 | `pool_creator` |   |   | `pool_state.owner`로 기록됩니다. `payer`와 다를 수 있습니다. |
| 3 | `permission` |   |   | PDA `["permission", payer]`. Anchor는 존재하지 않거나 프로그램이 소유하지 않으면 호출을 거부합니다. |
| … | (`amm_config`, `pool_state`, 민트, 금고, `observation_state`, `tick_array_bitmap`, 토큰/시스템 프로그램) | | | `CreateCustomizablePool`과 같음. |

`pool_state` PDA는 `["pool", amm_config, token_mint_0, token_mint_1, seed_index.to_le_bytes()]`에서 파생됩니다.

**전제 조건**

- `seed_index != 0`. `seed_index`가 `0`이면 레거시 풀용으로 예약되어 있으며 여기서 거부됩니다. `[0, 0]` 시드 구성 요소는 레거시 풀 주소를 클래식 4시드 형식으로 축소하는 것입니다.
- `payer`에 대한 `permission` PDA가 존재합니다 (관리자가 `CreatePermissionPda`를 통해 생성함).
- `CreatePool`과 동일한 민트/허용 목록 규칙.

**사후 조건**

- 새로운 `pool_state`가 `seed_index` 파생 주소에 존재하며, `pool_state.seed_index = seed_index`.
- 다른 모든 사후 상태는 `CreateCustomizablePool`과 일치합니다 (수수료 모드, 선택적 동적 수수료).

<Note>
  이 명령어는 일반 풀 생성 접근을 **확대하지 않습니다**. 무허가 생성은 `CreatePool` / `CreateCustomizablePool`을 통해 계속되며, 이는 쌍당 하나의 풀로 유지됩니다. `CreatePermissionedPool`은 화이트리스트된 운영자가 같은 쌍에 대해 여러 풀이 필요한 특정 경우 (예: 다른 초기 가격 또는 출시 코호트)에 존재하며 관리자가 부여한 `Permission` PDA를 보유합니다.
</Note>

## `OpenPositionV2` / `OpenPositionWithToken22Nft`

기존 풀 내에 새로운 포지션을 만듭니다.

**인자**

```
tick_lower_index: i32
tick_upper_index: i32
tick_array_lower_start_index: i32
tick_array_upper_start_index: i32
liquidity:  u128              // 원하는 L (또는 아래 금액을 사용하려면 0)
amount_0_max: u64
amount_1_max: u64
with_metadata: bool           // NFT 메타데이터 작성 (Metaplex)
base_flag: Option<bool>       // true = amount0에 맞춤; false = amount1에 맞춤
```

**계정 (축약)**

| # | 이름 | W | S |
|---|------|---|---|
| 1 | `payer` | W | S |
| 2 | `position_nft_owner` |   |   |
| 3 | `position_nft_mint` | W | S (keypair) |
| 4 | `position_nft_account` | W |   | NFT에 대한 소유자의 ATA. |
| 5 | `metadata_account` | W |   | Metaplex (`with_metadata`인 경우 선택 사항). |
| 6 | `pool_state` | W |   |
| 7 | `protocol_position` |   |   |   |
| 8 | `tick_array_lower` | W |   | 초기화되지 않으면 생성됨. |
| 9 | `tick_array_upper` | W |   | 동일. |
| 10 | `personal_position` | W |   | 여기서 생성됨. |
| 11 | `token_account_0`, `token_account_1` | W |   | 사용자 소스 ATA. |
| 12 | `token_vault_0`, `token_vault_1` | W |   | |
| 13 | `rent`, `system_program`, `token_program` |   |   | |
| 14 | `associated_token_program` |   |   | |
| 15 | `metadata_program` |   |   | 선택 사항. |
| 16 | `token_program_2022` |   |   | V2. |
| 17 | `vault_0_mint`, `vault_1_mint` |   |   | V2. |
| 18 | `tick_array_bitmap_extension` | W |   | V2 (터치된 경우). |

**수학** — [`products/clmm/math`](/ko/products/clmm/math) 참조. `base_flag`가 주어지면, 프로그램은 `liquidity` 또는 `(amount_0_max, amount_1_max)` 중 하나를 실제 `L`과 소비된 실제 토큰 금액으로 해석합니다.

**전제 조건**

- `tick_lower < tick_upper`, 둘 다 `pool.tick_spacing`의 배수, `[MIN_TICK, MAX_TICK]` 범위 내.
- 필요한 틱 배열이 전달되고 초기화됨 (또는 트랜잭션에서 `InitTickArray` CPI를 통해 여기서 생성됨).
- 사용자는 소스 ATA에 최소 `amount_0_max`와 `amount_1_max`를 보유합니다.

**사후 조건**

- `personal_position`이 존재하며, `liquidity` 설정, `fee_growth_inside_last` 스냅샷.
- `tick_lower`와 `tick_upper`의 틱 배열 항목 업데이트 (`liquidity_gross += L`, `liquidity_net ± L`, 수수료 성장 스냅샷 유지).
- 포지션이 범위 내인 경우 `pool_state.liquidity += L` (`tick_lower ≤ tick_current < tick_upper`).

**일반적인 오류** — `InvalidTickIndex`, `NotApproved`, `ZeroAmountSpecified`, `TransactionTooLarge` (틱 배열이 너무 많은 경우).

## `IncreaseLiquidityV2`

이미 열린 포지션에 유동성을 추가합니다.

**인자**

```
liquidity: u128
amount_0_max: u64
amount_1_max: u64
base_flag: Option<bool>
```

**계정** — `OpenPosition`과 유사하지만 NFT 민트 제외 (포지션이 이미 존재함. NFT는 1개 토큰을 보유하는 소유자의 ATA로 전달됨).

**효과**

- 사용자에서 금고로 `amount_0_actual` / `amount_1_actual`을 전송합니다.
- `personal_position.liquidity`와 `pool_state.liquidity` (범위 내인 경우)를 증가시키고, 끝점 틱 `liquidity_gross` / `liquidity_net`을 그에 따라 증가시킵니다.
- **마지막 터치 이후 미지급 수수료 및 보상을 수집**하고 `tokens_fees_owed_{0,1}` / `reward_amount_owed`에 입금합니다. 이들은 `DecreaseLiquidity` 또는 `CollectReward`에서만 지불되며, 증가 시에는 아닙니다.

## `DecreaseLiquidityV2`

포지션에서 유동성을 제거합니다.

**인자**

```
liquidity: u128
amount_0_min: u64
amount_1_min: u64
```

**계정** — `IncreaseLiquidity`와 동일한 형태.

**효과**

- 현재 `sqrt_price_x64`가 주어진 제거된 `L`에 대해 `(amount_0, amount_1)`을 계산합니다.
- 마지막 터치 이후 발생한 수수료/보상을 정산합니다 (`IncreaseLiquidity`와 동일한 패턴).
- `amount_0 + fees_owed_0`과 `amount_1 + fees_owed_1`을 금고에서 사용자에게 전송합니다.
- 유동성 카운터를 감소시킵니다. 새로운 `personal_position.liquidity == 0`이면, 포지션은 `ClosePosition`에 적합합니다.

**슬리피지** — `amount_0_min`과 `amount_1_min`은 출력 측의 Token-2022 전송 수수료를 차감한 사용자가 수락하는 최소값입니다.

## `ClosePosition`

포지션 NFT를 소각하고 `PersonalPositionState`를 닫습니다.

**전제 조건**

- `personal_position.liquidity == 0`.
- `tokens_fees_owed_{0,1} == 0`.
- 모든 보상 카운터 `reward_amount_owed == 0`.

(즉, 모든 것을 수집하고 먼저 0으로 감소시킵니다.)

**효과**

- NFT를 소각합니다.
- NFT 민트 계정과 `personal_position` 계정을 닫고, 임차료를 `payer`에게 환불합니다.

## `SwapV2`

유동성 곡선을 따라갑니다. `is_base_input`에 따라 정확한 입력 **또는** 정확한 출력.

**인자**

```
amount: u64                  // is_base_input=true이면 입력, 그렇지 않으면 출력
other_amount_threshold: u64  // 최소 출력 또는 최대 입력
sqrt_price_limit_x64: u128   // 하드 바운드; 0 ⇒ 무제한
is_base_input: bool
```

**계정 (축약)**

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| 1 | `payer` |   | S | |
| 2 | `amm_config` |   |   | |
| 3 | `pool_state` | W |   | |
| 4 | `input_token_account` | W |   | |
| 5 | `output_token_account` | W |   | |
| 6 | `input_vault` | W |   | |
| 7 | `output_vault` | W |   | |
| 8 | `observation_state` | W |   | |
| 9 | `token_program` |   |   | |
| 10 | `token_program_2022` |   |   | V2. |
| 11 | `memo_program` |   |   | V2 (일부 Token-2022 경로에 필요). |
| 12 | `input_vault_mint`, `output_vault_mint` |   |   | V2. |
| 13 | `tick_array_bitmap_extension` (선택 사항) | W |   | 스왑이 확장으로 들어가면. |
| 14+ | `tick_array` (나머지) | W |   | 스왑 워크의 예상 범위를 포함할 충분한 배열. |

호출자는 예상 스왑 워크를 포함하는 순위가 지정된 틱 배열 목록을 전달합니다. 프로그램은 필요한 만큼 사용합니다. SDK는 `PoolUtils.computeAmountOutFormat` 또는 API의 견적 끝점을 통해 이 목록을 계산합니다.

**전제 조건**

- `pool_state.status`가 스왑을 허용합니다.
- `now >= open_time`.
- `sqrt_price_limit_x64`는 방향에 대해 `sqrt_price_x64`의 올바른 측에 있습니다.

**일반적인 오류** — `ExceededSlippage`, `SqrtPriceLimitOverflow`, `TickArrayNotFound`, `LiquidityInsufficient`.

**`SwapV2`가 내부적으로 수행하는 작업 (호출자가 알아야 할 사항)** (2025년 이후 릴리스):

1. **동적 수수료 할증** — `pool.dynamic_fee_info`가 0이 아니면, 프로그램은 마지막 스왑 이후 순회한 틱 거리를 사용하여 변동성 누적기를 업데이트합니다 ([`products/clmm/fees`](/ko/products/clmm/fees)의 필터/감소 규칙 포함) 그리고 `AmmConfig.trade_fee_rate` 위에 `dynamic_fee_component`를 추가합니다. 총 수수료는 10% (`MAX_FEE_RATE_NUMERATOR / 1_000_000`)로 제한됩니다.
2. **지정가 주문 매칭** — 가격 워크가 공개 지정가 주문을 보유한 틱을 지날 때, 프로그램은 먼저 해당 틱에서 사용 가능한 지정가 주문 유동성을 채웁니다 (`order_phase`로 FIFO), 그 다음 LP 유동성 곡선을 따라갑니다. 채워진 금액은 `tick.unfilled_ratio_x64`와 `tick.part_filled_orders_remaining`을 나중 정산을 위해 업데이트합니다. 주문 자체는 소유자가 `SettleLimitOrder`를 호출할 때까지 미지급 상태로 유지됩니다.
3. **단측 수수료 라우팅** — `pool.fee_on = Token0Only` 또는 `Token1Only`일 때, 스왑 단계는 여전히 동일한 입력-출력 거래를 계산합니다. 수수료는 구성된 측으로 라우팅됩니다. 구성된 수수료 측이 출력인 방향의 경우, 수수료는 스왑 출력에서 차감됩니다 (사용자는 `out − fee`를 받음). 구성된 측이 입력인 방향의 경우, 동작은 `FromInput`과 일치합니다. `PoolState`의 `is_fee_on_input(zero_for_one)`과 `is_fee_on_token0(zero_for_one)`을 참조하세요.

`Swap` (V1)은 `SwapV2`와 동일한 동적 수수료, 단측 수수료 라우팅, 지정가 주문 매칭을 구현합니다. 유일하게 부족한 기능은 Token-2022 지원입니다. 두 금고 모두 클래식 SPL Token이어야 합니다. Token-2022 민트가 있는 풀은 `SwapV2`를 통해 스왑해야 합니다. 애그리게이터와 SDK는 이미 모든 CLMM 레그에 대해 V2를 선호하므로 호출자는 민트 유형에 따라 분기할 필요가 없습니다.

## `OpenLimitOrder`

특정 틱에서 판매 주문을 배치합니다. 주문은 틱별 FIFO 코호트에 앉아 있으며 가격이 지나갈 때 채워집니다.

**인자**

```
nonce_index:    u8       // 사용자가 선택한 nonce 계정 인덱스 (지갑당 0..255)
zero_for_one:   bool     // true: token0을 token1로 판매; false: token1을 token0으로 판매
tick_index:     i32      // pool.tick_spacing의 배수여야 함
amount:         u64      // 입력 토큰 금액
```

**계정 (축약)**

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| 1 | `payer` | W | S | 주문 소유자. 임차료를 지불합니다. |
| 2 | `pool_state` | W |   | |
| 3 | `tick_array` | W |   | `tick_index`를 포함하는 틱 배열. |
| 4 | `limit_order_nonce` | W |   | PDA. `init_if_needed` — 사용자의 이 `nonce_index` 아래 첫 주문에서 생성됨. |
| 5 | `limit_order` | W |   | PDA. 여기서 `init`. |
| 6 | `input_token_account` | W |   | 사용자의 입력 ATA. `frozen` 확인됨. |
| 7 | `output_token_account` | W |   | 사용자의 **출력** ATA. 열기 시 차감되지 않음. 프로그램이 `frozen`이 아님을 확인할 수 있도록 필요함 (즉, 채우기는 나중에 전달될 수 있음). |
| 8 | `input_vault` | W |   | 풀 입력 금고. |
| 9 | `output_vault` | W |   | 풀 출력 금고. `output_token_account` / `output_vault_mint` 확인을 위해 출력 민트를 식별합니다. |
| 10 | `input_vault_mint` |   |   | Token-2022 수수료 처리. |
| 11 | `output_vault_mint` |   |   | 출력 측의 민트 (frozen 확인 쌍). |
| 12 | `input_token_program` |   |   | SPL 또는 Token-2022. |
| 13 | `system_program`, `rent` |   |   | |

<Note>
  **계정 목록 변경 (2026-07 릴리스).** `OpenLimitOrder`는 이제 입력 측 외에도 **출력 측** 계정 — `output_token_account`, `output_vault`, `output_vault_mint` —을 사용합니다. 이들은 검증에만 사용됩니다. 프로그램은 소유자의 입력 **또는** 출력 토큰 계정이 frozen이면 주문을 거부합니다. 이는 채우기가 실제로 소유자의 출력 ATA로 정산될 수 있음을 보장합니다. 이는 허용 목록/기본 frozen Token-2022 민트 (예: 허가된 토큰)에 중요합니다. 여기서 계정이 아직 해제되지 않았을 수 있습니다. 이전 단측 계정 목록에 대해 구축된 클라이언트는 3개의 출력 계정을 추가해야 합니다.
</Note>

**전제 조건**

- `input_token_account`와 `output_token_account` 모두 frozen이 아닙니다 (그렇지 않으면 `NotApproved`).
- `pool_state.status`는 스왑 (비트 4)과 지정가 주문 (비트 5) 작업을 모두 허용합니다 (그렇지 않으면 `NotApproved`).
- `tick_index % pool.tick_spacing == 0` 그리고 `[MIN_TICK, MAX_TICK]` 범위 내.
- `tick_index`는 선택된 방향에 대해 **`pool.tick_current`의 올바른 측**에 있습니다 (token0 판매 → 틱은 현재 위에 있어야 함, 그 반대도 마찬가지). 이미 지나간 틱에서 판매하면 즉시 매칭되며 거부됩니다.

**사후 조건**

- `limit_order`가 존재하며, 열기 시간에 `tick.order_phase`와 `tick.unfilled_ratio_x64`를 스냅샷합니다.
- `tick.orders_amount += amount` (현재 코호트에서).
- `limit_order_nonce.order_nonce += 1`.
- `OpenLimitOrderEvent` 발행됨.

**일반적인 오류** — `NotApproved` (입력 또는 출력 토큰 계정 frozen, 또는 풀이 스왑/지정가 주문 비활성화), `InvalidLimitOrderAmount` (0 또는 풀의 최소값 이하), `InvalidTickIndex` (`[MIN_TICK, MAX_TICK]` 범위 밖, 또는 선택된 방향에 대해 `tick_current`의 잘못된 측), `TickAndSpacingNotMatch` (`tick_index % pool.tick_spacing != 0`), `OrderPhaseSaturated`.

## `IncreaseLimitOrder`

기존 공개 주문에 추가합니다. 주문의 `owner`만 호출할 수 있습니다.

**인자**

```
amount: u64    // 추가 입력 토큰 금액
```

**계정** — `OpenLimitOrder`와 유사하지만 nonce 계정 제외. `limit_order` PDA는 직접 전달됩니다.

**전제 조건**

- `limit_order.owner == signer`.
- 주문은 여전히 동일한 코호트에 있습니다 (`tick.order_phase == limit_order.order_phase`). 코호트가 이미 채우기를 시작했으면, 주문은 부분적으로 정산됩니다. 호출자는 먼저 `DecreaseLimitOrder` 또는 `SettleLimitOrder`를 호출하여 앞으로 이동해야 합니다.

**효과**

- 소유자 ATA에서 `input_vault`로 `amount`를 전송합니다.
- `limit_order.total_amount += amount`; `tick.orders_amount += amount`.

## `DecreaseLimitOrder`

공개 주문을 줄이거나 완전히 취소합니다. 미체결 나머지를 소유자에게 지불하고, 과거 부분 채우기로 이미 정산된 출력도 지불합니다.

**인자**

```
amount:     u64    // 인출할 입력 토큰 금액 (최대 = 미체결 나머지)
amount_min: u64    // 입력 측 인출에 대한 슬리피지 하한
```

**계정** — 입력 및 출력 토큰 측:

| # | 이름 | W | S |
|---|------|---|---|
| 1 | `owner` |   | S |
| 2 | `pool_state` | W |   |
| 3 | `tick_array` | W |   |
| 4 | `limit_order` | W |   |
| 5 | `input_token_account` | W |   |
| 6 | `output_token_account` | W |   |
| 7 | `input_vault` | W |   |
| 8 | `output_vault` | W |   |
| 9 | `input_vault_mint`, `output_vault_mint` |   |   |
| 10 | `token_program`, `token_program_2022` |   |   |

**효과**

- 열기 이후 코호트의 `unfilled_ratio_x64`에서 주문의 채워진 금액을 재계산합니다.
- 채워진 출력을 `output_token_account`로 보냅니다.
- 미체결 입력의 `amount`를 `input_token_account`로 다시 보냅니다.
- `limit_order`를 그에 따라 업데이트합니다. 새로운 미체결 나머지가 0이면, 프로그램은 계정을 닫고 임차료를 `owner`에게 환불합니다.

## `SettleLimitOrder`

주문의 미체결 나머지를 변경하지 않고 채워진 출력 토큰을 소유자에게 푸시합니다. `auto_withdraw` 키퍼가 장기 실행 부분 채우기를 점진적으로 지불하려고 할 때 유용합니다.

**호출자** — 주문의 `owner` 또는 프로그램의 `limit_order_admin` (자동화된 키퍼 루프를 실행하는 오프체인 운영 핫 지갑). 키퍼는 다른 권한이 없습니다. 주문의 `owner` ATA에 채워진 출력을 푸시하는 것 외에는 사용자 자금을 이동할 수 없습니다.

**계정**

| # | 이름 | W | S |
|---|------|---|---|
| 1 | `signer` |   | S | `owner` *또는* `limit_order_admin` |
| 2 | `pool_state` |   |   | |
| 3 | `tick_array` |   |   | |
| 4 | `limit_order` | W |   | |
| 5 | `output_token_account` | W |   | 소유자의 출력 ATA. |
| 6 | `output_vault` | W |   | 풀 출력 금고. |
| 7 | `output_vault_mint` |   |   | |
| 8 | `output_token_program` |   |   | |

**효과**

- `(limit_order.unfilled_ratio_x64, tick.unfilled_ratio_x64)`를 사용하여 누적 출력 미지급액을 계산합니다.
- 델타를 `output_token_account`로 전송합니다.
- `limit_order.settled_output`을 업데이트합니다.
- 주문을 닫지 **않습니다**. 남은 입력에 대해 여전히 공개 상태입니다.

## `CloseLimitOrder`

완전히 소비된 주문 계정을 닫습니다. 임차료는 누가 서명하든 항상 `limit_order.owner`에게 반환됩니다.

**호출자** — `owner` 또는 `limit_order_admin`.

**전제 조건**

- 주문의 미체결 나머지가 0입니다 (`amount == total_amount`가 채워지고 정산되었거나, 소유자가 이전에 주문을 0으로 감소시키고 닫기를 잊었음).

**효과**

- `limit_order`를 닫습니다. 임차료는 `limit_order.owner`로 전송됩니다.

## `CreateDynamicFeeConfig` (관리자)

u16 인덱스 아래에 재사용 가능한 매개변수 집합을 만듭니다.

**인자**

```
index:                       u16
filter_period:               u16   // 초; 예: 30
decay_period:                u16   // 초; 예: 600. filter_period보다 커야 함
reduction_factor:            u16   // 1..10_000; 예: 5_000 = 감소 윈도우당 50% 유지
dynamic_fee_control:         u32   // 1..100_000; 변동성-수수료 곡선의 이득
max_volatility_accumulator:  u32   // 상한
```

**계정**

| # | 이름 | W | S | 설명 |
|---|------|---|---|-------|
| 1 | `owner` | W | S | 하드코딩된 관리자 공개 키. |
| 2 | `dynamic_fee_config` | W |   | PDA, 여기서 `init`. |
| 3 | `system_program` |   |   | |

**일반적인 오류** — `InvalidDynamicFeeConfigParams` (`decay_period <= filter_period` 또는 0 값 필드가 범위를 벗어남).

## `UpdateDynamicFeeConfig` (관리자)

기존 `DynamicFeeConfig`를 수정합니다. 생성 시 구성을 이미 스냅샷한 풀은 **소급하여 업데이트되지 않습니다**. 이 구성을 참조하는 새로 생성된 풀만 새 값을 선택합니다.

**인자** — `CreateDynamicFeeConfig`와 동일한 5개의 보정 필드 (`filter_period`, `decay_period`, `reduction_factor`, `dynamic_fee_control`, `max_volatility_accumulator`). `index`는 생성 시 고정되며 여기서 다시 전달되지 않습니다.

## `CollectProtocolFee` / `CollectFundFee`

CPMM의 `CollectProtocolFee` / `CollectFundFee`와 동일한 형태. 서명자는 `AmmConfig.owner` / `AmmConfig.fund_owner`와 일치해야 합니다. 풀의 금고에서 누적된 프로토콜/펀드 수수료를 수신자에게 정리하고, 해당 `PoolState.protocol_fees_*` / `fund_fees_*` 필드를 0으로 설정합니다.

## `InitializeReward`

풀에 새로운 보상 스트림을 추가합니다. 최대 3개의 스트림이 한 번에 활성화될 수 있습니다.

**인자**

```
open_time:     u64
end_time:      u64
emissions_per_second_x64: u128   // Q64.64
```

**계정**

| # | 이름 | W | S |
|---|------|---|---|
| 1 | `reward_funder` | W | S |
| 2 | `funder_token_account` | W |   |
| 3 | `amm_config` |   |   |
| 4 | `pool_state` | W |   |
| 5 | `operation_state` |   |   | 보상 생성을 제어하는 CLMM 작업 상태 PDA. |
| 6 | `reward_token_mint` |   |   |
| 7 | `reward_token_vault` | W |   | 여기서 `init`. |
| 8 | `reward_token_program` |   |   |
| 9 | `system_program`, `rent` |   |   |

**전제 조건**

- 풀에서 현재 3개 미만의 스트림이 활성화되어 있습니다.
- 펀더는 이 명령어의 일부로 `total_emission = emissions_per_second × (end_time − open_time)` 상당의 보상 토큰을 금고에 입금합니다.
- `operation_state`에 따라 화이트리스트된 보상 민트.

## `SetRewardParams`

기존 보상 스트림의 기간 연장, 충전 또는 배출 속도 변경. 일반적으로 풀 생성자 또는 Raydium 멀티시그에서 호출합니다. 제약은 온체인에 있습니다. 일반적으로 `end_time`을 연장하거나 배출을 증가시킬 수 있지만, 소급하여 축소할 수는 없습니다. `operation_state`의 소유자 목록을 확인하세요.

## `UpdateRewardInfos`

순수 부기 — `reward_growth_global_x64`를 현재 시간으로 정산합니다. `emissions_per_second × Δt / liquidity`를 곱합니다. 모든 유동성 터치 명령어에 의해 내부적으로 호출됩니다. 외부 행위자 (UI, 크랭크)가 때때로 이를 트리거하려고 하기 때문에 독립 실행형 명령어로 노출됩니다.

## `CollectReward`

포지션 소유자가 미지급 보상 토큰을 청구합니다.

**계정**

| # | 이름 | W | S |
|---|------|---|---|
| 1 | `nft_owner` |   | S |
| 2 | `nft_account` |   |   | 포지션 NFT를 보유하는 소유자의 ATA. |
| 3 | `personal_position` | W |   |
| 4 | `pool_state` | W |   |
| 5 | `protocol_position` |   |   |   |
| 6 | `reward_token_vault` | W |   |
| 7 | `recipient_token_account` | W |   |
| 8 | `token_program` |   |   |
| 9 | `token_program_2022` |   |   |

**효과**

- 보상 성장을 정산합니다 (수수료와 동일한 패턴).
- 미지급 금액을 수신자 ATA로 전송하고, `reward_amount_owed[i]`를 0으로 설정합니다.

## 상태 변경 매트릭스

| 명령어 | `pool.liquidity` | `pool.fee_growth_global` | `pool.reward_growth_global` | `personal_position.liquidity` | 틱 배열 |
|-------------|------------------|---------------------------|------------------------------|--------------------------------|------------|
| `CreatePool` | 0 | 0 | — | — | — |
| `OpenPosition` | + (범위 내인 경우) | — | — | 신규 | liquidity_gross/net 추가 |
| `IncreaseLiquidity` | + (범위 내인 경우) | 미지급액 정산 | 미지급액 정산 | + | 조정 |
| `DecreaseLiquidity` | − (범위 내인 경우) | 미지급액 정산 | 미지급액 정산 | − | 조정 |
| `ClosePosition` | — | — | — | 파괴됨 | — |
| `SwapV2` | ± (교차 시) | + | — | — | 교차 & 외부 뒤집기; 지정가 주문 코호트 매칭 |
| `OpenLimitOrder` | — | — | — | — | 대상 틱의 `orders_amount += amount` |
| `IncreaseLimitOrder` | — | — | — | — | `orders_amount += amount` |
| `DecreaseLimitOrder` | — | — | — | — | `orders_amount -=`, 코호트 닫을 수 있음 |
| `SettleLimitOrder` | — | — | — | — | — (틱에서 읽기 전용) |
| `CloseLimitOrder` | — | — | — | — | — |
| `CreateCustomizablePool` | 0 | 0 | — | — | — |
| `UpdateRewardInfos` | — | — | + | — | — |
| `CollectReward` | — | — | 미지급액 정산 | — | — |

## 다음 단계

- [`products/clmm/code-demos`](/ko/products/clmm/code-demos) — 실행 가능한 TypeScript 샘플.
- [`products/clmm/fees`](/ko/products/clmm/fees) — 수수료 및 보상 발생에 대한 세부 정보.
- [`reference/error-codes`](/ko/reference/error-codes) — 완전한 CLMM Anchor 오류 테이블.

출처:

- [`raydium-io/raydium-clmm` — `programs/amm/src/instructions`](https://github.com/raydium-io/raydium-clmm)
- Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`
