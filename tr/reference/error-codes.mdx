---
title: "Hata kodları"
description: "Raydium'un her bir zincir üstü programı için Anchor hata numaralandırmaları, nedenleri ve önerilen UX mesajları ile birlikte."
---

<Info>
  **Bu sayfa yapay zekâ tarafından otomatik olarak çevrilmiştir. İngilizce sürüm esas alınır.**

  [İngilizce sürümü görüntüle →](/reference/error-codes)
</Info>

<Info>
  **Güvenilir kaynak.** Aşağıdaki tablolar, her programın genel Raydium depolarındaki `error.rs` dosyasından yeniden oluşturulur. Bir program yükseltildiğinde ve yeni bir varyant eklendiğinde, tabloyu yeniden düzenlemek yerine çıkarımı yeniden çalıştırın (her tablonun altındaki bağlantı) ve yeni satırları ekleyin — Anchor hata kodları **kaynak sırasına** göre atanır, ad sırasına göre değil, bu nedenle yeniden düzenleme entegratör hata işlemesini bozar.
</Info>

## Anchor hata kodları nasıl çalışır?

Anchor, bir programın `ErrorCode` numaralandırmasının her varyantına `6000` ile başlayan sayısal bir kod atar. Başarısız bir işlem şunları ortaya çıkarır:

- **Sayısal hata kodu** (örn. `0x1771` = 6001) işlem günlüklerinde.
- **Hata adı** (örn. `InvalidOwner`) IDL'den.
- **`#[msg(...)]` dizesi** Anchor tarafından `log_messages` içinde yayılan.

Entegratörler **sayısal koda** göre eşleşme yapmalıdır, mesaj dizesine göre değil (dize bir sürüm çıkarmadan yeniden yazılabilir).

```ts
// Örnek: CPMM kayma hatasını algılayın ve temiz bir UX mesajı gösterin.
const CPMM_EXCEEDED_SLIPPAGE = 6005;

try {
  await sdk.cpmm.swap({ ... });
} catch (err: any) {
  const code = err?.error?.errorCode?.number ?? err?.code;
  if (code === CPMM_EXCEEDED_SLIPPAGE) {
    showToast("Fiyat kayma toleransınızı aştı. Kaymayı artırın veya yeniden deneyin.");
    return;
  }
  throw err;
}
```

## CPMM (Standart AMM) hataları

Program ID: bkz. [reference/program-addresses](/tr/reference/program-addresses#on-chain-programs). Kaynak: `raydium-cp-swap/programs/cp-swap/src/error.rs`.

| Kod | Varyant | `#[msg]` dizesi | Tipik neden | Önerilen UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Onaylanmadı | Çağıran, bir yönetici talimatı için yapılandırılmış yetkilendirici değildir. | "Yalnızca havuz yöneticisi bu işlemi gerçekleştirebilir." |
| 6001 | `InvalidOwner` | Giriş hesabı sahibi program adresi değildir | Geçirilen bir hesap yanlış program tarafından sahiplenilmiştir (genellikle yanlış token programı veya yanlış program PDA'sı). | "İç: hesap sahibi uyuşmazlığı — yenileyin ve yeniden deneyin." |
| 6002 | `EmptySupply` | Giriş token hesabı boş | LP veya token hesabı bakiyesi, pozitif bir miktar gerektiren bir adımda sıfırdır. | "Çekilecek bir şey yok." |
| 6003 | `InvalidInput` | Geçersiz Giriş | Genel kötü argüman (aralık dışı miktar, yanlış bayrak). | "Geçersiz giriş — miktarları kontrol edin ve yeniden deneyin." |
| 6004 | `IncorrectLpMint` | Sağlanan lp token mint adresinin adresi yanlış | Geçirilen LP mint hesabı `pool_state.lp_mint` ile eşleşmez. | "İç: yanlış LP mint — havuz verilerini yenileyin." |
| 6005 | `ExceededSlippage` | İstenen kayma sınırını aşıyor | Yürütülen fiyat, kullanıcının `minAmountOut` / `maxAmountIn` değerinden daha kötüdür. | "Fiyat kayma toleransınızı aştı. Kaymayı artırın veya yeniden deneyin." |
| 6006 | `ZeroTradingTokens` | Verilen havuz token miktarı sıfır işlem tokenine neden olur | Yatırma / çekme matematiği bir tarafı sıfıra yuvarladı (pozisyon çok küçük). | "Miktar bu havuz için minimum değerin altında." |
| 6007 | `NotSupportMint` | Token_2022 mint uzantısını desteklemez | Havuz, güvenli bir şekilde işleyemediği bir Token-2022 uzantısıyla karşılaştı (örn. `TransferHook`, `DefaultAccountState=Frozen`). | "Bu token, Raydium'un CPMM'de desteklemediği bir uzantıya sahip." |
| 6008 | `InvalidVault` | Geçersiz kasa | Geçirilen kasa hesabı, `pool_state` içinde kaydedilen hesapla eşleşmez. | "İç: yanlış kasa — yenileyin ve yeniden deneyin." |
| 6009 | `InitLpAmountTooLess` | İlk lp miktarı çok az (100 miktar lp kilitlenecek olduğundan) | Havuz başlatılırken, hesaplanan LP arzı kalıcı kilit miktarının altındadır. | "İlk likidite çok küçük. Yatırımı artırın." |
| 6010 | `TransferFeeCalculateNotMatch` | TransferFee hesaplaması eşleşmiyor | Token-2022 ücretli mint için gözlenen transfer sonrası miktarı, önceden hesaplanan beklentiyle eşleşmedi. | "Token transfer ücreti işlem sırasında değişti. Yeniden deneyin." |
| 6011 | `MathOverflow` | Matematik taşması | Ara swap / yatırma / ücret hesaplaması taştı. | "Miktar bu havuz için çok büyük." |
| 6012 | `InsufficientVault` | Yetersiz kasa | Havuz kasa bakiyesi istenen çıktıyı karşılamak için çok düşük. | "Bu boyut için havuzda yeterli likidite yok." |
| 6013 | `InvalidFeeModel` | Geçersiz ücret modeli | Yönetici, zincir üstünde reddedilen bir `AmmConfig` parametre kombinasyonu ayarladı. | Yok — yalnızca yönetici yolu. |
| 6014 | `NoFeeCollect` | Ücret sıfır | Bir protokol, fon veya yaratıcı ücret toplama talimatı toplanabilir ücret bulamadı. `CollectCreatorFeePermissionless` için her iki yaratıcı sayacı da sıfırdı. | "Toplanacak bir şey yok." Bunu iyi huylu bir yarış olarak değerlendirin ve durdurun. |

Yeniden oluşturma kaynağı: [github.com/raydium-io/raydium-cp-swap — error.rs](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs).

## CLMM hataları

Program ID: bkz. [reference/program-addresses](/tr/reference/program-addresses#on-chain-programs). Kaynak: `raydium-clmm/programs/amm/src/error.rs`.

| Kod | Varyant | `#[msg]` dizesi | Tipik neden | Önerilen UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Onaylanmadı | Çağıran bu talimat için yapılandırılmış yönetici değildir; **veya** (izin talimatları) imzalayan ne `admin` ne de `permission_pda_admin` değildir; **veya** (`OpenLimitOrder`) giriş/çıkış token hesabı dondurulmuş veya havuzun swap / limit-order devre dışı bırakılmıştır; **veya** (`ClosePosition`, dondurulmuş yol) sağlanan kalan hesap `personal_position.pool_id` değildir. | "Yalnızca havuz yöneticisi bu işlemi gerçekleştirebilir." / "Bu token hesabı dondurulmuş — işlemi gerçekleştirmeden önce çözdürün." / "İç: yanlış pozisyon havuz hesabı." |
| 6001 | `InvalidUpdateConfigFlag` | Geçersiz amm config güncellemesi bayrağı | Yönetici `update_amm_config` için tanınmayan bir `param` değeri geçti. | Yok — yalnızca yönetici yolu. |
| 6002 | `AccountLack` | Hesap eksik | İşlemden gerekli kalan hesap eksik (tipik olarak tick-array veya oracle uzantısı); veya `ClosePosition` dondurulmuş bir NFT'yi ilk kalan hesap olarak havuz olmadan kapatmaya çalışıyor. | "İç: eksik hesap — havuz verilerini yenileyin veya pozisyon kapatma oluşturucusunu güncelleyin." |
| 6003 | `ClosePositionErr` | Likiditeyi kaldırın, ödenmemiş ücretleri ve ödülü toplayın, ardından pozisyon hesabını kapatabilirsiniz | Hala likiditeye, toplanmamış ücretlere veya toplanmamış ödüllere sahip bir pozisyonu kapatmaya çalıştı. | "Pozisyonu kapatmadan önce tüm likiditeyi çekin ve ücretleri/ödülleri talep edin." |
| 6004 | `InvalidTickIndex` | Tick aralık dışında | `tick_lower` veya `tick_upper` `[-443636, 443636]` dışındadır. | "Fiyat aralığı bu havuz için sınırların dışında." |
| 6005 | `TickInvalidOrder` | Alt tick, üst tick'in altında olmalıdır | `tick_lower >= tick_upper`. | "Alt fiyat, üst fiyatın altında olmalıdır." |
| 6006 | `TickLowerOverflow` | Tick, minimum tick'ten büyük veya ona eşit olmalıdır(-443636) | Alt tick taşması. | "Alt fiyat çok düşük." |
| 6007 | `TickUpperOverflow` | Tick, maksimum tick'ten küçük veya ona eşit olmalıdır(443636) | Üst tick taşması. | "Üst fiyat çok yüksek." |
| 6008 | `TickAndSpacingNotMatch` | tick % tick_spacing sıfır olmalıdır | Gönderilen tick, havuzun `tick_spacing` katı değildir. | "Fiyatı en yakın geçerli artışa yapıştırın." |
| 6009 | `InvalidTickArray` | Geçersiz tick array hesabı | Bir tick-array yuvası için yanlış PDA geçildi. | "İç: yanlış tick array — havuz verilerini yenileyin." |
| 6010 | `InvalidTickArrayBoundary` | Geçersiz tick array sınırı | Tick-array indekslendirmesinde bire bir hata. | "İç: tick-array sınırı hatası." |
| 6011 | `SqrtPriceLimitOverflow` | Karekök fiyat sınırı taşması | Çağıran tarafından sağlanan `sqrt_price_limit` geçerli aralık dışında. | "Fiyat sınırı aralık dışında." |
| 6012 | `SqrtPriceX64` | sqrt_price_x64 aralık dışında | Havuzun mevcut karekök fiyatı swap sırasında aralık dışına çıktı. | "Swap'ı yeniden deneyin." |
| 6013 | `LiquiditySubValueErr` | Likidite sub delta L öncekinden daha küçük olmalıdır | Likiditeyi azaltmada iç değişmez ihlali. | "İç: likidite muhasebesi hatası." |
| 6014 | `LiquidityAddValueErr` | Likidite add delta L öncekinden büyük veya ona eşit olmalıdır | Likiditeyi artırmada iç değişmez ihlali. | "İç: likidite muhasebesi hatası." |
| 6015 | `ForbidBothZeroForSupplyLiquidity` | Likidite sağlarken her iki token miktarı da sıfır olmamalıdır | Likiditeyi artırma çağrısı, her iki `amount_0_max` ve `amount_1_max` sıfır ile. | "En az bir token sağlayın." |
| 6016 | `LiquidityInsufficient` | Likidite yetersiz | Pozisyon, çekmeyi karşılamak için yeterli likiditeye sahip değildir. | "Çekme miktarı pozisyon likidite miktarını aşıyor." |
| 6017 | `PriceSlippageCheck` | Fiyat kayma kontrolü | Yürütme fiyatı çağıranın kayma korumasını geçemedi. | "Fiyat kayma toleransınızı aştı. Kaymayı artırın veya yeniden deneyin." |
| 6018 | `TooLittleOutputReceived` | Çok az çıktı alındı | `SwapBaseInput`: çıkış miktarı `other_amount_threshold` altında. | "Kayma aşıldı — minimum çıktı karşılanmadı." |
| 6019 | `TooMuchInputPaid` | Çok fazla giriş ödendi | `SwapBaseOutput`: giriş miktarı `other_amount_threshold` üzerinde. | "Kayma aşıldı — maksimum giriş aşıldı." |
| 6020 | `ZeroAmountSpecified` | Swap özel miktarı sıfır olamaz | Bir swap talimatında sıfır `amount`. | "Sıfırdan büyük bir miktar girin." |
| 6021 | `InvalidInputPoolVault` | Giriş havuz kasası geçersiz | Swap'ın giriş-kasa hesabı havuzun kaydedilen kasasıyla eşleşmez. | "İç: yanlış giriş kasası — havuz verilerini yenileyin." |
| 6022 | `TooSmallInputOrOutputAmount` | Swap giriş veya çıkış miktarı çok küçük | Swap matematiği sıfıra yuvarlandı, tipik olarak çok küçük toz. | "Miktar bu havuzda swap yapmak için çok küçük." |
| 6023 | `NotEnoughTickArrayAccount` | Yeterli tick array hesabı yok | Swap aralığı için yeterli tick-array kalan hesabı sağlanmadı. | "İç: yetersiz tick-array — havuz verilerini yenileyin." |
| 6024 | `InvalidFirstTickArrayAccount` | Geçersiz ilk tick array hesabı | Geçirilen ilk tick-array hesabı mevcut tick'i kapsamaz. | "İç: yanlış ilk tick array — havuz verilerini yenileyin." |
| 6025 | `InvalidRewardIndex` | Geçersiz ödül indeksi | `reward_index` `[0, 2]` dışında. | Yok — yönetici yolu. |
| 6026 | `FullRewardInfo` | İlk ödül token'ı maksimuma ulaştı | Havuz zaten maksimum (3) ödül mint'i yapılandırılmış. | Yok — yönetici yolu. |
| 6027 | `RewardTokenAlreadyInUse` | İlk ödül token'ı zaten kullanımda | Ödül mint'i mevcut olanı çoğaltıyor. | Yok — yönetici yolu. |
| 6028 | `ExceptRewardMint` | Ödül token'ları, son ödül hariç havuz kasa mint'lerinden birini içermelidir | Ön-slot 2, ödül mint'i havuzun kasa mint'lerinden birine eşit olmalıdır. | Yok — yönetici yolu. |
| 6029 | `InvalidRewardInitParam` | Geçersiz ödül başlatma parametresi | Kötü emisyon başlangıç/bitiş zamanı veya saniye başına oran. | Yok — yönetici yolu. |
| 6030 | `InvalidRewardInputAccountNumber` | Geçersiz ödül giriş hesabı sayısı | `CollectReward` için geçirilen yanlış sayıda kalan hesap. | "İç: yanlış hesap sayısı — yenileyin ve yeniden deneyin." |
| 6031 | `InvalidRewardPeriod` | Geçersiz ödül dönemi | Ödül emisyon dönemi geçersiz (`end <= start`, sıfır süre). | Yok — yönetici yolu. |
| 6032 | `NotApproveUpdateRewardEmissions` | Emisyonların değiştirilmesi, önceki döngünün sonundan 72 saat içinde izin verilir | Yönetici, döngü sınırları etrafındaki 72 saatlik pencere dışında emisyonları değiştirmeye çalıştı. | Yok — yönetici yolu. |
| 6033 | `UnInitializedRewardInfo` | Başlatılmamış ödül bilgisi | Ödül yuvası başlatılmamış ancak referans verilmiş. | Yok — yönetici yolu. |
| 6034 | `NotSupportMint` | Token_2022 mint uzantısını desteklemez | CLMM, işleyemediği bir Token-2022 uzantısıyla karşılaştı. | "Bu token, Raydium CLMM'nin desteklemediği bir uzantıya sahip." |
| 6035 | `MissingTickArrayBitmapExtensionAccount` | Eksik tickarray bitmap uzantı hesabı | Swap, temel bitmap tarafından kapsanmayan bir tick aralığını geçti; bitmap uzantı hesabı gerekli. | "İç: eksik tick-array uzantısı — havuz verilerini yenileyin." |
| 6036 | `InsufficientLiquidityForDirection` | Bu yön için yetersiz likidite | Swap yönünde yeterli likidite yok. | "Bu swap boyutu için yetersiz likidite." |
| 6037 | `MaxTokenOverflow` | Maksimum token taşması | Giriş/çıkış miktarı u64'ü aştı. | "Miktar bu havuz için çok büyük." |
| 6038 | `CalculateOverflow` | Hesaplama taşması | Ücret / likidite aritmetiği taşması. | "Miktar bu havuz için çok büyük." |
| 6039 | `TransferFeeCalculateNotMatch` | TransferFee hesaplaması eşleşmiyor | Token-2022 transfer ücreti gözlenen miktarı beklenenle eşleşmedi. | "Token transfer ücreti işlem sırasında değişti. Yeniden deneyin." |
| 6040 | `OrderAlreadyFilled` | Sipariş zaten tamamen dolduruldu, değiştirilemiyor | `IncreaseLimitOrder` / `DecreaseLimitOrder` doldurulmamış kısmı sıfır olan bir siparişte çağrıldı. | "Bu limit siparişi zaten dolduruldu — çıktıyı almak için kapatın." |
| 6041 | `InvalidOrderPhase` | Geçersiz sipariş aşaması | FIFO kohort aşaması artık tick'in mevcut kohortuyla eşleşmeyen bir siparişi değiştirme. | "Siparişin mevcut durumunda bu işlemi gerçekleştiremezsiniz." |
| 6042 | `InvalidLimitOrderAmount` | Geçersiz limit sipariş miktarı | Açık / artırma / azaltma zamanında havuzun minimumunun altında (veya sıfır) sipariş girdisi. | "Sipariş boyutu havuzun minimumunun altında." |
| 6043 | `OrderPhaseSaturated` | Tick sipariş aşaması doymuş | Tick'teki kohort'un `order_phase` sayacı doymuş; mevcut kohortlar kapatılıp devredilene kadar o tick'te başka siparişler açılamaz. | "Bu fiyatta çok fazla aktif sipariş; yakındaki bir tick'i deneyin veya siparişlerin kapatılmasını bekleyin." |
| 6044 | `InvalidDynamicFeeConfigParams` | Geçersiz dinamik ücret config parametreleri | `CreateDynamicFeeConfig` / `UpdateDynamicFeeConfig` reddedildi; veya `CreateCustomizablePool` geçerli bir config olmadan dinamik ücret etkinleştirdi. | Yönetici yolunda yok; kullanıcı yolunda "Dinamik ücret config geçersiz". |
| 6045 | `InvalidFeeOn` | Geçersiz ücret hangi token'da (0, 1 veya 2 olmalıdır) | `CreateCustomizablePool` `collect_fee_on` değerini `{0, 1, 2}` dışında geçti. | "İç: geçersiz ücret modu." |
| 6046 | `ZeroSqrtPrice` | sqrt_price_x64 0'dan büyük olmalıdır | `CreateCustomizablePool` (veya özelleştirilebilir ilk karekök fiyatı kabul eden başka bir yol) `sqrt_price_x64 == 0` ile çağrıldı. | "İlk fiyat sıfırdan büyük olmalıdır." |
| 6047 | `ZeroLiquidity` | Likidite 0'dan büyük olmalıdır | Likidite sağlama yolu `liquidity == 0` ve telafi edici miktar olmadan çağrıldı. | "Likidite miktarı sıfırdan büyük olmalıdır." |
| 6048 | `MissingBaseFlag` | Likidite sıfır olduğunda base_flag gereklidir | Bir açık-pozisyon-miktara-göre yolu `liquidity == 0` hesapladı ve çağıran hangi tarafın taban olduğunu belirsizleştirmek için `base_flag` sağlamadı. | "Sıfır olmayan bir likidite sağlayın veya hangi token'ın taban olduğunu belirtin." |
| 6049 | `MissingMintAccount` | Mint hesabı gerekli ancak sağlanmadı | Token-2022 farkında bir yol, uzantıları doğrulamak ve transfer ücretlerini doğrulamak için gereken giriş/çıkış mint hesabı olmadan çağrıldı. | "İç: eksik mint hesabı — havuz verilerini yenileyin." |
| 6050 | `MissingTokenProgram2022` | Token-2022 programı gerekli ancak sağlanmadı | Yukarıdakiyle aynı SPL-Token-2022 program hesabı için. | "İç: eksik Token-2022 programı — yenileyin ve yeniden deneyin." |

> **Yeniden numaralandırma hakkında not.** CLMM `ErrorCode` numaralandırması bu sürümde yeniden numaralandırıldı: beş eski varyant (`LOK`, `ZeroMintAmount`, `InvalidLiquidity`, `TransactionTooOld`, `InvalidRewardDesiredAmount`) ve birkaç yazım hatası (`Liquitity`, `enought`, `emissiones`) kaldırıldı/düzeltildi ve on bir yeni varyant eklendi. Anchor hataları kaynak sırasına göre numaralandırdığından, `6000` veya sonrasındaki her kod ön-sürüm derlemelerine göre kaymıştır. Sayısal kodları önceki bir sürüme karşı sabit kodlayan istemcilerin yeniden eşlemesi gerekir.

Yeniden oluşturma kaynağı: [github.com/raydium-io/raydium-clmm — error.rs](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs).

## AMM v4, Farm v3 / v5 / v6, LaunchLab hataları

Bu programlar ilgili bölümlerinde belgelenmiştir (bkz. [`products/amm-v4/instructions`](/tr/products/amm-v4/instructions), [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions), [`products/launchlab/instructions`](/tr/products/launchlab/instructions)). Bu programlar Anchor ve düz Solana hata yüzeylerinin bir karışımını kullandığından, hata tabloları burada yerine talimat referansının yanında yer alır. Aşağıdaki kodlar bu bölümler tarafından ayrılmıştır:

| Program | Kod aralığı (Yalnızca Anchor) | Referans |
|---------|--------------------------|-----------|
| AMM v4 | Özel u32 kodları, Anchor tarzı değil | [`products/amm-v4/instructions`](/tr/products/amm-v4/instructions) |
| Farm v3 / v5 | Özel u32 kodları | [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions) |
| Farm v6 | 6000+ (Anchor) | [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions) |
| LaunchLab | 6000+ (Anchor) | [`products/launchlab/instructions`](/tr/products/launchlab/instructions) |

LaunchLab sürümü kodları `6000`–`6021` değişmeden tutar. Eski platform-global-erişim hatalarını kaldırır ve yeni izin-hesabı doğrulama hatasına `6022` atar:

| Kod | Ad | Anlam |
|---|---|---|
| 6018 | `NotEnoughRemainingAccounts` | Gerekli kalan hesap eksik. Buna gerekli `PlatformAllowConfig` ve genişletilmiş CPMM geçiş hesabı seti dahildir. |
| 6022 | `InvalidPlatformAllowConfig` | Sağlanan izin hesabı beklenen platform ve global config çiftini kodlamaz. |

Eski `PlatformGlobalAccessDenied` (`6022`) ve `InvalidPlatformGlobalAccess` (`6023`) varyantları kaldırılmıştır. Önceki IDL'yi kullanarak `6022` kodunu çözmeyin.

## SDK hatalarını program hatalarıyla eşleştirme

Resmi TypeScript SDK, zincir üstü hataları `SendTransactionError` ve Anchor programları için `AnchorError` içine sarar:

```ts
import { AnchorError } from "@coral-xyz/anchor";

try {
  await sdk.cpmm.swap({ ... });
} catch (err) {
  if (err instanceof AnchorError) {
    console.log(err.error.errorCode.number, err.error.errorCode.code, err.error.errorMessage);
    // 6005 ExceededSlippage "Exceeds desired slippage limit"
  } else {
    // Ham SendTransactionError — "custom program error: 0x..." satırı için err.logs'u inceleyin.
  }
}
```

İstemci tarafında Anchor kullanmıyorsanız, işlem günlüklerini ayrıştırın:

```
Program <ProgramID> invoke [1]
Program log: AnchorError caused by account: pool_state. Error Code: ExceededSlippage. Error Number: 6005. Error Message: Exceeds desired slippage limit.
```

`Error Number: (\d+)` deseni Anchor sürümleri arasında kararlıdır ve eşleştirmek için güvenlidir.

## Bu tabloları yeniden oluşturma

Bir program yükseltildiğinde ve yeni bir hata eklediğinde, kaynaktan yeniden çıkarın:

```bash
# Depoyu klonlayın ve hata numaralandırmasını sırasıyla grep'leyin.
git clone --depth=1 https://github.com/raydium-io/raydium-cp-swap
awk '/#\[msg\(/{ gsub(/^[ \t]*#\[msg\("|"\)\][ \t]*/,""); m=$0; getline; gsub(/,/,""); gsub(/^[ \t]+/,""); print "6000+++\t" $0 "\t" m }' \
  raydium-cp-swap/programs/cp-swap/src/error.rs
# 6000+++' yerine monoton artan 6000,6001,... yazın. Yukarıdaki tabloya yeni satırlar ekleyin.
```

Bir yeni varyant eklendiğinde her zaman [`reference/changelog`](/tr/reference/changelog) güncelleyin, böylece SDK'yı yükselten entegratörler hata işleyicilerini yenilemeyi bilirler.

Kaynaklar:

- [raydium-cp-swap error.rs (mainnet CPMM)](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)
- [raydium-clmm error.rs (mainnet CLMM)](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)
