---
title: "Hata kodları"
description: "Raydium'un her zincir üstü programı için Anchor hata numaralandırmaları, nedenleri ve önerilen UX mesajları ile birlikte."
---

<Info>
  **Bu sayfa yapay zekâ tarafından otomatik olarak çevrilmiştir. İngilizce sürüm esas alınır.**

  [İngilizce sürümü görüntüle →](/reference/error-codes)
</Info>

## Anchor hata kodları nasıl çalışır?

Anchor, bir programın `ErrorCode` numaralandırmasının her varyantına `6000` ile başlayan sayısal bir kod atar. Başarısız bir işlem şunları ortaya çıkarır:

- **Sayısal hata kodu** (örn. `0x1771` = 6001) işlem günlüklerinde.
- **Hata adı** (örn. `InvalidOwner`) IDL'den.
- **`#[msg(...)]` dizesi** Anchor tarafından `log_messages` içinde yayılan.

Entegratörler **sayısal koda** göre eşleştirme yapmalıdır, mesaj dizesine göre değil (dize sürüm yükseltmesi olmadan yeniden yazılabilir).

```ts
// Örnek: CPMM kayma hatasını algıla ve temiz bir UX mesajı göster.
const CPMM_EXCEEDED_SLIPPAGE = 6005;

try {
  await sdk.cpmm.swap({ ... });
} catch (err: any) {
  const code = err?.error?.errorCode?.number ?? err?.code;
  if (code === CPMM_EXCEEDED_SLIPPAGE) {
    showToast("Price moved past your slippage tolerance. Increase slippage or retry.");
    return;
  }
  throw err;
}
```

## CPMM (Standart AMM) hataları

Program ID: bkz. [reference/program-addresses](/tr/reference/program-addresses#on-chain-programs). Kaynak: `raydium-cp-swap/programs/cp-swap/src/error.rs`.

| Kod | Varyant | `#[msg]` dizesi | Tipik neden | Önerilen UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Çağıran, bir yönetici talimatı için yapılandırılmış yetkilendirme değildir. | "Yalnızca havuz yöneticisi bu işlemi gerçekleştirebilir." |
| 6001 | `InvalidOwner` | Input account owner is not the program address | Geçirilen bir hesap yanlış program tarafından sahiplenilmiştir (genellikle yanlış token programı veya yanlış program PDA'sı). | "İç hata: hesap sahibi uyuşmazlığı — yenile ve yeniden dene." |
| 6002 | `EmptySupply` | Input token account empty | LP veya token hesabı bakiyesi, pozitif bir miktar gerektiren bir adımda sıfırdır. | "Çekilecek bir şey yok." |
| 6003 | `InvalidInput` | InvalidInput | Genel kötü argüman (aralık dışı miktar, yanlış bayrak). | "Geçersiz giriş — miktarları kontrol et ve yeniden dene." |
| 6004 | `IncorrectLpMint` | Address of the provided lp token mint is incorrect | Geçirilen LP mint hesabı `pool_state.lp_mint` ile eşleşmemektedir. | "İç hata: yanlış LP mint — havuz verilerini yenile." |
| 6005 | `ExceededSlippage` | Exceeds desired slippage limit | Yürütülen fiyat, kullanıcının `minAmountOut` / `maxAmountIn` değerinden daha kötüdür. | "Fiyat kayma toleransını aştı. Kaymayı artır veya yeniden dene." |
| 6006 | `ZeroTradingTokens` | Given pool token amount results in zero trading tokens | Yatırma / çekme matematiği bir tarafı sıfıra yuvarladı (pozisyon çok küçük). | "Miktar bu havuz için minimum değerin altındadır." |
| 6007 | `NotSupportMint` | Not support token_2022 mint extension | Havuz, güvenli bir şekilde işleyemediği bir Token-2022 uzantısı ile karşılaştı (örn. `TransferHook`, `DefaultAccountState=Frozen`). | "Bu token, Raydium'un CPMM'de desteklemediği bir uzantıya sahiptir." |
| 6008 | `InvalidVault` | invaild vault | Geçirilen vault hesabı `pool_state` içinde kaydedilen hesapla eşleşmemektedir. | "İç hata: yanlış vault — yenile ve yeniden dene." |
| 6009 | `InitLpAmountTooLess` | Init lp amount is too less(Because 100 amount lp will be locked) | Havuz başlatılması sırasında, hesaplanan LP arzı kalıcı kilit miktarının altındadır. | "İlk likidite çok küçük. Yatırımı artır." |
| 6010 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Token-2022 ücretli mint için gözlenen transfer sonrası miktar, önceden hesaplanan beklentiyle eşleşmemektedir. | "Token transfer ücreti işlem sırasında değişti. Yeniden dene." |
| 6011 | `MathOverflow` | Math overflow | Bir ara swap / yatırma / ücret hesaplaması taştı. | "Miktar bu havuz için çok büyüktür." |
| 6012 | `InsufficientVault` | Insufficient vault | Havuz vault bakiyesi istenen çıktıyı karşılamak için çok düşüktür. | "Bu boyut için havuzda yeterli likidite yok." |
| 6013 | `InvalidFeeModel` | Invalid fee model | Yönetici, zincir üstünde reddedilen bir `AmmConfig` parametre kombinasyonu ayarladı. | Yok — yalnızca yönetici yolu. |
| 6014 | `NoFeeCollect` | Fee is zero | `collect_protocol_fee` / `collect_fund_fee` sıfır toplanabilir ücretlerle çağrıldı. | Yok — yönetici yolu; araçlar sessizce yok saymalıdır. |

Yeniden oluşturma kaynağı: [github.com/raydium-io/raydium-cp-swap — error.rs](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs).

## CLMM hataları

Program ID: bkz. [reference/program-addresses](/tr/reference/program-addresses#on-chain-programs). Kaynak: `raydium-clmm/programs/amm/src/error.rs`.

| Kod | Varyant | `#[msg]` dizesi | Tipik neden | Önerilen UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Çağıran bu talimat için yapılandırılmış yönetici değildir; **veya** (izin talimatları) imzalayan ne `admin` ne de `permission_pda_admin` değildir; **veya** (`OpenLimitOrder`) giriş/çıkış token hesabı dondurulmuş veya havuzda swap / limit-order devre dışı bırakılmıştır. | "Yalnızca havuz yöneticisi bu işlemi gerçekleştirebilir." / "Bu token hesabı dondurulmuş — siparişi vermeden önce çöz." |
| 6001 | `InvalidUpdateConfigFlag` | invalid update amm config flag | Yönetici `update_amm_config` için tanınmayan bir `param` değeri geçti. | Yok — yalnızca yönetici yolu. |
| 6002 | `AccountLack` | Account lack | İşlemde gerekli kalan hesap eksik (tipik olarak tick-array veya oracle uzantısı). | "İç hata: eksik hesap — havuz verilerini yenile." |
| 6003 | `ClosePositionErr` | Remove liquidity, collect fees owed and reward then you can close position account | Hala likiditeye, toplanmamış ücretlere veya toplanmamış ödüllere sahip bir pozisyonu kapatmaya çalışıldı. | "Pozisyonu kapatmadan önce tüm likiditeyi çek ve ücretleri/ödülleri talep et." |
| 6004 | `InvalidTickIndex` | Tick out of range | `tick_lower` veya `tick_upper` `[-443636, 443636]` aralığının dışındadır. | "Fiyat aralığı bu havuz için sınırların dışındadır." |
| 6005 | `TickInvalidOrder` | The lower tick must be below the upper tick | `tick_lower >= tick_upper`. | "Alt fiyat üst fiyattan düşük olmalıdır." |
| 6006 | `TickLowerOverflow` | The tick must be greater, or equal to the minimum tick(-443636) | Alt tick taşması. | "Alt fiyat çok düşüktür." |
| 6007 | `TickUpperOverflow` | The tick must be lesser than, or equal to the maximum tick(443636) | Üst tick taşması. | "Üst fiyat çok yüksektir." |
| 6008 | `TickAndSpacingNotMatch` | tick % tick_spacing must be zero | Gönderilen tick, havuzun `tick_spacing` değerinin katı değildir. | "Fiyatı en yakın geçerli artışa yasla." |
| 6009 | `InvalidTickArray` | Invalid tick array account | Bir tick-array yuvası için yanlış PDA geçti. | "İç hata: yanlış tick array — havuz verilerini yenile." |
| 6010 | `InvalidTickArrayBoundary` | Invalid tick array boundary | Tick-array indekslendirmesinde bire bir hata. | "İç hata: tick-array sınırı hatası." |
| 6011 | `SqrtPriceLimitOverflow` | Square root price limit overflow | Çağıran tarafından sağlanan `sqrt_price_limit` geçerli aralığın dışındadır. | "Fiyat sınırı aralığın dışındadır." |
| 6012 | `SqrtPriceX64` | sqrt_price_x64 out of range | Havuzun mevcut sqrt fiyatı swap sırasında aralığın dışına çıktı. | "Swap'ı yeniden dene." |
| 6013 | `LiquiditySubValueErr` | Liquidity sub delta L must be smaller than before | Likidite azaltmada iç değişmez ihlali. | "İç hata: likidite muhasebesi hatası." |
| 6014 | `LiquidityAddValueErr` | Liquidity add delta L must be greater, or equal to before | Likidite artırmada iç değişmez ihlali. | "İç hata: likidite muhasebesi hatası." |
| 6015 | `ForbidBothZeroForSupplyLiquidity` | Both token amount must not be zero while supply liquidity | Hem `amount_0_max` hem de `amount_1_max` sıfır olan likidite artırma çağrısı. | "En az bir token sağla." |
| 6016 | `LiquidityInsufficient` | Liquidity insufficient | Pozisyon çekişi karşılamak için yeterli likiditeye sahip değildir. | "Çekme miktarı pozisyon likidite miktarını aşıyor." |
| 6017 | `PriceSlippageCheck` | Price slippage check | Yürütme fiyatı çağıranın kayma korumasını geçemedi. | "Fiyat kayma toleransını aştı. Kaymayı artır veya yeniden dene." |
| 6018 | `TooLittleOutputReceived` | Too little output received | `SwapBaseInput`: çıkış miktarı `other_amount_threshold` altında. | "Kayma aşıldı — minimum çıkış karşılanmadı." |
| 6019 | `TooMuchInputPaid` | Too much input paid | `SwapBaseOutput`: giriş miktarı `other_amount_threshold` üstünde. | "Kayma aşıldı — maksimum giriş aşıldı." |
| 6020 | `ZeroAmountSpecified` | Swap special amount can not be zero | Bir swap talimatında sıfır `amount`. | "Sıfırdan büyük bir miktar gir." |
| 6021 | `InvalidInputPoolVault` | Input pool vault is invalid | Swap'ın giriş-vault hesabı havuzun kaydedilen vault'u ile eşleşmemektedir. | "İç hata: yanlış giriş vault — havuz verilerini yenile." |
| 6022 | `TooSmallInputOrOutputAmount` | Swap input or output amount is too small | Swap matematiği sıfıra yuvarlandı, tipik olarak çok küçük toz. | "Miktar bu havuzda swap yapmak için çok küçüktür." |
| 6023 | `NotEnoughTickArrayAccount` | Not enough tick array account | Swap aralığı için yeterli tick-array kalan hesabı sağlanmadı. | "İç hata: yetersiz tick-array — havuz verilerini yenile." |
| 6024 | `InvalidFirstTickArrayAccount` | Invalid first tick array account | Geçirilen ilk tick-array hesabı mevcut tick'i kapsamaz. | "İç hata: yanlış ilk tick array — havuz verilerini yenile." |
| 6025 | `InvalidRewardIndex` | Invalid reward index | `reward_index` `[0, 2]` aralığının dışındadır. | Yok — yönetici yolu. |
| 6026 | `FullRewardInfo` | The init reward token reach to the max | Havuzda zaten maksimum (3) ödül mint'i yapılandırılmıştır. | Yok — yönetici yolu. |
| 6027 | `RewardTokenAlreadyInUse` | The init reward token already in use | Ödül mint'i mevcut olanı çoğaltır. | Yok — yönetici yolu. |
| 6028 | `ExceptRewardMint` | The reward tokens must contain one of pool vault mint except the last reward | Slot 2 öncesi, ödül mint'i havuzun vault mint'lerinden birine eşit olmalıdır. | Yok — yönetici yolu. |
| 6029 | `InvalidRewardInitParam` | Invalid reward init param | Kötü emisyon başlangıç/bitiş zamanı veya saniye başına oran. | Yok — yönetici yolu. |
| 6030 | `InvalidRewardInputAccountNumber` | Invalid collect reward input account number | `CollectReward` için geçirilen yanlış sayıda kalan hesap. | "İç hata: yanlış hesap sayısı — yenile ve yeniden dene." |
| 6031 | `InvalidRewardPeriod` | Invalid reward period | Ödül emisyon dönemi geçersiz (`end <= start`, sıfır süre). | Yok — yönetici yolu. |
| 6032 | `NotApproveUpdateRewardEmissions` | Modification of emissions is allowed within 72 hours from the end of the previous cycle | Yönetici, döngü sınırları etrafındaki 72 saatlik pencere dışında emisyonları değiştirmeye çalıştı. | Yok — yönetici yolu. |
| 6033 | `UnInitializedRewardInfo` | uninitialized reward info | Ödül yuvası başlatılmamış ancak referans verilmiş. | Yok — yönetici yolu. |
| 6034 | `NotSupportMint` | Not support token_2022 mint extension | CLMM, işleyemediği bir Token-2022 uzantısı ile karşılaştı. | "Bu token, Raydium CLMM'nin desteklemediği bir uzantıya sahiptir." |
| 6035 | `MissingTickArrayBitmapExtensionAccount` | Missing tickarray bitmap extension account | Swap, temel bitmap tarafından kapsanmayan bir tick aralığını geçti; uzantı bitmap hesabı gerekli. | "İç hata: eksik tick-array uzantısı — havuz verilerini yenile." |
| 6036 | `InsufficientLiquidityForDirection` | Insufficient liquidity for this direction | Swap yönünde yeterli likidite yok. | "Bu swap boyutu için yetersiz likidite." |
| 6037 | `MaxTokenOverflow` | Max token overflow | Giriş/çıkış miktarı u64'ü aştı. | "Miktar bu havuz için çok büyüktür." |
| 6038 | `CalculateOverflow` | Calculate overflow | Ücret / likidite aritmetiği taşması. | "Miktar bu havuz için çok büyüktür." |
| 6039 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Token-2022 transfer ücreti gözlenen miktar beklenenle eşleşmemektedir. | "Token transfer ücreti işlem sırasında değişti. Yeniden dene." |
| 6040 | `OrderAlreadyFilled` | Order already fully filled, cannot modify | `IncreaseLimitOrder` / `DecreaseLimitOrder` doldurulmamış kısmı sıfır olan bir siparişte çağrıldı. | "Bu limit siparişi zaten doldurulmuş — çıktıyı almak için kapatın." |
| 6041 | `InvalidOrderPhase` | Invalid order phase | FIFO kohort fazı artık tick'in mevcut kohortu ile eşleşmeyen bir siparişi değiştirme. | "Bu siparişin mevcut durumunda bu işlemi gerçekleştiremezsiniz." |
| 6042 | `InvalidLimitOrderAmount` | Invalid limit order amount | Sipariş girdisi havuzun minimumunun altında (veya sıfır) açma / artırma / azaltma zamanında. | "Sipariş boyutu havuzun minimumunun altındadır." |
| 6043 | `OrderPhaseSaturated` | Tick order phase saturated | Tick'teki kohort'un `order_phase` sayacı doymuştur; mevcut kohortlar kapatılıp devredilene kadar o tick'te başka siparişler açılamaz. | "Bu fiyatta çok fazla aktif sipariş; yakındaki bir tick'i dene veya siparişlerin kapatılmasını bekle." |
| 6044 | `InvalidDynamicFeeConfigParams` | Invalid dynamic fee config params | `CreateDynamicFeeConfig` / `UpdateDynamicFeeConfig` reddedildi; veya `CreateCustomizablePool` geçerli bir yapılandırma olmadan dinamik ücret etkinleştirdi. | Yönetici yolunda yok; kullanıcı yolunda "Dinamik ücret yapılandırması geçersiz". |
| 6045 | `InvalidFeeOn` | Invalid fee on which token (must be 0, 1, or 2) | `CreateCustomizablePool` `collect_fee_on` değerini `{0, 1, 2}` dışında geçti. | "İç hata: geçersiz ücret modu." |
| 6046 | `ZeroSqrtPrice` | sqrt_price_x64 must be greater than 0 | `CreateCustomizablePool` (veya özelleştirilebilir ilk sqrt fiyatını kabul eden başka bir yol) `sqrt_price_x64 == 0` ile çağrıldı. | "İlk fiyat sıfırdan büyük olmalıdır." |
| 6047 | `ZeroLiquidity` | liquidity must be greater than 0 | Likidite sağlama yolu `liquidity == 0` ve telafi edici miktar olmadan çağrıldı. | "Likidite miktarı sıfırdan büyük olmalıdır." |
| 6048 | `MissingBaseFlag` | base_flag is required when liquidity is zero | Bir açık-pozisyon-miktara-göre yolu `liquidity == 0` hesapladı ve çağıran hangi tarafın taban olduğunu belirsizleştirmek için `base_flag` sağlamadı. | "Sıfır olmayan bir likidite sağla veya hangi token'ın taban olduğunu belirt." |
| 6049 | `MissingMintAccount` | Mint account is required but not provided | Token-2022 farkında bir yol, uzantıları doğrulamak ve transfer ücretlerini almak için gereken giriş/çıkış mint hesabı olmadan çağrıldı. | "İç hata: eksik mint hesabı — havuz verilerini yenile." |
| 6050 | `MissingTokenProgram2022` | Token-2022 program is required but not provided | Yukarıdakiyle aynı SPL-Token-2022 program hesabı için. | "İç hata: eksik Token-2022 programı — yenile ve yeniden dene." |

> **Yeniden numaralandırma hakkında not.** CLMM `ErrorCode` numaralandırması bu sürümde yeniden numaralandırıldı: beş eski varyant (`LOK`, `ZeroMintAmount`, `InvalidLiquidity`, `TransactionTooOld`, `InvalidRewardDesiredAmount`) ve birkaç yazım hatası (`Liquitity`, `enought`, `emissiones`) kaldırıldı/düzeltildi ve on bir yeni varyant eklendi. Anchor hataları kaynak sırasına göre numaralandırdığından, `6000` veya sonrasındaki her kod ön-sürüm derlemelerine göre kaymıştır. Sayısal kodları önceki bir sürüme karşı sabit kodlayan istemcilerin yeniden eşlemesi gerekir.

Yeniden oluşturma kaynağı: [github.com/raydium-io/raydium-clmm — error.rs](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs).

## AMM v4, Farm v3 / v5 / v6, LaunchLab hataları

Bu programlar ilgili bölümlerinde belgelenmiştir (bkz. [`products/amm-v4/instructions`](/tr/products/amm-v4/instructions), [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions), [`products/launchlab/instructions`](/tr/products/launchlab/instructions)). Bu programlar Anchor ve düz Solana hata yüzeylerinin bir karışımını kullandığından, hata tabloları burada yerine talimat referansının yanında yer alır. Aşağıdaki kodlar bu bölümler tarafından ayrılmıştır:

| Program | Kod aralığı (Yalnızca Anchor) | Referans |
|---------|--------------------------|-----------|
| AMM v4 | Özel u32 kodları, Anchor tarzı değil | [`products/amm-v4/instructions`](/tr/products/amm-v4/instructions) |
| Farm v3 / v5 | Özel u32 kodları | [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions) |
| Farm v6 | 6000+ (Anchor) | [`products/farm-staking/instructions`](/tr/products/farm-staking/instructions) |
| LaunchLab | 6000+ (Anchor) | [`products/launchlab/instructions`](/tr/products/launchlab/instructions) |

## SDK hatalarını program hatalarına eşleme

Resmi TypeScript SDK, zincir üstü hataları `SendTransactionError` ve Anchor programları için `AnchorError` içine sarar:

```ts
import { AnchorError } from "@coral-xyz/anchor";

try {
  await sdk.cpmm.swap({ ... });
} catch (err) {
  if (err instanceof AnchorError) {
    console.log(err.error.errorCode.number, err.error.errorCode.code, err.error.errorMessage);
    // 6005 ExceededSlippage "Exceeds desired slippage limit"
  } else {
    // Ham SendTransactionError — "custom program error: 0x..." satırı için err.logs'u incele.
  }
}
```

İstemci tarafında Anchor kullanmıyorsanız, işlem günlüklerini ayrıştırın:

```
Program <ProgramID> invoke [1]
Program log: AnchorError caused by account: pool_state. Error Code: ExceededSlippage. Error Number: 6005. Error Message: Exceeds desired slippage limit.
```

`Error Number: (\d+)` deseni Anchor sürümleri arasında kararlıdır ve eşleştirmek için güvenlidir.

## Bu tabloları yeniden oluşturma

Bir program yükseltildiğinde ve yeni bir hata eklediğinde, kaynaktan yeniden çıkart:

```bash
# Depoyu klonla ve hata numaralandırmasını sırasıyla grep'le.
git clone --depth=1 https://github.com/raydium-io/raydium-cp-swap
awk '/#\[msg\(/{ gsub(/^[ \t]*#\[msg\("|"\)\][ \t]*/,""); m=$0; getline; gsub(/,/,""); gsub(/^[ \t]+/,""); print "6000+++\t" $0 "\t" m }' \
  raydium-cp-swap/programs/cp-swap/src/error.rs
# 6000+++' yerine monoton artan 6000,6001,... koy. Yukarıdaki tabloya yeni satırlar ekle.
```

Bir yeni varyant eklendiğinde her zaman [`reference/changelog`](/tr/reference/changelog) güncelle, böylece SDK'yı yükselten entegratörler hata işleyicilerini yenilemeyi bilirler.

Kaynaklar:

- [raydium-cp-swap error.rs (mainnet CPMM)](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)
- [raydium-clmm error.rs (mainnet CLMM)](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)
