---
title: "LaunchLab hesapları"
description: "PoolState (başlatma başına kök), temel ve alıntı kasaları, yetki PDA'sı ve başlatma başına hesapların küçük grafiği. Protokol düzeyindeki GlobalConfig ve platform düzeyindeki PlatformConfig kendi sayfalarında yer alır."
---

<Info>
  **Bu sayfa yapay zekâ tarafından otomatik olarak çevrilmiştir. İngilizce sürüm esas alınır.**

  [İngilizce sürümü görüntüle →](/products/launchlab/accounts)
</Info>

<Info>
  Bu sayfa **başlatma başına** hesap grafiğini belgeler: `PoolState` (bir başlatma için kök durum hesabı), iki kasası, yetki PDA'sı ve başlatma kapatıldığında kazandığı mezuniyet sonrası referansları.

  Her başlatmayı sınırlayan protokol düzeyindeki yapılandırma için bkz. [`products/launchlab/global-config`](/tr/products/launchlab/global-config). Platform başına ek katman için bkz. [`products/launchlab/platform-config`](/tr/products/launchlab/platform-config). Vesting hesapları (`VestingSchedule` on `PoolState`, `VestingRecord` başına yararlanıcı) için bkz. [`products/launchlab/vesting`](/tr/products/launchlab/vesting).
</Info>

## Hesap envanteri

| Hesap | Sahibi | Amaç |
|---------|-------|---------|
| [`GlobalConfig`](/tr/products/launchlab/global-config) | LaunchLab programı | Protokol düzeyindeki kurallar: ücretler, arz tabanları, göç cüzdanları. `(curve_type, index)` başına bir tane. |
| [`PlatformConfig`](/tr/products/launchlab/platform-config) | LaunchLab programı | Platform başına ek katman: marka, platform ücreti, mezuniyet sırasında NFT bölünmesi, eğri şekli beyaz listesi. |
| `PoolState` | LaunchLab programı | Başlatma başına kök durum: mintler, kasalar, eğri parametreleri, satılan sayaçlar, ücret sayaçları, vesting takvimi, mezuniyet durumu. |
| `authority` | LaunchLab programı | Tohum `[b"vault_auth_seed"]` ile türetilen tek PDA, tüm başlatmalar arasında kasalara sahip ve mezuniyet sonrası CPI'yi imzalar. |
| `base_vault` | SPL Token / Token-2022 | Satılmayan temel tokenleri tutan başlatma başına kasa. |
| `quote_vault` | SPL Token | Birikmiş alıntı tokenlerini tutan başlatma başına kasa. |
| [`VestingRecord`](/tr/products/launchlab/vesting) | LaunchLab programı | Başına yararlanıcı cliff + doğrusal kilit açma kaydı. İsteğe bağlı. |
| `creator_fee_vault` | SPL Token | Başına yaratıcı + başına alıntı-mint kasası, birikmiş yaratıcı ücretlerini tutar, `ClaimCreatorFee` tarafından temizlenir. |
| `platform_fee_vault` | SPL Token | Başına platform + başına alıntı-mint kasası, birikmiş platform ücretlerini tutar, `ClaimPlatformFeeFromVault` tarafından temizlenir. |
| [`PlatformAllowConfig`](/tr/products/launchlab/platform-config) | LaunchLab programı | Bir `(PlatformConfig, GlobalConfig)` çifti için isteğe bağlı platform tarafından yönetilen izin. Yalnızca platform kısıtlamayı etkinleştirdiğinde kontrol edilir. |
| (mezuniyet sonrası) `cpmm_pool_state` | CPMM programı | Her yeni başlatma için `MigrateToCpswap` tarafından oluşturulan havuz. Eski durum hala bir AMM v4 mezuniyetine başvurabilir. |
| (mezuniyet sonrası) Ücret Anahtarı NFT | LP-Lock programı | 2026-08-14 yükseltmesinden önce, platform ve yaratıcı ölçekleri ayrı Ücret Anahtarları üretebilirdi. Bundan sonra yürütülen göçler her iki ölçeği tek bir platform Ücret Anahtarında birleştirir. |

SDK'nın `raydium.launchpad.getLaunchById` işlevi `PoolState` artı başlatmanın mezun olup olmadığını gösteren bir bayrak döndürür; eğer mezun olduysa, göç sonrası havuz kimliği dahil edilir.

## `PoolState`

Başlatma başına kök durum. Aşağıdaki alan adları zincir üstü Rust yapısıyla eşleşir (`states/pool.rs`); bazı değerler okunabilirlik için basitleştirilmiştir — tam bellek düzeni için kaynağa başvurun.

```rust
// states/pool.rs (kısaltılmış)
pub struct PoolState {
    pub epoch:                     u64,        // recent_epoch tracker
    pub auth_bump:                 u8,
    pub status:                    u8,         // PoolStatus enum
    pub base_decimals:             u8,
    pub quote_decimals:            u8,
    pub migrate_type:              u8,         // 0 = AMM v4, 1 = CPMM
    pub supply:                    u64,        // total base tokens (no decimals)
    pub total_base_sell:           u64,        // base supply allocated to the curve
    pub virtual_base:              u64,        // virtual-reserve seed
    pub virtual_quote:             u64,        // virtual-reserve seed
    pub real_base:                 u64,        // base sold so far
    pub real_quote:                u64,        // quote raised so far (excl. fees)
    pub total_quote_fund_raising:  u64,        // graduation threshold in quote

    pub quote_protocol_fee:        u64,        // accrued, pending CollectFee
    pub platform_fee:              u64,        // accrued, pending ClaimPlatformFee
    pub migrate_fee:               u64,        // accrued, pending CollectMigrateFee
    pub vesting_schedule:          VestingSchedule,

    pub global_config:             Pubkey,
    pub platform_config:           Pubkey,
    pub creator:                   Pubkey,
    pub base_mint:                 Pubkey,
    pub quote_mint:                Pubkey,
    pub base_vault:                Pubkey,
    pub quote_vault:               Pubkey,

    pub amm_creator_fee_on:        u8,         // 0 = QuoteToken, 1 = BothToken (CPMM-only)
    pub token_program_flag:        u8,         // bit flags for SPL Token vs Token-2022
    pub padding:                   [u8; 54],
}

#[derive(Default, Debug, Clone)]
pub struct VestingSchedule {
    pub total_locked_amount:    u64,
    pub cliff_period:           u64,
    pub unlock_period:          u64,
    pub start_time:             u64,           // set at graduation
    pub allocated_share_amount: u64,           // running sum across VestingRecords
}
```

`PoolStatus` değerleri (Anchor IDL'den):

```
0 = Funding         — alım/satımları kabul eder; quote_reserve_target'ın altında
1 = Migrate         — quote_reserve_target'a ulaşıldı; MigrateTo* bekliyor
2 = Migrated        — MigrateTo* zaten yürütüldü; vesting saati çalışıyor
```

Entegratör tarafından kullanılan alanlar:

- **`status`** — üç değer, monoton (Funding → Migrate → Migrated). Okumalar her zaman güvenli; yazımlar kapılı.
- **`real_base`**, **`real_quote`** — mevcut eğri durumu. `virtual_base` / `virtual_quote` ile birlikte kasalara dokunmadan spot fiyatı hesaplamak için yeterlidirler. Bkz. [`bonding-curve`](/tr/products/launchlab/bonding-curve).
- **`total_base_sell`** vs `real_base` — UI'ler için "mezuniyete doğru ilerleme" oranı.
- **`migrate_type`** — yeni başlatma `1` (`CPSWAP`) gerektirir. Depolanan `0` yalnızca CPMM-only uygulaması öncesinde oluşturulan mevcut bir başlatma için anlamlı kalır.
- **`amm_creator_fee_on`** — mezuniyet sonrası CPMM havuzunda `creator_fee_on = OnlyQuoteToken` (`0`) veya `BothToken` (`1`) seçer. Göç hedefini seçmez. Bkz. [`creator-fees`](/tr/products/launchlab/creator-fees).
- **`quote_protocol_fee` / `platform_fee` / `migrate_fee`** — üç bağımsız ücret sayacı. Her birinin kendi talep talimatı vardır; bkz. [`instructions`](/tr/products/launchlab/instructions).
- **`vesting_schedule`** — her `PoolState` üzerinde mevcut ancak `total_locked_amount == 0` olduğunda etkin değil. Tam yaşam döngüsü için bkz. [`vesting`](/tr/products/launchlab/vesting).

## Yetki PDA'sı

LaunchLab, tüm başlatmalar arasında başlatma başına tohum olmadan türetilen **tek** bir yetki PDA'sı kullanır:

```ts
const LAUNCHLAB_PROGRAM_ID = new PublicKey("<LAUNCHLAB_ID>"); // bkz. reference/program-addresses

const [authority, bump] = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);
```

Bu tek PDA:

- Her başlatmanın `base_vault` ve `quote_vault` üzerindeki yetki.
- Her başlatmanın `base_mint` üzerindeki `mint_authority` (mezuniyet öncesi).
- CPMM mezuniyet CPI'sinde ve mevcut durum için eski AMM v4 mezuniyetinde imzalayan.
- `ClaimVestedToken` transferlerinde temel kasanın dışına imzalayan.

`mint_authority`, `MigrateToCpswap` veya eski `MigrateToAmm` çağrısından hemen sonra iptal edilir, böylece arz kalıcı olarak sabitlenir.

İki ek PDA ücret kasalarını kapılar:

```ts
const [creatorFeeVaultAuthority] = PublicKey.findProgramAddressSync(
  [Buffer.from("creator_fee_vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);

const [platformFeeVaultAuthority] = PublicKey.findProgramAddressSync(
  [Buffer.from("platform_fee_vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);
```

Bunlar `ClaimCreatorFee` ve `ClaimPlatformFeeFromVault` sırasında ilgili ücret kasalarının dışına transferi imzalarlar.

## Temel mint

`Initialize` tarafından satır içi oluşturulur:

- `mint_authority = authority` (mezuniyette iptal edilir).
- `freeze_authority = None`.
- `supply = supply`, tamamen `base_vault` içine basılır.
- `decimals` yaratıcı tarafından `Initialize` sırasında seçilir (yaygın olarak 6).

Tam arz önceden basıldığından, `base_mint.supply` başlatmanın ömrü boyunca sabittir. Eğri alımları tokenleri `base_vault`'tan alıcıya taşır ancak `mint_to` çağırmaz.

`Initialize` / `InitializeV2` SPL Token başlatmaları oluşturur. Özel `InitializeWithToken2022` talimatı, temel mintin bir Token-2022 mint'i olmasını sağlar (isteğe bağlı `TransferFeeConfig` ile); alıntı mint hala SPL Token'dir. Üç başlatma yolu da CPMM mezuniyeti gerektirir.

## Kasalar

Hem `base_vault` hem de `quote_vault` LaunchLab `authority` PDA'sı tarafından sahip olunan standart SPL Token hesaplarıdır. Adresler `PoolState` üzerinde depolanır ve ayrıca türetilebilir:

```ts
const [baseVault] = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), baseMint.toBuffer()],
  LAUNCHLAB_PROGRAM_ID,
);
const [quoteVault] = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), quoteMint.toBuffer()],
  LAUNCHLAB_PROGRAM_ID,
);
```

(Üretimde bir türetmeye güvenmeden önce kaynak `Initialize` hesapları yapısından tam tohum öneklerini doğrulayın.)

## Ücret kasaları

İki PDA ücretleri başlatmalar arasında toplar:

- **Yaratıcı ücret kasası** — tohum `[creator, quote_mint]` adresindeki PDA. Aynı alıntı mint'inde aynı yaratıcı ücretlerini kazanan her başlatma aynı kasaya dökülür. Yaratıcı bunu `ClaimCreatorFee` aracılığıyla temizler.
- **Platform ücret kasası** — tohum `[platform_config, quote_mint]` adresindeki PDA. Aynı platform aracılığıyla yönlendirilen ve aynı alıntı mint'ini kullanan her başlatma aynı kasaya dökülür. Platform'un `platform_fee_wallet`'ı bunu `ClaimPlatformFeeFromVault` aracılığıyla temizler. Başlatma başına `quote_vault`'tan doğrudan çeken bir başlatma başına temizleme varyantı da vardır (`ClaimPlatformFee`) ve toplu kasadan geçmez.

Toplu kasa deseni, yüksek hacimli bir yaratıcı veya platformun ücret birikiminin kira maliyetini birçok başlatma arasında amortize etmesini sağlar.

## Alıntı kasası ↔ `real_quote`

`quote_vault.balance` ve `PoolState.real_quote` senkronize kalmalıdır. Üç bekleyen ücret sayacının toplamı kadar sapabilirler (`quote_protocol_fee`, `platform_fee`, `migrate_fee`), bunlar kasada oturur ancak ücret sayaçlarına ve eğri rezervine değil. Eğri matematik her zaman `real_quote` kullanır, asla ham kasa bakiyesini değil.

Mezuniyet öncesi değişmez:

```
quote_vault.balance ==
   real_quote
 + quote_protocol_fee
 + platform_fee
 + migrate_fee
```

## Yaşam döngüsü hesap geçişleri

| Olay | Durum | `real_base` | `real_quote` | Mezuniyet sonrası havuz |
|-------|--------|-------------|--------------|----------------------|
| `Initialize` | Funding | 0 | 0 | — |
| `BuyExactIn` / `BuyExactOut` | Funding | `+base_out` | `+quote_in_after_fee` | — |
| `SellExactIn` / `SellExactOut` | Funding | `−base_in` | `−quote_out_before_fee` | — |
| `MigrateToCpswap` (veya eski `MigrateToAmm`) | Funding → Migrate → Migrated | (dondurulmuş) | (dondurulmuş) | oluşturulmuş; CPMM LP, platform tarafından sahip olunan kilitli LP ve yakılan LP'ye bölünür |
| `ClaimCreatorFee` / `ClaimPlatformFee*` | herhangi | — | — | bir ücret kasasını boşaltır |
| `CreateVestingAccount` | Funding | — | — | `vesting_schedule.allocated_share_amount` artırır |
| `ClaimVestedToken` | Yalnızca Migrated | — | — | `base_vault` boşaltır |

## Sonraki adım

- [`products/launchlab/bonding-curve`](/tr/products/launchlab/bonding-curve) — `real_base` ↔ `real_quote` arkasındaki matematik.
- [`products/launchlab/instructions`](/tr/products/launchlab/instructions) — talimat başına hesap listeleri.
- [`products/launchlab/global-config`](/tr/products/launchlab/global-config) — protokol düzeyindeki bağlama.
- [`products/launchlab/platform-config`](/tr/products/launchlab/platform-config) — platform ek katmanı.
- [`products/launchlab/vesting`](/tr/products/launchlab/vesting) — kilitli arz mekaniği.
- [`products/cpmm/accounts`](/tr/products/cpmm/accounts) — mezuniyet sonrası `cpmm_pool_state` neye benziyor.

Kaynaklar:

- `raydium-launch/programs/launchpad/src/states/pool.rs` — `PoolState`, `PoolStatus`, `VestingSchedule`, `AmmCreatorFeeOn`.
- `raydium-launch/programs/launchpad/src/lib.rs` — PDA tohum sabitleri (`AUTH_SEED`, `CREATOR_FEE_VAULT_AUTH_SEED`, `PLATFORM_FEE_VAULT_AUTH_SEED`).
- [Raydium SDK v2 `launchpad` modülü](https://github.com/raydium-io/raydium-sdk-V2).
