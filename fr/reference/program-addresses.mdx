---
title: "Adresses des programmes"
description: "IDs de programmes Raydium canoniques et PDAs partagées sur Solana mainnet-beta et devnet. Chaque autre page renvoie ici au lieu de coder en dur les adresses."
---

<Info>
  **Cette page est traduite automatiquement par IA. La version anglaise fait foi.**

  [Voir la version anglaise →](/reference/program-addresses)
</Info>

<Info>
  **Source de vérité.** Cette page est le seul endroit de la documentation qui liste les adresses des programmes textuellement. Les autres pages renvoient ici. Si un ID change, mettez-le à jour **uniquement ici** et chaque référence du site reste cohérente.

  Vérifiez toujours les valeurs par rapport à l'API en direct (`https://api-v3.raydium.io/main/info`) avant de signer des transactions réelles.
</Info>

## Mainnet-beta

<Tip>
  Règle générale : si un ID de programme dans la nature ne correspond pas au tableau ci-dessous, **ne signez pas la transaction.** Un ID de programme mal assorti est le moyen le plus facile de perdre des fonds sur Solana.
</Tip>

### Programmes on-chain

| Programme | ID du programme | Source |
|---------|------------|--------|
| **AMM v4** (AMM hybride + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (AMM standard) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (Liquidité concentrée) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (courbe de type StableSwap) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | source non disponible publiquement |
| **Farm v3** (staking RAY hérité) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | source non disponible publiquement |
| **Farm v5** (fermes écosystème héritées) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | source non disponible publiquement |
| **Farm v6** (fermes écosystème actuelles) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | source non disponible publiquement |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | source non disponible publiquement |
| **Routage AMM** (routeur on-chain de Raydium) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | source non disponible publiquement |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | source non disponible publiquement |

Notes :

- AMM v4 et Farm v3/v5 sont **conservés pour les pools et positions existants**. L'interface utilisateur Raydium et le SDK acheminent la création de nouveaux pools et les nouvelles fermes écosystème vers CPMM, CLMM et Farm v6 respectivement — voir [`protocol-overview/versions-and-migration`](/fr/protocol-overview/versions-and-migration).
- L'ID Farm v3 ci-dessus sert également de programme de staking d'actif unique \$RAY. Le comportement est identique à Farm v3 pour le staking LP.
- **Stable AMM** est un programme séparé que le programme de routage AMM peut cibler aux côtés d'AMM v4, CPMM et CLMM. La liquidité est faible par rapport aux trois autres programmes et le SDK n'expose pas une API de première classe pour celui-ci ; les intégrateurs qui l'acheminent généralement le font via le routeur.
- **Disponibilité du code source.** Parmi les programmes on-chain ci-dessus, seuls `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM) et `raydium-clmm` (CLMM) sont fournis avec des dépôts source publics sous [`github.com/raydium-io`](https://github.com/raydium-io). Stable AMM, LaunchLab, Routage AMM, Burn & Earn / LP Lock et les programmes Farm ne sont pas disponibles publiquement — vérifiez-les par rapport à l'API en direct, au bytecode on-chain et aux IDLs publiés dans [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl) à la place.

### Autorité admin partagée

Tous les programmes basés sur Anchor (CLMM, CPMM, LaunchLab, Lock) partagent une seule `Pubkey` admin codée en dur pour le contrôle d'accès au niveau des instructions vers les chemins admin (tels que `CreateAmmConfig` ou `UpdatePoolStatus`) :

| Cluster | Admin |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

Les autorités opérationnelles au niveau du compte (par exemple `protocol_owner`, `fund_owner` sur CPMM/CLMM `AmmConfig`, ou les portefeuilles de migration sur LaunchLab `GlobalConfig`) sont stockées on-chain et peuvent différer de l'admin du programme. Lisez-les directement depuis le compte de configuration pertinent avant d'envoyer des transactions critiques.

### Autorité créatrice de PDA de permission CPMM

`CreatePermissionPda` accepte soit l'admin du programme partagé ci-dessus, soit un propriétaire dédié. Depuis la mise à niveau CPMM de 2026-09, `ClosePermissionPda` accepte les deux mêmes signataires ; avant cela, c'était réservé à l'admin.

| Cluster | Propriétaire dédié `CreatePermissionPda` |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### Autorité du registre de support-mint CPMM

`CreateSupportMintAssociated` et `CloseSupportMintAssociated` acceptent soit l'admin du programme partagé, soit le propriétaire dédié ci-dessous. L'enregistrement d'un mint l'admet au-delà de la liste d'autorisation des extensions Token-2022 de CPMM — voir [`reference/token-2022-support`](/fr/reference/token-2022-support#the-one-bypass-the-per-mint-registry).

| Cluster | Propriétaire de support-mint dédié |
|---------|------------------------------|
| mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |

### Portefeuilles propriétaires de frais CPMM

Depuis la mise à niveau CPMM de 2026-09, `CreateAmmConfig` écrit ces clés fixes dans les `protocol_owner` et `fund_owner` de la nouvelle configuration au lieu de copier la clé du signataire admin. Ce sont les signataires que `CollectProtocolFee` et `CollectFundFee` acceptent pour toute configuration créée à partir de cette mise à niveau.

| Cluster | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  Ce sont les valeurs que le programme *écrit dans les nouvelles configurations*, pas une description des configurations existantes. Les comptes `AmmConfig` créés avant la mise à niveau conservent ce qui y est stocké. Lisez toujours `protocol_owner` / `fund_owner` du compte avant de construire une transaction de collecte.
</Warning>

### Portefeuilles de collecte des lamports excédentaires

Chacun des trois programmes mis à niveau le 2026-09-09 a acquis une instruction admin qui retourne les lamports détenus au-dessus du minimum exempt de loyer sur les comptes contrôlés par le programme, suivant la [réduction de loyer SIMD-0437](/solana-fundamentals/rent-and-reclaimable-rent). Chaque programme code en dur son propre portefeuille de destination, et chacun est une clé dédiée — pas l'admin du programme partagé.

| Programme | Instruction | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM et LaunchLab acceptent également l'admin du programme partagé comme signataire. AMM v4 accepte **uniquement** le portefeuille ci-dessus — il n'a pas de secours admin. AMM v4 compile également une clé testnet séparée (`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`) sous sa fonctionnalité `testnet`.

Ces instructions ne déplacent que les lamports ; elles ne touchent jamais un solde de jeton, ne ferment un compte ou ne changent l'état du pool. Voir [`products/amm-v4/instructions`](/fr/products/amm-v4/instructions#withdrawexcesslamports), [`products/cpmm/instructions`](/fr/products/cpmm/instructions#collectexcesslamports) et [`products/launchlab/instructions`](/fr/products/launchlab/instructions#collectexcesslamports).

### Créateur de configuration délégué LaunchLab

`CreateConfig` accepte soit l'admin du programme partagé, soit l'autorité déléguée ci-dessous. Le rôle délégué peut créer un nouveau `GlobalConfig` ; il ne peut pas appeler le chemin `UpdateConfig` réservé à l'admin pour un compte existant.

| Cluster | Autorité `CreateConfig` déléguée |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### Autorités de gel d'émetteur restreint CLMM

Lorsque l'une des deux mints de coffre dans un pool CLMM a l'une des autorités de gel ci-dessous, les nouvelles positions ouvertes via un chemin V2 ont leur compte de jeton NFT de position gelé. La liste est `frozen_position_nft_authorities::IDS` dans le programme CLMM, et son entrée unique est l'autorité d'émetteur que la détection Superstate supprimée depuis longtemps de CLMM correspondait également. Ce sont **des autorités d'émetteur d'actif sous-jacent utilisées uniquement comme clés de correspondance**. Le PDA du pool CLMM, pas la clé d'émetteur, devient l'autorité de gel pour la mint NFT de position.

| Cluster | Autorité de gel d'émetteur restreint |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

La liste est codée en dur dans le programme CLMM. L'ajout ou la suppression d'une autorité nécessite une mise à niveau du programme.

### Conventions de configuration partagée / PDA

Plusieurs programmes Raydium exposent des **comptes de configuration** dont les clés publiques sont stables et listées sur l'API publique. Préférez la recherche API au codage en dur :

```
# Configurations de frais CPMM (retourne un tableau de {id, index, tradeFeeRate, ...})
GET https://api-v3.raydium.io/main/cpmm-config

# Configurations de frais CLMM
GET https://api-v3.raydium.io/main/clmm-config
```

Le destinataire des frais de création de pool de CPMM est un compte fixe, pas dérivé ou recherché via l'API : `create_pool_fee_reveiver::ID` dans `lib.rs` de `raydium-cp-swap`, actuellement `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8`. L'instruction `initialize` de CPMM le valide par adresse exacte, donc il doit déjà exister sur n'importe quel cluster sur lequel vous testez.

Paramètres de frais de l'index `AmmConfig` CPMM par défaut `0` (pool standard 0,25%), pour référence :

| Champ | Valeur | Signification |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 0,25% du volume d'échange |
| `protocol_fee_rate` | `120000` | 12% **des frais d'échange** (pas du volume) acheminés vers le protocole |
| `fund_fee_rate` | `40000` | 4% **des frais d'échange** acheminés vers le multisig du fonds |
| `creator_fee_rate` | `500` | 0,05% du volume d'échange au créateur du pool (optionnel) |

Voir [`products/cpmm/fees`](/fr/products/cpmm/fees) pour savoir comment les divisions se composent, et [`reference/fee-comparison`](/fr/reference/fee-comparison) pour la matrice inter-produits.

### Graines PDA

Les graines ci-dessous sont **canoniques** et utilisées à la fois par le SDK et les CPIs on-chain. Calculez toujours les PDAs ; ne codez pas en dur les adresses dérivées.

```ts
// CPMM — toutes les graines sont des chaînes ASCII statiques sauf indication contraire.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` sont **triés par ordre d'octets de clé publique** (token0 < token1) avant le hachage. Se tromper produit un PDA valide pour un pool inexistant.

Les graines CLMM équivalentes suivent le même style ; voir [`products/clmm/accounts`](/fr/products/clmm/accounts).

## Devnet

| Programme | ID du programme |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **Routage AMM** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DLockwT7X7sxtLmGH9g5kmfcjaBtncdbUmi738m5bvQC` |
| **Farm v3 / v5 / v6** | Non publié de manière fiable pour devnet — confirmez via l'API en direct (`https://api-v3-devnet.raydium.io/main/info`) avant utilisation. |

Base de l'API REST Devnet : `https://api-v3-devnet.raydium.io/` (même forme de route que mainnet).

## Comment vérifier une adresse on-chain

1. **Solana Explorer.** Collez l'adresse dans [explorer.solana.com](https://explorer.solana.com) et confirmez qu'elle est marquée `Program` avec une autorité de mise à niveau actuelle. Mainnet-beta devrait afficher les déploiements signés par l'autorité de mise à niveau de Raydium.
2. **CLI.** Utilisez `solana program show <PROGRAM_ID> -u mainnet-beta` pour inspecter l'emplacement de déploiement, le chargeur BPF, l'autorité de mise à niveau et la longueur des données. Enregistrez-les dans votre runbook.
3. **Attachement IDL.** Interrogez l'IDL on-chain avec `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet`. Le champ `address` de l'IDL doit correspondre. Les mêmes IDLs sont mirrorés sur [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) — comparez l'IDL on-chain par rapport au dépôt avant de lui faire confiance.
4. **Autorités de configuration/admin.** Pour les comptes de configuration CPMM/CLMM, lisez le champ `owner`/`protocol_owner` et confirmez qu'il correspond au multisig Raydium actuel publié dans [`security/admin-and-multisig`](/fr/security/admin-and-multisig).

Si l'une des quatre vérifications ci-dessus est en désaccord avec cette page, considérez cette page comme incorrecte et ouvrez un problème avant d'écrire un nouveau code par rapport aux adresses.

## Mise à jour de cette page

- Les modifications des IDs de programme sont critiques pour la sécurité. Ne **pas** livrer une modification d'ID de programme sans (a) lier à l'annonce Raydium, (b) ajouter une entrée dans [`reference/changelog`](/fr/reference/changelog) et (c) exécuter une vérification de lien sur la documentation pour confirmer qu'aucune page ne référence toujours l'ancienne valeur.
- Les dépréciations restent dans le tableau avec une note de statut plutôt que d'être supprimées — les pools existants se résolvent toujours via l'ancien programme.

Sources :

- API en direct : `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
