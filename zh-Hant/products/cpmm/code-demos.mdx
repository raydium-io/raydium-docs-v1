---
title: "CPMM 程式碼示例"
description: "每個 CPMM 操作的端到端 TypeScript 範例 — 建立流動性池、交換、存入、提取、收取費用 — 加上最小化的 Rust CPI 骨架。"
---

<Info>
  **本頁內容由 AI 自動翻譯，所有內容以英文版本為準。**

  [查看英文版 →](/products/cpmm/code-demos)
</Info>

<Info>
  **版本說明。** 所有 TypeScript 示例針對 `@raydium-io/raydium-sdk-v2@0.2.64-alpha`；最後執行於 `0.2.42-alpha`（2026-04），其呼叫簽名已針對 `0.2.64-alpha` 原始碼重新檢查（2026-09-09），針對 Solana mainnet-beta。Rust CPI 骨架針對 `raydium-cp-swap` 的 `chore/upgrade-anchor` 分支，Anchor `1.0.2` — `master` 仍然固定於 `0.32.1`。程式 ID 透過 [`reference/program-addresses`](/zh-Hant/reference/program-addresses) 的常數提取。
</Info>

## 前置需求

```bash
npm install @raydium-io/raydium-sdk-v2 @solana/web3.js @solana/spl-token bn.js decimal.js
```

本頁的每個示例都對應 [`raydium-sdk-V2-demo/src/cpmm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/cpmm) 中的一個檔案；GitHub 連結位於每個部分旁邊。初始化遵循示例倉庫的 `config.ts.template`（[來源](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/config.ts.template)）：

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);

const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});
```

`Raydium` 實例是 SDK 的外觀 — 下面的每個示例都使用它。`Raydium.load` 是非同步的，因為它預設載入代幣列表；傳遞 `disableLoadToken: true` 以跳過該提取。可用性功能檢查已被跳過，除非你傳遞 `disableFeatureCheck: false`。費用配置透過 `raydium.api.getCpmmConfigs()` 在首次使用時延遲提取。

## 建立 CPMM 流動性池

來源：[`src/cpmm/createCpmmPool.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/createCpmmPool.ts)

```ts
import { PublicKey } from "@solana/web3.js";
import BN from "bn.js";
import { CREATE_CPMM_POOL_PROGRAM, CREATE_CPMM_POOL_FEE_ACC }
  from "@raydium-io/raydium-sdk-v2";
// (如果你需要一次性取得每個流動性池的 PDA，匯出的輔助函式是 `getCreatePoolKeys`。
//  沒有 `getCpmmPdas`。)

const mintA = new PublicKey("So11111111111111111111111111111111111111112"); // wSOL
const mintB = new PublicKey("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"); // USDC

// 1. 選擇一個費用配置。index=0 是 0.25% 層級。
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0)!;

// 2. 提取 mint 元資料，以便 SDK 可以處理 Token-2022 擴展。
const mintAInfo = await raydium.token.getTokenInfo(mintA);
const mintBInfo = await raydium.token.getTokenInfo(mintB);

// 3. 建立交易。
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA:           mintAInfo,
  mintB:           mintBInfo,
  mintAAmount:     new BN(1_000_000_000),   // 1 SOL（假設 9 位小數）
  mintBAmount:     new BN(   160_000_000),  // 160 USDC（以 160/SOL 計）
  startTime:       new BN(0),               // 立即開放
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("流動性池建立於", extInfo.address.poolId.toBase58());
console.log("交易：", txId);
```

SDK 悄悄處理的幾件事：

- 在衍生 PDA 之前將 mint 排序為 token0/token1 順序。
- 向 `poolFeeAccount` 支付一次性 `create_pool_fee`。
- 如果缺少，建立呼叫者的關聯代幣帳戶。
- 根據每一側選擇正確的代幣程式（SPL Token 與 Token-2022）。

確認後，你可以使用以下方式提取即時流動性池狀態：

```ts
// getPoolInfoFromRpc 接受位置 base58 字串，不是 PublicKey，也不是物件。
const { poolKeys, poolInfo, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(
  extInfo.address.poolId.toBase58(),
);
```

## 交換（基礎輸入）

來源：[`src/cpmm/swap.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swap.ts)

```ts
import { CurveCalculator, FeeOn } from "@raydium-io/raydium-sdk-v2";

const POOL_ID = "<POOL_ID>";   // base58 字串

// 1. 直接從 RPC 載入目前流動性池狀態（不是從 API）。
const { poolInfo, poolKeys, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(POOL_ID);

const inputMint  = poolInfo.mintA.address;                // 交換 A → B
const amountIn   = new BN(100_000_000);                   // 0.1 SOL
const slippage   = 0.005;                                 // 0.5%

// 2. 在本地報價。CurveCalculator 鏡像鏈上數學，包括
//    Token-2022 轉移費用在任一側。所有八個引數都是必需的。
const baseIn = inputMint === poolInfo.mintA.address;
const swapResult = CurveCalculator.swapBaseInput(
  amountIn,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// swapResult 欄位為 inputAmount / outputAmount / tradeFee / protocolFee /
// fundFee / creatorFee / newInputVaultAmount / newOutputVaultAmount。
// 沒有 destinationAmountSwapped 或 sourceAmountSwapped。

// 3. 建立並傳送。在此傳遞 `slippage` 是應用邊界的方式 —
//    不需要手動最小輸出算術。
const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: amountIn,
  swapResult,
  slippage,
  baseIn,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("交換交易：", txId);
```

注意：SDK **總是**在 `getPoolInfoFromRpc` 內從 RPC 重新提取流動性池狀態。不要從 `api-v3.raydium.io` 報價你即將簽署的交易 — 一個區塊陳舊的報價可能在著陸時滑入 `ExceededSlippage`。

## 交換（基礎輸出）

來源：[`src/cpmm/swapBaseOut.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swapBaseOut.ts)

```ts
const amountOutWanted = new BN(15_000_000);        // 15 USDC
const slippage        = 0.005;

const baseIn = false; // false → mintB 是輸入側
const swapResult = CurveCalculator.swapBaseOutput(
  amountOutWanted,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// swapResult.inputAmount 是所需的輸入；沒有 sourceAmountSwapped。

const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: new BN(0),   // 當 fixedOut 時未使用 — 金額來自 swapResult
  fixedOut:    true,
  swapResult,               // 必需，它攜帶目標輸出金額
  baseIn,
  slippage,
  txVersion:   TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## 存入流動性

來源：[`src/cpmm/deposit.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/deposit.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";
import Decimal from "decimal.js";

// addLiquidity 由輸入金額驅動，不是 LP 金額。
const uiInputAmount = "0.0001";
const inputAmount = new BN(
  new Decimal(uiInputAmount).mul(10 ** poolInfo.mintA.decimals).toFixed(0),
);
const slippage = new Percent(1, 100);   // 一個 Percent，不是數字

const { execute } = await raydium.cpmm.addLiquidity({
  poolInfo,
  poolKeys,
  inputAmount,
  baseIn: true,           // inputAmount 以 mintA 計價
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

SDK 從流動性池的目前儲備衍生配對金額，將每一側膨脹滑點以用於指令的 `maximum_*` 引數，並在必要時建立 ATA 建立。`raydium.cpmm.computePairAmount({ ... })` 如果你想要它們用於 UI 預覽，會傳回相同的數字。

## 提取流動性

來源：[`src/cpmm/withdraw.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/withdraw.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";

const lpAmount = new BN(100_000);           // 要燒毀的 LP
const slippage = new Percent(1, 100);       // 一個 Percent，不是數字

const { execute } = await raydium.cpmm.withdrawLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## 收取協議/基金/建立者費用

來源：[`src/cpmm/collectCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectCreatorFee.ts)、[`src/cpmm/collectAllCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectAllCreatorFee.ts)

協議和基金收集仍然受權限限制。建立者費用有兩條路徑：建立者簽署的 `CollectCreatorFee` 和加法 `CollectCreatorFeePermissionless`，任何支付者都可以呼叫，但只能轉移到建立者的規範 ATA。`0.2.64-alpha` 為兩個建立者路徑提供模組級別的建構器：

```ts
// 建立者簽署
const { execute } = await raydium.cpmm.collectCreatorFees({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// 無許可 — 任何支付者；收益進入建立者的規範 ATA
const { execute } = await raydium.cpmm.collectCreatorFeesPermissionless({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// 一次多個流動性池，建立者簽署
await raydium.cpmm.collectMultiCreatorFees({ poolInfoList, txVersion: TxVersion.V0 });
```

<Warning>
  **兩個建立者指令在 2026-09-13 程式升級中改變了它們的帳戶列表。** 每個現在都採用 `["creator_fee_share", creator, ammConfig]` 處的 `creator_fee_share` PDA，無許可的也採用 `amm_config` — 兩者都插入在保管庫之前。固定到升級前 SDK 版本的建構器發出舊版配置，交易無法反序列化。在針對升級後的程式使用這些輔助函式之前，檢查你安裝的版本是否包含新帳戶，如果你從它生成客戶端，請重新整理 IDL。

  即使 PDA 從未被建立，傳遞它也是強制性的 — 衍生它並傳遞它；當帳戶為空時，程式回退到 `AmmConfig.creator_fee_share_rate`。
</Warning>

<Note>
  兩個建立者指令有**不同的判別器和帳戶配置** — 無許可的用單獨的 `payer` 和非簽署的 `creator` 替換建立者簽署者。使用匹配的建構器，而不是在另一個上手動交換帳戶。

  SDK 中沒有協議或基金費用建構器（`makeCollectProtocolFeeInstruction` 和 `makeCollectFundFeeInstruction` 在任何名稱下都不存在） — 那些是管理員路徑，`CreateCreatorFeeShare` / `CloseCreatorFeeShare` 也是，它們也沒有建構器。**確實**存在的低級指令建構器是 `collectCpFeeInstruction`、`makeCollectCreatorFeeInstruction` 和 `collectCreatorFeePermissionlessInInstruction`。
</Note>

離鏈，你可以直接從 `PoolState` 讀取應計費用：

```ts
const pool = await raydium.cpmm.getRpcPoolInfo(POOL_ID);   // base58 字串
console.log("應計協議費用 mintA：", pool.protocolFeesMintA.toString());
console.log("應計協議費用 mintB：", pool.protocolFeesMintB.toString());
// CpmmPoolInfoLayout 欄位是 protocolFeesMintA/B、fundFeesMintA/B 和
// creatorFeesMintA/B — 沒有 ...Token0/...Token1 欄位。
//
// creatorFeesMintA/B 是*總*應計。建立者收到它減去在收集時解決的
// 協議份額；protocolFeesMintA/B 收到該份額，所以它也在建立者費用
// 收集時增長，不僅在交換時。
// 見 products/cpmm/fees#protocol-share-of-the-creator-fee。
```

## Rust CPI 骨架

如果你想從自己的 Anchor 程式呼叫 CPMM — 例如，代表其存款人進行交換的保管庫 — CPI 上下文看起來像這樣。帳戶順序遵循 [`products/cpmm/instructions`](/zh-Hant/products/cpmm/instructions)。

```rust
// Cargo.toml — 分支很重要：`master` 仍然固定 anchor-lang 0.32.1。
// raydium-cp-swap = { git = "https://github.com/raydium-io/raydium-cp-swap",
//                     branch = "chore/upgrade-anchor", features = ["cpi"] }
// anchor-lang      = "=1.0.2"   // 必須符合 raydium-cp-swap 固定的內容
// anchor-spl       = "=1.0.2"
//
// `cpi` 功能是必需的 — `raydium_cp_swap::cpi` 模組只在
// 它下面存在。注意此 crate 不能與 products/clmm/code-demos 中的 CLMM 示例
// 共享程式，後者固定 anchor-lang =0.32.1。

use anchor_lang::prelude::*;
use anchor_spl::token_interface::{TokenAccount, TokenInterface, Mint};
use raydium_cp_swap::cpi::accounts::Swap;
use raydium_cp_swap::cpi;
use raydium_cp_swap::program::RaydiumCpSwap;

#[derive(Accounts)]
pub struct ProxySwap<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,

    /// CHECK: 由 CPMM 程式驗證
    pub authority:     UncheckedAccount<'info>,
    /// CHECK:
    pub amm_config:    UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub pool_state:    UncheckedAccount<'info>,

    #[account(mut)]
    pub input_token_account:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_token_account: InterfaceAccount<'info, TokenAccount>,

    #[account(mut)]
    pub input_vault:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_vault: InterfaceAccount<'info, TokenAccount>,

    pub input_token_program:  Interface<'info, TokenInterface>,
    pub output_token_program: Interface<'info, TokenInterface>,

    pub input_token_mint:  InterfaceAccount<'info, Mint>,
    pub output_token_mint: InterfaceAccount<'info, Mint>,

    #[account(mut)]
    /// CHECK: 環形緩衝區
    pub observation_state: UncheckedAccount<'info>,

    pub cpmm_program: Program<'info, RaydiumCpSwap>,
}

pub fn proxy_swap_base_input(
    ctx: Context<ProxySwap>,
    amount_in: u64,
    minimum_amount_out: u64,
) -> Result<()> {
    let cpi_accounts = Swap {
        payer:                ctx.accounts.payer.to_account_info(),
        authority:            ctx.accounts.authority.to_account_info(),
        amm_config:           ctx.accounts.amm_config.to_account_info(),
        pool_state:           ctx.accounts.pool_state.to_account_info(),
        input_token_account:  ctx.accounts.input_token_account.to_account_info(),
        output_token_account: ctx.accounts.output_token_account.to_account_info(),
        input_vault:          ctx.accounts.input_vault.to_account_info(),
        output_vault:         ctx.accounts.output_vault.to_account_info(),
        input_token_program:  ctx.accounts.input_token_program.to_account_info(),
        output_token_program: ctx.accounts.output_token_program.to_account_info(),
        input_token_mint:     ctx.accounts.input_token_mint.to_account_info(),
        output_token_mint:    ctx.accounts.output_token_mint.to_account_info(),
        observation_state:    ctx.accounts.observation_state.to_account_info(),
    };
    // Anchor 1.0 在此採用程式的 Pubkey，而不是其 AccountInfo。
    let cpi_ctx = CpiContext::new(
        *ctx.accounts.cpmm_program.key,
        cpi_accounts,
    );
    cpi::swap_base_input(cpi_ctx, amount_in, minimum_amount_out)
}
```

如果你的 CPI 作為 PDA 簽署（例如，你代表存款人管理保管庫），將 `CpiContext::new` 交換為 `CpiContext::new_with_signer` 並傳遞你的種子。

## 常見陷阱

開啟支援票證前的簡短檢查清單：

- **排序的 mint。** 如果你衍生的 `poolState` PDA 與鏈上流動性池不符，你可能忘記排序 mint。
- **陳舊的 API 報價。** 永遠不要將 `api-v3.raydium.io` 的儲備值傳遞到 `CurveCalculator.swap`。從 RPC 提取。
- **錯誤的代幣程式。** Token-2022 mint 的保管庫由 Token-2022 程式擁有，而不是 SPL Token。始終使用流動性池的 `token_0_program` / `token_1_program` 欄位。
- **轉移費用 mint 的滑點未充分計價。** 如果流動性池的任一側是 Token-2022 轉移費用 mint，你的 `minimum_amount_out` 必須以使用者實際收到的計價，而不是保管庫傳送的。
- **交換時 `NotApproved`。** 檢查 `PoolState.status` — 管理員可能已暫停該流動性池上的交換。見 [`products/cpmm/instructions`](/zh-Hant/products/cpmm/instructions) 以取得狀態位元遮罩。

## 接下來去哪裡

- [`sdk-api/typescript-sdk`](/zh-Hant/sdk-api/typescript-sdk) — 完整 SDK 參考。
- [`sdk-api/rest-api`](/zh-Hant/sdk-api/rest-api) — 報價和流動性池元資料端點。
- [`user-flows/create-cpmm-pool`](/zh-Hant/user-flows/create-cpmm-pool) — 相同流程的非程式碼逐步解說。
- [`integration-guides/aggregator`](/zh-Hant/integration-guides/aggregator) — 如果你正在將 CPMM 路由為多跳路徑的一部分。

來源：

- [Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`](https://github.com/raydium-io/raydium-sdk-V2)
- [Raydium SDK v2 示例 — `raydium-sdk-v2-demo`](https://github.com/raydium-io/raydium-sdk-V2-demo)
- [`raydium-io/raydium-cp-swap`](https://github.com/raydium-io/raydium-cp-swap)
