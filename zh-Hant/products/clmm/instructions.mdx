---
title: "CLMM 指令"
description: "完整的 CLMM 指令參考：池生命週期、頭寸管理、交換和獎勵流。"
---

<Info>
  **本頁內容由 AI 自動翻譯，所有內容以英文版本為準。**

  [查看英文版 →](/products/clmm/instructions)
</Info>

本頁與 [`products/clmm/accounts`](/zh-Hant/products/clmm/accounts)（帳戶是什麼）和 [`products/clmm/math`](/zh-Hant/products/clmm/math)（數學是什麼）配套。它對參數和帳戶順序具有權威性；具體的位元組佈局來自 IDL。

## 指令清單

| 分組 | 指令 | 說明 |
|-------|-------------|-------|
| 管理員 | `CreateAmmConfig` | 定義新的費用等級。 |
| 管理員 | `UpdateAmmConfig` | 更改現有等級的費率。 |
| 管理員 | `UpdatePoolStatus` | 暫停/恢復池上的操作。 |
| 管理員 | `CreateSupportMintAssociated` | 允許列表 Token-2022 鑄幣擴展配置以在 CLMM 池中使用。 |
| 管理員 | `CreateOperationAccount` | 初始化程序級操作帳戶（一次性）。 |
| 管理員 | `UpdateOperationAccount` | 修改操作帳戶白名單。 |
| 管理員 | `CreateDynamicFeeConfig` | 在 u16 索引下創建可重用的動態費用參數集。 |
| 管理員 | `UpdateDynamicFeeConfig` | 修改現有的 `DynamicFeeConfig`。已快照的池不受影響。 |
| 管理員 | `CreatePermissionPda` | 通過初始化其 `Permission` PDA 授予權限以調用 `CreatePermissionedPool`。 |
| 管理員 | `ClosePermissionPda` | 通過關閉其 `Permission` PDA 撤銷先前授予的權限；租金退款給調用者。 |
| 池 | `CreatePool` | 初始化綁定到 `AmmConfig` 的 CLMM 池。標準、FromInput 費用路徑。與 `CreateCustomizablePool` 共存。 |
| 池 | `CreateCustomizablePool` | 推薦用於新池。與 `CreatePool` 相同的形狀加上 `collect_fee_on` 和可選的 `enable_dynamic_fee` 標誌。 |
| 池 | `CreatePermissionedPool` | 為已有一個池的交易對創建**額外**池，使用客戶端提供的 `seed_index` 折疊到池 PDA 中。支付者必須持有 `Permission` PDA。 |
| 頭寸 | `OpenPosition` / `OpenPositionV2` / `OpenPositionWithToken22Nft` | 鑄造頭寸 NFT。`OpenPositionV2` 取代 V1（具有位圖擴展槽的較新帳戶佈局）；`OpenPositionWithToken22Nft` 將頭寸 NFT 作為 Token-2022 而不是 SPL Token 發行。新代碼應使用 V2 或 Token-2022 變體。 |
| 頭寸 | `IncreaseLiquidity` / `IncreaseLiquidityV2` | 向現有頭寸添加流動性。 |
| 頭寸 | `DecreaseLiquidity` / `DecreaseLiquidityV2` | 移除流動性；收集應付費用。 |
| 頭寸 | `ClosePosition` | 銷毀 NFT 並關閉 `PersonalPositionState`。 |
| 頭寸 | `CloseProtocolPosition` | 管理員專用掃描舊版 `ProtocolPositionState` PDA。當前程序不再創建或讀取 `ProtocolPositionState` — 此指令僅用於回收由舊程序版本創建的帳戶上的租金。 |
| 交換 | `Swap` / `SwapV2` | 恆定流動性交換。兩個變體都應用動態費用、單邊費用路由和限價單匹配；唯一的區別是 `SwapV2` 接受 Token-2022 鑄幣（V1 變體要求兩個保管庫都是經典 SPL Token）。 |
| 交換 | `SwapRouterBaseIn` | 多跳，由路由器使用。 |
| 限價單 | `OpenLimitOrder` | 在某個 tick 處下達賣單。未成交的代幣留在 tick 上；匹配引擎在價格穿過時填充它們。 |
| 限價單 | `IncreaseLimitOrder` | 添加到現有未平倉訂單。 |
| 限價單 | `DecreaseLimitOrder` | 減少或取消未平倉訂單；支付未成交的剩餘部分加上已結算的任何輸出。 |
| 限價單 | `SettleLimitOrder` | 將已成交的輸出代幣推送給訂單所有者。可由所有者或操作保管人調用。 |
| 限價單 | `CloseLimitOrder` | 關閉完全消耗的訂單帳戶。租金始終返回給訂單的 `owner`。可由所有者或保管人調用。 |
| 費用 | `CollectProtocolFee` | 管理員掃描協議費用。 |
| 費用 | `CollectFundFee` | 管理員掃描基金費用。 |
| 獎勵 | `InitializeReward` | 將新的獎勵流附加到池。 |
| 獎勵 | `SetRewardParams` | 更改現有獎勵的發放速率/結束時間。 |
| 獎勵 | `UpdateRewardInfos` | 將獎勵增長結算到現在（由任何交換/頭寸變化調用）。 |
| 獎勵 | `TransferRewardOwner` | 轉移可以設置或補充獎勵流的權限。 |
| 獎勵 | `CollectRemainingRewards` | 在獎勵流的 `end_time` 之後，將任何未分配的代幣掃回給資金提供者。 |
| 實用程序 | `InitTickArray` | 初始化 tick 陣列帳戶（通常與 `OpenPosition` 捆綁）。 |

大多數管理員專用指令（`CreateAmmConfig`、`UpdateAmmConfig`、`UpdatePoolStatus`、`CreateSupportMintAssociated`、`CreateOperationAccount`、`UpdateOperationAccount`、`CloseProtocolPosition`）由程序的硬編碼 `admin` 公鑰控制。`CreatePermissionPda` / `ClosePermissionPda` 接受 `admin` 公鑰**或**專用的 `permission_pda_admin` 密鑰。獎勵流管理員指令（`TransferRewardOwner`、`CollectRemainingRewards`）由獎勵資金提供者控制，而不是程序管理員。

V2 後綴表示「支持保管庫/NFT 上的 Token-2022，需要位圖擴展槽」。SDK 預設為新池選擇 V2。

## `CreatePool`

**參數**

```
sqrt_price_x64: u128           // 初始價格
open_time:      u64            // 此時間之前拒絕交換
```

**帳戶（簡化）**

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| 1 | `pool_creator` | W | S | |
| 2 | `amm_config` |   |   | 選定的費用等級。 |
| 3 | `pool_state` | W |   | 在此初始化。 |
| 4 | `token_mint_0` |   |   | 已排序。 |
| 5 | `token_mint_1` |   |   | |
| 6 | `token_vault_0` | W |   | 在此初始化，由池權限 PDA 擁有。 |
| 7 | `token_vault_1` | W |   | |
| 8 | `observation_state` | W |   | 在此初始化。 |
| 9 | `tick_array_bitmap_extension` | W |   | 在此初始化（V2）。 |
| 10 | `token_program` |   |   | |
| 11 | `token_program_2022` |   |   | |
| 12 | `system_program`、`rent` |   |   | |

**前置條件**

- `token_mint_0 < token_mint_1`（按位元組順序）。
- `amm_config.disable_create_pool == false`。
- 鑄幣未被 Token-2022 擴展允許列表拒絕。

**後置條件**

- `pool_state.sqrt_price_x64 = sqrt_price_x64`，`tick_current = floor(log_{1.0001}(price))`。
- `pool_state.liquidity = 0`（尚無頭寸）。
- `pool_state.fee_on = FromInput`（舊版預設）。
- `pool_state.dynamic_fee_info` 被清零（動態費用禁用）。

## `CreateCustomizablePool`

推薦用於新池。與 `CreatePool` 相同的效果加上每池費用收集模式和可選的動態費用選擇加入。

**參數**

```rust
pub struct CreateCustomizableParams {
    pub sqrt_price_x64:    u128,
    pub collect_fee_on:    CollectFeeOn,   // FromInput | Token0Only | Token1Only
    pub enable_dynamic_fee: bool,
}
```

**帳戶（簡化）** — 與 `CreatePool` 相同，加上當 `enable_dynamic_fee = true` 時：

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| N | `dynamic_fee_config` |   |   | 要快照的共享配置。必須已存在。 |

**前置條件** — 與 `CreatePool` 相同。如果 `enable_dynamic_fee = false`，`dynamic_fee_config` 被忽略。

**後置條件**

- `pool_state.fee_on` 設置為選定的 `CollectFeeOn` 變體。
- 如果啟用了動態費用：`pool_state.dynamic_fee_info` 從提供的 `DynamicFeeConfig` 初始化（複製五個校準參數；狀態欄位清零）。
- 否則：`pool_state.dynamic_fee_info` 被清零（= 此池永遠不活躍動態費用）。

`fee_on` 和動態費用啟用位**僅**在池創建時設置。沒有就地升級 — 通過舊版 `CreatePool` 創建的池無法追溯地獲得動態費用或單邊費用。新部署應默認使用此指令。

## `CreatePermissionedPool`

`CreatePool` 和 `CreateCustomizablePool` 都從 `["pool", amm_config, token_mint_0, token_mint_1]` 派生池 PDA，因此每個 `(config, mint0, mint1)` 三元組恰好有**一個**規範池地址 — 在相同種子處的第二個 `init` 失敗。`CreatePermissionedPool` 通過將客戶端提供的 `seed_index: u16` 折疊到池 PDA 種子中來解除該限制，允許**同一交易對和費用等級的多個池** — 每個在其自己的地址處。因為任意池地址是特權功能，支付者必須持有授權它的 `Permission` PDA。

關於池的其他一切與 `CreateCustomizablePool` 相同：它採用相同的 `CreateCustomizableParams` 並支持單邊費用和動態費用選擇加入。

**參數**

```rust
customizable_params: CreateCustomizableParams   // 與 CreateCustomizablePool 相同的結構
seed_index:          u16                          // 必須非零；折疊到池 PDA 種子中
```

**帳戶（簡化）** — 與 `CreateCustomizablePool` 相同，加上前面的：

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| 1 | `payer` | W | S | 支付租金。必須是下面 `permission` PDA 的權限。 |
| 2 | `pool_creator` |   |   | 記錄為 `pool_state.owner`。可能與 `payer` 不同。 |
| 3 | `permission` |   |   | PDA `["permission", payer]`。如果不存在/不由程序擁有，Anchor 拒絕調用。 |
| … | （`amm_config`、`pool_state`、鑄幣、保管庫、`observation_state`、`tick_array_bitmap`、代幣/系統程序） | | | 如 `CreateCustomizablePool` 中所示。 |

`pool_state` PDA 派生自 `["pool", amm_config, token_mint_0, token_mint_1, seed_index.to_le_bytes()]`。

**前置條件**

- `seed_index != 0`。`seed_index` 為 `0` 保留給舊版池並在此被拒絕；`[0, 0]` 種子分量是使舊版池地址折疊為經典四種子形式的原因。
- `payer` 的 `permission` PDA 存在（由管理員通過 `CreatePermissionPda` 創建）。
- 與 `CreatePool` 相同的鑄幣/允許列表規則。

**後置條件**

- 新的 `pool_state` 存在於 `seed_index` 派生的地址處，`pool_state.seed_index = seed_index`。
- 所有其他後狀態與 `CreateCustomizablePool` 匹配（費用模式、可選動態費用）。

<Note>
  此指令**不**擴大一般池創建訪問 — 無權限創建繼續通過 `CreatePool` / `CreateCustomizablePool`，每對仍為一個池。`CreatePermissionedPool` 存在於白名單操作者需要同一交易對的多個池（例如不同的初始價格或啟動隊列）並持有由管理員授予的 `Permission` PDA 的特定情況。
</Note>

## `OpenPositionV2` / `OpenPositionWithToken22Nft`

在現有池內創建新頭寸。

**參數**

```
tick_lower_index: i32
tick_upper_index: i32
tick_array_lower_start_index: i32
tick_array_upper_start_index: i32
liquidity:  u128              // 所需 L（或 0 以使用下面的金額）
amount_0_max: u64
amount_1_max: u64
with_metadata: bool           // 寫入 NFT 元數據（Metaplex）
base_flag: Option<bool>       // true = 適應 amount0；false = 適應 amount1
```

**帳戶（簡化）**

| # | 名稱 | W | S |
|---|------|---|---|
| 1 | `payer` | W | S |
| 2 | `position_nft_owner` |   |   |
| 3 | `position_nft_mint` | W | S（密鑰對） |
| 4 | `position_nft_account` | W |   | 所有者的 NFT ATA。 |
| 5 | `metadata_account` | W |   | Metaplex（可選，如果 `with_metadata`）。 |
| 6 | `pool_state` | W |   |
| 7 | `protocol_position` |   |   |   |
| 8 | `tick_array_lower` | W |   | 如果未初始化則創建。 |
| 9 | `tick_array_upper` | W |   | 相同。 |
| 10 | `personal_position` | W |   | 在此創建。 |
| 11 | `token_account_0`、`token_account_1` | W |   | 用戶源 ATA。 |
| 12 | `token_vault_0`、`token_vault_1` | W |   | |
| 13 | `rent`、`system_program`、`token_program` |   |   | |
| 14 | `associated_token_program` |   |   | |
| 15 | `metadata_program` |   |   | 可選。 |
| 16 | `token_program_2022` |   |   | V2。 |
| 17 | `vault_0_mint`、`vault_1_mint` |   |   | V2。 |
| 18 | `tick_array_bitmap_extension` | W |   | V2（如果觸及）。 |

**數學** — 見 [`products/clmm/math`](/zh-Hant/products/clmm/math)。給定 `base_flag`，程序將 `liquidity` 或 `(amount_0_max, amount_1_max)` 解析為實際 `L` 和消耗的實際代幣金額。

**前置條件**

- `tick_lower < tick_upper`，都是 `pool.tick_spacing` 的倍數，在 `[MIN_TICK, MAX_TICK]` 內。
- 所需 tick 陣列已傳遞並初始化（或通過交易中的 `InitTickArray` CPI 在此創建）。
- 用戶在源 ATA 中至少有 `amount_0_max` 和 `amount_1_max`。

**後置條件**

- `personal_position` 存在，`liquidity` 設置，`fee_growth_inside_last` 快照。
- `tick_lower` 和 `tick_upper` 處的 tick 陣列條目已更新（`liquidity_gross += L`，`liquidity_net ± L`，維護費用增長快照）。
- 如果頭寸在範圍內（`tick_lower ≤ tick_current < tick_upper`），`pool_state.liquidity += L`。

**常見錯誤** — `InvalidTickIndex`、`NotApproved`、`ZeroAmountSpecified`、`TransactionTooLarge`（如果 tick 陣列過多）。

## `IncreaseLiquidityV2`

向已開放的頭寸添加流動性。

**參數**

```
liquidity: u128
amount_0_max: u64
amount_1_max: u64
base_flag: Option<bool>
```

**帳戶** — 如 `OpenPosition` 減去 NFT 鑄幣（頭寸已存在；NFT 作為所有者的 ATA 傳遞，持有 1 個代幣）。

**效果**

- 從用戶轉移 `amount_0_actual` / `amount_1_actual` → 保管庫。
- 遞增 `personal_position.liquidity` 和 `pool_state.liquidity`（如果在範圍內），以及端點 tick 的 `liquidity_gross` / `liquidity_net`。
- **收集自上次觸及以來應付的費用和獎勵**並將其記入 `tokens_fees_owed_{0,1}` / `reward_amount_owed`。這些僅在 `DecreaseLiquidity` 或 `CollectReward` 時支付，不在增加時支付。

## `DecreaseLiquidityV2`

從頭寸移除流動性。

**參數**

```
liquidity: u128
amount_0_min: u64
amount_1_min: u64
```

**帳戶** — 與 `IncreaseLiquidity` 相同的形狀。

**效果**

- 給定當前 `sqrt_price_x64`，計算移除的 `L` 的 `(amount_0, amount_1)`。
- 結算自上次觸及以來累積的費用/獎勵，與 `IncreaseLiquidity` 相同。
- 從保管庫轉移 `amount_0 + fees_owed_0` 和 `amount_1 + fees_owed_1` 到用戶。
- 遞減流動性計數器；如果新的 `personal_position.liquidity == 0`，頭寸符合 `ClosePosition` 的條件。

**滑點** — `amount_0_min` 和 `amount_1_min` 是用戶在輸出端 Token-2022 轉移費用淨額接受的最小值。

## `ClosePosition`

銷毀頭寸 NFT 並關閉 `PersonalPositionState`。

**前置條件**

- `personal_position.liquidity == 0`。
- `tokens_fees_owed_{0,1} == 0`。
- 所有獎勵計數器 `reward_amount_owed == 0`。

（即，先收集所有內容並減少到零。）

**效果**

- 銷毀 NFT。
- 關閉 NFT 鑄幣帳戶和 `personal_position` 帳戶，將租金退款給 `payer`。

## `SwapV2`

沿著流動性曲線行走；根據 `is_base_input` 精確輸入**或**精確輸出。

**參數**

```
amount: u64                  // 如果 is_base_input=true 則輸入，否則輸出
other_amount_threshold: u64  // 最小輸出或最大輸入
sqrt_price_limit_x64: u128   // 硬邊界；0 ⇒ 無邊界
is_base_input: bool
```

**帳戶（簡化）**

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| 1 | `payer` |   | S | |
| 2 | `amm_config` |   |   | |
| 3 | `pool_state` | W |   | |
| 4 | `input_token_account` | W |   | |
| 5 | `output_token_account` | W |   | |
| 6 | `input_vault` | W |   | |
| 7 | `output_vault` | W |   | |
| 8 | `observation_state` | W |   | |
| 9 | `token_program` |   |   | |
| 10 | `token_program_2022` |   |   | V2。 |
| 11 | `memo_program` |   |   | V2（某些 Token-2022 路徑需要）。 |
| 12 | `input_vault_mint`、`output_vault_mint` |   |   | V2。 |
| 13 | `tick_array_bitmap_extension`（可選） | W |   | 如果交換進入擴展。 |
| 14+ | `tick_array`（剩餘） | W |   | 足夠的陣列以跨越行走的預期範圍。 |

調用者傳遞覆蓋預期交換行走的排名 tick 陣列列表；程序使用所需的數量。SDK 通過 `PoolUtils.computeAmountOutFormat` 或 API 的報價端點計算此列表。

**前置條件**

- `pool_state.status` 允許交換。
- `now >= open_time`。
- `sqrt_price_limit_x64` 在 `sqrt_price_x64` 的正確一側用於該方向。

**常見錯誤** — `ExceededSlippage`、`SqrtPriceLimitOverflow`、`TickArrayNotFound`、`LiquidityInsufficient`。

**`SwapV2` 在內部執行的調用者應該知道的內容**（2025 年後發布）：

1. **動態費用附加費** — 如果 `pool.dynamic_fee_info` 非零，程序使用自上次交換以來遍歷的 tick 距離更新波動率累加器（使用 [`products/clmm/fees`](/zh-Hant/products/clmm/fees) 中的過濾/衰減規則），並在 `AmmConfig.trade_fee_rate` 之上添加 `dynamic_fee_component`。總費用上限為 10%（`MAX_FEE_RATE_NUMERATOR / 1_000_000`）。
2. **限價單匹配** — 當價格行走穿過持有未平倉限價單的 tick 時，程序首先填充該 tick 處的可用限價單流動性（按 `order_phase` 的 FIFO），然後沿著 LP 流動性曲線進行。已成交金額更新 `tick.unfilled_ratio_x64` 和 `tick.part_filled_orders_remaining` 以供稍後結算；訂單本身保持未花費，直到其所有者調用 `SettleLimitOrder`。
3. **單邊費用路由** — 當 `pool.fee_on = Token0Only` 或 `Token1Only` 時，交換步驟仍計算相同的輸入輸出交易；費用隨後被路由到配置的一側。對於配置費用側是輸出的方向，費用從交換輸出中扣除（用戶收到 `out − fee`）；對於配置費用側是輸入的方向，行為與 `FromInput` 匹配。見 `PoolState` 上的 `is_fee_on_input(zero_for_one)` 和 `is_fee_on_token0(zero_for_one)`。

`Swap`（V1）實現與 `SwapV2` 相同的動態費用、單邊費用路由和限價單匹配；它缺少的唯一功能是 Token-2022 支持 — 兩個保管庫都必須是經典 SPL Token。任何 Token-2022 鑄幣的池必須通過 `SwapV2` 交換。聚合器和 SDK 已經為每個 CLMM 腿優先選擇 V2，因此調用者不必根據鑄幣類型進行分支。

## `OpenLimitOrder`

在特定 tick 處下達賣單。訂單位於每 tick FIFO 隊列中，並在價格穿過時填充。

**參數**

```
nonce_index:    u8       // 用戶選擇的 nonce 帳戶索引（每個錢包 0..255）
zero_for_one:   bool     // true：出售 token0 換 token1；false：出售 token1 換 token0
tick_index:     i32      // 必須是 pool.tick_spacing 的倍數
amount:         u64      // 輸入代幣金額
```

**帳戶（簡化）**

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| 1 | `payer` | W | S | 訂單所有者；支付租金。 |
| 2 | `pool_state` | W |   | |
| 3 | `tick_array` | W |   | 包含 `tick_index` 的 tick 陣列。 |
| 4 | `limit_order_nonce` | W |   | PDA。`init_if_needed` — 在用戶在此 `nonce_index` 下的第一個訂單時創建。 |
| 5 | `limit_order` | W |   | PDA。在此初始化。 |
| 6 | `input_token_account` | W |   | 用戶的輸入 ATA。檢查 `frozen`。 |
| 7 | `output_token_account` | W |   | 用戶的**輸出** ATA。開放時不扣除 — 僅需要以便程序可以確認它不是 `frozen`（即填充稍後可以交付）。 |
| 8 | `input_vault` | W |   | 池輸入保管庫。 |
| 9 | `output_vault` | W |   | 池輸出保管庫。識別輸出鑄幣以進行 `output_token_account` / `output_vault_mint` 檢查。 |
| 10 | `input_vault_mint` |   |   | Token-2022 費用處理。 |
| 11 | `output_vault_mint` |   |   | 輸出端的鑄幣（凍結檢查配對）。 |
| 12 | `input_token_program` |   |   | SPL 或 Token-2022。 |
| 13 | `system_program`、`rent` |   |   | |

<Note>
  **帳戶列表變更（2026-07 發布）。** `OpenLimitOrder` 現在還採用**輸出端**帳戶 — `output_token_account`、`output_vault` 和 `output_vault_mint` — 除了輸入端。它們僅用於驗證：如果所有者的輸入**或**輸出代幣帳戶被凍結，程序拒絕訂單。這保證了填充實際上可以結算到所有者的輸出 ATA，這對於允許列表/預設凍結 Token-2022 鑄幣（例如許可代幣）很重要，其中帳戶可能尚未解凍。針對舊版單邊帳戶列表構建的客戶端必須添加三個輸出帳戶。
</Note>

**前置條件**

- `input_token_account` 和 `output_token_account` 都未凍結（否則 `NotApproved`）。
- `pool_state.status` 允許交換（位 4）和限價單（位 5）操作（否則 `NotApproved`）。
- `tick_index % pool.tick_spacing == 0` 且在 `[MIN_TICK, MAX_TICK]` 內。
- `tick_index` 在**`pool.tick_current` 的正確一側**用於選定的方向（出售 token0 → tick 必須在當前上方，反之亦然）。在已穿過的 tick 處出售會立即匹配並被拒絕。

**後置條件**

- `limit_order` 存在，快照開放時的 `tick.order_phase` 和 `tick.unfilled_ratio_x64`。
- `tick.orders_amount += amount`（在當前隊列中）。
- `limit_order_nonce.order_nonce += 1`。
- 發出 `OpenLimitOrderEvent`。

**常見錯誤** — `NotApproved`（輸入或輸出代幣帳戶凍結，或池禁用交換/限價單）、`InvalidLimitOrderAmount`（零或低於池的最小值）、`InvalidTickIndex`（超出 `[MIN_TICK, MAX_TICK]`，或在 `tick_current` 的錯誤一側用於選定方向）、`TickAndSpacingNotMatch`（`tick_index % pool.tick_spacing != 0`）、`OrderPhaseSaturated`。

## `IncreaseLimitOrder`

添加到現有未平倉訂單。僅可由訂單的 `owner` 調用。

**參數**

```
amount: u64    // 額外的輸入代幣金額
```

**帳戶** — 如 `OpenLimitOrder` 減去 nonce 帳戶；`limit_order` PDA 直接傳遞。

**前置條件**

- `limit_order.owner == signer`。
- 訂單仍在同一隊列中（`tick.order_phase == limit_order.order_phase`）。如果隊列已開始填充，訂單已部分結算 — 調用者應先調用 `DecreaseLimitOrder` 或 `SettleLimitOrder` 以向前推進。

**效果**

- 從所有者 ATA 轉移 `amount` 到 `input_vault`。
- `limit_order.total_amount += amount`；`tick.orders_amount += amount`。

## `DecreaseLimitOrder`

減少或完全取消未平倉訂單。將未成交的剩餘部分支付回所有者，加上過去部分填充已結算的任何輸出。

**參數**

```
amount:     u64    // 要提取的輸入代幣金額（最大 = 未成交剩餘）
amount_min: u64    // 輸入端提取的滑點下限
```

**帳戶** — 輸入和輸出代幣兩側：

| # | 名稱 | W | S |
|---|------|---|---|
| 1 | `owner` |   | S |
| 2 | `pool_state` | W |   |
| 3 | `tick_array` | W |   |
| 4 | `limit_order` | W |   |
| 5 | `input_token_account` | W |   |
| 6 | `output_token_account` | W |   |
| 7 | `input_vault` | W |   |
| 8 | `output_vault` | W |   |
| 9 | `input_vault_mint`、`output_vault_mint` |   |   |
| 10 | `token_program`、`token_program_2022` |   |   |

**效果**

- 從開放以來隊列的 `unfilled_ratio_x64` 重新計算訂單的已成交金額。
- 將已成交輸出發送到 `output_token_account`。
- 將 `amount` 的未成交輸入發送回 `input_token_account`。
- 相應地更新 `limit_order`。如果新的未成交剩餘為零，程序關閉帳戶並將租金退款給 `owner`。

## `SettleLimitOrder`

將已成交的輸出代幣推送給所有者，而不改變訂單的未成交剩餘。當 `auto_withdraw` 保管人想要滴灌支付長期運行的部分填充時很有用。

**調用者** — 訂單的 `owner`，或程序的 `limit_order_admin`（運行自動保管人循環的離線操作熱錢包）。保管人沒有其他權限 — 它不能將用戶資金移出推送已成交輸出到訂單的 `owner` ATA 之外。

**帳戶**

| # | 名稱 | W | S |
|---|------|---|---|
| 1 | `signer` |   | S | 所有者*或* `limit_order_admin` |
| 2 | `pool_state` |   |   | |
| 3 | `tick_array` |   |   | |
| 4 | `limit_order` | W |   | |
| 5 | `output_token_account` | W |   | 所有者的輸出 ATA。 |
| 6 | `output_vault` | W |   | 池輸出保管庫。 |
| 7 | `output_vault_mint` |   |   | |
| 8 | `output_token_program` |   |   | |

**效果**

- 使用 `(limit_order.unfilled_ratio_x64, tick.unfilled_ratio_x64)` 計算累積應付輸出。
- 轉移增量到 `output_token_account`。
- 更新 `limit_order.settled_output`。
- **不**關閉訂單；它仍對任何剩餘輸入開放。

## `CloseLimitOrder`

關閉完全消耗的訂單帳戶。租金始終返回給 `limit_order.owner`，無論誰簽署。

**調用者** — `owner` 或 `limit_order_admin`。

**前置條件**

- 訂單的未成交剩餘為零（`amount == total_amount` 已填充並結算，或所有者之前將訂單減少到零並忘記關閉）。

**效果**

- 關閉 `limit_order`；租金發送到 `limit_order.owner`。

## `CreateDynamicFeeConfig`（管理員）

在 u16 索引下創建可重用的參數集。

**參數**

```
index:                       u16
filter_period:               u16   // 秒；例如 30
decay_period:                u16   // 秒；例如 600。必須 > filter_period
reduction_factor:            u16   // 1..10_000；例如 5_000 = 每個衰減窗口 50% 保留
dynamic_fee_control:         u32   // 1..100_000；波動率到費用曲線的增益
max_volatility_accumulator:  u32   // 上限
```

**帳戶**

| # | 名稱 | W | S | 說明 |
|---|------|---|---|-------|
| 1 | `owner` | W | S | 硬編碼管理員公鑰。 |
| 2 | `dynamic_fee_config` | W |   | PDA，在此初始化。 |
| 3 | `system_program` |   |   | |

**常見錯誤** — 如果 `decay_period <= filter_period` 或任何 0 值欄位超出邊界，則 `InvalidDynamicFeeConfigParams`。

## `UpdateDynamicFeeConfig`（管理員）

修改現有的 `DynamicFeeConfig`。已在創建時快照配置的池**不**被追溯更新；僅引用此配置的新創建池將選擇新值。

**參數** — 與 `CreateDynamicFeeConfig` 相同的五個校準欄位（`filter_period`、`decay_period`、`reduction_factor`、`dynamic_fee_control`、`max_volatility_accumulator`）；`index` 在創建時固定，此處不重新傳遞。

## `CollectProtocolFee` / `CollectFundFee`

與 CPMM 的 `CollectProtocolFee` / `CollectFundFee` 相同的形狀。簽署者必須與 `AmmConfig.owner` / `AmmConfig.fund_owner` 匹配。從池的保管庫掃描累積的協議/基金費用到收件人，將相應的 `PoolState.protocol_fees_*` / `fund_fees_*` 欄位清零。

## `InitializeReward`

向池添加新的獎勵流。最多 3 個流可同時活躍。

**參數**

```
open_time:     u64
end_time:      u64
emissions_per_second_x64: u128   // Q64.64
```

**帳戶**

| # | 名稱 | W | S |
|---|------|---|---|
| 1 | `reward_funder` | W | S |
| 2 | `funder_token_account` | W |   |
| 3 | `amm_config` |   |   |
| 4 | `pool_state` | W |   |
| 5 | `operation_state` |   |   | CLMM 操作狀態 PDA 控制獎勵創建。 |
| 6 | `reward_token_mint` |   |   |
| 7 | `reward_token_vault` | W |   | 在此初始化。 |
| 8 | `reward_token_program` |   |   |
| 9 | `system_program`、`rent` |   |   |

**前置條件**

- 池上當前活躍的流少於 3 個。
- 資金提供者作為此指令的一部分將 `total_emission = emissions_per_second × (end_time − open_time)` 價值的獎勵代幣存入保管庫。
- 根據 `operation_state` 白名單獎勵鑄幣。

## `SetRewardParams`

延長、補充或更改現有獎勵流的發放速率。通常由池創建者或 Raydium 多簽調用。約束在鏈上：你通常可以延長 `end_time` 或增加發放，而不是追溯縮減。檢查 `operation_state` 的所有者列表。

## `UpdateRewardInfos`

純簿記 — 通過將 `emissions_per_second × Δt / liquidity` 相乘將 `reward_growth_global_x64` 結算到當前時間。由每個流動性觸及指令內部調用。作為獨立指令公開，因為外部參與者（UI、cranks）有時想觸發它。

## `CollectReward`

頭寸所有者聲稱應付的獎勵代幣。

**帳戶**

| # | 名稱 | W | S |
|---|------|---|---|
| 1 | `nft_owner` |   | S |
| 2 | `nft_account` |   |   | 所有者的 ATA 持有頭寸 NFT。 |
| 3 | `personal_position` | W |   |
| 4 | `pool_state` | W |   |
| 5 | `protocol_position` |   |   |   |
| 6 | `reward_token_vault` | W |   |
| 7 | `recipient_token_account` | W |   |
| 8 | `token_program` |   |   |
| 9 | `token_program_2022` |   |   |

**效果**

- 結算獎勵增長（與費用相同的模式）。
- 將應付金額轉移到收件人 ATA，將 `reward_amount_owed[i]` 清零。

## 狀態變更矩陣

| 指令 | `pool.liquidity` | `pool.fee_growth_global` | `pool.reward_growth_global` | `personal_position.liquidity` | Tick 陣列 |
|-------|------------------|---------------------------|------------------------------|--------------------------------|------------|
| `CreatePool` | 0 | 0 | — | — | — |
| `OpenPosition` | + 如果在範圍內 | — | — | 新建 | 添加 liquidity_gross/net |
| `IncreaseLiquidity` | + 如果在範圍內 | 結算應付 | 結算應付 | + | 調整 |
| `DecreaseLiquidity` | − 如果在範圍內 | 結算應付 | 結算應付 | − | 調整 |
| `ClosePosition` | — | — | — | 已銷毀 | — |
| `SwapV2` | ± 在穿過時 | + | — | — | 穿過並翻轉外部；匹配限價單隊列 |
| `OpenLimitOrder` | — | — | — | — | 目標 tick 上的 `orders_amount += amount` |
| `IncreaseLimitOrder` | — | — | — | — | `orders_amount += amount` |
| `DecreaseLimitOrder` | — | — | — | — | `orders_amount -=`，可能關閉隊列 |
| `SettleLimitOrder` | — | — | — | — | —（tick 上唯讀） |
| `CloseLimitOrder` | — | — | — | — | — |
| `CreateCustomizablePool` | 0 | 0 | — | — | — |
| `UpdateRewardInfos` | — | — | + | — | — |
| `CollectReward` | — | — | 結算應付 | — | — |

## 後續步驟

- [`products/clmm/code-demos`](/zh-Hant/products/clmm/code-demos) — 可運行的 TypeScript 示例。
- [`products/clmm/fees`](/zh-Hant/products/clmm/fees) — 費用和獎勵累積的詳細信息。
- [`reference/error-codes`](/zh-Hant/reference/error-codes) — 完整的 CLMM Anchor 錯誤表。

來源：

- [`raydium-io/raydium-clmm` — `programs/amm/src/instructions`](https://github.com/raydium-io/raydium-clmm)
- Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`
