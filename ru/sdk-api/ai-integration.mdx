---
title: "Интеграция с ИИ"
description: "Подключите документацию Raydium к вашему кодирующему агенту или LLM-воркфлоу — MCP-сервер, индекс llms.txt, копирование по страницам и готовые файлы контекста."
---

<Info>
  **Эта страница переведена с помощью ИИ. За эталон принимается английская версия.**

  [Открыть английскую версию →](/sdk-api/ai-integration)
</Info>

<Info>
  Эта документация разработана для использования как ИИ-инструментами, так и людьми. Если вы разрабатываете с помощью кодирующего агента (Claude Code, Cursor, Windsurf, Continue и т. д.) или запускаете RAG над документацией, приведённые ниже интерфейсы позволяют подключить документацию Raydium без пользовательского парсинга.
</Info>

## Доступные интерфейсы

| Интерфейс | URL-паттерн | Когда использовать |
|---|---|---|
| **MCP-сервер** | `https://docs.raydium.io/mcp` | Вы хотите, чтобы ваш ИИ-редактор (Claude Code, Cursor, Windsurf и т. д.) запрашивал и цитировал документацию Raydium нативно. |
| **Индекс `llms.txt`** | `https://docs.raydium.io/llms.txt` | Вы строите RAG-конвейер и нужен плоский индекс всех страниц. |
| **Тело `llms-full.txt`** | `https://docs.raydium.io/llms-full.txt` | Вы хотите весь корпус как один объединённый файл для офлайн-индексирования. |
| **Меню копирования по страницам** | Кнопка `Copy page` в верхней части каждой страницы | Вы вставляете одну страницу в чат с LLM. |
| **Глубокие ссылки по страницам** | `View as Markdown`, `Open in ChatGPT`, `Open in Claude`, `Open in Cursor`, `Open in VS Code` | Одноклик переход со страницы документации на выбранный инструмент. |

Кнопка `Copy page` и меню глубоких ссылок находятся в верхнем правом углу каждой страницы (рядом с названием страницы). Оба работают через контекстное меню платформы документации.

## MCP-сервер

MCP (Model Context Protocol) сервер позволяет ИИ-клиентам запрашивать документацию Raydium как инструмент. После настройки ваш агент может спросить «поищи в документации Raydium параметры `addLiquidity`» и получить авторитетные ответы со ссылками.

### Claude Code

```bash
claude mcp add --transport http raydium-docs https://docs.raydium.io/mcp
```

После добавления попросите Claude Code «поискать в документации Raydium X» и он будет использовать инструмент.

### Cursor

Добавьте в настройки Cursor (`Cmd/Ctrl + ,` → MCP):

```json
{
  "mcpServers": {
    "raydium-docs": {
      "url": "https://docs.raydium.io/mcp"
    }
  }
}
```

### Windsurf, Continue, универсальные клиенты

Любой MCP-совместимый клиент может указывать на `https://docs.raydium.io/mcp`. Эта конечная точка *и есть* поверхность обнаружения — `GET` возвращает конфигурацию сервера (имя, версию, транспорт, инструкции и список инструментов), поэтому отдельный файл манифеста не нужен и не отдаётся.

### Что предоставляет сервер

MCP-сервер документации Raydium предоставляет один основной инструмент — `search_docs(query: string)`, который возвращает наиболее релевантные отрывки для запроса вместе с их каноническими URL. Агент отвечает за цитирование использованного URL; мы не отслеживаем и не ограничиваем по агентам.

## `llms.txt` для RAG

`llms.txt` — это развивающийся стандарт для «машиночитаемого индекса документации». Raydium публикует:

- `https://docs.raydium.io/llms.txt` — плоский список всех страниц с названием и однострочным описанием, организованный по главам.
- `https://docs.raydium.io/llms-full.txt` — полное тело Markdown всех страниц, объединённое, с границами страниц, сохранёнными как заголовки.

Вариант `full` регенерируется при каждом развёртывании документации. Загружайте его по расписанию (ежедневно достаточно) или запрашивайте по требованию.

```bash
# Поместите весь корпус в ваш RAG-конвейер.
curl -sSL https://docs.raydium.io/llms-full.txt -o raydium-docs.md
```

## Меню передачи по страницам

Каждая страница содержимого имеет контекстное меню (верхний правый угол, рядом с названием) с этими одноклик-действиями:

- **Copy** — скопировать страницу как простой Markdown.
- **View as Markdown** — открыть исходный `.md` в новой вкладке, чтобы вы могли его сохранить.
- **Open in ChatGPT / Claude / Perplexity** — предзагрузить содержимое страницы в приглашение чата для названного инструмента.
- **Open in Cursor / VS Code** — открыть буфер в названном редакторе с содержимым страницы.

Это правильный интерфейс, когда вы хотите задать модели вопрос об одной странице без настройки MCP.

## Готовые файлы контекста для кодирующих агентов

Если вы интегрируетесь с Raydium и хотите, чтобы ваш агент имел правильные базовые знания с самого начала, поместите эти файлы в ваш проект:

### `.cursorrules` / `.windsurfrules` / системное приглашение агента

```text
You are integrating with Raydium, a Solana DeFi protocol with five product
surfaces: AMM v4, CPMM, CLMM, Farm, and LaunchLab. Authoritative docs live
at docs.raydium.io. The official SDK is `@raydium-io/raydium-sdk-v2` (pin
the version you've verified against). For server-built swaps, prefer the
Trade API at transaction-v1.raydium.io. Always:

- Pass a `Connection` and `cluster` that match.
- Keep all amounts as `BN` instances; never call `.toNumber()` on amounts.
- Pre-fund the user's wallet for ATA creation rent.
- Pass an explicit `computeBudgetConfig` for any tx that may compete in
  high-volume windows.
- Re-fetch `poolInfo` immediately before high-value transactions; cached
  state goes stale.

When uncertain about an instruction's accounts list, defer to
`docs.raydium.io/products/<product>/accounts` and the on-chain IDL.
```

Сохраните как `.cursorrules` (Cursor), `.windsurfrules` (Windsurf) или `CLAUDE.md` (Claude Code) — или вставьте в системное приглашение вашего агента. Точное имя файла и расположение варьируются в зависимости от версии инструмента, поэтому проверьте документацию настроек вашего инструмента, если он не подхватывает файл автоматически.

### Список закреплённого контекста

Для кодирующих агентов, которые принимают список «всегда включать» страницы, этот список — минимальный полезный контекст для большинства интеграций Raydium:

```text
https://docs.raydium.io/sdk-api/typescript-sdk
https://docs.raydium.io/sdk-api/trade-api
https://docs.raydium.io/products/cpmm/instructions
https://docs.raydium.io/products/clmm/instructions
https://docs.raydium.io/integration-guides/priority-fee-tuning
https://docs.raydium.io/reference/program-addresses
https://docs.raydium.io/reference/error-codes
```

Отрегулируйте в зависимости от того, какие продукты вы интегрируете.

## Рецепт индексирования

Если вы строите свой собственный RAG и не хотите использовать `llms-full.txt`, вот канонический рецепт:

```ts
import { fetch } from "undici";

const INDEX_URL = "https://docs.raydium.io/llms.txt";
const BASE      = "https://docs.raydium.io";

const index    = await (await fetch(INDEX_URL)).text();
const pageUrls = [...index.matchAll(/^- \[(.+?)\]\((.+?)\)/gm)].map(m => m[2]);

for (const path of pageUrls) {
  const md = await (await fetch(`${BASE}${path}.md`)).text();
  // ingest md into your vector store, keyed by path
}
```

Каждая страница обслуживается по своему каноническому пути с добавленным суффиксом `.md` (например, `/sdk-api/typescript-sdk` → `/sdk-api/typescript-sdk.md`).

## Оговорки

- **Не вставляйте приватное состояние в приглашения.** MCP-сервер знает только то, что находится в публичной документации. Ключи кошельков, учётные данные RPC и подобные секреты никогда не должны попадать в контекст агента.
- **Вывод ИИ не авторитетен.** Модели галлюцинируют адреса программ, имена инструкций и списки счётов с тревожной лёгкостью. Всегда проверяйте против документации и IDL перед тем, как доверять сгенерированным агентом транзакциям.
- **Дрейф версий.** SDK v2 находится в pre-1.0; агенты, обученные на более старых релизах, могут выдавать код, который не компилируется против вашей закреплённой версии. Включите закреплённую версию SDK в системное приглашение вашего агента.

## Ссылки

- [`sdk-api/typescript-sdk`](/ru/sdk-api/typescript-sdk) — основной справочник SDK.
- [`sdk-api/trade-api`](/ru/sdk-api/trade-api) — транзакции свопов, построенные на сервере, идеальны для дружественных к агентам интеграций.
- [`reference/program-addresses`](/ru/reference/program-addresses) — проверенные ID программ.
- [Model Context Protocol](https://modelcontextprotocol.io) — спецификация MCP.
- [`llms.txt` proposal](https://llmstxt.org) — контекст стандарта индекса.

Источники:

- [Поддержка Mintlify `llms.txt`](https://www.mintlify.com/docs/ai/llmstxt) и [AI ingestion](https://mintlify.com/docs/ai-ingestion).
- Развёртывание документации Raydium.
