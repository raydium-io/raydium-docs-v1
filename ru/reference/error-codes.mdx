---
title: "Коды ошибок"
description: "Перечисления ошибок Anchor для всех программ Raydium на цепи с описанием причин и рекомендуемыми сообщениями для пользователя."
---

<Info>
  **Эта страница переведена с помощью ИИ. За эталон принимается английская версия.**

  [Открыть английскую версию →](/reference/error-codes)
</Info>

<Info>
  **Источник истины.** Таблицы ниже регенерируются из файла `error.rs` каждой программы в публичных репозиториях Raydium. Когда программа обновляется и добавляется новый вариант, повторно запустите извлечение (ссылка в конце каждой таблицы) и добавьте строку в таблицу вместо переупорядочивания — коды ошибок Anchor назначаются по **порядку в исходном коде**, а не по имени, поэтому переупорядочивание нарушает обработку ошибок у интеграторов.
</Info>

## Как работают коды ошибок Anchor

Anchor присваивает каждому варианту перечисления `ErrorCode` программы числовой код, начиная с `6000`. Неудачная транзакция выводит:

- **Числовой код ошибки** (например, `0x1771` = 6001) в логах транзакции.
- **Имя ошибки** (например, `InvalidOwner`) из IDL.
- **Строку `#[msg(...)]`**, которую выдал Anchor в `log_messages`.

Интеграторы должны сопоставлять по **числовому коду**, а не по строке сообщения (строка может быть переформулирована без изменения версии).

```ts
// Пример: обнаружить ошибку slippage CPMM и показать чистое сообщение пользователю.
const CPMM_EXCEEDED_SLIPPAGE = 6005;

try {
  await sdk.cpmm.swap({ ... });
} catch (err: any) {
  const code = err?.error?.errorCode?.number ?? err?.code;
  if (code === CPMM_EXCEEDED_SLIPPAGE) {
    showToast("Price moved past your slippage tolerance. Increase slippage or retry.");
    return;
  }
  throw err;
}
```

## Ошибки CPMM (Standard AMM)

ID программы: см. [reference/program-addresses](/ru/reference/program-addresses#on-chain-programs). Источник: `raydium-cp-swap/programs/cp-swap/src/error.rs`.

| Код | Вариант | Строка `#[msg]` | Типичная причина | Рекомендуемое сообщение |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Вызывающий не является настроенным администратором для инструкции администратора. | "Only the pool admin can perform this action." |
| 6001 | `InvalidOwner` | Input account owner is not the program address | Переданный счёт принадлежит неправильной программе (часто неправильная программа токенов или PDA неправильной программы). | "Internal: account owner mismatch — refresh and retry." |
| 6002 | `EmptySupply` | Input token account empty | Баланс счёта LP или токена равен нулю на этапе, требующем положительной суммы. | "Nothing to withdraw." |
| 6003 | `InvalidInput` | InvalidInput | Неправильный аргумент (сумма вне диапазона, неправильный флаг). | "Invalid input — check amounts and try again." |
| 6004 | `IncorrectLpMint` | Address of the provided lp token mint is incorrect | Переданный счёт LP mint не совпадает с `pool_state.lp_mint`. | "Internal: wrong LP mint — refresh pool data." |
| 6005 | `ExceededSlippage` | Exceeds desired slippage limit | Выполненная цена хуже, чем `minAmountOut` / `maxAmountIn` пользователя. | "Price moved past your slippage tolerance. Increase slippage or retry." |
| 6006 | `ZeroTradingTokens` | Given pool token amount results in zero trading tokens | Математика депозита / вывода округлила одну сторону до нуля (позиция слишком мала). | "Amount is below the minimum for this pool." |
| 6007 | `NotSupportMint` | Not support token_2022 mint extension | Пул встретил расширение Token-2022, которое не может безопасно обработать (например, `TransferHook`, `DefaultAccountState=Frozen`). | "This token has an extension Raydium does not support in CPMM." |
| 6008 | `InvalidVault` | invaild vault | Переданный счёт хранилища не совпадает с записанным в `pool_state`. | "Internal: wrong vault — refresh and retry." |
| 6009 | `InitLpAmountTooLess` | Init lp amount is too less(Because 100 amount lp will be locked) | При инициализации пула вычисленное предложение LP ниже постоянно заблокированной суммы. | "Initial liquidity too small. Increase deposit." |
| 6010 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Наблюдаемая сумма после передачи для Token-2022 с комиссией не совпадает с предварительно вычисленным ожиданием. | "Token transfer fee changed mid-transaction. Retry." |
| 6011 | `MathOverflow` | Math overflow | Промежуточный расчёт swap / депозита / комиссии переполнился. | "Amount is too large for this pool." |
| 6012 | `InsufficientVault` | Insufficient vault | Баланс хранилища пула слишком низок для покрытия запрошенного выхода. | "Not enough liquidity in the pool for this size." |
| 6013 | `InvalidFeeModel` | Invalid fee model | Администратор установил комбинацию параметров `AmmConfig`, которая отклоняется на цепи. | N/A — путь только для администратора. |
| 6014 | `NoFeeCollect` | Fee is zero | `collect_protocol_fee` / `collect_fund_fee` вызвана с нулевыми собираемыми комиссиями. | N/A — путь администратора; инструменты должны игнорировать. |

Источник регенерации: [github.com/raydium-io/raydium-cp-swap — error.rs](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs).

## Ошибки CLMM

ID программы: см. [reference/program-addresses](/ru/reference/program-addresses#on-chain-programs). Источник: `raydium-clmm/programs/amm/src/error.rs`.

| Код | Вариант | Строка `#[msg]` | Типичная причина | Рекомендуемое сообщение |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Вызывающий не является настроенным администратором для этой инструкции; **или** (инструкции разрешений) подписант не является ни `admin`, ни `permission_pda_admin`; **или** (`OpenLimitOrder`) счёт входного/выходного токена заморожен или пул имеет отключённый swap / limit-order. | "Only the pool admin can perform this action." / "This token account is frozen — thaw it before placing the order." |
| 6001 | `InvalidUpdateConfigFlag` | invalid update amm config flag | Администратор передал неизвестное значение `param` в `update_amm_config`. | N/A — путь только для администратора. |
| 6002 | `AccountLack` | Account lack | Требуемый оставшийся счёт отсутствует в транзакции (обычно tick-array или расширение oracle). | "Internal: missing account — refresh pool data." |
| 6003 | `ClosePositionErr` | Remove liquidity, collect fees owed and reward then you can close position account | Попытка закрыть позицию, которая всё ещё имеет ликвидность, невыплаченные комиссии или невыплаченные награды. | "Withdraw all liquidity and claim fees/rewards before closing the position." |
| 6004 | `InvalidTickIndex` | Tick out of range | `tick_lower` или `tick_upper` находится вне `[-443636, 443636]`. | "Price range out of bounds for this pool." |
| 6005 | `TickInvalidOrder` | The lower tick must be below the upper tick | `tick_lower >= tick_upper`. | "Lower price must be below upper price." |
| 6006 | `TickLowerOverflow` | The tick must be greater, or equal to the minimum tick(-443636) | Нижний tick переполнился вниз. | "Lower price too low." |
| 6007 | `TickUpperOverflow` | The tick must be lesser than, or equal to the maximum tick(443636) | Верхний tick переполнился вверх. | "Upper price too high." |
| 6008 | `TickAndSpacingNotMatch` | tick % tick_spacing must be zero | Переданный tick не кратен `tick_spacing` пула. | "Snap price to the nearest valid increment." |
| 6009 | `InvalidTickArray` | Invalid tick array account | Неправильный PDA передан для слота tick-array. | "Internal: wrong tick array — refresh pool data." |
| 6010 | `InvalidTickArrayBoundary` | Invalid tick array boundary | Ошибка на единицу в индексировании tick-array. | "Internal: tick-array boundary error." |
| 6011 | `SqrtPriceLimitOverflow` | Square root price limit overflow | Переданный вызывающим `sqrt_price_limit` вне допустимого диапазона. | "Price limit out of range." |
| 6012 | `SqrtPriceX64` | sqrt_price_x64 out of range | Текущая sqrt цена пула вышла из диапазона во время swap. | "Retry swap." |
| 6013 | `LiquiditySubValueErr` | Liquidity sub delta L must be smaller than before | Нарушение внутреннего инварианта при уменьшении ликвидности. | "Internal: liquidity accounting error." |
| 6014 | `LiquidityAddValueErr` | Liquidity add delta L must be greater, or equal to before | Нарушение внутреннего инварианта при увеличении ликвидности. | "Internal: liquidity accounting error." |
| 6015 | `ForbidBothZeroForSupplyLiquidity` | Both token amount must not be zero while supply liquidity | Вызов increase-liquidity с обоими `amount_0_max` и `amount_1_max` равными нулю. | "Provide at least one token." |
| 6016 | `LiquidityInsufficient` | Liquidity insufficient | Позиция не имеет достаточной ликвидности для удовлетворения вывода. | "Withdraw amount exceeds position liquidity." |
| 6017 | `PriceSlippageCheck` | Price slippage check | Цена выполнения не прошла проверку slippage вызывающего. | "Price moved past your slippage tolerance. Increase slippage or retry." |
| 6018 | `TooLittleOutputReceived` | Too little output received | `SwapBaseInput`: выходная сумма ниже `other_amount_threshold`. | "Slippage exceeded — minimum output not met." |
| 6019 | `TooMuchInputPaid` | Too much input paid | `SwapBaseOutput`: входная сумма выше `other_amount_threshold`. | "Slippage exceeded — maximum input exceeded." |
| 6020 | `ZeroAmountSpecified` | Swap special amount can not be zero | Нулевая `amount` в инструкции swap. | "Enter an amount greater than zero." |
| 6021 | `InvalidInputPoolVault` | Input pool vault is invalid | Счёт входного хранилища swap не совпадает с записанным в пуле. | "Internal: wrong input vault — refresh pool data." |
| 6022 | `TooSmallInputOrOutputAmount` | Swap input or output amount is too small | Математика swap округлила до нуля, обычно очень маленькая пыль. | "Amount too small to swap in this pool." |
| 6023 | `NotEnoughTickArrayAccount` | Not enough tick array account | Недостаточно оставшихся счётов tick-array для диапазона swap. | "Internal: insufficient tick-arrays — refresh pool data." |
| 6024 | `InvalidFirstTickArrayAccount` | Invalid first tick array account | Первый переданный счёт tick-array не охватывает текущий tick. | "Internal: wrong first tick array — refresh pool data." |
| 6025 | `InvalidRewardIndex` | Invalid reward index | `reward_index` вне `[0, 2]`. | N/A — путь администратора. |
| 6026 | `FullRewardInfo` | The init reward token reach to the max | Пул уже имеет максимум (3) настроенных reward mints. | N/A — путь администратора. |
| 6027 | `RewardTokenAlreadyInUse` | The init reward token already in use | Reward mint дублирует существующий. | N/A — путь администратора. |
| 6028 | `ExceptRewardMint` | The reward tokens must contain one of pool vault mint except the last reward | До слота 2, reward mint должен равняться одному из vault mints пула. | N/A — путь администратора. |
| 6029 | `InvalidRewardInitParam` | Invalid reward init param | Неправильное время начала/конца выпуска или скорость в секунду. | N/A — путь администратора. |
| 6030 | `InvalidRewardInputAccountNumber` | Invalid collect reward input account number | Неправильное количество оставшихся счётов, переданных в `CollectReward`. | "Internal: wrong account count — refresh and retry." |
| 6031 | `InvalidRewardPeriod` | Invalid reward period | Период выпуска награды неправильный (`end <= start`, нулевая длительность). | N/A — путь администратора. |
| 6032 | `NotApproveUpdateRewardEmissions` | Modification of emissions is allowed within 72 hours from the end of the previous cycle | Администратор попытался изменить выпуск вне 72-часового окна вокруг границ цикла. | N/A — путь администратора. |
| 6033 | `UnInitializedRewardInfo` | uninitialized reward info | Слот награды не инициализирован, но на него ссылаются. | N/A — путь администратора. |
| 6034 | `NotSupportMint` | Not support token_2022 mint extension | CLMM встретил расширение Token-2022, которое не может обработать. | "This token has an extension Raydium CLMM does not support." |
| 6035 | `MissingTickArrayBitmapExtensionAccount` | Missing tickarray bitmap extension account | Swap пересёк диапазон tick, не охватываемый базовой битовой картой; требуется счёт расширения битовой карты. | "Internal: missing tick-array extension — refresh pool data." |
| 6036 | `InsufficientLiquidityForDirection` | Insufficient liquidity for this direction | Недостаточно ликвидности в направлении swap. | "Insufficient liquidity for this swap size." |
| 6037 | `MaxTokenOverflow` | Max token overflow | Входная/выходная сумма превысила u64. | "Amount too large for this pool." |
| 6038 | `CalculateOverflow` | Calculate overflow | Переполнение арифметики комиссии / ликвидности. | "Amount too large for this pool." |
| 6039 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Наблюдаемая сумма комиссии Token-2022 не совпадает с ожидаемой. | "Token transfer fee changed mid-transaction. Retry." |
| 6040 | `OrderAlreadyFilled` | Order already fully filled, cannot modify | `IncreaseLimitOrder` / `DecreaseLimitOrder` вызвана для ордера, чья невыполненная часть равна нулю. | "This limit order is already filled — settle to receive output." |
| 6041 | `InvalidOrderPhase` | Invalid order phase | Изменение ордера, чья фаза когорты FIFO больше не совпадает с текущей когортой tick. | "Cannot perform this action in the order's current state." |
| 6042 | `InvalidLimitOrderAmount` | Invalid limit order amount | Входная сумма ордера ниже минимума пула (или нулевая) при открытии / увеличении / уменьшении. | "Order size is below the pool's minimum." |
| 6043 | `OrderPhaseSaturated` | Tick order phase saturated | Счётчик `order_phase` когорты на tick насыщен; дальнейшие ордера не могут быть открыты на этом tick, пока существующие когорты не урегулируются и не перейдут. | "Too many active orders at this price; try a nearby tick or wait for orders to settle." |
| 6044 | `InvalidDynamicFeeConfigParams` | Invalid dynamic fee config params | `CreateDynamicFeeConfig` / `UpdateDynamicFeeConfig` отклонена; или `CreateCustomizablePool` включила динамическую комиссию без правильной конфигурации. | N/A на пути администратора; "Dynamic fee config invalid" на пути пользователя. |
| 6045 | `InvalidFeeOn` | Invalid fee on which token (must be 0, 1, or 2) | `CreateCustomizablePool` передала значение `collect_fee_on` вне `{0, 1, 2}`. | "Internal: invalid fee mode." |
| 6046 | `ZeroSqrtPrice` | sqrt_price_x64 must be greater than 0 | `CreateCustomizablePool` (или другой путь, принимающий настраиваемую начальную sqrt цену) вызвана с `sqrt_price_x64 == 0`. | "Initial price must be greater than zero." |
| 6047 | `ZeroLiquidity` | liquidity must be greater than 0 | Путь предоставления ликвидности вызван с `liquidity == 0` и без компенсирующей суммы. | "Liquidity amount must be greater than zero." |
| 6048 | `MissingBaseFlag` | base_flag is required when liquidity is zero | Путь открытия позиции по сумме вычислил `liquidity == 0`, и вызывающий не предоставил `base_flag` для уточнения, какая сторона является базой. | "Provide either a non-zero liquidity or specify which token is the base." |
| 6049 | `MissingMintAccount` | Mint account is required but not provided | Путь, осведомлённый о Token-2022, вызван без счёта входного/выходного mint, необходимого для проверки расширений и комиссий передачи. | "Internal: missing mint account — refresh pool data." |
| 6050 | `MissingTokenProgram2022` | Token-2022 program is required but not provided | То же самое для счёта программы SPL-Token-2022. | "Internal: missing Token-2022 program — refresh and retry." |

> **Примечание о переномеровании.** Перечисление `ErrorCode` CLMM было переномеровано в этом выпуске: пять устаревших вариантов (`LOK`, `ZeroMintAmount`, `InvalidLiquidity`, `TransactionTooOld`, `InvalidRewardDesiredAmount`) и несколько опечаток (`Liquitity`, `enought`, `emissiones`) были удалены/исправлены, и одиннадцать новых вариантов добавлены. Поскольку Anchor нумерует ошибки по порядку в исходном коде, каждый код на или после `6000` сместился относительно сборок до выпуска. Клиенты, которые жёстко кодировали числовые коды для более ранней версии, должны переотобразить.

Источник регенерации: [github.com/raydium-io/raydium-clmm — error.rs](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs).

## Ошибки AMM v4, Farm v3 / v5 / v6, LaunchLab

Эти программы документированы в их соответствующих главах (см. [`products/amm-v4/instructions`](/ru/products/amm-v4/instructions), [`products/farm-staking/instructions`](/ru/products/farm-staking/instructions), [`products/launchlab/instructions`](/ru/products/launchlab/instructions)). Поскольку эти программы используют смесь поверхностей ошибок Anchor и простого Solana, их таблицы ошибок находятся рядом со справочником инструкций, а не здесь. Коды ниже зарезервированы этими главами:

| Программа | Диапазон кодов (только Anchor) | Справочник |
|---------|--------------------------|-----------|
| AMM v4 | Пользовательские коды u32, не в стиле Anchor | [`products/amm-v4/instructions`](/ru/products/amm-v4/instructions) |
| Farm v3 / v5 | Пользовательские коды u32 | [`products/farm-staking/instructions`](/ru/products/farm-staking/instructions) |
| Farm v6 | 6000+ (Anchor) | [`products/farm-staking/instructions`](/ru/products/farm-staking/instructions) |
| LaunchLab | 6000+ (Anchor) | [`products/launchlab/instructions`](/ru/products/launchlab/instructions) |

## Сопоставление ошибок SDK с ошибками программы

Официальный TypeScript SDK оборачивает ошибки на цепи в `SendTransactionError` и для программ Anchor в `AnchorError`:

```ts
import { AnchorError } from "@coral-xyz/anchor";

try {
  await sdk.cpmm.swap({ ... });
} catch (err) {
  if (err instanceof AnchorError) {
    console.log(err.error.errorCode.number, err.error.errorCode.code, err.error.errorMessage);
    // 6005 ExceededSlippage "Exceeds desired slippage limit"
  } else {
    // Raw SendTransactionError — inspect err.logs for the "custom program error: 0x..." line.
  }
}
```

Если вы не используете Anchor на клиентской стороне, разберите логи транзакции:

```
Program <ProgramID> invoke [1]
Program log: AnchorError caused by account: pool_state. Error Code: ExceededSlippage. Error Number: 6005. Error Message: Exceeds desired slippage limit.
```

Паттерн `Error Number: (\d+)` стабилен во всех версиях Anchor и безопасен для сопоставления.

## Регенерация этих таблиц

Когда программа обновляется и добавляется новая ошибка, повторно извлеките из исходного кода:

```bash
# Clone and grep the error enum in order.
git clone --depth=1 https://github.com/raydium-io/raydium-cp-swap
awk '/#\[msg\(/{ gsub(/^[ \t]*#\[msg\("|"\)\][ \t]*/,""); m=$0; getline; gsub(/,/,""); gsub(/^[ \t]+/,""); print "6000+++\t" $0 "\t" m }' \
  raydium-cp-swap/programs/cp-swap/src/error.rs
# Replace 6000+++ with monotonically-increasing 6000,6001,... Append new rows to the table above.
```

Всегда обновляйте [`reference/changelog`](/ru/reference/changelog) при добавлении нового варианта, чтобы интеграторы, обновляющие SDK, знали, что нужно обновить обработчики ошибок.

Источники:

- [raydium-cp-swap error.rs (mainnet CPMM)](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)
- [raydium-clmm error.rs (mainnet CLMM)](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)
