---
title: "CPMM コード例"
description: "すべての CPMM 操作のエンドツーエンド TypeScript 例 — プール作成、スワップ、流動性供給、流動性引き出し、手数料回収 — および最小限の Rust CPI スケルトン。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/products/cpmm/code-demos)

  **バージョン バナー。** すべての TypeScript 例は `@raydium-io/raydium-sdk-v2@0.2.64-alpha` を対象としています。最後に実行されたのは `0.2.42-alpha`（2026-04）に対してで、呼び出しシグネチャは 2026-09-09 に `0.2.64-alpha` のソースに対して、Solana メインネット ベータ上で再確認されました。Rust CPI スケルトンは `chore/upgrade-anchor` ブランチの `raydium-cp-swap`、Anchor `1.0.2` を対象としています — `master` は今も `0.32.1` に固定されています。プログラム ID は [`reference/program-addresses`](/ja/reference/program-addresses)の定数経由で取得されます。
</Info>

<Info>
  **バージョン情報。** すべての TypeScript デモは `@raydium-io/raydium-sdk-v2@0.2.64-alpha` を対象とし、Solana mainnet-beta で検証済み（2026年4月）。Rust CPI スケルトンは `raydium-cp-swap` の `master` ブランチ、Anchor `0.30.x` を対象としています。プログラム ID は [`reference/program-addresses`](/ja/reference/program-addresses) の定数から取得されます。
</Info>

## 前提条件

```bash
npm install @raydium-io/raydium-sdk-v2 @solana/web3.js @solana/spl-token bn.js decimal.js
```

このページのすべてのデモは [`raydium-sdk-V2-demo/src/cpmm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/cpmm) のファイルに対応しており、GitHub リンクは各セクションの横に配置されています。ブートストラップはデモリポジトリの `config.ts.template` に従います（[ソース](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/config.ts.template)）：

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);

const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});
```

`Raydium` インスタンスは SDK のファサードです — 以下のすべてのデモで使用されます。`Raydium.load` はデフォルトでトークンリストを読み込むため非同期です。そのフェッチをスキップするには `disableLoadToken: true` を渡してください。可用性のフィーチャーチェックは、`disableFeatureCheck: false` を渡さない限り既にスキップされます。手数料設定は初回使用時に `raydium.api.getCpmmConfigs()` 経由で遅延フェッチされます。

## CPMM プールの作成

ソース：[`src/cpmm/createCpmmPool.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/createCpmmPool.ts)

```ts
import { PublicKey } from "@solana/web3.js";
import BN from "bn.js";
import { CREATE_CPMM_POOL_PROGRAM, CREATE_CPMM_POOL_FEE_ACC }
  from "@raydium-io/raydium-sdk-v2";
// (If you need every pool PDA at once, the exported helper is `getCreatePoolKeys`.
//  There is no `getCpmmPdas`.)

const mintA = new PublicKey("So11111111111111111111111111111111111111112"); // wSOL
const mintB = new PublicKey("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"); // USDC

// 1. 手数料設定を選択します。index=0 は 0.25% ティアです。
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0)!;

// 2. ミントメタデータを取得して、SDK が Token-2022 拡張機能を処理できるようにします。
const mintAInfo = await raydium.token.getTokenInfo(mintA);
const mintBInfo = await raydium.token.getTokenInfo(mintB);

// 3. トランザクションを構築します。
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA:           mintAInfo,
  mintB:           mintBInfo,
  mintAAmount:     new BN(1_000_000_000),   // 1 SOL（9 デシマルと仮定）
  mintBAmount:     new BN(   160_000_000),  // 160 USDC（160/SOL）
  startTime:       new BN(0),               // 即座にオープン
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Pool created at", extInfo.address.poolId.toBase58());
console.log("Tx:", txId);
```

SDK が静かに処理する事項：

- PDA を導出する前にミントを token0/token1 の順序にソートします。
- 1 回限りの `create_pool_fee` を `poolFeeAccount` に支払います。
- 呼び出し元の関連トークンアカウントが不足している場合は作成します。
- 各側に対して正しいトークンプログラム（SPL Token vs Token-2022）を選択します。

確認後、ライブプール状態を取得できます：

```ts
// getPoolInfoFromRpc takes a positional base58 string, not a PublicKey and not an object.
const { poolKeys, poolInfo, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(
  extInfo.address.poolId.toBase58(),
);
```

## スワップ（ベース入力）

ソース：[`src/cpmm/swap.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swap.ts)

```ts
import { CurveCalculator, FeeOn } from "@raydium-io/raydium-sdk-v2";

const POOL_ID = "<POOL_ID>";   // base58 string

// 1. RPC から直接現在のプール状態を読み込みます（API からではなく）。
const { poolInfo, poolKeys, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(POOL_ID);

const inputMint  = poolInfo.mintA.address;                // A → B をスワップ
const amountIn   = new BN(100_000_000);                   // 0.1 SOL
const slippage   = 0.005;                                 // 0.5%

// 2. ローカルで見積もります。CurveCalculator はオンチェーン数学をミラーリングし、
//    両側の Token-2022 転送手数料も含みます。8 つの引数すべてが必須です。
const baseIn = inputMint === poolInfo.mintA.address;
const swapResult = CurveCalculator.swapBaseInput(
  amountIn,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// swapResult fields are inputAmount / outputAmount / tradeFee / protocolFee /
// fundFee / creatorFee / newInputVaultAmount / newOutputVaultAmount.
// There is no destinationAmountSwapped or sourceAmountSwapped.

// 3. 構築して送信します。ここで `slippage` を渡すことが境界を適用します —
//    手動での最小出力の計算は不要です。
const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: amountIn,
  swapResult,
  slippage,
  baseIn,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Swap tx:", txId);
```

注意：SDK は `getPoolInfoFromRpc` 内で **常に** RPC からプール状態を再フェッチします。署名しようとしているトランザクションの見積もりを `api-v3.raydium.io` から取得しないでください — 1 ブロック古い見積もりはランド時に `ExceededSlippage` にスリップする可能性があります。

## スワップ（ベース出力）

ソース：[`src/cpmm/swapBaseOut.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swapBaseOut.ts)

```ts
const amountOutWanted = new BN(15_000_000);        // 15 USDC
const slippage        = 0.005;

const baseIn = false; // false → mintB が入力側
const swapResult = CurveCalculator.swapBaseOutput(
  amountOutWanted,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// swapResult.inputAmount is the required input; there is no sourceAmountSwapped.

const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: new BN(0),   // unused when fixedOut — the amount comes from swapResult
  fixedOut:    true,
  swapResult,               // required, and it carries the target output amount
  baseIn,
  slippage,
  txVersion:   TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## 流動性の供給

ソース：[`src/cpmm/deposit.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/deposit.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";
import Decimal from "decimal.js";

// addLiquidity は LP 量ではなく INPUT 量で駆動されます。
const uiInputAmount = "0.0001";
const inputAmount = new BN(
  new Decimal(uiInputAmount).mul(10 ** poolInfo.mintA.decimals).toFixed(0),
);
const slippage = new Percent(1, 100);   // Percent であり、数値ではありません

const { execute } = await raydium.cpmm.addLiquidity({
  poolInfo,
  poolKeys,
  inputAmount,
  baseIn: true,           // inputAmount is denominated in mintA
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

SDK はプールの現在のリザーブからペアとなる金額を導出し、命令の `maximum_*` 引数のために各側をスリッページ分だけ増やし、必要に応じて ATA 作成を構築します。UI プレビュー用にこれらの数値が必要な場合は、`raydium.cpmm.computePairAmount({ ... })` が同じ値を返します。

## 流動性の引き出し

ソース：[`src/cpmm/withdraw.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/withdraw.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";

const lpAmount = new BN(100_000);           // バーンする LP
const slippage = new Percent(1, 100);       // Percent であり、数値ではありません

const { execute } = await raydium.cpmm.withdrawLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## プロトコル/ファンド/クリエイター手数料の回収

ソース：[`src/cpmm/collectCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectCreatorFee.ts)、[`src/cpmm/collectAllCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectAllCreatorFee.ts)

プロトコルとファンドの回収は権限ゲートのままです。クリエイター手数料には 2 つのパスがあります：クリエイター署名の `CollectCreatorFee` と、追加の `CollectCreatorFeePermissionless` で、後者は任意のペイヤーが呼び出せますが、クリエイターの正規 ATA にのみ転送できます。`0.2.64-alpha` は、両方のクリエイター パス向けのモジュールレベルのビルダーを同梱しています：

```ts
// creator-signed
const { execute } = await raydium.cpmm.collectCreatorFees({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// permissionless — any payer; proceeds go to the creator's canonical ATAs
const { execute } = await raydium.cpmm.collectCreatorFeesPermissionless({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// several pools at once, creator-signed
await raydium.cpmm.collectMultiCreatorFees({ poolInfoList, txVersion: TxVersion.V0 });
```

<Note>
  2 つのクリエイター命令は **ディスクリミネータとアカウント レイアウトが異なります** —
  パーミッションレス版はクリエイター署名者 / `amm_config` の位置を、別の `payer` と署名しない
  `creator` に置き換えます。もう一方でアカウントを手動で入れ替えるのではなく、対応するビルダーを
  使用してください。

  SDK にプロトコル手数料やファンド手数料のビルダーはありません（`makeCollectProtocolFeeInstruction`
  と `makeCollectFundFeeInstruction` はどの名前でも存在しません） — それらは管理者パスです。
  実際に *存在する* 低レベル命令ビルダーは `collectCpFeeInstruction`、
  `makeCollectCreatorFeeInstruction`、`collectCreatorFeePermissionlessInInstruction` です。
</Note>

オフチェーンでは、`PoolState` から直接蓄積手数料を読み取ることができます：

```ts
const pool = await raydium.cpmm.getRpcPoolInfo(POOL_ID);   // base58 string
console.log("Accrued protocol fee mintA:", pool.protocolFeesMintA.toString());
console.log("Accrued protocol fee mintB:", pool.protocolFeesMintB.toString());
// The CpmmPoolInfoLayout fields are protocolFeesMintA/B, fundFeesMintA/B and
// creatorFeesMintA/B — there are no ...Token0/...Token1 fields.
```

## Rust CPI スケルトン

独自の Anchor プログラムから CPMM を呼び出したい場合 — 例えば、デポジターの代わりにスワップするボールト — CPI コンテキストは次のようになります。アカウント順序は [`products/cpmm/instructions`](/ja/products/cpmm/instructions) に従います。

```rust
// Cargo.toml — the branch matters: `master` still pins anchor-lang 0.32.1.
// raydium-cp-swap = { git = "https://github.com/raydium-io/raydium-cp-swap",
//                     branch = "chore/upgrade-anchor", features = ["cpi"] }
// anchor-lang      = "=1.0.2"   // raydium-cp-swap がピン留めしたものと一致する必要があります
// anchor-spl       = "=1.0.2"
//
// The `cpi` feature is required — the `raydium_cp_swap::cpi` module only exists
// under it. Note this crate cannot share a program with the CLMM sample in
// products/clmm/code-demos, which pins anchor-lang =0.32.1.

use anchor_lang::prelude::*;
use anchor_spl::token_interface::{TokenAccount, TokenInterface, Mint};
use raydium_cp_swap::cpi::accounts::Swap;
use raydium_cp_swap::cpi;
use raydium_cp_swap::program::RaydiumCpSwap;

#[derive(Accounts)]
pub struct ProxySwap<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,

    /// CHECK: CPMM プログラムで検証されます
    pub authority:     UncheckedAccount<'info>,
    /// CHECK:
    pub amm_config:    UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub pool_state:    UncheckedAccount<'info>,

    #[account(mut)]
    pub input_token_account:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_token_account: InterfaceAccount<'info, TokenAccount>,

    #[account(mut)]
    pub input_vault:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_vault: InterfaceAccount<'info, TokenAccount>,

    pub input_token_program:  Interface<'info, TokenInterface>,
    pub output_token_program: Interface<'info, TokenInterface>,

    pub input_token_mint:  InterfaceAccount<'info, Mint>,
    pub output_token_mint: InterfaceAccount<'info, Mint>,

    #[account(mut)]
    /// CHECK: リングバッファ
    pub observation_state: UncheckedAccount<'info>,

    pub cpmm_program: Program<'info, RaydiumCpSwap>,
}

pub fn proxy_swap_base_input(
    ctx: Context<ProxySwap>,
    amount_in: u64,
    minimum_amount_out: u64,
) -> Result<()> {
    let cpi_accounts = Swap {
        payer:                ctx.accounts.payer.to_account_info(),
        authority:            ctx.accounts.authority.to_account_info(),
        amm_config:           ctx.accounts.amm_config.to_account_info(),
        pool_state:           ctx.accounts.pool_state.to_account_info(),
        input_token_account:  ctx.accounts.input_token_account.to_account_info(),
        output_token_account: ctx.accounts.output_token_account.to_account_info(),
        input_vault:          ctx.accounts.input_vault.to_account_info(),
        output_vault:         ctx.accounts.output_vault.to_account_info(),
        input_token_program:  ctx.accounts.input_token_program.to_account_info(),
        output_token_program: ctx.accounts.output_token_program.to_account_info(),
        input_token_mint:     ctx.accounts.input_token_mint.to_account_info(),
        output_token_mint:    ctx.accounts.output_token_mint.to_account_info(),
        observation_state:    ctx.accounts.observation_state.to_account_info(),
    };
    // Anchor 1.0 はここでプログラムの AccountInfo ではなく Pubkey を取ります。
    let cpi_ctx = CpiContext::new(
        *ctx.accounts.cpmm_program.key,
        cpi_accounts,
    );
    cpi::swap_base_input(cpi_ctx, amount_in, minimum_amount_out)
}
```

CPI が PDA として署名する場合（例えば、デポジターの代わりにボールトを管理する場合）、`CpiContext::new` を `CpiContext::new_with_signer` に置き換えてシードを渡します。

## よくある落とし穴

サポートチケットを開く前の短いチェックリスト：

- **ソート済みミント。** 導出した `poolState` PDA がオンチェーンプールと一致しない場合、おそらくミントのソートを忘れています。
- **古い API 見積もり。** `api-v3.raydium.io` からのリザーブ値を `CurveCalculator.swap` に渡さないでください。RPC から取得してください。
- **間違ったトークンプログラム。** Token-2022 ミントのボールトは SPL Token ではなく Token-2022 プログラムが所有しています。常にプールの `token_0_program` / `token_1_program` フィールドを使用してください。
- **転送手数料ミントのスリッページが過小評価。** プールのどちらかが Token-2022 転送手数料ミントの場合、`minimum_amount_out` はボールトが送信するものではなく、ユーザーが実際に受け取るものの単位である必要があります。
- **スワップで `NotApproved`。** `PoolState.status` を確認してください — 管理者がそのプールのスワップを一時停止している可能性があります。ステータスビットマスクについては [`products/cpmm/instructions`](/ja/products/cpmm/instructions) を参照してください。

## 次のステップ

- [`sdk-api/typescript-sdk`](/ja/sdk-api/typescript-sdk) — 完全な SDK リファレンス。
- [`sdk-api/rest-api`](/ja/sdk-api/rest-api) — 見積もりとプールメタデータエンドポイント。
- [`user-flows/create-cpmm-pool`](/ja/user-flows/create-cpmm-pool) — 同じフローのコードなしのウォークスルー。
- [`integration-guides/aggregator`](/ja/integration-guides/aggregator) — マルチホップパスの一部として CPMM をルーティングしている場合。

ソース：

- [Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`](https://github.com/raydium-io/raydium-sdk-V2)
- [Raydium SDK v2 デモ — `raydium-sdk-v2-demo`](https://github.com/raydium-io/raydium-sdk-V2-demo)
- [`raydium-io/raydium-cp-swap`](https://github.com/raydium-io/raydium-cp-swap)
