---
title: "AMM v4 コード例"
description: "既存の AMM v4 プールに対するスワップとリクイディティ提供の TypeScript 例。新規プール作成は対象外です — CPMM を使用してください。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/products/amm-v4/code-demos)

  **バージョン バナー。** すべての例は Solana メインネット ベータに対する `@raydium-io/raydium-sdk-v2@0.2.64-alpha` を対象としており、**2026-09-09 に検証済み**です — 以下のビルダー名、パラメータ名、パラメータ型はすべて、そのリリースのソースおよび [`raydium-sdk-V2-demo/src/amm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/amm) に対して確認されています。プログラム ID：`675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8`（[`reference/program-addresses`](/ja/reference/program-addresses)を参照）。

  最初の統合でほとんどの人がはまる 2 つの罠：`computeAmountOut` の `slippage` は `Percent` ではなく素の**数値**（`0.005` = 0.5%）であること。そして `liquidity.swap` は `inputMint` を base58 文字列として **必須** とすることです。
</Info>

<Info>
  **バージョン情報。** すべてのデモは `@raydium-io/raydium-sdk-v2@0.2.64-alpha` を対象とし、Solana mainnet-beta に対して検証済み（2026-04）。プログラム ID：`675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8`（[`reference/program-addresses`](/ja/reference/program-addresses) を参照）。
</Info>

<Info>
  **新規プール作成はここでは説明していません。** Raydium UI は AMM v4 プール作成を提供しなくなり、新しいペアはデフォルトで [CPMM](/ja/products/cpmm/code-demos) になります。AMM v4 プログラム自体は引き続きオンチェーンで `Initialize2` を受け入れていますが、推奨されるパスではありません。以下のデモは、すべてのインテグレーターが必要とするライブプール操作をカバーしています：スワップ、デポジット、ウィズドロー。
</Info>

## セットアップ

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);
const raydium = await Raydium.load({ owner, connection, cluster: "mainnet" });
```

## ID でプールを取得

```ts
import { PublicKey } from "@solana/web3.js";

// `liquidity.getPoolInfoFromRpc` takes the pool id as a base58 STRING, not a PublicKey.
const poolId = "58oQChx4yWmvKdwLLZzBi4ChoCc2fqCUWBkwMihLYQo2"; // SOL-USDC, see reference/program-addresses

const data = await raydium.liquidity.getPoolInfoFromRpc({ poolId });
const { poolInfo, poolKeys, poolRpcData } = data;

console.log("Pair:", poolInfo.mintA.symbol, "/", poolInfo.mintB.symbol);
console.log("Version:", poolInfo.version);       // AMM v4 の場合は 4
console.log("Market:", poolKeys.marketId);       // already a base58 string
```

`poolKeys` はインストラクションビルダーが使用する構造体です。プログラムが期待する順序ですべての AMM v4 アカウントを保持し、さらに現在のプログラムが位置的に受け取るもののもう読み取らないレガシーなマーケット フィールドも含みます。

## スワップ（ベースイン）

```ts
import BN from "bn.js";

const amountIn = new BN(1_000_000);            // 1 USDC（6 小数点のクォート）
const inputMint = new PublicKey(poolInfo.mintB.address);  // USDC
const slippage  = 0.005;

const computed = raydium.liquidity.computeAmountOut({
  poolInfo,
  amountIn,
  mintIn: inputMint,
  mintOut: new PublicKey(poolInfo.mintA.address),
  slippage,
});

const { execute } = await raydium.liquidity.swap({
  poolInfo,
  poolKeys,
  amountIn,
  amountOut: computed.minAmountOut,
  fixedSide: "in",
  inputMint: poolInfo.mintB.address,   // a base58 STRING, and it is required
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Swap tx:", txId);
```

SDK は AMM v4 スワップを V2 エントリーポイント経由でルーティングします。これらは OpenBook アカウントを保持していません。（2026-07 アップグレード後、マーケットアカウントはレガシー v1 パスでも検証されなくなります。）

## スワップ（ベースアウト）

```ts
const amountOut = new BN(1_000_000_000);       // 1 SOL（9 小数点のベース）
const slippage  = 0.005;

const computed = raydium.liquidity.computeAmountIn({
  poolInfo,
  amountOut,
  mintOut: new PublicKey(poolInfo.mintA.address),
  mintIn: new PublicKey(poolInfo.mintB.address),
  slippage,
});

const { execute } = await raydium.liquidity.swap({
  poolInfo,
  poolKeys,
  amountIn: computed.maxAmountIn,
  amountOut,
  fixedSide: "out",
  inputMint: poolInfo.mintB.address,   // base58 string, not a PublicKey
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## リクイディティを追加

```ts
import { Percent, TokenAmount, toToken } from "@raydium-io/raydium-sdk-v2";

const amountARaw = new BN(100_000_000);        // 0.1 SOL, in base units

// computePairAmount takes a UI-decimal string (or Decimal) and a Percent —
// not a BN and not a number — and it is synchronous.
const { anotherAmount, maxAnotherAmount, minAnotherAmount } =
  raydium.liquidity.computePairAmount({
    poolInfo,
    amount: "0.1",
    baseIn: true,
    slippage: new Percent(1, 100),
  });

// addLiquidity takes THREE TokenAmounts; otherAmountMin is required.
const { execute } = await raydium.liquidity.addLiquidity({
  poolInfo,
  poolKeys,
  amountInA: new TokenAmount(toToken(poolInfo.mintA), amountARaw),
  amountInB: maxAnotherAmount,
  otherAmountMin: minAnotherAmount,
  fixedSide: "a",
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

`fixedSide: "a"` は、正確な `amountInA` を指定し、`amountInB` は最大で `maxAnotherAmount` であることを SDK に伝えます。比例計算はボルト残高から蓄積された PnL を差し引いた値に基づいて行われます — 先に決済すべきオンブックのコンポーネントはもう存在しません。

## リクイディティを削除

```ts
const lpAmount = new BN(50_000);               // バーンする LP

const { execute } = await raydium.liquidity.removeLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  baseAmountMin: new BN(0),
  quoteAmountMin: new BN(0),
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

スリッページ最小値は、プリクォートとランドタイム間でプールの状態が変わるのを防ぎます。

## コンピュートユニット / プライオリティフィーの調整

OpenBook の削除以降、AMM v4 のスワップはマーケット状態を一切検証しなくなったため、V1 の `SwapBaseIn` は以前よりも明らかに安価です — そして `SwapBaseInV2`（8 アカウント、マーケットのプレースホルダなし）はさらに安価です。過去の数値から予算を組むのではなく、自分の経路を測定し、常に明示的なコンピュートユニット制限を渡してください：

```ts
const { execute, transaction } = await raydium.liquidity.swap({
  /* ...params... */
  computeBudgetConfig: {
    units: 400_000,
    microLamports: 50_000,       // CU あたりのプライオリティフィー
  },
});
```

`computeBudgetConfig` を省略した場合、SDK は独自のデフォルトを使用する可能性があります。返された `transaction` を検査して確認してください。[`integration-guides/priority-fee-tuning`](/ja/integration-guides/priority-fee-tuning) を参照してください。

## 直接 Rust CPI

独自の Anchor プログラムから AMM v4 に CPI する必要がある場合、`SwapBaseIn` のアカウントリストを逐語的にモデル化する必要があります。最小限のスケッチ：

```rust
use anchor_lang::prelude::*;
use anchor_lang::solana_program::program::invoke_signed;
use anchor_lang::solana_program::instruction::Instruction;

const AMM_V4_PROGRAM_ID: Pubkey = pubkey!("675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8");

#[derive(Accounts)]
pub struct ProxyAmmV4Swap<'info> {
    /// CHECK:
    pub token_program: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub amm:          UncheckedAccount<'info>,
    /// CHECK:
    pub amm_authority: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub amm_open_orders: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub amm_target_orders: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub pool_coin_token_account: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub pool_pc_token_account: UncheckedAccount<'info>,
    /// CHECK:
    pub market_program: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market_bids: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market_asks: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market_event_queue: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market_coin_vault: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub market_pc_vault: UncheckedAccount<'info>,
    /// CHECK:
    pub market_vault_signer: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub user_source: UncheckedAccount<'info>,
    #[account(mut)] /// CHECK:
    pub user_dest: UncheckedAccount<'info>,
    pub user_owner: Signer<'info>,
}

pub fn proxy_swap(
    ctx: Context<ProxyAmmV4Swap>,
    amount_in: u64,
    minimum_amount_out: u64,
) -> Result<()> {
    // SwapBaseIn のインストラクション判別子は AMM v4 では 9 です。
    let mut data = vec![9u8];
    data.extend_from_slice(&amount_in.to_le_bytes());
    data.extend_from_slice(&minimum_amount_out.to_le_bytes());

    let ix = Instruction {
        program_id: AMM_V4_PROGRAM_ID,
        accounts: vec![
            AccountMeta::new_readonly(ctx.accounts.token_program.key(), false),
            AccountMeta::new(ctx.accounts.amm.key(), false),
            AccountMeta::new_readonly(ctx.accounts.amm_authority.key(), false),
            AccountMeta::new(ctx.accounts.amm_open_orders.key(), false),
            AccountMeta::new(ctx.accounts.amm_target_orders.key(), false),
            AccountMeta::new(ctx.accounts.pool_coin_token_account.key(), false),
            AccountMeta::new(ctx.accounts.pool_pc_token_account.key(), false),
            AccountMeta::new_readonly(ctx.accounts.market_program.key(), false),
            AccountMeta::new(ctx.accounts.market.key(), false),
            AccountMeta::new(ctx.accounts.market_bids.key(), false),
            AccountMeta::new(ctx.accounts.market_asks.key(), false),
            AccountMeta::new(ctx.accounts.market_event_queue.key(), false),
            AccountMeta::new(ctx.accounts.market_coin_vault.key(), false),
            AccountMeta::new(ctx.accounts.market_pc_vault.key(), false),
            AccountMeta::new_readonly(ctx.accounts.market_vault_signer.key(), false),
            AccountMeta::new(ctx.accounts.user_source.key(), false),
            AccountMeta::new(ctx.accounts.user_dest.key(), false),
            AccountMeta::new_readonly(ctx.accounts.user_owner.key(), true),
        ],
        data,
    };
    invoke_signed(&ix, &ctx.accounts.to_account_infos(), &[])?;
    Ok(())
}
```

AMM v4 は CPI 用の Anchor クレートを提供していません。上記のスケッチは手動で構築された `Instruction` を使用しています。

<Note>
  上記の CPI スケッチは、既存のトランザクションを読み取り/再現するために **レガシー v1 `SwapBaseIn` レイアウト**（タグ 9、17 アカウント）を使用しています。2026-07 アップグレード以降、マーケットアカウントは受け入れられていますが無視されます。**新しいコードの場合は `SwapBaseInV2` / `SwapBaseOutV2`** （タグ 16 / 17）を優先してください。これらはマーケットアカウント（および `amm_open_orders`）を削除し、8 つのアカウントのみを渡します：`token_program`、`amm`、`amm_authority`、2 つのプールボルト、2 つのユーザートークンアカウント、および `user_owner`。
</Note>

## よくある落とし穴

- **v1 スワップでのアカウント数の誤り。** レガシー `SwapBaseIn` / `SwapBaseOut` は引き続き完全な 17 アカウント（または 18 アカウント）リストを必要とします — 不一致の *数* は `WrongAccountsNumber` で戻ります。マーケットアカウントの *内容* はもはや検証されていませんが、それでもスロットを占める必要があります。これを避けるために V2 エントリーポイントを優先してください。
- **生のボルト残高を読み取る。** リザーブはボルトのみになりました。累積 PnL（`need_take_pnl_*`）を差し引いてください。SDK クォートまたは `api-v3.raydium.io/pools/info/ids` がこれを処理します。
- **削除されたインストラクションを呼び出す。** `MonitorStep`、`MigrateToOpenBook`、`WithdrawSrm`、`SimulateInfo`、`AdminCancelOrders`、およびレガシー `Initialize` / `PreInitialize` は現在戻ります。プール作成には `Initialize2` を使用してください。呼び出すクランクはありません。
- **Token-2022 ミント。** サポートされていません。AMM v4 プールは Token-2022 ミントに対して作成できません。Token-2022 ペアは CPMM または CLMM にある必要があります。

## 次のステップ

- [`products/amm-v4/instructions`](/ja/products/amm-v4/instructions) — これらのデモの背後にあるインストラクションレベルのサーフェス。
- [`user-flows/migrate-amm-v4-to-cpmm`](/ja/user-flows/migrate-amm-v4-to-cpmm) — LP が移行を検討している場合。
- [`integration-guides/priority-fee-tuning`](/ja/integration-guides/priority-fee-tuning) — 重い AMM v4 スワップのプライオリティフィーサイジング。

ソース：

- [Raydium SDK v2](https://github.com/raydium-io/raydium-sdk-V2)
- [Raydium AMM プログラム](https://github.com/raydium-io/raydium-amm)
