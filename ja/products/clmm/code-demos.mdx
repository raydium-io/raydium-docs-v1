---
title: "CLMM コード例"
description: "エンドツーエンドの TypeScript 例：CLMM プールの作成、選択した価格範囲でのポジション開設、流動性の調整、スワップ、手数料とリワードの回収。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/products/clmm/code-demos)
</Info>

<Info>
  **バージョン情報。** すべてのデモは `@raydium-io/raydium-sdk-v2@0.2.42-alpha` を対象とし、Solana mainnet-beta に対して検証済み（2026-04）。プログラム ID は SDK 経由で [`reference/program-addresses`](/ja/reference/program-addresses) から取得します。
</Info>

## セットアップ

```bash
npm install @raydium-io/raydium-sdk-v2 @solana/web3.js @solana/spl-token bn.js decimal.js
```

このページのすべてのデモは [`raydium-sdk-V2-demo/src/clmm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/clmm) のファイルに対応しており、各セクションの横に GitHub リンクがあります。ブートストラップはデモリポジトリの `config.ts.template`（[ソース](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/config.ts.template)）に従います。`disableFeatureCheck: true` は非自明な統合の推奨設定です：

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);
const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});
export const txVersion = TxVersion.V0;
```

## CLMM プールの作成

ソース：[`src/clmm/createPool.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/createPool.ts)

```ts
import { PublicKey } from "@solana/web3.js";
import { CLMM_PROGRAM_ID } from "@raydium-io/raydium-sdk-v2";
import BN from "bn.js";
import Decimal from "decimal.js";

const mintA = await raydium.token.getTokenInfo(
  new PublicKey("So11111111111111111111111111111111111111112"));   // wSOL
const mintB = await raydium.token.getTokenInfo(
  new PublicKey("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"));   // USDC

const ammConfigs = await raydium.api.getClmmConfigs();
const ammConfig  = ammConfigs.find((c) => c.index === 1)!;        // 0.05% tier

const initialPrice = new Decimal(160);        // 160 USDC per SOL
const { execute, extInfo } = await raydium.clmm.createPool({
  programId: CLMM_PROGRAM_ID,
  mint1:     mintA,
  mint2:     mintB,
  ammConfig,
  initialPrice,
  startTime: new BN(0),
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Pool:", extInfo.address.id.toBase58(), "tx:", txId);
```

SDK は以下を実行します：

- `mint1`/`mint2` をバイト順でソートしてから導出します。
- `sqrt_price_x64 = floor(sqrt(initialPrice × 10^(dB−dA)) × 2^64)` を計算します。
- `observation` と `tick_array_bitmap_extension` アカウントを作成します。
- `ammConfig` で定義されたプール作成手数料を支払います。

## 選択した範囲でポジションを開設

ソース：[`src/clmm/createPosition.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/createPosition.ts)

```ts
import { PoolUtils, TickUtils } from "@raydium-io/raydium-sdk-v2";

const poolId = new PublicKey("<POOL_ID>");
const { poolInfo, poolKeys, rpcData } = await raydium.clmm.getPoolInfoFromRpc(poolId);

// 価格範囲を選択。ここでは現在価格の ±10%。
const currentPrice = new Decimal(poolInfo.price);
const lowerPrice   = currentPrice.mul(0.9);
const upperPrice   = currentPrice.mul(1.1);

// このプールの tick_spacing に対して有効なティックにスナップします。
const { tick: tickLower } = TickUtils.getPriceAndTick({
  poolInfo, price: lowerPrice, baseIn: true,
});
const { tick: tickUpper } = TickUtils.getPriceAndTick({
  poolInfo, price: upperPrice, baseIn: true,
});

// 各トークンの預金額。
const inputAmount = new BN(10_000_000);  // 0.01 SOL
const inputMint   = poolInfo.mintA.address;

const res = PoolUtils.getLiquidityAmountOutFromAmountIn({
  poolInfo,
  slippage: 0.01,
  inputA: true,
  tickUpper,
  tickLower,
  amount: inputAmount,
  add: true,
  amountHasFee: true,
  epochInfo: await raydium.fetchEpochInfo(),
});

const { execute } = await raydium.clmm.openPositionFromBase({
  poolInfo,
  poolKeys,
  tickUpper,
  tickLower,
  base: "MintA",
  ownerInfo: { useSOLBalance: true },
  baseAmount: inputAmount,
  otherAmountMax: res.amountSlippageB.amount,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Position opened, tx:", txId);
```

SDK は範囲が接するティック配列を自動的に計算し、未初期化のものがあれば `InitTickArray` 命令をバンドルします。

## 既存ポジションの流動性を増加

ソース：[`src/clmm/increaseLiquidity.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/increaseLiquidity.ts)

```ts
const positionNftMint = new PublicKey("<POSITION_NFT_MINT>");

const positionAccount = await raydium.clmm.getPositionInfo({
  nftMint: positionNftMint,
});

const { execute } = await raydium.clmm.increasePositionFromBase({
  poolInfo,
  poolKeys,
  ownerPosition: positionAccount,
  base: "MintA",
  baseAmount: new BN(5_000_000),
  otherAmountMax: new BN(1_000_000_000),
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## 流動性を減少（同時に手数料を回収）

ソース：[`src/clmm/decreaseLiquidity.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/decreaseLiquidity.ts) および [`src/clmm/closePosition.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/closePosition.ts)

```ts
const { execute } = await raydium.clmm.decreaseLiquidity({
  poolInfo,
  poolKeys,
  ownerPosition: positionAccount,
  liquidity: positionAccount.liquidity.divn(2),   // 半減
  amountMinA: new BN(0),
  amountMinB: new BN(0),
  closePosition: false,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

**手数料のみを回収** するには、`liquidity = new BN(0)` で `decreaseLiquidity` を呼び出します。命令の副作用は `tokens_fees_owed_{0,1}` を決済し、それらを転送することです。

流動性と手数料をゼロにした後、ポジション全体を閉じるには、最終的な `decreaseLiquidity` 呼び出しで `closePosition: true` を渡します。SDK は `ClosePosition` を追加し、NFT をバーンします。

<Warning>
  **制限付き発行者ポジションは互換性のあるクローズビルダーが必要です。** これらのポジションは凍結された NFT トークンアカウントを持ちます。`ClosePosition` は、CLMM がアカウントをバーンする前に解凍できるように、ポジションのプール ID を最初の残りアカウントとして追加する必要があります。プログラムソースブランチには SDK 変更が含まれていません。制限付き発行者ポジション作成を有効にする前に、SDK リリースが凍結クローズパスを明示的にサポートしていることを確認してください。
</Warning>

直接 Anchor クライアントの場合、宣言されたアカウントを変更せず、プールを追加します：

```ts
// CLMM プログラム：reference/program-addresses を参照；ソース ecb1577；
// Solana mainnet-beta；最終検証 2026-08-13。
await program.methods
  .closePosition()
  .accounts({
    nftOwner,
    positionNftMint,
    positionNftAccount,
    personalPosition,
    systemProgram: SystemProgram.programId,
    tokenProgram: positionNftTokenProgram,
  })
  .remainingAccounts([
    { pubkey: poolId, isWritable: false, isSigner: false },
  ])
  .rpc();
```

すべてのクローズで `poolId` を渡すことができます。CLMM は `positionNftAccount` が凍結されている場合にのみ読み込み、これにより 1 つのクライアントパスが古いポジションと新しいポジションの両方と互換性を保ちます。

## リワードを回収

ソース：[`src/clmm/harvestAllRewards.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/harvestAllRewards.ts)

```ts
const { execute } = await raydium.clmm.harvestAllRewards({
  ownerInfo: { useSOLBalance: true },
  allPoolInfo: { [poolInfo.id]: poolInfo },
  allPositions: { [poolInfo.id]: [positionAccount] },
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

`harvestAllRewards` は渡されたすべてのプール上のすべてのポジションを走査し、`CollectReward`（および任意の `UpdateRewardInfos`）命令をバッチ処理し、必要に応じてトランザクション間で分割します。

## スワップ

ソース：[`src/clmm/swap.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/clmm/swap.ts)

```ts
import { PoolUtils } from "@raydium-io/raydium-sdk-v2";

const amountIn = new BN(10_000_000);
const baseIn   = true;               // A（SOL）→ B（USDC）をスワップ
const slippage = 0.005;

const { minAmountOut, remainingAccounts, priceImpact } =
  PoolUtils.computeAmountOutFormat({
    poolInfo,
    tickArrayCache: await raydium.clmm.fetchTickArrays({ poolInfo }),
    amountIn,
    tokenOut: poolInfo.mintB,
    slippage,
    epochInfo: await raydium.fetchEpochInfo(),
  });

const { execute } = await raydium.clmm.swap({
  poolInfo,
  poolKeys,
  inputMint: new PublicKey(poolInfo.mintA.address),
  amountIn,
  amountOutMin: minAmountOut.amount,
  observationId: poolKeys.observationId,
  remainingAccounts,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

`computeAmountOutFormat` はオンチェーンプログラムと同じロジックを使用してティックマップをオフチェーンで走査し、以下を返します：

- 予想される出力額、
- スリッページ後の最小出力額、
- 実際のスワップが接するティック配列アカウントのリスト（`remainingAccounts`）。

シミュレーションで返された `remainingAccounts` を常に渡します。渡すものが少なすぎると、スワップは `TickArrayNotFound` で途中で戻ります。古いものを渡すと、計算が無駄になります。

## カスタマイズ可能な CLMM プールの作成

`createCustomizablePool` は、プール作成時に動的手数料と片側手数料トグルを公開する新しいエントリポイントです。`createPool` と同じ形状に 3 つの追加があります：

```ts
import { CLMM_PROGRAM_ID, CollectFeeOn } from "@raydium-io/raydium-sdk-v2";

const dynamicFeeConfigs = await raydium.api.getClmmDynamicConfigs();    // GET /main/clmm-dynamic-config
const dynamicFeeConfig  = dynamicFeeConfigs.find((c) => c.index === 0); // キャリブレーション層を選択

const { execute, extInfo } = await raydium.clmm.createCustomizablePool({
  programId: CLMM_PROGRAM_ID,
  mint1:     mintA,
  mint2:     mintB,
  ammConfig,
  initialPrice,
  startTime: new BN(0),
  // 新しいフィールド：
  collectFeeOn:        CollectFeeOn.Token1Only,   // 0 = FromInput, 1 = Token0Only, 2 = Token1Only
  enableDynamicFee:    true,
  dynamicFeeConfigId:  dynamicFeeConfig?.id,      // enableDynamicFee が false の場合は省略
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
console.log("Customizable pool:", extInfo.address.id.toBase58());
```

`createPool` はデフォルト手数料、リミットオーダーなし、動的手数料なしのパスで機能し続けます。3 つの新しいノブのいずれかが必要な場合は `createCustomizablePool` を使用します。オンチェーンアカウントリストについては [`products/clmm/instructions`](/ja/products/clmm/instructions) を参照してください。

## リミットオーダー

リミットオーダーはユーザー入力を単一のティックに駐車し、スワップがそのティックを横切るときに FIFO で満たされます。出力は決済時にオーナーの ATA にプッシュされます。オーナーが満たされるためにオンラインである必要はありません。

### リミットオーダーを開設

```ts
import { TickUtils } from "@raydium-io/raydium-sdk-v2";

const limitConfigs = await raydium.api.getClmmLimitOrderConfigs(); // GET /main/clmm-limit-order-config
const limitConfig  = limitConfigs.find((c) => c.poolId === poolInfo.id);

// リミット価格は tick_spacing に量子化される必要があります。
const targetPrice = new Decimal(180);                              // SOL を 180 USDC で売却
const { tick: limitTick } = TickUtils.getPriceAndTick({
  poolInfo, price: targetPrice, baseIn: true,
});

const { execute } = await raydium.clmm.openLimitOrder({
  poolInfo,
  poolKeys,
  limitOrderConfig: limitConfig,
  inputMint: poolInfo.mintA.address,         // SOL を売却
  inputAmount: new BN(50_000_000),           // 0.05 SOL
  tick: limitTick,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Limit order opened, tx:", txId);
```

SDK は `(pool, owner, tick, nonce)` から `LimitOrderState` PDA を導出し、`(pool, owner)` ごとの `LimitOrderNonce` をバンプし、そのティックの FIFO コホートにオーダーを挿入します。

### オープンオーダーを増加 / 減少

```ts
await raydium.clmm.increaseLimitOrder({
  poolInfo,
  poolKeys,
  limitOrderId: <LIMIT_ORDER_PUBKEY>,
  addAmount: new BN(20_000_000),
  txVersion: TxVersion.V0,
}).then((b) => b.execute({ sendAndConfirm: true }));

await raydium.clmm.decreaseLimitOrder({
  poolInfo,
  poolKeys,
  limitOrderId: <LIMIT_ORDER_PUBKEY>,
  removeAmount: new BN(10_000_000),
  txVersion: TxVersion.V0,
}).then((b) => b.execute({ sendAndConfirm: true }));
```

`decreaseLimitOrder` はオーダーの **未充足** 部分からのみ削除できます。充足部分は決済まで ロックされます。両方の命令は、オーダーが既に完全に充足されている場合、`InvalidOrderPhase` で戻ります。

### 充足されたオーダーを決済

```ts
await raydium.clmm.settleLimitOrder({
  poolInfo,
  poolKeys,
  limitOrderId: <LIMIT_ORDER_PUBKEY>,
  txVersion: TxVersion.V0,
}).then((b) => b.execute({ sendAndConfirm: true }));
```

`settleLimitOrder` はオーダーの `unfilled_ratio_x64` をコホートトラッカーに対して読み込み、充足出力を計算し、オーナーの ATA に転送します。オーナーは自分で呼び出すことができます。`limit_order_admin`（オフチェーン運用キーパー）もオーナーの代わりに呼び出すことができます。出力はまだオーナーに行きます。

完全に決済されたオーダーを閉じて家賃を回収するには、`closeLimitOrder`（単一）または `closeAllLimitOrder`（バッチ）を使用します。多くを一度に決済するには、`settleAllLimitOrder` は v0 tx に収まるだけ多くの `SettleLimitOrder` 呼び出しをパックします。

### ウォレットの駐車オーダーをリスト（オフチェーン）

```ts
// API ヘルパー。api-reference/temp-api-v1 を参照。
const active = await fetch(
  `https://temp-api-v1.raydium.io/limit-order/order/list?wallet=<your-wallet-pubkey>`,
).then((r) => r.json());
```

アクティブオーダーエンドポイントは、未充足と部分充足の両方のオーダーを 1 つのペイロードで返します（`totalAmount` / `filledAmount` / `pendingSettle` はフェーズを区別します）。閉じたオーダー履歴については `/limit-order/history/order/list-by-user?wallet=…`（ウォレットごと、`nextPageId` でページネーション）を使用します。特定のオーダーの完全なイベントログについては `/limit-order/history/event/list-by-pda?pda=…` を使用します。

## Rust CPI スケルトン

```rust
use anchor_lang::prelude::*;
use raydium_amm_v3::cpi;
use raydium_amm_v3::program::AmmV3;
use raydium_amm_v3::cpi::accounts::SwapV2;

#[derive(Accounts)]
pub struct ProxyClmmSwap<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    /// CHECK:
    pub amm_config: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub pool_state: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub input_token_account: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub output_token_account: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub input_vault: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub output_vault: UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub observation_state: UncheckedAccount<'info>,
    /// CHECK:
    pub token_program: UncheckedAccount<'info>,
    /// CHECK:
    pub token_program_2022: UncheckedAccount<'info>,
    /// CHECK:
    pub memo_program: UncheckedAccount<'info>,
    /// CHECK:
    pub input_vault_mint: UncheckedAccount<'info>,
    /// CHECK:
    pub output_vault_mint: UncheckedAccount<'info>,
    pub clmm_program: Program<'info, AmmV3>,
    // `remaining_accounts` は tick_array と bitmap_extension アカウントを運びます。
}

pub fn proxy_swap(
    ctx: Context<ProxyClmmSwap>,
    amount: u64,
    other_amount_threshold: u64,
    sqrt_price_limit_x64: u128,
    is_base_input: bool,
) -> Result<()> {
    let cpi_accounts = SwapV2 {
        payer:                 ctx.accounts.payer.to_account_info(),
        amm_config:            ctx.accounts.amm_config.to_account_info(),
        pool_state:            ctx.accounts.pool_state.to_account_info(),
        input_token_account:   ctx.accounts.input_token_account.to_account_info(),
        output_token_account:  ctx.accounts.output_token_account.to_account_info(),
        input_vault:           ctx.accounts.input_vault.to_account_info(),
        output_vault:          ctx.accounts.output_vault.to_account_info(),
        observation_state:     ctx.accounts.observation_state.to_account_info(),
        token_program:         ctx.accounts.token_program.to_account_info(),
        token_program_2022:    ctx.accounts.token_program_2022.to_account_info(),
        memo_program:          ctx.accounts.memo_program.to_account_info(),
        input_vault_mint:      ctx.accounts.input_vault_mint.to_account_info(),
        output_vault_mint:     ctx.accounts.output_vault_mint.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(ctx.accounts.clmm_program.to_account_info(), cpi_accounts)
        .with_remaining_accounts(ctx.remaining_accounts.to_vec());
    cpi::swap_v2(cpi_ctx, amount, other_amount_threshold, sqrt_price_limit_x64, is_base_input)
}
```

`SwapV2` の残りアカウント順序：

```
[tick_array_bitmap_extension?, tick_array_0, tick_array_1, …]
```

スワップが拡張機能を必要としない場合は、それを省略します。そうでない場合は、最初の残りアカウントです。

## よくある落とし穴

- **スペーシング外のティックエンドポイント** → `InvalidTickIndex`。常に `TickUtils.getPriceAndTick` 経由でスナップします。
- **`SwapV2` で提供されるティック配列が不足** → `TickArrayNotFound`。`computeAmountOutFormat` を使用して完全なリストを取得します。
- **ビットマップ拡張なしのフルレンジポジション** → 拡張 PDA は書き込み可能である必要があります。SDK はこれを自動的に処理します。
- **`sqrt_price_x64` を `price` と間違える** → ここでの 2 倍の混乱は特に痛いです。疑わしい場合は、SDK に人間が読める価格から計算させます。
- **リワードを早すぎるタイミングで回収** → 各回収は 1 つのトランザクションがかかります。多くのポジション間で `harvestAllRewards` 経由でバッチ処理します。
- **NFT アカウントを自分で閉じる** → `ClosePosition` は NFT をバーンし、その ATA を閉じます。Token-2022 NFT ミントも閉じます。古典的な SPL Token ミントは供給ゼロのままです。そのプログラムはミントを閉じることができないためです。サポートされているアカウントを別途閉じないでください。そうしないと命令が戻ります。
- **スペーシングされていないティックでリミットオーダーを開設** → `InvalidTickIndex`。常に `TickUtils.getPriceAndTick` 経由で量子化します。
- **完全に充足されたオーダーで `decreaseLimitOrder` を呼び出す** → `InvalidOrderPhase`。代わりに `settleLimitOrder` その後 `closeLimitOrder` を使用します。
- **`enableDynamicFee: true` を渡しながら `dynamicFeeConfigId` を忘れる** → `CreateCustomizablePool` の戻りは `InvalidDynamicFeeConfigParams` です。動的手数料をオフにするか、`/main/clmm-dynamic-config` からコンフィグを選択します。

## 次のステップ

- [`sdk-api/typescript-sdk`](/ja/sdk-api/typescript-sdk) — 完全な SDK サーフェス。
- [`sdk-api/rest-api`](/ja/sdk-api/rest-api) — クォートとプールメタデータエンドポイント。
- [`user-flows/create-clmm-pool`](/ja/user-flows/create-clmm-pool) — コードなしのウォークスルー。
- [`integration-guides/aggregator`](/ja/integration-guides/aggregator) — パスの一部として CLMM をルーティング。

ソース：

- [Raydium SDK v2](https://github.com/raydium-io/raydium-sdk-V2)
- [Raydium SDK v2 デモ](https://github.com/raydium-io/raydium-sdk-V2-demo)
- [`raydium-io/raydium-clmm`](https://github.com/raydium-io/raydium-clmm)
