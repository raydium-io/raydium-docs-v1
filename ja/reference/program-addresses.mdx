---
title: "プログラムアドレス"
description: "Solana mainnet-beta と devnet における Raydium プログラムの正規 ID と共有 PDA。このページ以外のすべてのページはここにリンクしており、アドレスをハードコードしていません。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/reference/program-addresses)
</Info>

<Info>
  **信頼できる情報源。** このページは、プログラムアドレスを逐語的にリストする唯一の場所です。他のページはここにリンクしています。ID が変更された場合は、**ここだけで更新**してください。サイト全体のすべての参照が一貫性を保ちます。

  実際のトランザクションに署名する前に、必ずライブ API（`https://api-v3.raydium.io/main/info`）に対して値をクロスチェックしてください。
</Info>

## Mainnet-beta

<Tip>
  経験則：野生のプログラム ID が下の表と一致しない場合は、**トランザクションに署名しないでください。** プログラム ID の不一致は、Solana で資金を失う最も簡単な方法です。
</Tip>

### オンチェーンプログラム

| プログラム | プログラム ID | ソース |
|---------|------------|--------|
| **AMM v4**（ハイブリッド AMM + OpenBook） | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM**（標準 AMM） | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM**（集中流動性） | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM**（StableSwap スタイルカーブ） | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | ソースは公開されていません |
| **Farm v3**（レガシー RAY ステーキング） | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | ソースは公開されていません |
| **Farm v5**（レガシーエコシステムファーム） | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | ソースは公開されていません |
| **Farm v6**（現在のエコシステムファーム） | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | ソースは公開されていません |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | ソースは公開されていません |
| **AMM ルーティング**（Raydium のオンチェーンルーター） | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | ソースは公開されていません |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | ソースは公開されていません |

注：

- AMM v4 と Farm v3/v5 は**既存のプールとポジションのために保持**されています。Raydium UI と SDK は、新しいプール作成と新しいエコシステムファームを CPMM、CLMM、および Farm v6 にそれぞれルーティングします。詳細は [`protocol-overview/versions-and-migration`](/ja/protocol-overview/versions-and-migration) を参照してください。
- 上記の Farm v3 ID は、\$RAY シングルアセットステーキングプログラムとしても機能します。LP ステーキングの動作は Farm v3 と同じです。
- **Stable AMM** は、AMM ルーティングプログラムが AMM v4、CPMM、CLMM と並行してターゲットにできる別のプログラムです。流動性は他の 3 つのプログラムと比べて薄く、SDK はそれに対する第一級 API を公開していません。それを通じてルーティングするインテグレーターは、通常ルーターを経由して行います。
- **ソースコード可用性。** 上記のオンチェーンプログラムのうち、`raydium-amm`（AMM v4）、`raydium-cp-swap`（CPMM）、および `raydium-clmm`（CLMM）のみが [`github.com/raydium-io`](https://github.com/raydium-io) の下で公開ソースリポジトリを備えています。Stable AMM、LaunchLab、AMM ルーティング、Burn & Earn / LP Lock、およびファームプログラムは公開されていません。ライブ API、オンチェーンバイトコード、および [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl) で公開されている IDL に対して検証してください。

### 共有管理者権限

すべての Anchor ベースのプログラム（CLMM、CPMM、LaunchLab、Lock）は、管理パス（`CreateAmmConfig` や `UpdatePoolStatus` など）への命令レベルのアクセス制御のために、単一のハードコードされた管理者 `Pubkey` を共有します：

| クラスター | 管理者 |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

アカウントレベルの運用権限（例：CPMM/CLMM `AmmConfig` の `protocol_owner`、`fund_owner`、または LaunchLab `GlobalConfig` のマイグレーションウォレット）はオンチェーンに保存され、プログラム管理者と異なる場合があります。高リスクのトランザクションを送信する前に、関連するコンフィグアカウントから直接読み取ってください。

### CPMM パーミッション PDA 作成者権限

`CreatePermissionPda` は、上記の共有プログラム管理者または専用所有者のいずれかを受け入れます。2026-09 CPMM アップグレード以降、`ClosePermissionPda` は同じ 2 つの署名者を受け入れます。それ以前は管理者のみでした。

| クラスター | 専用 `CreatePermissionPda` 所有者 |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### CPMM サポートミントレジストリ権限

`CreateSupportMintAssociated` と `CloseSupportMintAssociated` は、共有プログラム管理者または以下の専用所有者のいずれかを受け入れます。ミントを登録すると、CPMM の Token-2022 拡張許可リストを通過できます。詳細は [`reference/token-2022-support`](/ja/reference/token-2022-support#the-one-bypass-the-per-mint-registry) を参照してください。

| クラスター | 専用サポートミント所有者 |
|---------|------------------------------|
| mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |

### CPMM フィー所有者ウォレット

2026-09 CPMM アップグレード以降、`CreateAmmConfig` はこれらの固定キーを新しいコンフィグの `protocol_owner` と `fund_owner` に書き込みます。これらは、そのアップグレード以降に作成されたコンフィグに対して `CollectProtocolFee` と `CollectFundFee` が受け入れる署名者です。

| クラスター | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  これらはプログラムが新しいコンフィグに**書き込む値**であり、既存のコンフィグの説明ではありません。アップグレード前に作成された `AmmConfig` アカウントは、それらに保存されているものを保持します。コレクショントランザクションを構築する前に、必ずアカウントから `protocol_owner` / `fund_owner` を読み取ってください。
</Warning>

### 超過ラムポート収集ウォレット

2026-09-09 にアップグレードされた 3 つのプログラムのそれぞれが、[SIMD-0437 レント削減](/ja/solana-fundamentals/rent-and-reclaimable-rent)に従い、プログラム制御アカウントのレント免除最小値を超えて保持されているラムポートを返す管理命令を獲得しました。各プログラムは独自の宛先ウォレットをハードコードしており、各プログラムは専用キーです。共有プログラム管理者ではありません。

| プログラム | 命令 | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM と LaunchLab はさらに共有プログラム管理者を署名者として受け入れます。AMM v4 は**上記のウォレットのみ**を受け入れます。管理者フォールバックはありません。AMM v4 はまた、その `testnet` フィーチャーの下で別のテストネットキー（`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`）をコンパイルします。

これらの命令はラムポートのみを移動します。トークン残高に触れたり、アカウントを閉じたり、プール状態を変更したりすることはありません。詳細は [`products/amm-v4/instructions`](/ja/products/amm-v4/instructions#withdrawexcesslamports)、[`products/cpmm/instructions`](/ja/products/cpmm/instructions#collectexcesslamports)、および [`products/launchlab/instructions`](/ja/products/launchlab/instructions#collectexcesslamports) を参照してください。

### LaunchLab 委任コンフィグ作成者

`CreateConfig` は、共有プログラム管理者または以下の委任権限のいずれかを受け入れます。委任ロールは新しい `GlobalConfig` を作成できます。既存のアカウントに対して管理者のみの `UpdateConfig` パスを呼び出すことはできません。

| クラスター | 委任 `CreateConfig` 権限 |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### CLMM 制限発行者フリーズ権限

CLMM プール内のいずれかのボルトミントが以下のフリーズ権限の 1 つを持つ場合、V2 パスを通じて開かれた新しいポジションは、ポジション NFT トークンアカウントがフリーズされます。リストは CLMM プログラムの `frozen_position_nft_authorities::IDS` であり、その単一エントリは、CLMM の削除されたスーパーステート検出も一致した発行者権限です。これらは**マッチキーとしてのみ使用される基礎資産発行者権限**です。CLMM プール PDA（発行者キーではない）がポジション NFT ミントのフリーズ権限になります。

| クラスター | 制限発行者フリーズ権限 |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

リストは CLMM プログラムにハードコードされています。権限を追加または削除するには、プログラムアップグレードが必要です。

### 共有コンフィグ / PDA 規約

複数の Raydium プログラムは、公開キーが安定している**コンフィグアカウント**を公開 API にリストしています。ハードコーディングよりも API ルックアップを優先してください：

```
# CPMM フィーコンフィグ（{id, index, tradeFeeRate, ...} の配列を返す）
GET https://api-v3.raydium.io/main/cpmm-config

# CLMM フィーコンフィグ
GET https://api-v3.raydium.io/main/clmm-config
```

CPMM のプール作成フィー受信者は、固定アカウントであり、API を通じて派生またはルックアップされません：`raydium-cp-swap` の `lib.rs` の `create_pool_fee_reveiver::ID`。現在は `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` です。CPMM の `initialize` 命令は正確なアドレスで検証するため、テストしているクラスターに既に存在する必要があります。

デフォルト CPMM `AmmConfig` インデックス `0`（標準 0.25% プール）フィーパラメーター（参考用）：

| フィールド | 値 | 意味 |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | トレード量の 0.25% |
| `protocol_fee_rate` | `120000` | トレードフィーの 12%（量ではなく）がプロトコルにルーティング |
| `fund_fee_rate` | `40000` | トレードフィーの 4% がファンドマルチシグにルーティング |
| `creator_fee_rate` | `500` | トレード量の 0.05% がプール作成者に（オプション） |

分割がどのように構成されるかについては [`products/cpmm/fees`](/ja/products/cpmm/fees) を参照し、クロスプロダクトマトリックスについては [`reference/fee-comparison`](/ja/reference/fee-comparison) を参照してください。

### PDA シード

以下のシードは**正規**であり、SDK とオンチェーン CPI の両方で使用されます。常に PDA を計算してください。派生アドレスをハードコードしないでください。

```ts
// CPMM — すべてのシードは特に記載がない限り静的 ASCII 文字列です。
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` は、ハッシング前に**公開キーバイト順でソート**されます（token0 < token1）。これを間違えると、存在しないプールの有効な PDA が生成されます。

同等の CLMM シードは同じスタイルに従います。詳細は [`products/clmm/accounts`](/ja/products/clmm/accounts) を参照してください。

## Devnet

| プログラム | プログラム ID |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM ルーティング** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DLockwT7X7sxtLmGH9g5kmfcjaBtncdbUmi738m5bvQC` |
| **Farm v3 / v5 / v6** | devnet に対して確実に公開されていません。使用前にライブ API（`https://api-v3-devnet.raydium.io/main/info`）で確認してください。 |

Devnet REST API ベース：`https://api-v3-devnet.raydium.io/`（mainnet と同じルート形状）。

## オンチェーンでアドレスを検証する方法

1. **Solana Explorer。** アドレスを [explorer.solana.com](https://explorer.solana.com) に貼り付けて、`Program` とマークされ、現在のアップグレード権限があることを確認します。Mainnet-beta は Raydium のアップグレード権限で署名されたデプロイを表示する必要があります。
2. **CLI。** `solana program show <PROGRAM_ID> -u mainnet-beta` を使用して、デプロイスロット、BPF ローダー、アップグレード権限、およびデータ長を検査します。これらをランブックに記録します。
3. **IDL 添付。** `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet` でオンチェーン IDL をクエリします。IDL の `address` フィールドが一致する必要があります。同じ IDL は [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) にミラーされています。信頼する前に、オンチェーン IDL をリポジトリに対して diff してください。
4. **コンフィグ/管理者権限。** CPMM/CLMM コンフィグアカウントの場合、`owner`/`protocol_owner` フィールドを読み取り、[`security/admin-and-multisig`](/ja/security/admin-and-multisig) で公開されている現在の Raydium マルチシグと一致することを確認します。

上記の 4 つのチェックのいずれかがこのページと異なる場合は、このページが間違っていると見なし、新しいコードをアドレスに対して書く前に問題を開いてください。

## このページを更新する

- プログラム ID への変更は安全性が重要です。プログラム ID の変更を出荷する際は、(a) Raydium アナウンスへのリンク、(b) [`reference/changelog`](/ja/reference/changelog) へのエントリ追加、(c) ドキュメント全体のリンクチェック実行を行わずに出荷**しないでください**。古い値をまだ参照しているページがないことを確認します。
- 廃止予定は、削除されるのではなく、ステータスノート付きでテーブルに留まります。既存のプールは古いプログラムを通じて解決されます。

ソース：

- ライブ API：`https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
