---
title: "変更履歴"
sidebarTitle: "概要"
description: "このドキュメンテーションセットの更新タイムラインインデックス。各リリースは、どの章が変更されたか、その理由、およびコンテンツがオンチェーン状態とプログラムソースに対して検証された時期を詳述する独自のページにリンクしています。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/reference/changelog)
</Info>

<Info>
  これはドキュメンテーションの変更履歴です。プロジェクトのローンチ以降、これらのページへの更新の記録です。以下の各リリースは独自のエントリにリンクしています。1 つを開くと、完全な概要、影響を受けた章、および検証日が表示されます。プロトコル自体の歴史的タイムラインについては、[`introduction/history-and-milestones`](/ja/introduction/history-and-milestones) を参照してください。
</Info>

## リリース

<Update label="2026-09-19" description="CPMM: プロトコルがクリエイターフィーの一部を保有">
  CPMM はクリエイターフィーの一部を保有できるようになりました。分割はフィーが**徴収される**ときに適用されます。フィーが請求されるときではありません。スワップは引き続き全体のクリエイターフィーを `creator_fees_token*` に蓄積し、`CollectCreatorFee` / `CollectCreatorFeePermissionless` はその後、シェアをプールの既存の `protocol_fees_token*` カウンターに移動し、クリエイターに残りを支払います。切り捨てが行われるため、ダストはクリエイターに残ります。レートは新しい `AmmConfig.creator_fee_share_rate`（パディングから取得；アカウントは依然として 236 バイトですが、古い `padding[0]` は現在ライブフィールドです）またはそれをオーバーライドする per-creator `CreatorFeeShare` PDA から取得されます。これは新しい管理者 `CreateCreatorFeeShare` / `CloseCreatorFeeShare` によって管理されます。**クリエイターフィークライアント向けの破壊的変更**: 両方の徴収命令は、アカウントリストの末尾に追加された新しいアカウントを取得します。既存のアカウントは位置を維持しますが、新しいアカウントは必須であるため、アップグレード前のトランザクションはアカウント不足で拒否されます。`UpdateAmmConfig` は `param = 8` を取得します。`PoolState`、スワップ数学、および `k` チェックは変更されていません。エラーコードは追加されていません。同時に実施：`CollectExcessLamports` での 2 パス順序付け修正。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-19-cpmm-creator-fee-protocol-share)
</Update>

<Update label="2026-09-09" description="LaunchLab: Anchor 1.0、余剰ラムポート回復、トランジションゲートの終了">
  LaunchLab は Agave `3.1.10` 上の Anchor `1.0.2` に移行し、トランジションスキャフォルディングを廃止します。非推奨の `Initialize` は `NotApproved` で無条件に失敗するようになりました。`MigrateToAmm` は**ハード破壊的**です。マイグレーションウォレットの場合：3 つの引数と 9 つの OpenBook アカウントはすべて削除されました。これは AMM v4 の OpenBook 削除に従っており、プログラムは CPI によってマーケットを初期化しなくなりました。`get_upgrade_timestamp` クロックゲートは削除されたため、3 つのトレード `remaining_accounts` は無条件に必須です（`system_program` スロットは検証されるようになりました）。`MigrateToCpswap` は常にパーミッションされた CPMM パスを取得し、`InitializeWithToken2022` はその `amm_fee_on` 制限を削除します。新しい管理者 `CollectExcessLamports` は SIMD-0437 によって解放されたレントを返します。ソースアカウントは 3 つのボルト権限 PDA のいずれが所有しているかでグループ化する必要があります。3 つの `MigrateToCpswap` アドレス制約は命令本体に移動され、`2012` を `2502` に変更します。`6031` が追加されました。アカウントレイアウト、トレード命令、またはフィー動作は変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-launchlab-anchor-1-and-excess-lamports)
</Update>

<Update label="2026-09-09" description="CPMM: Anchor 1.0、余剰ラムポート回復、固定フィーオーナー">
  CPMM は Agave `3.1.10` 上の Anchor `1.0.2` に移行し、SIMD-0437 によって解放されたレントをボルト、LP ミント、PDA から返す管理者 `CollectExcessLamports` を追加します。ラップされた SOL ボルトは、`SyncNative` / `UnwrapLamports` ラウンドトリップを介して含まれ、ラップされたバランスは変更されません。3 つの動作変更が同時に実施されます：`CreateAmmConfig` は署名者の代わりにハードコードされた `protocol_fee_owner` および `fund_fee_owner` キーを書き込むようになりました（既存の設定は**移行されません** — フィールドを読み取ります）。ハードコードされた 4 アドレス Token-2022 `MINT_WHITELIST` は削除されたため、`SupportMintAssociated` レジストリ PDA が唯一の残りのバイパスです。`ClosePermissionPda` は専用の付与権限を受け入れるようになりました。`6015` が追加されました。ユーザー向けの命令またはアカウントレイアウトは変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-cpmm-anchor-1-and-excess-lamports)
</Update>

<Update label="2026-09-09" description="AMM v4: Solana 3.0 依存関係と余剰ラムポート回復">
  AMM v4 は `solana-program` `3.0.0`、`spl-token` `9.0.0`、`spl-associated-token-account` `8.0.0`、および新しい `solana-system-interface` クレートに対して再構築され、SIMD-0437 によって解放されたレントを返す管理者のみの `WithdrawExcessLamports`（タグ `18`）を追加します。すべてのトレーダーおよび LP 向けの命令はそのアカウント、引数、および数学を保持し、リリースの何も破壊的ではありません：`CreateConfigAccount` は末尾のレント sysvar の読み取りを停止しましたが、古い 5 アカウントリストを受け入れます。`AmmError` はコード `60` を取得します（`6000` ではなく `0` から番号付けされます）。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-amm-v4-solana-3-and-excess-lamports)
</Update>

<Update label="2026-08-31" description="LaunchLab: プラットフォームカーブルールがカーブパラメータホワイトリストに置き換わります">
  ローンチパラメータ制限は `PlatformConfig` から per-config `PlatformCurveRule` アカウントに移動します。ルールは最大 10 個のチェックグループを保持し、最大 25 個の `(field, op, value)` 制約を 19 個のローンチパラメータに対して保持します。`Eq` / `Gte` / `Lte` / `Neq` があります。したがって、プラットフォームは値バンド、代替ティア、卒業評価キャップ、マイグレーションフロア、トークンタイプゲーティング、または日付で切り替わるグループを最終的に表現できます。これらはすべて、等式のみのホワイトリストではできませんでした。`PlatformConfig` は 944 バイトのサイズを保持します：`restrict_curve_param`、`curve_rule_manager`、および削除されたベクトルの長さプレフィックスはすべてパディングから出ます。デコーダーは末尾の `Vec<PlatformCurveParam>` を削除する必要があり、ローンチビルダーはフラグがオンの間、ルール PDA を `remaining_accounts` に追加する必要があります。2 つの命令が削除され、4 つが追加され、2 つの `UpdatePlatformConfig` バリアントが追加され、`6024`–`6030` が追加されました。IDL リフレッシュが必要です。SDK は 2 つの純粋なオフチェーンチェックを提供するため、クリエイターは復帰したトランザクションからルールを学ぶ必要がありません。エントリは、メインネットでフラグを有効にする前に devnet リハーサルを文書化します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-31-launchlab-platform-curve-rules)
</Update>

<Update label="2026-08-27" description="LaunchLab: プラットフォームはミント作成から引き出し保留権限を保有します">
  `InitializeWithToken2022` は `PlatformConfig.transfer_fee_extension_auth` を新しいベースミントの `withdraw_withheld_authority` に書き込むようになりました。これは、ローンチ `authority` PDA の代わりに、プラットフォームが卒業を待つのではなく、ボンディングカーブフェーズ中に保留された転送フィーをスイープできるようにします。プログラム自体には引き出し保留命令がありません。`MigrateToCpswap` はその権限を PDA がまだ保有している場合にのみ再割り当てします。これにより、卒業が両世代のミントで機能し続けます。`transfer_fee_config_authority` は卒業時にのみ移動するため、ローンチ中に `transfer_fee_extension_auth` を回転させると、2 つの権限が異なるキーに残ります。アカウントレイアウト、命令、引数、またはエラーコードは変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-27-launchlab-token2022-withheld-authority)
</Update>

<Update label="2026-08-26" description="LaunchLab: プラットフォームフィーレート上限が 500 bps に引き上げられました">
  プラットフォーム `fee_rate` 上限は、それを検証する両方のパスで `50000`（500 bps）に移動します。`CreatePlatformConfig` はプログラムの最初のリリース以来 `10000`（100 bps）でキャップされていました。`UpdatePlatformConfig` は 2026-01-27 以来 `25000`（250 bps）でキャップされていました。2 つのチェックが一致するようになったため、許可されたレートで作成されたプラットフォームは、それでも更新できます。`AllInfo` バルクバリアントを含めて、他のすべてのフィールドを書き直しながら `fee_rate` を再検証します。2 つのパスは依然として異なるエラーを発生させます（作成時に `InvalidInput`、更新時に `InvalidPlatformInfo`）。アカウントレイアウト、命令、またはエラーコードは変更されていません。既存のプラットフォームまたはローンチは再価格化されません。`GlobalConfig.max_share_fee_rate` は 100 bps のままで、トランザクションごとのレファーラル `share_fee_rate` のみをバウンドします。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-26-launchlab-platform-fee-rate-cap)
</Update>

<Update label="2026-08-24" description="LaunchLab: Token-2022 クォートミント">
  ローンチは Token-2022 ミントで引用できるようになりました。`CreateConfig`、`InitializeV2`、`InitializeWithToken2022`、4 つのスワップ命令、およびすべてのフィー請求は、クォートプログラムスロットで両方のトークンプログラムを受け入れます。非推奨の `Initialize` はレガシーのみのままです。スワップスリッページ境界は、ペイヤーが実際に支払うまたは受け取るものと比較されるようになりました。クォートミントの転送フィーを差し引いた額です。`PoolState.token_program_flag` bit1 は意味を持つようになったため、バイト全体を `0` に対してテストするデコーダーはレガシーベースミントを Token-2022 として誤読します。`MigrateToCpswap` は 2 つのトークンプログラムアカウントを `token_program` / `token_program_2022` に名前変更し、`6023` は `CalculateOverflow` に再利用されます。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-24-launchlab-token2022-quote-mint)
</Update>

<Update label="2026-08-17" description="LaunchLab: CPMM のみのローンチとプラットフォーム設定コントロール">
  新しいローンチ初期化は CPMM を必須にするようになりました。レガシー AMM v4 バウンド状態は移行可能なままです。このリリース前に、`creator_scale` はクリエイター所有のフィーキーを生成しました。アップグレード後に実行された CPMM マイグレーションは、代わりに `platform_scale + creator_scale` を 1 つのプラットフォーム所有のフィーキーシェアに統合します。プラットフォームは独自の `PlatformAllowConfig` PDA でローンチを制限することもでき、マイグレーションビルダーは両方の CPMM サポートミント PDA を追加する必要があります。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-launchlab-cpmm-only-platform-config)
</Update>

<Update label="2026-08-17" description="CLMM: 制限付き発行者ポジション NFT フリーズ">
  新しい CLMM ポジション NFT ミントはプールをフリーズ権限として使用しますが、トークンアカウントはデフォルトではフリーズされません。フリーズは `OpenPositionV2` または `OpenPositionWithToken22Nft` でのみ発生します。基礎となるボルトミントのいずれかのフリーズ権限が制限付き発行者リストと一致する場合。一致するポジションは転送または所有者変更はできませんが、流動性を管理できます。その `ClosePosition` 呼び出しはプールを追加する必要があります。CLMM はアトミックにシオ、バーン、クローズできます。既存のポジションは変更されません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-clmm-restricted-position-nft-freeze)
</Update>

<Update label="2026-08-17" description="CPMM: パーミッションレスクリエイターフィー徴収">
  加算的な `CollectCreatorFeePermissionless` 命令により、任意のペイヤーがすべての蓄積されたクリエイターフィーをスイープできます。受益者と両方のトークン宛先を `PoolState.pool_creator` とクリエイターの正規 ATA に制限します。元のクリエイター署名パスは変更されません。`CreatePermissionPda` は専用の付与権限も受け入れますが、`ClosePermissionPda` は管理者のみのままです。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-cpmm-permissionless-creator-fee-collection)
</Update>

<Update label="2026-07-30" description="CLMM: パーミッションされたマルチプールとリミットオーダーフリーズアカウントガード">
  2 つの加算的で後方互換性のある CLMM プログラム更新。`CreatePermissionedPool` はクライアント提供の非ゼロ `seed_index` をプール PDA シードに折り込み、ホワイトリストに登録されたオペレーター（`Permission` PDA を保有している）が `(config, mint0, mint1)` ごとに複数のプールを作成できるようにします。したがって、プール ID はペアの正規ではなくなります。新しい管理者命令 `CreatePermissionPda` / `ClosePermissionPda` はそれらの付与を管理し、`PoolState` は `seed_index` フィールドを取得します（パディングから取得、サイズ変更なし）。別に、`OpenLimitOrder` は出力側アカウントを取得し、入力または出力トークンアカウントがフリーズされているオーダーを拒否するようになりました（`NotApproved`）。

  [完全なエントリを読む →](/ja/reference/changelog/2026-07-30-clmm-permissioned-pools)
</Update>

<Update label="2026-07-22" description="AMM v4: OpenBook / Serum 依存関係を削除">
  AMM v4 は長年休止していた OpenBook/Serum 依存関係、すべてのオーダーブック CPI、および死んだマーケットメイキング命令を削除します。`SwapBaseIn` / `SwapBaseOut`、`Deposit`、および `Withdraw` はレイアウトを保持します（削除されたマーケットアカウントは検証されず、無視されます）。**ハード破壊的な** `WithdrawPnl`（17 → 10、互換性なし）および `SetParams`（削除されたアカウント + 番号付け直された `param`）。`Initialize`、`PreInitialize`、`MonitorStep`、`MigrateToOpenBook`、`WithdrawSrm`、`SimulateInfo`、`AdminCancelOrders` はもはや呼び出せません。オンチェーンアカウントレイアウトとエラーコードは安定したままです。スワップを V2 エントリポイントに移行します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-07-22-amm-v4-openbook-removal)
</Update>

<Update label="2026-06-22" description="Stable AMM: 死んだ OpenBook（マーケット）コードを削除">
  Stable AMM は長年休止していた OpenBook マーケットメイキングアカウントとコードを削除します。より小さい `SwapBaseIn` / `SwapBaseOut`（18 → 9）、`Deposit`（14 → 12）、および `Withdraw`（21/22 → 12）レイアウト（古いレイアウトは依然として互換性があります）。**ハード破壊的な** `WithdrawPnl` 変更（16 → 10、互換性なし）。レファーラルフィーは廃止されました。簡略化されたボルトのみのプールアセット公式。他のほとんどの Stable 命令はもはや呼び出せません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-06-22-stable-amm-openbook-cleanup)
</Update>

<Update label="2026-05-18" description="CLMM: リミットオーダー、単一側フィー、動的フィー">
  3 つのオプトイン、後方互換性のある CLMM 機能：ファーストクラスのリミットオーダー（`limit_order_admin` 決済キーパー付き）、単一側フィー徴収（`CollectFeeOn`）、および変動性追跡動的フィー。`CreateCustomizablePool`、`PoolState` リシェイプ（**インデクサー破壊的変更**）、新しい `TickState` フィールド、11 個の新しいエラーコード（数値シフト付き）、および一致する SDK / API 追加を追加します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-05-18-clmm-limit-orders)
</Update>

<Update label="2026-04-26" description="初期公開">
  Raydium ドキュメンテーションセットの最初の公開リリース。ライブ mainnet-beta デプロイメントと `@raydium-io/raydium-sdk-v2@0.2.42-alpha` に対して検証されました。

  [完全なエントリを読む →](/ja/reference/changelog/2026-04-26-initial-publication)
</Update>

## ドキュメンテーション規約

- **バージョニング**: このドキュメンテーションはカレンダーベースのバージョニング（YYYY-MM-DD）を使用します。各更新は新しいエントリページを追加し、上記のタイムラインの上部に新しい行を追加します。
- **リリースごとに 1 ページ**: すべてのリリース概要は `reference/changelog/` の下の独自のページに存在するため、このインデックスは短いままで、各エントリは独立してリンク可能です。
- **検証日**: すべてのエントリは、コンテンツがオンチェーン / API 状態とプログラムソースに対して最後にクロスチェックされた時期を記録します。記載されていない場合は、エントリのメイン日付を想定してください。
- **破壊的変更**: 影響を受けたページのボックス化された警告でコールアウトされ、エントリでタグ付けされます。
- **カバレッジ**: この変更履歴はドキュメンテーションセット自体をカバーしています。プロトコル自体の歴史的タイムラインは [`introduction/history-and-milestones`](/ja/introduction/history-and-milestones) に存在し、「Raydium で X が発生したのはいつか」の真実の源です。

## 修正

このドキュメンテーションでエラーを見つけた場合は、ドキュメンテーションリポジトリで問題またはプルリクエストを開いてください。修正は変更履歴エントリとしてログされます。

## ポインター

- [`introduction/history-and-milestones`](/ja/introduction/history-and-milestones) — プロトコル自体のタイムライン。
- [`security/audits`](/ja/security/audits) — 監査履歴。
- [`ray/protocol-fees`](/ja/ray/protocol-fees) — プロトコルフィー分割。
- [`reference/program-addresses`](/ja/reference/program-addresses) — プログラム ID の真実の源。
