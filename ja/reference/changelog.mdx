---
title: "チェンジログ"
sidebarTitle: "概要"
description: "このドキュメンテーションセットの更新タイムラインインデックス。各リリースは、どの章が変更されたか、なぜ変更されたか、およびコンテンツがオンチェーン状態とプログラムソースに対して検証された時期を詳述する独自のページにリンクしています。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/reference/changelog)
</Info>

<Info>
  これはドキュメンテーションのチェンジログです。プロジェクトのローンチ以降、これらのページへの更新の記録です。以下の各リリースは独自のエントリにリンクしています。リンクを開くと、完全なサマリー、影響を受けた章、および検証日を確認できます。プロトコル自体の歴史的タイムラインについては、[`introduction/history-and-milestones`](/ja/introduction/history-and-milestones)を参照してください。
</Info>

## リリース

<Update label="2026-09-09" description="LaunchLab: Anchor 1.0、余剰ラムポート回収、トランジションゲートの終了">
  LaunchLab は Agave `3.1.10` 上の Anchor `1.0.2` に移行し、トランジション スキャフォルディングを廃止します。非推奨の `Initialize` は `NotApproved` で無条件に失敗するようになります。`MigrateToAmm` は**ハード ブレーキング**で、マイグレーション ウォレットの場合、3 つのすべての引数と 9 つの OpenBook アカウントが削除されます。これは AMM v4 自体の OpenBook 削除に従い、プログラムは CPI でマーケットを初期化しなくなります。`get_upgrade_timestamp` クロック ゲートが削除されるため、3 つのトレード `remaining_accounts` は無条件に必須になります（`system_program` スロットが検証されるようになります）。`MigrateToCpswap` は常にパーミッション CPMM パスを取り、`InitializeWithToken2022` は `amm_fee_on` 制限を削除します。新しい管理者 `CollectExcessLamports` は SIMD-0437 によって解放されたレントを返します。ソース アカウントは、3 つのボールト オーソリティ PDA のいずれが所有しているかでグループ化する必要があります。3 つの `MigrateToCpswap` アドレス制約が命令本体に移動し、`2012` が `2502` に変更されます。`6031` が追加されました。アカウント レイアウト、トレード命令、またはフィー動作は変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-launchlab-anchor-1-and-excess-lamports)
</Update>

<Update label="2026-09-09" description="CPMM: Anchor 1.0、余剰ラムポート回収、固定フィー オーナー">
  CPMM は Agave `3.1.10` 上の Anchor `1.0.2` に移行し、SIMD-0437 によって解放されたレントをボールト、LP ミント、PDA から返す管理者 `CollectExcessLamports` を追加します。ラップされた SOL ボールトは `SyncNative` / `UnwrapLamports` ラウンドトリップを介して含まれ、ラップされたバランスは変更されません。3 つの動作変更が伴います。`CreateAmmConfig` は署名者の代わりにハードコードされた `protocol_fee_owner` と `fund_fee_owner` キーを書き込むようになります（既存の設定は**移行されません** — フィールドを読み取ってください）。ハードコードされた 4 アドレスの Token-2022 `MINT_WHITELIST` が削除されるため、`SupportMintAssociated` レジストリ PDA が唯一の残りのバイパスになります。`ClosePermissionPda` は専用のグラント オーソリティを受け入れるようになります。`6015` が追加されました。ユーザー向けの命令またはアカウント レイアウトは変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-cpmm-anchor-1-and-excess-lamports)
</Update>

<Update label="2026-09-09" description="AMM v4: Solana 3.0 依存関係と余剰ラムポート回収">
  AMM v4 は `solana-program` `3.0.0`、`spl-token` `9.0.0`、`spl-associated-token-account` `8.0.0`、および新しい `solana-system-interface` クレートに対して再構築され、SIMD-0437 によって解放されたレントを返す管理者のみの `WithdrawExcessLamports`（タグ `18`）を追加します。すべてのトレーダーおよび LP 向けの命令はそのアカウント、引数、および数学を保持し、リリースの何もブレーキングではありません。`CreateConfigAccount` は末尾のレント sysvar の読み取りを停止しましたが、古い 5 アカウント リストを受け入れます。`AmmError` はコード `60` を取得します（`6000` ではなく `0` から番号が付けられます）。

  [完全なエントリを読む →](/ja/reference/changelog/2026-09-09-amm-v4-solana-3-and-excess-lamports)
</Update>

<Update label="2026-08-31" description="LaunchLab: プラットフォーム曲線ルールがカーブ パラメータ ホワイトリストに置き換わります">
  ローンチ パラメータ制限は `PlatformConfig` から、設定ごとの `PlatformCurveRule` アカウントに移動します。ルールは、19 のローンチ パラメータに対する最大 25 個の `(field, op, value)` 制約を持つ最大 10 個のチェック グループを保持し、`Eq` / `Gte` / `Lte` / `Neq` を使用します。これにより、プラットフォームは値バンド、代替ティア、卒業評価キャップ、マイグレーション フロア、トークン タイプ ゲーティング、または日付で切り替わるグループを表現できるようになります。これらはすべて、等号のみのホワイトリストではできませんでした。`PlatformConfig` は 944 バイトのサイズを保持します。`restrict_curve_param`、`curve_rule_manager`、および削除された vec の長さプレフィックスはすべてパディングから削除されます。デコーダーは末尾の `Vec<PlatformCurveParam>` を削除する必要があり、ローンチ ビルダーはフラグがオンの間、ルール PDA を `remaining_accounts` に追加する必要があります。2 つの命令が削除され、4 つが追加され、2 つの `UpdatePlatformConfig` バリアントが追加され、`6024`–`6030` が追加されました。IDL リフレッシュが必要です。SDK は 2 つの純粋なオフチェーン チェックを提供するため、クリエーターはリバートされたトランザクションからルールを学ぶ必要がありません。エントリは、メインネットでフラグを有効にする前に devnet リハーサルを文書化しています。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-31-launchlab-platform-curve-rules)
</Update>

<Update label="2026-08-27" description="LaunchLab: プラットフォームはミント作成から引き出し保留オーソリティを保持します">
  `InitializeWithToken2022` は `PlatformConfig.transfer_fee_extension_auth` を新しいベース ミントの `withdraw_withheld_authority` に書き込むようになり、ローンチ `authority` PDA の代わりに、プラットフォームは卒業を待つのではなく、ボンディング曲線フェーズ中に保留された転送フィーをスイープできます。プログラム自体には引き出し保留命令がありません。`MigrateToCpswap` はその権限を PDA がまだ保持している場合にのみ再割り当てし、これにより両世代のミントの卒業が機能します。`transfer_fee_config_authority` は卒業時にのみ移動するため、ローンチ中に `transfer_fee_extension_auth` をローテーションすると、2 つのオーソリティが異なるキーに残ります。アカウント レイアウト、命令、引数、またはエラー コードは変更されていません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-27-launchlab-token2022-withheld-authority)
</Update>

<Update label="2026-08-26" description="LaunchLab: プラットフォーム フィー レート キャップが 500 bps に引き上げられました">
  プラットフォーム `fee_rate` の上限は、それを検証する両方のパスで `50000`（500 bps）に移動します。`CreatePlatformConfig` はプログラムの最初のリリース以来 `10000`（100 bps）でキャップされていました。`UpdatePlatformConfig` は 2026-01-27 以来 `25000`（250 bps）でキャップされていました。2 つのチェックが一致するようになったため、許可されたレートで作成されたプラットフォームは、`AllInfo` バルク バリアントを含め、それでも更新できます。これは `fee_rate` を再検証しながら他のすべてのフィールドを書き直します。2 つのパスは依然として異なるエラーを発生させます（作成時は `InvalidInput`、更新時は `InvalidPlatformInfo`）。アカウント レイアウト、命令、またはエラー コードは変更されていません。既存のプラットフォームまたはローンチは再価格化されていません。`GlobalConfig.max_share_fee_rate` は 100 bps のままで、トランザクションごとのリファーラル `share_fee_rate` のみを制限します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-26-launchlab-platform-fee-rate-cap)
</Update>

<Update label="2026-08-24" description="LaunchLab: Token-2022 クォート ミント">
  ローンチは Token-2022 ミントで引用できるようになりました。`CreateConfig`、`InitializeV2`、`InitializeWithToken2022`、4 つのスワップ命令、およびすべてのフィー クレームは、クォート プログラム スロットで両方のトークン プログラムを受け入れます。非推奨の `Initialize` はレガシーのみのままです。スワップ スリッページ バウンドは、クォート ミントの転送フィーを差し引いた、支払人が実際に支払うまたは受け取るものと比較されるようになります。`PoolState.token_program_flag` bit1 は意味を持つようになるため、バイト全体を `0` に対してテストするデコーダーはレガシー ベース ミントを Token-2022 として誤読します。`MigrateToCpswap` は 2 つのトークン プログラム アカウントを `token_program` / `token_program_2022` に名前変更し、`6023` は `CalculateOverflow` に再利用されます。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-24-launchlab-token2022-quote-mint)
</Update>

<Update label="2026-08-17" description="LaunchLab: CPMM のみのローンチとプラットフォーム設定コントロール">
  新しいローンチ初期化は CPMM を必須にするようになり、レガシー AMM v4 バウンド状態は移行可能なままです。このリリース前は、`creator_scale` はクリエーター所有のフィー キーを生成しました。アップグレード後に実行される CPMM マイグレーションは、代わりに `platform_scale + creator_scale` を 1 つのプラットフォーム所有のフィー キー シェアに統合します。プラットフォームは独自の `PlatformAllowConfig` PDA でローンチを制限することもでき、マイグレーション ビルダーは両方の CPMM サポート ミント PDA を追加する必要があります。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-launchlab-cpmm-only-platform-config)
</Update>

<Update label="2026-08-17" description="CLMM: 制限付き発行者ポジション NFT フリーズ">
  新しい CLMM ポジション NFT ミントはプールをフリーズ オーソリティとして使用しますが、トークン アカウントはデフォルトではフリーズされません。フリーズは `OpenPositionV2` または `OpenPositionWithToken22Nft` でのみ発生し、基礎となるボールト ミントのいずれかのフリーズ オーソリティが制限付き発行者リストと一致する場合です。一致するポジションは転送または所有者変更はできませんが、流動性を管理できます。その `ClosePosition` 呼び出しはプールを追加する必要があり、CLMM はアトミックにシオ、バーン、クローズできます。既存のポジションは変更されません。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-clmm-restricted-position-nft-freeze)
</Update>

<Update label="2026-08-17" description="CPMM: パーミッションレス クリエーター フィー コレクション">
  加算的な `CollectCreatorFeePermissionless` 命令により、任意の支払人がすべての発生したクリエーター フィーをスイープできます。受益者と両方のトークン宛先を `PoolState.pool_creator` とクリエーターの正規 ATA に制限します。元のクリエーター署名パスは変更されません。`CreatePermissionPda` は専用のグラント オーソリティも受け入れますが、`ClosePermissionPda` は管理者のみのままです。

  [完全なエントリを読む →](/ja/reference/changelog/2026-08-17-cpmm-permissionless-creator-fee-collection)
</Update>

<Update label="2026-07-30" description="CLMM: パーミッション マルチ プールとリミット オーダー フリーズ アカウント ガード">
  2 つの加算的で後方互換性のある CLMM プログラム更新。`CreatePermissionedPool` はクライアント提供の 0 以外の `seed_index` をプール PDA シードに折り込み、ホワイトリストに登録されたオペレーター（`Permission` PDA を保持している）が `(config, mint0, mint1)` ごとに複数のプールを作成できるようにします。これにより、プール ID はペアの正規ではなくなります。新しい管理者命令 `CreatePermissionPda` / `ClosePermissionPda` はそれらのグラントを管理し、`PoolState` は `seed_index` フィールドを取得します（パディングから削除され、サイズ変更なし）。別に、`OpenLimitOrder` は出力側アカウントを取得し、入力または出力トークン アカウントがフリーズされている注文を拒否するようになります（`NotApproved`）。

  [完全なエントリを読む →](/ja/reference/changelog/2026-07-30-clmm-permissioned-pools)
</Update>

<Update label="2026-07-22" description="AMM v4: OpenBook / Serum 依存関係を削除">
  AMM v4 は長年休止していた OpenBook/Serum 依存関係、すべてのオーダーブック CPI、および死んだマーケット メイキング命令を削除します。`SwapBaseIn` / `SwapBaseOut`、`Deposit`、および `Withdraw` はレイアウトを保持します（削除されたマーケット アカウントは検証されず無視されます）。**ハード ブレーキング** `WithdrawPnl`（17 → 10、互換性なし）および `SetParams`（削除されたアカウント + 番号が付け直された `param`）。`Initialize`、`PreInitialize`、`MonitorStep`、`MigrateToOpenBook`、`WithdrawSrm`、`SimulateInfo`、`AdminCancelOrders` は呼び出し可能ではなくなります。オンチェーン アカウント レイアウトとエラー コードは安定したままです。スワップを V2 エントリポイントに移行します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-07-22-amm-v4-openbook-removal)
</Update>

<Update label="2026-06-22" description="Stable AMM: 死んだ OpenBook（マーケット）コードを削除">
  Stable AMM は長年休止していた OpenBook マーケット メイキング アカウントとコードを削除します。より小さい `SwapBaseIn` / `SwapBaseOut`（18 → 9）、`Deposit`（14 → 12）、および `Withdraw`（21/22 → 12）レイアウト（古いレイアウトは互換性があります）。**ハード ブレーキング** `WithdrawPnl` 変更（16 → 10、互換性なし）。リファーラル フィーは廃止されました。簡略化されたボールトのみのプール資産フォーミュラ。ほとんどの他の Stable 命令は呼び出し可能ではなくなります。

  [完全なエントリを読む →](/ja/reference/changelog/2026-06-22-stable-amm-openbook-cleanup)
</Update>

<Update label="2026-05-18" description="CLMM: リミット オーダー、単一側フィー、動的フィー">
  3 つのオプトイン、後方互換性のある CLMM 機能。ファーストクラス リミット オーダー（`limit_order_admin` 決済キーパー付き）、単一側フィー コレクション（`CollectFeeOn`）、およびボラティリティ追跡動的フィー。`CreateCustomizablePool`、`PoolState` リシェイプ（**インデクサー ブレーキング変更**）、新しい `TickState` フィールド、11 の新しいエラー コード（数値シフト付き）、および一致する SDK / API 追加を追加します。

  [完全なエントリを読む →](/ja/reference/changelog/2026-05-18-clmm-limit-orders)
</Update>

<Update label="2026-04-26" description="初版公開">
  Raydium ドキュメンテーション セットの最初の公開リリース。ライブ mainnet-beta デプロイメントと `@raydium-io/raydium-sdk-v2@0.2.42-alpha` に対して検証されています。

  [完全なエントリを読む →](/ja/reference/changelog/2026-04-26-initial-publication)
</Update>

## ドキュメンテーション規約

- **バージョニング**: このドキュメンテーションはカレンダーベースのバージョニング（YYYY-MM-DD）を使用します。各更新は新しいエントリ ページを追加し、上記のタイムラインの上部に新しい行を追加します。
- **リリースごとに 1 ページ**: すべてのリリース サマリーは `reference/changelog/` の下の独自のページに存在するため、このインデックスは短く保たれ、各エントリは独立してリンク可能です。
- **検証日**: すべてのエントリは、コンテンツがオンチェーン / API 状態とプログラム ソースに対して最後に相互チェックされた時期を記録します。記載されていない場合は、エントリのメイン日付を想定してください。
- **ブレーキング変更**: 影響を受けるページのボックス化された警告で呼び出され、エントリでタグ付けされます。
- **カバレッジ**: このチェンジログはドキュメンテーション セット自体をカバーしています。プロトコル自体の歴史的タイムラインは [`introduction/history-and-milestones`](/ja/introduction/history-and-milestones) に存在し、「Raydium で X が発生したのはいつか」の真実のソースです。

## 修正

このドキュメンテーションでエラーを見つけた場合は、ドキュメンテーション リポジトリで issue または pull request を開いてください。修正はチェンジログ エントリとして記録されます。

## ポインタ

- [`introduction/history-and-milestones`](/ja/introduction/history-and-milestones) — プロトコル自体のタイムライン。
- [`security/audits`](/ja/security/audits) — 監査履歴。
- [`ray/protocol-fees`](/ja/ray/protocol-fees) — プロトコル フィー分割。
- [`reference/program-addresses`](/ja/reference/program-addresses) — プログラム ID の真実のソース。
