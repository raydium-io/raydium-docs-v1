---
title: "エラーコード"
description: "Raydium のすべてのオンチェーン プログラムの Anchor エラー列挙型。原因と推奨される UX メッセージを含みます。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/reference/error-codes)
</Info>

<Info>
  **信頼できる情報源。** 以下の表は、公開されている Raydium リポジトリの各プログラムの `error.rs` から再生成されます。プログラムがアップグレードされて新しいバリアントが追加されたときは、抽出を再実行し（各表の下部のリンク）、表を再構成するのではなく新しい行を追加してください。Anchor エラーコードは**ソースの順序**で割り当てられ、名前ではなく、並べ替えるとインテグレーターのエラーハンドリングが壊れます。
</Info>

## Anchor エラーコードの仕組み

Anchor は、プログラムの `ErrorCode` 列挙型の各バリアントに `6000` から始まる数値コードを割り当てます。失敗したトランザクションは以下を表示します：

- **数値エラーコード**（例：`0x1771` = 6001）がトランザクション ログに表示されます。
- **エラー名**（例：`InvalidOwner`）が IDL から取得されます。
- **`#[msg(...)]` 文字列**が Anchor によって `log_messages` に出力されます。

インテグレーターは、メッセージ文字列ではなく**数値コード**で照合する必要があります（文字列はバージョンをバンプせずに言い換えることができます）。

```ts
// 例：CPMM スリッページ エラーを検出し、クリーンな UX メッセージを表示します。
const CPMM_EXCEEDED_SLIPPAGE = 6005;

try {
  await sdk.cpmm.swap({ ... });
} catch (err: any) {
  const code = err?.error?.errorCode?.number ?? err?.code;
  if (code === CPMM_EXCEEDED_SLIPPAGE) {
    showToast("Price moved past your slippage tolerance. Increase slippage or retry.");
    return;
  }
  throw err;
}
```

## CPMM（標準 AMM）エラー

プログラム ID：[reference/program-addresses](/ja/reference/program-addresses#on-chain-programs) を参照してください。ソース：`raydium-cp-swap/programs/cp-swap/src/error.rs`。

| コード | バリアント | `#[msg]` 文字列 | 一般的な原因 | 推奨される UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | 呼び出し元が管理者命令の設定済み権限ではありません。 | "Only the pool admin can perform this action." |
| 6001 | `InvalidOwner` | Input account owner is not the program address | 渡されたアカウントが間違ったプログラムによって所有されています（多くの場合、間違ったトークン プログラムまたは間違ったプログラム PDA）。 | "Internal: account owner mismatch — refresh and retry." |
| 6002 | `EmptySupply` | Input token account empty | LP またはトークン アカウントの残高が、正の金額が必要なステップでゼロです。 | "Nothing to withdraw." |
| 6003 | `InvalidInput` | InvalidInput | 一般的な不正な引数（範囲外の金額、間違ったフラグ）。 | "Invalid input — check amounts and try again." |
| 6004 | `IncorrectLpMint` | Address of the provided lp token mint is incorrect | 渡された LP ミント アカウントが `pool_state.lp_mint` と一致しません。 | "Internal: wrong LP mint — refresh pool data." |
| 6005 | `ExceededSlippage` | Exceeds desired slippage limit | 実行された価格がユーザーの `minAmountOut` / `maxAmountIn` より悪くなっています。 | "Price moved past your slippage tolerance. Increase slippage or retry." |
| 6006 | `ZeroTradingTokens` | Given pool token amount results in zero trading tokens | デポジット / 引き出し計算が片側をゼロに丸めました（ポジションが小さすぎます）。 | "Amount is below the minimum for this pool." |
| 6007 | `NotSupportMint` | Not support token_2022 mint extension | プール が安全に処理できない Token-2022 拡張機能（例：`TransferHook`、`DefaultAccountState=Frozen`）に遭遇しました。 | "This token has an extension Raydium does not support in CPMM." |
| 6008 | `InvalidVault` | invaild vault | 渡されたボルト アカウントが `pool_state` に記録されているものと一致しません。 | "Internal: wrong vault — refresh and retry." |
| 6009 | `InitLpAmountTooLess` | Init lp amount is too less(Because 100 amount lp will be locked) | プール初期化時、計算された LP 供給量が永続ロック金額を下回っています。 | "Initial liquidity too small. Increase deposit." |
| 6010 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Token-2022 フィー ミントの転送後の観測額が事前計算の予想と一致しませんでした。 | "Token transfer fee changed mid-transaction. Retry." |
| 6011 | `MathOverflow` | Math overflow | 中間スワップ / デポジット / フィー計算がオーバーフローしました。 | "Amount is too large for this pool." |
| 6012 | `InsufficientVault` | Insufficient vault | プール ボルト残高が要求された出力をカバーするには低すぎます。 | "Not enough liquidity in the pool for this size." |
| 6013 | `InvalidFeeModel` | Invalid fee model | 管理者が、オンチェーンで拒否される `AmmConfig` パラメータの組み合わせを設定しました。 | N/A — admin-only path. |
| 6014 | `NoFeeCollect` | Fee is zero | プロトコル、ファンド、またはクリエーター フィー収集命令が収集可能なフィーを見つけませんでした。`CollectCreatorFeePermissionless` の場合、両方のクリエーター カウンターがゼロでした。 | "Nothing to collect." Treat it as a benign race and stop. |

再生成ソース：[github.com/raydium-io/raydium-cp-swap — error.rs](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)。

## CLMM エラー

プログラム ID：[reference/program-addresses](/ja/reference/program-addresses#on-chain-programs) を参照してください。ソース：`raydium-clmm/programs/amm/src/error.rs`。

| コード | バリアント | `#[msg]` 文字列 | 一般的な原因 | 推奨される UX |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | 呼び出し元がこの命令の設定済み管理者ではありません。**または**（権限命令）署名者が `admin` でも `permission_pda_admin` でもありません。**または**（`OpenLimitOrder`）入出力トークン アカウントがフリーズされているか、プールがスワップ / リミット オーダーを無効にしています。**または**（`ClosePosition`、フリーズ パス）提供された残りのアカウントが `personal_position.pool_id` ではありません。 | "Only the pool admin can perform this action." / "This token account is frozen — thaw it before placing the order." / "Internal: wrong position pool account." |
| 6001 | `InvalidUpdateConfigFlag` | invalid update amm config flag | 管理者が認識されない `param` 値を `update_amm_config` に渡しました。 | N/A — admin-only path. |
| 6002 | `AccountLack` | Account lack | 必要な残りのアカウントが tx から欠落しています（通常、ティック配列またはオラクル拡張機能）。または `ClosePosition` がフリーズされた NFT をプールを最初の残りのアカウントとして閉じています。 | "Internal: missing account — refresh pool data or update the position-close builder." |
| 6003 | `ClosePositionErr` | Remove liquidity, collect fees owed and reward then you can close position account | リクイディティ、未収フィー、または未収報酬がまだあるポジションを閉じようとしました。 | "Withdraw all liquidity and claim fees/rewards before closing the position." |
| 6004 | `InvalidTickIndex` | Tick out of range | `tick_lower` または `tick_upper` が `[-443636, 443636]` の範囲外です。 | "Price range out of bounds for this pool." |
| 6005 | `TickInvalidOrder` | The lower tick must be below the upper tick | `tick_lower >= tick_upper`。 | "Lower price must be below upper price." |
| 6006 | `TickLowerOverflow` | The tick must be greater, or equal to the minimum tick(-443636) | 下位ティック アンダーフロー。 | "Lower price too low." |
| 6007 | `TickUpperOverflow` | The tick must be lesser than, or equal to the maximum tick(443636) | 上位ティック オーバーフロー。 | "Upper price too high." |
| 6008 | `TickAndSpacingNotMatch` | tick % tick_spacing must be zero | 送信されたティックがプールの `tick_spacing` の倍数ではありません。 | "Snap price to the nearest valid increment." |
| 6009 | `InvalidTickArray` | Invalid tick array account | ティック配列スロットに対して間違った PDA が渡されました。 | "Internal: wrong tick array — refresh pool data." |
| 6010 | `InvalidTickArrayBoundary` | Invalid tick array boundary | ティック配列インデックスの 1 つのオフ。 | "Internal: tick-array boundary error." |
| 6011 | `SqrtPriceLimitOverflow` | Square root price limit overflow | 呼び出し元が指定した `sqrt_price_limit` が有効な範囲外です。 | "Price limit out of range." |
| 6012 | `SqrtPriceX64` | sqrt_price_x64 out of range | プールの現在の sqrt 価格がスワップ中に範囲外にドリフトしました。 | "Retry swap." |
| 6013 | `LiquiditySubValueErr` | Liquidity sub delta L must be smaller than before | リクイディティ削減時の内部不変式違反。 | "Internal: liquidity accounting error." |
| 6014 | `LiquidityAddValueErr` | Liquidity add delta L must be greater, or equal to before | リクイディティ増加時の内部不変式違反。 | "Internal: liquidity accounting error." |
| 6015 | `ForbidBothZeroForSupplyLiquidity` | Both token amount must not be zero while supply liquidity | `amount_0_max` と `amount_1_max` の両方がゼロのリクイディティ増加呼び出し。 | "Provide at least one token." |
| 6016 | `LiquidityInsufficient` | Liquidity insufficient | ポジションに引き出しを満たすのに十分なリクイディティがありません。 | "Withdraw amount exceeds position liquidity." |
| 6017 | `PriceSlippageCheck` | Price slippage check | 実行価格が呼び出し元のスリッページ ガードに失敗しました。 | "Price moved past your slippage tolerance. Increase slippage or retry." |
| 6018 | `TooLittleOutputReceived` | Too little output received | `SwapBaseInput`：出力額が `other_amount_threshold` 未満。 | "Slippage exceeded — minimum output not met." |
| 6019 | `TooMuchInputPaid` | Too much input paid | `SwapBaseOutput`：入力額が `other_amount_threshold` を超えています。 | "Slippage exceeded — maximum input exceeded." |
| 6020 | `ZeroAmountSpecified` | Swap special amount can not be zero | スワップ命令でゼロ `amount`。 | "Enter an amount greater than zero." |
| 6021 | `InvalidInputPoolVault` | Input pool vault is invalid | スワップの入力ボルト アカウントがプールの記録されたボルトと一致しません。 | "Internal: wrong input vault — refresh pool data." |
| 6022 | `TooSmallInputOrOutputAmount` | Swap input or output amount is too small | スワップ計算がゼロに丸められました。通常は非常に小さなダスト。 | "Amount too small to swap in this pool." |
| 6023 | `NotEnoughTickArrayAccount` | Not enough tick array account | スワップ範囲に対して十分なティック配列の残りのアカウントが提供されていません。 | "Internal: insufficient tick-arrays — refresh pool data." |
| 6024 | `InvalidFirstTickArrayAccount` | Invalid first tick array account | 渡された最初のティック配列アカウントが現在のティックをカバーしていません。 | "Internal: wrong first tick array — refresh pool data." |
| 6025 | `InvalidRewardIndex` | Invalid reward index | `reward_index` が `[0, 2]` の範囲外です。 | N/A — admin path. |
| 6026 | `FullRewardInfo` | The init reward token reach to the max | プールには既に最大（3）の報酬ミントが設定されています。 | N/A — admin path. |
| 6027 | `RewardTokenAlreadyInUse` | The init reward token already in use | 報酬ミントが既存のものと重複しています。 | N/A — admin path. |
| 6028 | `ExceptRewardMint` | The reward tokens must contain one of pool vault mint except the last reward | スロット 2 より前、報酬ミントはプールのボルト ミントの 1 つと等しい必要があります。 | N/A — admin path. |
| 6029 | `InvalidRewardInitParam` | Invalid reward init param | 不正な発行開始/終了時刻または 1 秒あたりのレート。 | N/A — admin path. |
| 6030 | `InvalidRewardInputAccountNumber` | Invalid collect reward input account number | `CollectReward` に渡された残りのアカウント数が間違っています。 | "Internal: wrong account count — refresh and retry." |
| 6031 | `InvalidRewardPeriod` | Invalid reward period | 報酬発行期間が無効です（`end <= start`、ゼロ期間）。 | N/A — admin path. |
| 6032 | `NotApproveUpdateRewardEmissions` | Modification of emissions is allowed within 72 hours from the end of the previous cycle | 管理者がサイクル境界周辺の 72 時間ウィンドウ外で発行を変更しようとしました。 | N/A — admin path. |
| 6033 | `UnInitializedRewardInfo` | uninitialized reward info | 報酬スロットが初期化されていませんが参照されています。 | N/A — admin path. |
| 6034 | `NotSupportMint` | Not support token_2022 mint extension | CLMM が処理できない Token-2022 拡張機能に遭遇しました。 | "This token has an extension Raydium CLMM does not support." |
| 6035 | `MissingTickArrayBitmapExtensionAccount` | Missing tickarray bitmap extension account | スワップがベース ビットマップでカバーされていないティック範囲を横切りました。拡張ビットマップ アカウントが必要です。 | "Internal: missing tick-array extension — refresh pool data." |
| 6036 | `InsufficientLiquidityForDirection` | Insufficient liquidity for this direction | スワップの方向に十分なリクイディティが存在しません。 | "Insufficient liquidity for this swap size." |
| 6037 | `MaxTokenOverflow` | Max token overflow | 入出力額が u64 を超えました。 | "Amount too large for this pool." |
| 6038 | `CalculateOverflow` | Calculate overflow | フィー / リクイディティ算術オーバーフロー。 | "Amount too large for this pool." |
| 6039 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Token-2022 転送フィーの観測額が予想と一致しませんでした。 | "Token transfer fee changed mid-transaction. Retry." |
| 6040 | `OrderAlreadyFilled` | Order already fully filled, cannot modify | `IncreaseLimitOrder` / `DecreaseLimitOrder` が、未充足部分がゼロのオーダーで呼び出されました。 | "This limit order is already filled — settle to receive output." |
| 6041 | `InvalidOrderPhase` | Invalid order phase | FIFO コホート フェーズがティックの現在のコホートと一致しなくなったオーダーを変更しています。 | "Cannot perform this action in the order's current state." |
| 6042 | `InvalidLimitOrderAmount` | Invalid limit order amount | オーダー入力がプールの最小値（またはゼロ）を下回っています（オープン / 増加 / 削減時）。 | "Order size is below the pool's minimum." |
| 6043 | `OrderPhaseSaturated` | Tick order phase saturated | ティックのコホートの `order_phase` カウンターが飽和しています。既存のコホートが決済してロールオーバーするまで、そのティックでさらにオーダーを開くことはできません。 | "Too many active orders at this price; try a nearby tick or wait for orders to settle." |
| 6044 | `InvalidDynamicFeeConfigParams` | Invalid dynamic fee config params | `CreateDynamicFeeConfig` / `UpdateDynamicFeeConfig` が拒否されました。または `CreateCustomizablePool` が有効な設定なしで動的フィーを有効にしました。 | N/A on admin path; "Dynamic fee config invalid" on user path. |
| 6045 | `InvalidFeeOn` | Invalid fee on which token (must be 0, 1, or 2) | `CreateCustomizablePool` が `collect_fee_on` 値を `{0, 1, 2}` の外に渡しました。 | "Internal: invalid fee mode." |
| 6046 | `ZeroSqrtPrice` | sqrt_price_x64 must be greater than 0 | `CreateCustomizablePool`（またはカスタマイズ可能な初期 sqrt 価格を受け入れる別のパス）が `sqrt_price_x64 == 0` で呼び出されました。 | "Initial price must be greater than zero." |
| 6047 | `ZeroLiquidity` | liquidity must be greater than 0 | リクイディティ提供パスが `liquidity == 0` で呼び出され、補償額がありません。 | "Liquidity amount must be greater than zero." |
| 6048 | `MissingBaseFlag` | base_flag is required when liquidity is zero | オープン ポジション バイ アマウント パスが `liquidity == 0` を計算し、呼び出し元がどちらの側がベースであるかを明確にするための `base_flag` を提供しませんでした。 | "Provide either a non-zero liquidity or specify which token is the base." |
| 6049 | `MissingMintAccount` | Mint account is required but not provided | Token-2022 対応パスが、拡張機能と転送フィーを検証するために必要な入出力ミント アカウントなしで呼び出されました。 | "Internal: missing mint account — refresh pool data." |
| 6050 | `MissingTokenProgram2022` | Token-2022 program is required but not provided | 上記と同じ SPL-Token-2022 プログラム アカウント用。 | "Internal: missing Token-2022 program — refresh and retry." |

> **番号付け変更に関する注記。** CLMM `ErrorCode` 列挙型はこのリリースで番号付けが変更されました：5 つのレガシー バリアント（`LOK`、`ZeroMintAmount`、`InvalidLiquidity`、`TransactionTooOld`、`InvalidRewardDesiredAmount`）といくつかのタイプミス（`Liquitity`、`enought`、`emissiones`）が削除/修正され、11 の新しいバリアントが追加されました。Anchor はエラーをソース順序で番号付けするため、`6000` 以上のすべてのコードは事前リリース ビルドに対して相対的にシフトしています。数値コードをハードコードしたクライアントは、以前のバージョンに対して再マップする必要があります。

再生成ソース：[github.com/raydium-io/raydium-clmm — error.rs](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)。

## AMM v4、Farm v3 / v5 / v6、LaunchLab エラー

これらのプログラムは、それぞれのチャプターで文書化されています（[`products/amm-v4/instructions`](/ja/products/amm-v4/instructions)、[`products/farm-staking/instructions`](/ja/products/farm-staking/instructions)、[`products/launchlab/instructions`](/ja/products/launchlab/instructions) を参照）。これらのプログラムは Anchor とプレーン Solana エラー サーフェスの混合を使用するため、エラー テーブルはここではなく命令リファレンスの隣に存在します。以下のコードはこれらのチャプターで予約されています：

| プログラム | コード範囲（Anchor のみ） | リファレンス |
|---------|--------------------------|-----------|
| AMM v4 | カスタム u32 コード、Anchor スタイルではない | [`products/amm-v4/instructions`](/ja/products/amm-v4/instructions) |
| Farm v3 / v5 | カスタム u32 コード | [`products/farm-staking/instructions`](/ja/products/farm-staking/instructions) |
| Farm v6 | 6000+（Anchor） | [`products/farm-staking/instructions`](/ja/products/farm-staking/instructions) |
| LaunchLab | 6000+（Anchor） | [`products/launchlab/instructions`](/ja/products/launchlab/instructions) |

最近の LaunchLab リリースではコード `6000`–`6021` を変更しないままにしています。2026-08-17 リリースは以前のプラットフォーム グローバル アクセス エラーを削除し、`6022` を許可アカウント検証エラーに割り当てました。2026-08-24 リリースは `6023` を追加します。2026-08-31 リリースは `6024`–`6030` を追加します：

| コード | 名前 | 意味 |
|---|---|---|
| 6018 | `NotEnoughRemainingAccounts` | 必要な残りのアカウントが欠落しています。これには、必要な `PlatformAllowConfig`、必要な `PlatformCurveRule`、および拡張 CPMM マイグレーション アカウント セットが含まれます。 |
| 6020 | `CurveParamIsNotExist` | 廃止。コードがシフトしないようにプレースホルダーとして保持されます。2026-08-31 リリースは最後の呼び出し元を削除しました。 |
| 6022 | `InvalidPlatformAllowConfig` | 提供された許可アカウントが予想されるプラットフォームとグローバル設定ペアをエンコードしていません。 |
| 6023 | `CalculateOverflow` | Token-2022 転送フィーまたは逆転送フィー計算を表現できませんでした。正確な出力トレード パスで到達可能です。クォート ミントのフィーを反転できない場合（例：100% フィー ミント）。 |
| 6024 | `InvalidPlatformCurveRule` | 提供されたルール アカウントが、起動中のプラットフォームと設定ペアの PDA ではありません。 |
| 6025 | `CurveParamNotMatchPlatformRule` | 起動パラメータはプラットフォームのルールのチェック グループを満たしていません。クリエーターが実際にヒットできる唯一のエラー — "this launch shape is not allowed on this platform" として表示します。 |
| 6026 | `CurveRuleGroupNotExist` | `RemovePlatformCurveRule` が、ルールが保持していない `group_id` で呼び出されました。 |
| 6027 | `CurveRuleGroupsExceeded` | ルールは 10 個以上のチェック グループを保持できません。 |
| 6028 | `InvalidCurveRuleConstraint` | 制約に不明なフィールドまたはオペレーター、グループに 25 個以上の制約、または同じ `(field, op)` ペアが 1 つのグループに 2 回表示されます。 |
| 6029 | `InvalidCurveRuleAuthority` | 署名者が `PlatformConfig.curve_rule_manager` でも、プラットフォーム管理者でもありません。 |
| 6030 | `CurveRuleFieldNotSupportedByCurve` | 制約が、曲線が導出するプール上の `total_base_sell` を読み取ります — 固定価格および線形価格曲線。ルールが書き込まれるときに発生します。起動が実行されるときではなく。 |

古い `PlatformGlobalAccessDenied`（`6022`）および `InvalidPlatformGlobalAccess`（`6023`）バリアントは削除されます。前の IDL を使用して `6022` をデコードしないでください。`6023` — 2026-08-17 リリースで空いた — は 2026-08-24 リリースで関連のないエラーに再利用されるため、どちらかの以前のバージョンの IDL でデコードしてはいけません。

`6020` は意図的に保持されます。Anchor はバリアントを位置で番号付けするため、削除すると後のすべてのコードがシフトし、`6021`–`6030` をデコードするすべてのクライアントが壊れます。

## SDK エラーをプログラム エラーにマッピングする

公式 TypeScript SDK はオンチェーン エラーを `SendTransactionError` にラップし、Anchor プログラムの場合は `AnchorError` にラップします：

```ts
import { AnchorError } from "@coral-xyz/anchor";

try {
  await sdk.cpmm.swap({ ... });
} catch (err) {
  if (err instanceof AnchorError) {
    console.log(err.error.errorCode.number, err.error.errorCode.code, err.error.errorMessage);
    // 6005 ExceededSlippage "Exceeds desired slippage limit"
  } else {
    // Raw SendTransactionError — inspect err.logs for the "custom program error: 0x..." line.
  }
}
```

クライアント側で Anchor を使用していない場合は、トランザクション ログを解析します：

```
Program <ProgramID> invoke [1]
Program log: AnchorError caused by account: pool_state. Error Code: ExceededSlippage. Error Number: 6005. Error Message: Exceeds desired slippage limit.
```

パターン `Error Number: (\d+)` は Anchor バージョン全体で安定しており、マッチングに安全です。

## これらのテーブルを再生成する

プログラムがアップグレードされて新しいエラーが追加されたときは、ソースから再抽出します：

```bash
# Clone and grep the error enum in order.
git clone --depth=1 https://github.com/raydium-io/raydium-cp-swap
awk '/#\[msg\(/{ gsub(/^[ \t]*#\[msg\("|"\)\][ \t]*/,""); m=$0; getline; gsub(/,/,""); gsub(/^[ \t]+/,""); print "6000+++\t" $0 "\t" m }' \
  raydium-cp-swap/programs/cp-swap/src/error.rs
# Replace 6000+++ with monotonically-increasing 6000,6001,... Append new rows to the table above.
```

プログラム バリアントが追加されたときは、常に [`reference/changelog`](/ja/reference/changelog) を更新して、SDK をアップグレードするインテグレーターがエラー ハンドラーをリフレッシュする必要があることを知ることができます。

ソース：

- [raydium-cp-swap error.rs (mainnet CPMM)](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)
- [raydium-clmm error.rs (mainnet CLMM)](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)
