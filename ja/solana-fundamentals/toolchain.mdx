---
title: "ツールチェーン"
description: "Raydium のオンチェーン状態を読み取り、トランザクションを構築し、プログラムに対して開発するための最小限の作業セットアップ。Solana CLI、Anchor、TypeScript と Python の SDK、および devnet/localnet のヒント。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/solana-fundamentals/toolchain)
</Info>

このドキュメント全体は、動作する Solana 開発ツールチェーンがあることを前提としています。このページでは、ツール、2026 年 9 月時点で Raydium と連携することが確認されているバージョン、および特定のセットアップの注意点をリストアップしています。後の章のコード例を実行する前に、ここから始めてください。

**2 つのバージョンセットが重要であり、それらは同じではありません。** Raydium プログラムを*構築またはフォーク*するには、プログラムがピンしている正確なツールチェーン（Rust 1.91.0、Agave 3.1.10、Anchor 1.0.2（2026-09-09 アップグレード時点））が必要です。TypeScript または Python からデプロイされたプログラムに対して*統合*するには、それらと通信するのに十分な最新のクライアント ライブラリのみが必要であり、ピンは参考情報です。以下の各セクションでは、どのケースをカバーしているかを示しています。

## コアツール

### Solana CLI

Solana クラスタと対話するための標準的なコマンドラインインターフェース — ウォレット管理、RPC 呼び出し、プログラムのデプロイ。

インストール：

```bash
# Raydium プログラムが構築およびテストされるバージョン
sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.10/install)"

# または、タグの代わりにチャネルを追跡する
sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
```

確認：

```bash
solana --version   # → solana-cli 3.1.10 (... client:Agave)
```

<Note>
  **インストールホストが移動しました。** `release.solana.com` は Solana Labs に属していましたが、クライアントは現在 Anza によって **Agave** として保守されており、リリースは `release.anza.xyz` から提供されています。自己更新バイナリは `agave-install` です（`solana-install` はレガシー エイリアスとして保持されています）。したがって、`agave-install update` と `agave-install init <version>` が現在のスペルです。Solana 自体のドキュメントでは、CLI、Rust、Anchor をまとめてプルする 1 コマンド インストーラーも提供しています：

  ```bash
  curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
  ```

  そのインストーラーは独自の現在の概念を追跡し、Raydium のプログラムがピンしているものとは異なる Anchor を提供する可能性があります — その後、`anchor --version` を確認してください。
</Note>

メインネット用に設定：

```bash
solana config set --url https://api.mainnet-beta.solana.com
solana config set --keypair ~/.config/solana/id.json
solana-keygen new                           # ウォレットがない場合は作成
solana address                              # あなたの公開鍵
```

Raydium テスト用には、ほぼ常に資金提供されたデブネット ウォレットが必要です：

```bash
solana config set --url https://api.devnet.solana.com
solana airdrop 2                            # テスト用に 2 SOL
```

### SPL Token CLI

SPL Token / Token-2022 操作用の公式 CLI — ミント作成、トークンミント、転送。

インストール（Solana CLI に含まれています）：

```bash
spl-token --version
```

一般的な操作：

```bash
spl-token create-token                                  # SPL Token ミントを作成
spl-token create-token --program-id TOKEN_2022 ...      # Token-2022 ミントを作成
spl-token create-account <MINT>                         # ATA を作成
spl-token mint <MINT> 1000                              # あなたの ATA に 1000 トークンをミント
spl-token transfer <MINT> 100 <RECIPIENT>               # 100 を転送
spl-token display <ACCOUNT_OR_MINT>                     # 検査
```

### Anchor CLI

Raydium IDL の取得、クライアントの構築、プログラムの検証に必要です。

インストール：

```bash
cargo install --git https://github.com/solana-foundation/anchor avm --locked --force
avm install 1.0.2
avm use 1.0.2
anchor --version   # → anchor-cli 1.0.2
```

<Note>
  **Anchor がリポジトリとメジャーバージョンを移動しました。** 標準的なリポジトリは現在 `github.com/solana-foundation/anchor` です（以前は `coral-xyz/anchor`）。Anchor 1.0 は Raydium プログラムが使用する最初のリリース ラインです — CPMM と LaunchLab は両方とも 2026-09-09 アップグレード時点で `anchor-lang`/`anchor-spl` `=1.0.2` をピンしており、`0.32.1` からアップグレードされています。`avm` は複数のバージョンを保持できます。`avm use 1.0.2` は `anchor build` が Raydium のアーティファクトを再現するようにします。Anchor 1.1 と 1.2 が存在し、独自のプログラムに対して正常にビルドされますが、ターゲット プログラムがピンしているものとは異なる `anchor-lang` マイナーに対してコンパイルされた CPI クレートはリンクされません — [`sdk-api/rust-cpi`](/ja/sdk-api/rust-cpi) を参照してください。
</Note>

一般的な操作：

```bash
anchor idl fetch <PROGRAM_ID> -o program.idl.json       # オンチェーン IDL を取得
anchor idl init -f idl.json <PROGRAM_ID>                # IDL をアップロード
anchor build                                             # ソースからプログラムをビルド
```

## TypeScript セットアップ

Raydium の主要な統合パス。

### パッケージ

```bash
npm install \
  @solana/web3.js \
  @solana/spl-token \
  @anchor-lang/core \
  @raydium-io/raydium-sdk-v2 \
  bn.js \
  decimal.js
```

2026 年 9 月時点で一緒に動作することが確認されているバージョン：

| パッケージ | バージョン | 注記 |
|---------|---------|------|
| `@solana/web3.js` | `^1.95` | 1.x API。`2.x` / `3.x` は異なる互換性のない API サーフェス。Raydium SDK は 1.x をターゲットにしています。 |
| `@solana/spl-token` | `^0.4.15` | 0.4.15 は `createUnwrapLamportsInstruction` を公開しています — [回収可能なレント](/ja/solana-fundamentals/rent-and-reclaimable-rent)を参照してください。 |
| `@anchor-lang/core` | `^1.0` | Anchor 1.x TypeScript クライアント。 |
| `@raydium-io/raydium-sdk-v2` | `0.2.64-alpha` | このドキュメント内のすべてのコード デモが使用するピン。 |

<Note>
  **`@anchor-lang/core` は凍結されています。パッケージは現在 `@anchor-lang/core` です。** `@anchor-lang/core` は Anchor が Solana Foundation に移動したときに `0.32.1` で停止しました。Anchor 1.x は `@anchor-lang/core` として公開され、Raydium 独自のプログラム リポジトリは 2026-09-09 アップグレードでテスト スイートをそれに切り替えました。実際に使用するインポートは変わりません — `import { Program, BN, AnchorProvider } from "@anchor-lang/core"` — したがって、移行はパッケージ名の変更とロックファイルの更新です。

  `0.3x` から移動する場合の 2 つの動作上の注記：`.accounts({...})` は現在、Anchor が自身で解決できないアカウントのみを受け入れるため、完全なセットを渡すビルダーは代わりに `.accountsPartial({...})` を呼び出す必要があります。また、`@anchor-lang/core/dist/cjs/utils/token` は `ASSOCIATED_PROGRAM_ID` などのヘルパーが移動した場所です。
</Note>

### 最小限のスクリプト

```ts
import { Connection, Keypair } from "@solana/web3.js";
import { Raydium } from "@raydium-io/raydium-sdk-v2";

const connection = new Connection("https://api.mainnet-beta.solana.com", "confirmed");
const owner      = Keypair.fromSecretKey(/* your key */);

const raydium = await Raydium.load({
  connection,
  owner,
  cluster:      "mainnet",
  disableFeatureCheck: true,
});

// `ids` はカンマ区切りリストであり、呼び出しは配列を返します
const [poolInfo] = await raydium.api.fetchPoolById({ ids: "..." });
console.log(poolInfo);
```

完全な SDK リファレンスについては、[`sdk-api/typescript-sdk`](/ja/sdk-api/typescript-sdk) を参照してください。

## Rust セットアップ

CPI 統合とオンチェーン プログラム用。

### ツールチェーン

```bash
rustup install 1.91.0           # Raydium プログラムがピンしているもの
rustup default 1.91.0
rustup component add rustfmt clippy
agave-install init 3.1.10       # SBF ツールチェーンが含まれています
```

`cargo build-sbf` でプログラムをビルドします（古い `cargo build-bpf` スペルは廃止されました）。

### Raydium CPI 統合用のプログラム Cargo.toml

```toml
# CPMM 統合（Anchor 1.0）
[dependencies]
anchor-lang        = "=1.0.2"
anchor-spl         = "=1.0.2"
raydium-cp-swap    = { version = "0.2", features = ["cpi"] }
```

```toml
# CLMM 統合 — まだ Anchor 0.32 なので、独自のクレートが必要です
[dependencies]
anchor-lang        = "=0.32.1"
anchor-spl         = "=0.32.1"
raydium-clmm       = { version = "0.1", features = ["cpi"] }
```

`anchor-lang` と `anchor-spl` を `=` でターゲット プログラムがピンしているバージョンにピンします。2026-09 時点で CPMM は `=1.0.2` をピンし、CLMM はまだ `=0.32.1` をピンしているため、**単一のクレートは両方に CPI できません** — [`sdk-api/rust-cpi`](/ja/sdk-api/rust-cpi#cargo-dependencies) を参照してください。

`"cpi"` フィーチャーは CPI ヘルパーを含む `cpi` モジュールをインポートします（`raydium_cp_swap::cpi::swap_base_input(...)` を使用できます）。[`sdk-api/rust-cpi`](/ja/sdk-api/rust-cpi) を参照してください。

## Python セットアップ

二次的な統合パス — ボット開発者に一般的です。

### パッケージ

```bash
pip install solders solana anchorpy
```

バージョン：

| パッケージ | PyPI 名 | バージョン |
|---------|-----------|---------|
| `solders` | `solders` | ≥0.29 |
| solana-py | `solana` | ≥0.40 |
| `anchorpy` | `anchorpy` | ≥0.21 |

<Note>
  2 つの命名トラップがあります。RPC クライアントは `solana` としてインポートされますが、プロジェクトは **solana-py** と呼ばれています — `pip install solana-py` は何も有用なものをインストールしません。また、**PyPI に `raydium-py` パッケージはありません**：コミュニティ Raydium ヘルパーはクローンするリポジトリ [`AL-THE-BOT-FATHER/raydium_py`](https://github.com/AL-THE-BOT-FATHER/raydium_py) であり、公開されたディストリビューションではありません。このページの以前のリビジョンでは、決して機能しなかった `pip install raydium-py` 行をリストアップしていました。
</Note>

### 最小限のスクリプト

```python
from solana.rpc.async_api import AsyncClient
from solders.keypair import Keypair
from anchorpy import Provider, Wallet, Program

async def main():
    conn = AsyncClient("https://api.mainnet-beta.solana.com")
    owner = Keypair()
    wallet = Wallet(owner)
    provider = Provider(conn, wallet)

    # IDL とプログラムをロード
    with open("cpmm.idl.json") as f:
        idl = json.load(f)
    program = Program(idl, CPMM_PROGRAM_ID, provider)

    pool = await program.account["PoolState"].fetch(pool_id)
    print(pool)
```

[`sdk-api/python-integration`](/ja/sdk-api/python-integration) を参照してください。

## RPC エンドポイント

パブリック メインネット RPC（`api.mainnet-beta.solana.com`）は大幅にレート制限されており、負荷がかかると調整されます。重要な使用法については、プライベート エンドポイントを取得してください：

| プロバイダー | 注記 |
|----------|-------|
| Helius | 最も人気。寛大な無料ティア。 |
| Triton | エンタープライズ。プレミアム価格、安定。 |
| QuickNode | 堅牢。サブ秒のレイテンシ。 |
| 独自の RPC ノード | ハードウェアで月額約 \$500。完全な制御。 |

設定：

```ts
const connection = new Connection("https://mainnet.helius-rpc.com/?api-key=...");
```

一部の RPC 操作には拡張機能が必要です：

- `getProgramAccounts` — 無制限スキャンは高コスト。一部のプロバイダーはゲートまたはスキャンごとに課金します。
- `getPriorityFeeEstimate` — Helius 固有のエンドポイント。他のプロバイダーには同等のものがあります。
- `geyser` / WebSocket ストリーミング — 低レイテンシ ボットに必要。すべてのプロバイダーが公開しているわけではありません。

## Devnet と Localnet

### Devnet

Solana のパブリック テスト クラスタ。Raydium には部分的な devnet デプロイメントがあります：

- テスト用に一部の CPMM プールが存在します。
- AMM v4 には履歴 devnet プールがあります。
- CLMM にはいくつかのデモ プールがあります。
- Farm v6 がデプロイされています。

```bash
solana config set --url https://api.devnet.solana.com
solana airdrop 2
```

SDK は `cluster: "devnet"` を介して devnet をサポートします：

```ts
const raydium = await Raydium.load({ connection, owner, cluster: "devnet" });
```

**注意**：devnet プールは流動性が薄く、状態が古い可能性があります。価格発見に devnet を使用しないでください。命令フロー構築とテストのみに使用してください。

### フォークされたメインネット状態を使用した Localnet

現実的なテストのために、メインネット状態をローカル バリデーターにフォークします：

```bash
solana-test-validator \
  --url https://api.mainnet-beta.solana.com \
  --clone CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C \
  --clone CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK \
  --clone <SOME_POOL_ID> \
  --clone <SOME_MINT_ID>
```

これにより、特定のプログラム/アカウント状態をローカルホストにクローンし、自由にトランザクションできます。理想的な用途：

- 実際の Raydium プログラムに対して CPI 統合をユニット テストする。
- 修正されたパラメーターでトランザクションを再生してデバッグする。
- 大規模な合成ボリュームでストレス テストする。

Raydium チームは、メインネット デプロイ前の回帰テストのために、フォークされた localnet を広範に使用しています。

## プロジェクト テンプレート

### TypeScript 統合スターター

```
my-raydium-app/
├── package.json
├── tsconfig.json
├── src/
│   ├── swap.ts           # スワップ例
│   ├── add-liquidity.ts
│   └── read-pool.ts
└── .env                  # RPC URL、ウォレット パス
```

最小限の `package.json`：

```json
{
  "name": "my-raydium-app",
  "type": "module",
  "scripts": { "swap": "tsx src/swap.ts" },
  "dependencies": {
    "@raydium-io/raydium-sdk-v2": "0.2.64-alpha",
    "@solana/web3.js": "^1.95",
    "@solana/spl-token": "^0.4.15"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5"
  }
}
```

### Anchor CPI プログラム スターター

```
my-raydium-cpi/
├── Anchor.toml
├── Cargo.toml
├── programs/
│   └── my-cpi/
│       ├── Cargo.toml
│       └── src/lib.rs
└── tests/
    └── my-cpi.ts
```

Anchor 1.0 を使用：

```bash
anchor init my-raydium-cpi
cd my-raydium-cpi
# Cargo.toml に raydium-cp-swap を features = ["cpi"] で追加
anchor build
anchor test
```

## 便利な CLI ユーティリティ

### `solana-keygen`

```bash
solana-keygen new --outfile ./wallet.json          # キーを生成
solana-keygen pubkey ./wallet.json                 # 公開鍵を表示
```

### 任意のアカウントを読む

```bash
solana account <ADDRESS> --output json-compact
```

デコーダーなしでプール状態を確認するのに便利です。

### プログラム ログ

```bash
solana logs | grep CPMMoo8                         # CPMM 関連の tx ログをテール
```

### トランザクション検査

```bash
solana confirm <TX_SIGNATURE> -v                   # ログをきれいに出力
```

または [explorer](https://explorer.solana.com) 経由 — シグネチャを貼り付けて「Program Logs」タブを確認します。

## 環境衛生

### 目的ごとに別々のウォレット

- **開発ウォレット**：テストネット SOL を保持、構築に使用。
- **本番ウォレット**：実際の SOL を保持、デプロイ/マルチシグ送信のみに使用。
- **ボット用ホット ウォレット**：小額残高、限定的なアクセス許可。

キーはハードコードされるべきではありません。環境変数またはシークレット マネージャーを使用してください。

### `.env` パターン

```
# .env（コミットされていない）
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=...
WALLET_PATH=/Users/you/.config/solana/id.json
```

```ts
import { config } from "dotenv";
config();

const connection = new Connection(process.env.SOLANA_RPC_URL!);
```

### バージョン ピンニング

すべての Solana エコシステム依存関係をロックします。エコシステムは急速に動きます。`@solana/web3.js` のマイナー バージョン バンプは以前に破壊的な変更を導入しています。`package-lock.json` / `Cargo.lock` を宗教的に使用してください。

## ポインター

- [`sdk-api/typescript-sdk`](/ja/sdk-api/typescript-sdk) — TypeScript SDK リファレンス。
- [`sdk-api/rust-cpi`](/ja/sdk-api/rust-cpi) — Rust CPI 使用法。
- [`sdk-api/python-integration`](/ja/sdk-api/python-integration) — Python セットアップ。
- [`solana-fundamentals/programs-and-anchor`](/ja/solana-fundamentals/programs-and-anchor) — Anchor の背景。

ソース：

- [Solana インストール ドキュメント](https://solana.com/docs/intro/installation)および [Agave CLI インストール](https://docs.anza.xyz/cli/install)。
- [Anchor インストール](https://www.anchor-lang.com/docs/installation)。
- [Raydium SDK v2 リポジトリ](https://github.com/raydium-io/raydium-sdk-V2)。
