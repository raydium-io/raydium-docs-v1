---
title: "CLIからのスワップ"
description: "CPMM スワップの見積もりと実行を行う Node スクリプト。4つの環境変数を設定して実行します。"
---

<Info>
  **このページは AI による自動翻訳です。すべての内容は英語版を正とします。**

  [英語版を表示 →](/quick-start/swap-from-cli)
</Info>

<Info>
  **このスクリプトの動作。** RPC から CPMM プールを読み込み、0.5% のスリッページで見積もりを取得し、トランザクションを構築して、キーペアで署名し、送信します。全体で約 30 行です。
</Info>

## セットアップ

[クイックスタートの前提条件](/ja/quick-start)を確認し、`RPC_URL`、`KEYPAIR`、および依存関係がインストールされていることを確認してください。

## スクリプト

`swap.mjs` として保存します：

```ts swap.mjs icon="js"
import { Connection, Keypair, PublicKey } from "@solana/web3.js";
import { Raydium, TxVersion, CurveCalculator } from "@raydium-io/raydium-sdk-v2";
import BN from "bn.js";
import fs from "node:fs";

// ── Config from env ──────────────────────────────────────────────
const RPC_URL    = process.env.RPC_URL    ?? "https://api.mainnet-beta.solana.com";
const KEYPAIR    = process.env.KEYPAIR    ?? `${process.env.HOME}/.config/solana/id.json`;
const POOL_ID    = process.env.POOL_ID;     // required, base58
const INPUT_MINT = process.env.INPUT_MINT;  // required, base58
const AMOUNT_RAW = process.env.AMOUNT_RAW;  // required, integer in raw units (e.g. 1_000_000_000 for 1 SOL)

if (!POOL_ID || !INPUT_MINT || !AMOUNT_RAW) {
  console.error("Set POOL_ID, INPUT_MINT, AMOUNT_RAW env vars.");
  process.exit(1);
}

// ── Setup ────────────────────────────────────────────────────────
const connection = new Connection(RPC_URL, "confirmed");
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(KEYPAIR, "utf8"))),
);
const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});

// ── Load pool ────────────────────────────────────────────────────
const { poolInfo, poolKeys, rpcData } =
  await raydium.cpmm.getPoolInfoFromRpc(new PublicKey(POOL_ID));

const baseIn = poolInfo.mintA.address === INPUT_MINT;

// ── Quote ────────────────────────────────────────────────────────
const inputAmount = new BN(AMOUNT_RAW);
const swapResult  = CurveCalculator.swap(
  inputAmount,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo.tradeFeeRate,
);

console.log(`Quote: ${inputAmount.toString()} -> ${swapResult.destinationAmountSwapped.toString()}`);
console.log(`Fee:   ${swapResult.tradeFee.toString()}`);

// ── Build and execute ────────────────────────────────────────────
const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount,
  swapResult,
  slippage: 0.005, // 0.5%
  baseIn,
  txVersion: TxVersion.V0,
  computeBudgetConfig: {
    units: 250_000,
    microLamports: 50_000,
  },
});

const { txId } = await execute({ sendAndConfirm: true });
console.log(`Swap landed: https://solscan.io/tx/${txId}`);
```

## 実行

流動性を持つ任意の CPMM プールを選択します。標準的な SOL/USDC CPMM プールの例：

```bash icon="terminal"
export POOL_ID="<CPMM プール ID を貼り付け — Raydium UI の Pools タブで見つけられます>"
export INPUT_MINT="So11111111111111111111111111111111111111112"
export AMOUNT_RAW="10000000"   # 0.01 SOL

node swap.mjs
```

期待される出力：

```text
Quote: 10000000 -> 1640000
Fee:   25000
Swap landed: https://solscan.io/tx/4Z9...
```

## 何が起きたのか

1. **`Raydium.load`** は SDK を初期化し、グローバル設定を取得し、ウォレットコンテキストをセットアップしました。
2. **`getPoolInfoFromRpc`** は RPC から直接ライブプール状態を取得しました（API キャッシュからではなく）。高額なスワップの場合は常に最新の状態が必要です。
3. **`CurveCalculator.swap`** はプールの手数料を差し引いた定積商品出力を計算しました。これはプログラムがオンチェーンで実行する数学と同じなので、オフチェーンとオンチェーンの見積もりを比較できます。
4. **`raydium.cpmm.swap`** は V0 形式（アドレスルックアップテーブル有効）でトランザクションを構築し、明示的なコンピュート予算設定を追加しました。コンピュート予算チップは、ビジーな時間帯でトランザクションが確認されるのに役立ちます。
5. **`execute({ sendAndConfirm: true })`** は署名、送信、確認を待ちました。

## よくあるエラー

- **`Pool not found`** — `POOL_ID` が間違っているか、クラスタが間違っています（mainnet プール ID を devnet RPC に対して使用するなど）。
- **`Insufficient funds for transaction`** — ウォレットにスワップ入力 + 手数料 + ATA レント用の SOL が不足しています。
- **`Slippage tolerance exceeded`** — 見積もりと実行の間にプールの価格が変動しました。再実行するか、`slippage` パラメータを上げるか、SDK の `computeAmountOut` を使用してください（常にリザーブを再取得します）。
- **`Token account not initialized`** — 出力トークンの ATA が存在せず、暗黙的な作成命令は着地しましたが何らかの理由で失敗しました。ウォレットの SOL 残高を確認して再試行してください。

## 次のステップ

- [`sdk-api/typescript-sdk`](/ja/sdk-api/typescript-sdk) — 完全な SDK リファレンス。
- [`products/cpmm/instructions`](/ja/products/cpmm/instructions) — オンチェーンのスワップ命令の詳細。
- [`integration-guides/priority-fee-tuning`](/ja/integration-guides/priority-fee-tuning) — 本番環境用の `computeBudgetConfig` のサイジング。
