---
title: "رسوم Stable AMM"
description: "مستوى رسوم منخفض واحد: رسم تداول إجمالي 0.02%، 88% LP / 12% بروتوكول. نفس هيكل Fees الخاص بـ AMM v4، بأرقام مختلفة. بدون رسم صندوق، بدون رسم منشئ."
---

<Info>
  **هذه الصفحة مُترجَمة آليًا بواسطة الذكاء الاصطناعي. النسخة الإنجليزية هي المرجع المعتمد.**

  [عرض النسخة الإنجليزية →](/products/stable/fees)
</Info>

## نموذج الرسوم

يُعيد Stable AMM استخدام **هيكل `Fees`** الخاص بـ AMM v4، لكن ليس **أرقامه**. فمجمعات Stable تفرض رسمًا أقل بكثير، وهو ما تتوقعه من مجمع مصمَّم لأصول متقاربة السعر. ويوجد فئة رسوم واحدة فقط لكل مجموعة (يتم تعيينها عند الإنشاء)؛ لا يمكن إعادة تكوين المجموعات إلى فئات أعلى.

| الحقل | القيمة على السلسلة | المعنى |
|-------|----------------|---------|
| `swap_fee_numerator / swap_fee_denominator` | `2 / 10_000` | **إجمالي رسم التداول: 0.02%** من حجم الإدخال. وهذا هو الرسم الوحيد المفروض فعليًا. |
| `trade_fee_numerator / trade_fee_denominator` | `0 / 10_000` | **صفر.** حقل قديم كان يُستخدم سابقًا لتسعير أوامر OpenBook؛ محفوظ لتوافق التخطيط، ولم يعد يُقرأ. |
| `pnl_numerator / pnl_denominator` | `12 / 100` | **حصة البروتوكول من الرسم: 12%** — أي `0.02% × 12% = 0.0024%` من الحجم. |
| `min_separate_numerator / min_separate_denominator` | `4 / 10_000` | حد الفصل الأدنى المستخدم عند فصل PnL من الرسم الإجمالي. |

<Note>
  هذه ليست "قيمًا افتراضية" — بل هي القيم الموجودة على **كل** مجمع Stable حيّ على mainnet. فالثلاثة كلها (USDT/USDC `2EXiumdi14E9b8Fy62QcA5Uh6WdHS2b38wtSxp72Mibj` و `9DTY3rv8xRa3CnoPoWJCMcQUSY7kUHZAoFKNsBhx8DDz` و `9f4FtV6ikxUZr8fAjKSGNPPnUHJEwi4jNk8d79twbyFf`) تحمل المجموعة المتطابقة أعلاه، وبعد إزالة `SetParams` في ترقية 2026-06-22 لم تبقَ أي تعليمة يمكنها تغييرها. تم التحقق بفك تشفير `AmmInfo.fees` من mainnet.
</Note>

لا توجد **رسم صندوق** ولا **رسم منشئ** — هذه اختراعات لاحقة لـ AMM v4 (CPMM/CLMM). يسبق Stable AMM هذه الاتفاقية.

## كيفية حساب التقسيم

في كل عملية مبادلة:

```
gross_fee = ceil(amount_in * (swap_fee_numerator / swap_fee_denominator))    // 0.02% من amount_in
pnl_portion = gross_fee * (pnl_numerator / pnl_denominator)                  // 12% من gross_fee
lp_portion = gross_fee − pnl_portion                                          // 88% من gross_fee
```

- **`lp_portion`** تبقى في الخزينة، تزيد `k`، وتفيد LPs عند الاسترجاع.
- **`pnl_portion`** تزيد `AmmInfo.out_put.need_take_pnl_coin` أو `need_take_pnl_pc` (حسب رمز الإدخال) ويتم مسحها بواسطة `WithdrawPnl`.

نفس **خدعة الحفاظ على الثابت** مثل CPMM: مبلغ PnL يجلس في الخزينة فعليًا لكن يتم طرحه من "الاحتياطيات الفعلية" المستخدمة في رياضيات المنحنى، لذا إزالته لا تغير السعر.

## OpenBook PnL (متقاعد)

في بداية حياتها، كانت المجموعة تنشر أيضًا أوامر محدودة على OpenBook؛ عند ملء تلك الأوامر، كانت تكسب أو تخسر فارق صانع السوق/المتلقي، يتم تسويته أثناء `MonitorStep`، ويتم تتبع النتيجة في عدادات `out_put.total_pnl_{coin,pc}`. **هذا المسار كان خامدًا لسنوات** — المجموعة لا تحتفظ بأوامر مفتوحة، لذا فإن الرسم الوحيد المتراكم هو حصة البروتوكول 0.0024% الموصوفة أعلاه. تبقى عدادات `total_pnl_*` في التخطيط لكن لم تعد تزداد. أزال [ترقية 2026-06-22](/ar/reference/changelog) الكود المتبقي من OpenBook (بما في ذلك `MonitorStep`). أصبح CPMM الافتراضي للمجموعات الجديدة جزئيًا بسبب هذا الاقتران السابق مع OpenBook؛ Stable AMM مستقل عن دفتر الأوامر.

## التجميع

يستدعي Raydium multisig (أو من يتحكم في `amm_admin`) `WithdrawPnl` لمسح:

1. نقل `need_take_pnl_coin` و `need_take_pnl_pc` من الخزائن إلى الحسابات المعينة من قبل المسؤول.
2. مسح العدادات.

العملية لا تحرك المنحنى. لا يرى LPs أي تغيير في السعر.

<Warning>
  تم تغيير `WithdrawPnl` في ترقية 2026-06-22: يستخدم الآن **تخطيط ثابت بـ 10 حسابات، حصري للمسؤول بدون مسار توافق** (التخطيطات القديمة تفشل مع أخطاء مثل `InvalidTokenCoin`)، وعندما تكون الأموال المتاحة في المجموعة غير كافية لتغطية PnL فإنها ترجع `TakePnlError` مباشرة بدلاً من تعطيل المجموعة. انظر [`products/stable/instructions`](/ar/products/stable/instructions#withdrawpnl).
</Warning>

## استرجاع رسوم LP

لا توجد تعليمات مخصصة "لجمع الرسوم". تتراكم رسوم LP في الخزائن، مما يزيد الاحتياطيات. يحقق LPs قيمتها بحرق LP عبر `Withdraw`. تنمو قيمة رمز LP مع نمو الاحتياطيات.

## التصور: حيث تذهب \$10,000 من الحجم

على مبادلة `Swap` بقيمة \$10,000 موجهة نحو USDC مقابل مجمع Stable حيّ:

```
إجمالي رسم التداول (0.02%):     $2.0000
  حصة LP   (0.0176%):         $1.7600  → تبقى في المجموعة، ترفع k
  حصة PnL  (0.0024%):         $0.2400  → need_take_pnl_pc، يتم مسحها بواسطة WithdrawPnl
يتلقى المستخدم (ناقص المنحنى):  $9,998.00
```

ونفس المبلغ \$10,000 عبر مجمع AMM v4 يدفع \$25.00، وعبر مجمع CPMM من الفئة `index=0` يدفع \$25.00 — **أكثر بـ 12.5×**. وهذه الفجوة هي جوهر المنتج: فالأزواج المستقرة تتحرك قليلاً جدًا، لذا يحتاج مزودو السيولة إلى تعويض ضئيل جدًا عن مخاطر المخزون، والمنحنى المحكم مع الرسم المنخفض هو ما يحفظ تنافسية المبادلات الكبيرة بين المستقرات.

## جدول المقارنة

| | Stable AMM | AMM v4 | CPMM `index=0` |
|---|---|---|---|
| رسم التداول | **0.02%** | 0.25% | 0.25% |
| LP | 0.0176% | 0.22% | 0.21% |
| البروتوكول | 0.0024% | 0.03% | 0.03% |
| الصندوق | بدون | بدون | 0.01% |
| المنشئ | بدون | بدون | 0 افتراضيًا |
| قابل للتكوين | لا | لا | نعم (لكل `AmmConfig`) |

المصفوفة الكاملة: [`reference/fee-comparison`](/ar/reference/fee-comparison).

## ملاحظات المدمج

- **الاقتباس:** اقرأ دائمًا `AmmInfo` من السلسلة؛ لا تقم بترميز الرسوم. مع إزالة `SetParams` الآن، معاملات الرسوم على المجموعات الموجودة فعليًا ثابتة.
- **لا تفترض معدل AMM v4.** هيكل `Fees` مشترك مع AMM v4، لذا يسهل نسخ `25 / 10_000` من AMM v4 إلى اقتباس Stable. أما مجمعات Stable فتفرض `2 / 10_000`. واقتباس مجمع Stable بنسبة 0.25% يبالغ في الرسم بمقدار 12.5×.
- **المنحنى مقابل الرسوم:** تطبيق الرسم مستقل عما إذا كان المنحنى صيغة (x·y=k في AMM v4) أو جدول بحث (Stable). كلاهما يخصم الرسم من مبلغ الإدخال قبل لمس المنحنى.
- **بدون مكافآت:** لا تدعم مجموعات Stable انبعاثات المكافآت على المجموعة. تتعامل مزارع النظام البيئي (Farm v3/v5/v6) مع الرهن في مكان آخر.

## الخطوات التالية

- [`products/stable/math`](/ar/products/stable/math) — تطبيق الرسم في رياضيات المبادلة.
- [`products/stable/instructions`](/ar/products/stable/instructions) — قائمة حسابات `WithdrawPnl`.
- [`products/amm-v4/fees`](/ar/products/amm-v4/fees) — اشتقاق رسوم أعمق لمسار OpenBook.
- [`reference/fee-comparison`](/ar/reference/fee-comparison) — جنبًا إلى جنب جميع المنتجات.

المصادر:

- `raydium-stable/program/src/state.rs` (هيكل `Fees`)
- حقول `AmmInfo.fees` على السلسلة على مجموعات mainnet المباشرة
