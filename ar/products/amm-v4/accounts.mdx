---
title: "حسابات AMM v4"
description: "الجرد الكامل لحسابات مجمع AMM v4 — AmmInfo و TargetOrders و OpenOrders والخزائن وعملة LP والسلطة — بالإضافة إلى حسابات OpenBook المرتبطة بالمجمع."
---

<Info>
  **هذه الصفحة مُترجَمة آليًا بواسطة الذكاء الاصطناعي. النسخة الإنجليزية هي المرجع المعتمد.**

  [عرض النسخة الإنجليزية →](/products/amm-v4/accounts)
</Info>

<Info>
  اعتبارًا من ترقية 2026-07، تم **إزالة** اعتماد AMM v4 على OpenBook / Serum — لا توجد تعليمات تقرأ أو تكتب حالة OpenBook بعد الآن. يتم الاحتفاظ بحسابات السوق أدناه فقط كعناصر نائبة موضعية على تخطيطات التعليمات v1 القديمة (مقبولة لكن مُتجاهلة) وكحقول مرجعية على `AmmInfo`. استخدم نقاط نهاية المبادلة V2، التي تحذفها تمامًا. تجمع هذه الصفحة الحسابات في أقسام "مملوكة للمجمع" و"OpenBook (قديمة)" لقراءة المعاملات الأقدم.
</Info>

## الجرد

يسجل مجمع AMM v4 سوقًا واحدًا مرتبطًا على `AmmInfo` عند الإنشاء. الصورة الكاملة:

| الفئة | الحساب | المالك | الدور |
|----------|---------|-------|------|
| المجمع | `AmmInfo` | برنامج AMM v4 | حالة المجمع: الرسوم المتراكمة والحالة والمراجع إلى الخزائن (وكحقول قديمة، السوق). |
| المجمع | `amm_authority` | برنامج AMM v4 | PDA مملوك للبرنامج يوقّع حركات الخزائن. مشترك بين جميع مجمعات AMM v4. |
| المجمع | `amm_open_orders` | OpenBook | مرجع `OpenOrders` قديم. لا يُنشئه ولا يقرأه البرنامج الحالي، الذي يكتب `AmmInfo.open_orders = Pubkey::default()` في كل مجمع جديد؛ المجمعات المنشأة قبل إزالة OpenBook هي وحدها التي تحمل قيمة حقيقية. لا يزال يُمرر موضعيًا على تخطيطات v1. |
| المجمع | `amm_target_orders` | برنامج AMM v4 | حساب الطلبات المستهدفة القديم. لا يزال مرجعيًا بواسطة `WithdrawPnl` (كحساب #10) لكن لا يتم بناء شبكة. |
| المجمع | `pool_coin_token_account` | SPL Token | خزينة جانب العملة للمجمع (ATA من `amm_authority`). |
| المجمع | `pool_pc_token_account` | SPL Token | خزينة جانب pc للمجمع. |
| المجمع | `lp_mint` | SPL Token | عملة LP قابلة للاستبدال. |
| قديم (OpenBook) | `serum_market` | OpenBook | السوق المرتبط. يُمرر على تخطيطات v1 لكن لم يعد يُتحقق منه أو يُستخدم. |
| قديم | `serum_bids`, `serum_asks` | OpenBook | قوائم الطلب والعرض. مُتجاهلة. |
| قديم | `serum_event_queue` | OpenBook | الأحداث المعلقة. مُتجاهلة. |
| قديم | `serum_coin_vault`, `serum_pc_vault` | SPL Token | خزائن مستوى السوق. مُتجاهلة. |
| قديم | `serum_vault_signer` | OpenBook | PDA مستوى السوق. مُتجاهل. |

ملاحظة: يتم الاحتفاظ بـ "serum" كبادئة في IDL وأسماء الحقول الخاصة بـ AMM v4 للتوافق العكسي. لم تعد هذه الحسابات وظيفية بعد إزالة OpenBook.

## `AmmInfo`

حساب حالة جذر المجمع. كبير (≈ 752 بايت) لأنه يحمل مراجع المجمع و OpenBook مضمنة.

```rust
// programs/amm/src/state.rs (مختصر؛ ترتيب الحقول والأسماء تتبع IDL)
pub struct AmmInfo {
    pub status: u64,           // bitmask: swap/deposit/withdraw/crank enabled
    pub nonce:  u64,           // bump used to derive amm_authority
    pub order_num: u64,
    pub depth: u64,
    pub coin_decimals: u64,
    pub pc_decimals:   u64,
    pub state:         u64,    // internal state machine
    pub reset_flag:    u64,
    pub min_size:      u64,
    pub vol_max_cut_ratio: u64,
    pub amount_wave: u64,
    pub coin_lot_size: u64,    // legacy (OpenBook lot size); now initialized to 0
    pub pc_lot_size:   u64,    // legacy; now initialized to 0
    pub min_price_multiplier: u64,
    pub max_price_multiplier: u64,
    pub sys_decimal_value: u64,

    pub fees: Fees,            // trade/protocol/fund fee rates
    pub state_data: StateData,

    // Pool-owned accounts:
    pub coin_vault: Pubkey,
    pub pc_vault:   Pubkey,
    pub coin_vault_mint: Pubkey,
    pub pc_vault_mint:   Pubkey,
    pub lp_mint:  Pubkey,
    pub open_orders: Pubkey,   // pool's OpenOrders on OpenBook
    pub market: Pubkey,        // OpenBook market
    pub market_program: Pubkey, // OpenBook program ID
    pub target_orders: Pubkey,
    pub padding1: [u64; 8],     // reserved — NOT withdraw_queue / lp_vault
    pub amm_owner: Pubkey,      // admin (multisig)
    pub lp_amount: u64,         // the pool's internal LP total; see the note below
    pub client_order_id: u64,
    pub recent_epoch: u64,
    pub padding2: u64,
}

pub struct Fees {
    pub min_separate_numerator:   u64,     // 5
    pub min_separate_denominator: u64,     // 10_000
    pub trade_fee_numerator:      u64,     // 25  → used by OpenBook integration
    pub trade_fee_denominator:    u64,     // 10_000
    pub pnl_numerator:            u64,     // 12  → protocol's share OF the swap fee
    pub pnl_denominator:          u64,     // 100 → so 12/100 = 12% of fee, = 0.03% of volume
    pub swap_fee_numerator:       u64,     // 25  → 0.25% gross swap fee
    pub swap_fee_denominator:     u64,     // 10_000
}

pub struct StateData {
    pub need_take_pnl_coin: u64,        // still updated — accrued protocol fee (coin side)
    pub need_take_pnl_pc:   u64,        // still updated — accrued protocol fee (pc side)
    pub total_pnl_pc:   u64,            // DEPRECATED — no longer updated
    pub total_pnl_coin: u64,            // DEPRECATED — no longer updated
    pub pool_open_time: u64,            // still used — pool open timestamp
    pub padding: [u64; 2],              // reserved
    pub orderbook_to_init_time: u64,    // DEPRECATED — no longer updated
    pub swap_coin_in_amount: u128,      // DEPRECATED — volume counters frozen
    pub swap_pc_out_amount:  u128,      // DEPRECATED
    pub swap_acc_pc_fee:    u64,        // DEPRECATED
    pub swap_pc_in_amount:  u128,       // DEPRECATED
    pub swap_coin_out_amount: u128,     // DEPRECATED
    pub swap_acc_coin_fee:  u64,        // DEPRECATED
}
```

<Note>
  تخطيط البنية **لم يتغير** (متوافق بايت) لذا تبقى المُفكّكات الموجودة تعمل، لكن بعد إزالة OpenBook الحقول المُشار إليها بـ `DEPRECATED` أعلاه **لم تعد تُكتب** — عدادات الحجم `swap_*` مجمدة عند آخر قيمة لها. لتحليلات الحجم، استخدم سجلات التداول بدلاً من هذه الحقول. فقط `need_take_pnl_*` و `pool_open_time` يتم صيانتهما بنشاط.
</Note>

الحقول الموجهة للمدمجين:

- **`coin_vault`**, **`pc_vault`** — خزائن SPL Token للمجمع. `coin` هو `token_0` حسب اتفاقية Serum/OpenBook (الأساس)، `pc` هو `token_1` (الاقتباس).
- **`coin_decimals`**, **`pc_decimals`** — مطابقة للعملات.
- **`open_orders`**, **`target_orders`**, **`market`** — حقول مرجعية قديمة. لا تزال موجودة على `AmmInfo` وتُمرر موضعيًا على تخطيطات التعليمات v1، لكن البرنامج لم يعد يقرأها أو يتحقق منها. نقاط نهاية المبادلة V2 تحذفها.
- **`fees.swap_fee_numerator / swap_fee_denominator`** — رسم التداول المدمج. الافتراضي `25 / 10_000 = 0.25%`.
- **`status`** — حالة `u64` مُعدَّدة واحدة تحدّ العمليات، وهي **ليست** bitmask. قابلة للتعيين من قبل المسؤول عبر `SetParams` مع `param = 0` (`Status`). انظر [الحالة](#status) أدناه.
- **`lp_amount`** — إجمالي LP الداخلي للمجمع. وهو **لا** يساوي `lp_mint.supply`: إنه أعلى بمقدار رمز LP كامل واحد بالضبط (`10^coin_decimals` وحدة أساسية)، لأن ذلك المقدار يُحسب عند التهيئة لكنه لا يُصدر أبدًا. كل الحسابات التناسبية تستخدم `lp_amount`، فاستخدمه أنت أيضًا.
- **`amm_owner`** — يُكتب عند الإنشاء من مفتاح المسؤول المضمّن في البرنامج، وليس من المنشئ.
- **`state_data.need_take_pnl_*`** — الفرق بين الرسوم المتراكمة الإجمالية وما تم مسحه. `TakePnl` يصفر هذه.

## توصيل OpenBook

<Note>
  **تم الحذف.** تم حذف اعتماد OpenBook / Serum من البرنامج (ترقية 2026-07). الحسابات الموصوفة في هذا القسم لم تعد يتم التحقق منها أو استخدامها. تبقى كحقول مرجعية على `AmmInfo` وكعناصر نائبة موضعية على تخطيطات التعليمات v1 القديمة. استخدم **نقاط نهاية المبادلة V2** ([`SwapBaseInV2` / `SwapBaseOutV2`](/ar/products/amm-v4/instructions)) التي تتخطى هذه الحسابات تمامًا.
</Note>

عند استدعاء تعليمة **v1** قديمة `SwapBaseIn` / `SwapBaseOut` أو `Deposit` أو `Withdraw`، يجب أن يطابق **عدد** الحسابات التخطيط القديم (حسابات السوق تحتل مواضعها التاريخية)، لكن **محتوياتها لم تعد يتم فحصها** — لا يتم إصدار CPI ضدها. يجب أن يستخدم الكود الجديد متغيرات المبادلة V2، التي لا تأخذ هذه الحسابات على الإطلاق.

```ts
const market = ...;  // OpenBook market PublicKey

// Fields OpenBook exposes on its market account:
const marketDecoded = OpenBookMarket.decode(marketAccountData);
const {
  bids:           serumBids,
  asks:           serumAsks,
  eventQueue:     serumEventQueue,
  requestQueue:   serumRequestQueue,
  baseVault:      serumCoinVault,
  quoteVault:     serumPcVault,
  vaultSignerNonce,
} = marketDecoded;

const serumVaultSigner = PublicKey.createProgramAddressSync(
  [market.toBuffer(), u64ToBytes(vaultSignerNonce)],
  OPENBOOK_PROGRAM_ID,
);
```

`amm_open_orders` للمجمع هو حساب مملوك لـ OpenBook يحتفظ بحالة الطلب المحدود للمجمع على هذا السوق: الطلبات النشطة والأرصدة المستقرة والمحيلون وما إلى ذلك. `amm_target_orders` من جانب AMM: يحتفظ بالشبكة *المقصودة* للمجمع (السعر/الحجم لكل فتحة طلب) حتى يتمكن البرنامج من المقارنة بسهولة مع ما يتم نشره حاليًا ووضع / إلغاء الفرق.

## PDAs السلطة

يوجد بالضبط PDA `amm_authority` واحد لبرنامج AMM v4 بأكمله. بذره تافهة (`["amm authority"]`) وتُخزن نتيجته على كل `AmmInfo`. توقّع هذه السلطة جميع حركات الرموز لجميع مجمعات AMM v4.

```ts
const AMM_V4_PROGRAM_ID = new PublicKey(
  "675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8",
);
const [ammAuthority] = PublicKey.findProgramAddressSync(
  [Buffer.from("amm authority")],
  AMM_V4_PROGRAM_ID,
);
```

لا توجد سلطة ثانية محدودة النطاق بالمجمع: هذا الـ PDA الواحد يغطي كل ما يوقّعه البرنامج. قيمة bump الخاصة به هي `254` على الشبكة الرئيسية وهي مُنسَخة على كل `AmmInfo` كـ `nonce`؛ وتعيد `WithdrawExcessLamports` اشتقاقها مع تضمين ذلك الـ nonce بشكل ثابت.

## الخزائن

خزائن SPL Token للمجمع هي حسابات رموز قياسية يكون `owner` لها `amm_authority`. ليست ATAs — عناوينها PDAs مشتقة عند `Initialize2` من `[AMM_V4_PROGRAM_ID, market, "coin_vault_associated_seed"]` و `[AMM_V4_PROGRAM_ID, market, "pc_vault_associated_seed"]`. العملة ليست بذرة، ولا `amm_id` كذلك. يتم تخزين العناوين على `AmmInfo`؛ الاشتقاق فضول لمرة واحدة.

Token-2022 **غير مدعوم**. يحمّل البرنامج معرّف برنامج SPL Token لجميع حركات الخزائن. محاولة ربط مجمع AMM v4 بعملة Token-2022 تفشل عند `Initialize2` بـ `InvalidSplTokenProgram`.

## عملة LP

عملة SPL Token كلاسيكية سلطتها `amm_authority`. إجمالي العرض يتتبع ملكية LP للمجمع؛ حرق LP يعيد الرموز من كلا الخزائن بنسبة.

**توجد** مرآة داخل الحالة: `AmmInfo.lp_amount`. وهي لا تساوي عرض العملة — إنها أعلى بمقدار رمز LP كامل واحد بالضبط (`10^coin_decimals` وحدة أساسية)، لأن ذلك المقدار يُحسب عند التهيئة لكنه لا يُصدر أبدًا. كل حساب تناسبي في البرنامج يقسم على `lp_amount`، فاستخدم ذلك الحقل بدلاً من عرض العملة على السلسلة.

## الحالة

`AmmInfo.status` هي حالة `u64` مُعدَّدة واحدة، وهي **ليست** bitmask. اختبرها بالتساوي — العميل الذي يختبر `status & 1` يصنّف كل مجمع نشط تصنيفًا خاطئًا، لأن حالة التداول العادية هي `6`، والتي بها البت 0 مضبوط.

| القيمة | `AmmStatus` | المبادلة | الإيداع | السحب |
|-------|-------------|------|---------|----------|
| 0 | `Uninitialized` | لا | لا | لا |
| 1 | `Initialized` | نعم | نعم | نعم |
| 2 | `Disabled` | لا | لا | لا |
| 3 | `WithdrawOnly` | لا | لا | نعم |
| 4 | `LiquidityOnly` | لا | نعم | نعم |
| 5 | `OrderBookOnly` | لا | نعم | نعم |
| 6 | `SwapOnly` | نعم | نعم | نعم |
| 7 | `WaitingTrade` | نعم، بعد أن يمضي `pool_open_time` | نعم | نعم |

تكتب `Initialize2` القيمة `7` عندما يكون `open_time` في المستقبل و `6` غير ذلك؛ والمجمع الموجود عند `7` يقلب نفسه إلى `6` عند أول مبادلة عند أو بعد `state_data.pool_open_time`. أي قيمة خارج `0..=7` تجعل `AmmStatus::from_u64` يتعطل.

تعيّن مجموعة Raydium متعددة التوقيع الحالة عبر `SetParams` مع `param = 0` (`Status`)؛ ولا تُقبل إلا القيم `1`–`7`. (تم إزالة `AdminCancelOrders`.)

## الملاحظة / الأوراكل

**لا يمتلك AMM v4 حساب مراقبة مخصصًا**، ومنذ إزالة OpenBook لم تبقَ حالة دفتر أوامر يمكن اشتقاق واحد منها أيضًا. إذا احتجت إلى TWAP من Raydium بدعم من البرنامج، فاستخدم CPMM أو CLMM — كلاهما يحتفظ بحاوية حلقية `ObservationState`. غير ذلك، فهرِس سجلات المبادلة خارج السلسلة.

## اشتقاق حسابات المجمع من الصفر

حسابات مجمع AMM v4 هي PDAs عادية مفتاحها السوق المرتبط — ليست مفاتيح مزروعة، وليست PDAs لكل زوج. كل واحد منها يستخدم نفس شكل البذور الثلاثية `[AMM_V4_PROGRAM_ID, market, <label>]` تحت `AMM_V4_PROGRAM_ID`:

| Account | `<label>` |
|---------|-----------|
| `amm_id` (`AmmInfo`) | `"amm_associated_seed"` |
| `amm_target_orders` | `"target_associated_seed"` |
| `lp_mint` | `"lp_mint_associated_seed"` |
| `pool_coin_token_account` | `"coin_vault_associated_seed"` |
| `pool_pc_token_account` | `"pc_vault_associated_seed"` |

```ts
const [ammId] = PublicKey.findProgramAddressSync(
  [
    AMM_V4_PROGRAM_ID.toBuffer(),
    marketPubkey.toBuffer(),
    Buffer.from("amm_associated_seed"),
  ],
  AMM_V4_PROGRAM_ID,
);
```

السوق المرتبط هو البذرة المتغيرة الوحيدة، ولهذا يقابل كل سوق مجمع AMM v4 واحدًا بالضبط. SDK والـ API يحسبان هذه مسبقًا لك؛ انظر `raydium-sdk-v2` الخاص بـ `Liquidity.getAssociatedPoolKeys`.

في الممارسة العملية، يقرأ المدمجون مجموعة الحسابات الكاملة للمجمع من `GET https://api-v3.raydium.io/pools/info/ids?ids=<POOL_ID>` أو من SDK. الاشتقاق اليدوي نادرًا ما يكون مطلوبًا.

## مرجع دورة الحياة السريع

| الحدث | الحسابات المُنشأة | الحسابات المحذوفة |
|-------|------------------|---------------------|
| `Initialize2` | `amm_info`, `amm_target_orders`, كلا الخزينتين, `lp_mint`, و ATA LP للمنشئ | — |
| `Deposit` | — (قد ينشئ ATA LP للمستخدم) | — |
| `Withdraw` | — | — |
| `SwapBaseIn` / `SwapBaseOut` (v1 / V2) | — (قد ينشئ ATA للمستخدم) | — |
| `WithdrawPnl` | — | — |

تستمر المجمعات وحساباتها إلى الأبد. حتى لو تم سحب السيولة بالكامل، `AmmInfo` يبقى.

## ما يجب قراءته أين

- **الرياضيات وحسابات الرسوم**: [`products/amm-v4/math`](/ar/products/amm-v4/math).
- **تقسيم الرسوم وكيفية مقارنتها بـ CPMM/CLMM**: [`products/amm-v4/fees`](/ar/products/amm-v4/fees).
- **قوائم حسابات التعليمات**: [`products/amm-v4/instructions`](/ar/products/amm-v4/instructions).
- **اشتقاق حساب OpenBook**: مصدر برنامج OpenBook ([`github.com/openbook-dex/program`](https://github.com/openbook-dex/program)) — مستودع Rust أحادي، وليس وثائق نصية.

المصادر:

- [برنامج Raydium AMM — `raydium-io/raydium-amm`](https://github.com/raydium-io/raydium-amm)
- [`reference/program-addresses`](/ar/reference/program-addresses) لمعرفات البرامج الكنسية
- بروتوكول OpenBook / Serum لحسابات الطرف الآخر
