---
title: "حسابات LaunchLab"
description: "PoolState (جذر الإطلاق)، والخزائن الأساسية والاقتباسية، وسلطة PDA، والرسم البياني الصغير لحسابات الإطلاق. تعيش GlobalConfig على مستوى البروتوكول و PlatformConfig على مستوى المنصة في صفحات منفصلة."
---

<Info>
  **هذه الصفحة مُترجَمة آليًا بواسطة الذكاء الاصطناعي. النسخة الإنجليزية هي المرجع المعتمد.**

  [عرض النسخة الإنجليزية →](/products/launchlab/accounts)
</Info>

تُوثّق هذه الصفحة رسم البياني للحسابات **لكل إطلاق**: `PoolState` (حساب الحالة الجذري لإطلاق واحد)، وخزائنه الاثنتان، وسلطة PDA، والمراجع اللاحقة للتخرج التي يكتسبها عند استقرار الإطلاق.

للحصول على الإعدادات على مستوى البروتوكول التي تحد من كل إطلاق، انظر [`products/launchlab/global-config`](/ar/products/launchlab/global-config). للحصول على الطبقة الإضافية لكل منصة، انظر [`products/launchlab/platform-config`](/ar/products/launchlab/platform-config). لحسابات الاستحقاق (`VestingSchedule` على `PoolState`، `VestingRecord` لكل مستفيد)، انظر [`products/launchlab/vesting`](/ar/products/launchlab/vesting).

## جرد الحسابات

| الحساب | المالك | الغرض |
|---------|-------|---------|
| [`GlobalConfig`](/ar/products/launchlab/global-config) | برنامج LaunchLab | قواعد على مستوى البروتوكول: الرسوم، حدود العرض، محافظ الهجرة. واحد لكل `(curve_type, index)`. |
| [`PlatformConfig`](/ar/products/launchlab/platform-config) | برنامج LaunchLab | طبقة إضافية لكل منصة: العلامات التجارية، رسم المنصة، تقسيم NFT عند التخرج، قائمة أشكال المنحنيات البيضاء. |
| `PoolState` | برنامج LaunchLab | حالة جذر الإطلاق: الإصدارات، الخزائن، معاملات المنحنى، عدادات المبيعات، عدادات الرسوم، جدول الاستحقاق، حالة التخرج. |
| `authority` | برنامج LaunchLab | PDA واحد عند البذرة `[b"vault_auth_seed"]` يمتلك الخزائن عبر جميع الإطلاقات ويوقع CPI بعد التخرج. |
| `base_vault` | SPL Token / Token-2022 | خزانة لكل إطلاق تحتفظ بالرموز الأساسية غير المباعة. |
| `quote_vault` | SPL Token / Token-2022 | خزانة لكل إطلاق تحتفظ برموز الاقتباس المتراكمة. تتبع أي برنامج يمتلك رمز الاقتباس. |
| [`VestingRecord`](/ar/products/launchlab/vesting) | برنامج LaunchLab | سجل الجرف + فتح خطي لكل مستفيد. اختياري. |
| `creator_fee_vault` | SPL Token / Token-2022 | خزانة لكل منشئ + لكل رمز اقتباس تحتفظ برسوم المنشئ المتراكمة، يتم مسحها بواسطة `ClaimCreatorFee`. تتبع برنامج رمز الاقتباس. |
| `platform_fee_vault` | SPL Token / Token-2022 | خزانة لكل منصة + لكل رمز اقتباس تحتفظ برسوم المنصة المتراكمة، يتم مسحها بواسطة `ClaimPlatformFeeFromVault`. تتبع برنامج رمز الاقتباس. |
| [`PlatformAllowConfig`](/ar/products/launchlab/platform-config) | برنامج LaunchLab | إذن اختياري يُدار من قبل المنصة لزوج واحد `(PlatformConfig, GlobalConfig)`. يتم التحقق منه فقط عندما تفعّل المنصة التقييد. |
| (بعد التخرج) `cpmm_pool_state` | برنامج CPMM | مجموعة تم إنشاؤها بواسطة `MigrateToCpswap` لكل إطلاق جديد. يمكن للحالة القديمة أن تشير إلى تخرج AMM v4 القديم. |
| (بعد التخرج) Fee Key NFT | برنامج LP-Lock | قبل ترقية 2026-08-17، يمكن لمقاييس المنصة والمنشئ أن تنتج مفاتيح رسوم منفصلة. الهجرات المنفذة بعد ذلك تجمع كلا المقياسين في مفتاح رسوم منصة واحد. |

يعيد `raydium.launchpad.getLaunchById` من SDK `PoolState` بالإضافة إلى علم يشير إلى ما إذا كان الإطلاق قد تخرج؛ إذا كان الأمر كذلك، يتم تضمين معرف المجموعة بعد الهجرة.

## `PoolState`

حالة جذر الإطلاق. أسماء الحقول أدناه تطابق بنية Rust على السلسلة (`states/pool.rs`)؛ تم تبسيط بعض القيم لسهولة القراءة — استشر المصدر للحصول على تخطيط الذاكرة الدقيق.

```rust
// states/pool.rs (abridged)
pub struct PoolState {
    pub epoch:                     u64,        // recent_epoch tracker
    pub auth_bump:                 u8,
    pub status:                    u8,         // PoolStatus enum
    pub base_decimals:             u8,
    pub quote_decimals:            u8,
    pub migrate_type:              u8,         // 0 = AMM v4, 1 = CPMM
    pub supply:                    u64,        // total base tokens (no decimals)
    pub total_base_sell:           u64,        // base supply allocated to the curve
    pub virtual_base:              u64,        // virtual-reserve seed
    pub virtual_quote:             u64,        // virtual-reserve seed
    pub real_base:                 u64,        // base sold so far
    pub real_quote:                u64,        // quote raised so far (excl. fees)
    pub total_quote_fund_raising:  u64,        // graduation threshold in quote

    pub quote_protocol_fee:        u64,        // accrued, pending CollectFee
    pub platform_fee:              u64,        // accrued, pending ClaimPlatformFee
    pub migrate_fee:               u64,        // accrued, pending CollectMigrateFee
    pub vesting_schedule:          VestingSchedule,

    pub global_config:             Pubkey,
    pub platform_config:           Pubkey,
    pub creator:                   Pubkey,
    pub base_mint:                 Pubkey,
    pub quote_mint:                Pubkey,
    pub base_vault:                Pubkey,
    pub quote_vault:               Pubkey,

    pub amm_creator_fee_on:        u8,         // 0 = QuoteToken, 1 = BothToken (CPMM-only)
    pub token_program_flag:        u8,         // bit0 = base mint, bit1 = quote mint; 0 = SPL Token, 1 = Token-2022
    pub padding:                   [u8; 54],
}

#[derive(Default, Debug, Clone)]
pub struct VestingSchedule {
    pub total_locked_amount:    u64,
    pub cliff_period:           u64,
    pub unlock_period:          u64,
    pub start_time:             u64,           // set at graduation
    pub allocated_share_amount: u64,           // running sum across VestingRecords
}
```

قيم `PoolStatus` (من Anchor IDL):

```
0 = Funding         — accepting buys / sells; below quote_reserve_target
1 = Migrate         — quote_reserve_target reached; awaiting MigrateTo*
2 = Migrated        — MigrateTo* already executed; vesting clock running
```

الحقول الموجهة للمدمج:

- **`status`** — ثلاث قيم، رتيبة (Funding → Migrate → Migrated). القراءات آمنة دائماً؛ الكتابات مبوابة.
- **`real_base`**, **`real_quote`** — حالة المنحنى الحالية. مع `virtual_base` / `virtual_quote` فهي كافية لحساب سعر البقعة دون لمس الخزائن. انظر [`bonding-curve`](/ar/products/launchlab/bonding-curve).
- **`total_base_sell`** مقابل `real_base` — نسبة "التقدم نحو التخرج" لواجهات المستخدم.
- **`migrate_type`** — يتطلب التهيئة الجديدة `1` (`CPSWAP`). يبقى `0` المخزن ذا معنى فقط لإطلاق موجود تم إنشاؤه قبل فرض CPMM فقط.
- **`amm_creator_fee_on`** — يختار `creator_fee_on = OnlyQuoteToken` (`0`) أو `BothToken` (`1`) على مجموعة CPMM بعد التخرج. لا يختار هدف الهجرة. انظر [`creator-fees`](/ar/products/launchlab/creator-fees).
- **`token_program_flag`** — حقل بت، وليس منطقي. Bit0 هو رمز الأساس و bit1 هو رمز الاقتباس، كل منهما `0` لـ SPL Token و `1` لـ Token-2022، لذا يأخذ البايت أربع قيم: `0` (كلاهما قديم)، `1` (Token-2022 base)، `2` (Token-2022 quote)، `3` (كلاهما Token-2022). اقرأ رمز واحد باستخدام `(token_program_flag >> bit) & 1`. مقارنة البايت بالكامل مقابل `0` لتصنيف رمز الأساس تحل `2` إلى البرنامج الخاطئ. كان Bit1 ثابتاً `0` حتى تم دعم رموز اقتباس Token-2022.
- **`quote_protocol_fee` / `platform_fee` / `migrate_fee`** — ثلاثة عدادات رسوم مستقلة. لكل منها تعليمة مطالبة خاصة بها؛ انظر [`instructions`](/ar/products/launchlab/instructions).
- **`vesting_schedule`** — موجود على كل `PoolState` لكن غير نشط عندما `total_locked_amount == 0`. انظر [`vesting`](/ar/products/launchlab/vesting) لدورة الحياة الكاملة.

## سلطة PDA

يستخدم LaunchLab سلطة PDA **واحدة** عبر جميع الإطلاقات، مشتقة بدون بذرة لكل إطلاق:

```ts
const LAUNCHLAB_PROGRAM_ID = new PublicKey("<LAUNCHLAB_ID>"); // see reference/program-addresses

const [authority, bump] = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);
```

هذا PDA الواحد هو:

- السلطة على `base_vault` و `quote_vault` لكل إطلاق.
- `mint_authority` على `base_mint` لكل إطلاق (قبل التخرج).
- الموقّع على CPI تخرج CPMM، وعلى تخرج AMM v4 القديم للحالة الموجودة.
- الموقّع على تحويلات `ClaimVestedToken` خارج الخزانة الأساسية.

يتم إلغاء `mint_authority` فوراً بعد استدعاء `MigrateToCpswap` أو استدعاء `MigrateToAmm` القديم، لذا يتم إصلاح العرض بشكل دائم.

يحرس PDA إضافيان خزائن الرسوم:

```ts
const [creatorFeeVaultAuthority] = PublicKey.findProgramAddressSync(
  [Buffer.from("creator_fee_vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);

const [platformFeeVaultAuthority] = PublicKey.findProgramAddressSync(
  [Buffer.from("platform_fee_vault_auth_seed")],
  LAUNCHLAB_PROGRAM_ID,
);
```

يوقعان التحويل خارج خزائن الرسوم المقابلة أثناء `ClaimCreatorFee` و `ClaimPlatformFeeFromVault`.

## رمز الأساس

تم إنشاؤه بشكل مضمن بواسطة `Initialize` مع:

- `mint_authority = authority` (تم إلغاؤه عند التخرج).
- `freeze_authority = None`.
- `supply = supply`، مصكوك بالكامل في `base_vault`.
- `decimals` اختاره المنشئ عند `Initialize` (عادة 6).

نظراً لأن العرض الكامل مصكوك مسبقاً، فإن `base_mint.supply` ثابت طوال حياة الإطلاق. تحريكات المنحنى تنقل الرموز من `base_vault` إلى المشتري، لكنها لا تستدعي `mint_to`.

ينشئ `Initialize` / `InitializeV2` رموز أساس SPL Token. تسمح تعليمة `InitializeWithToken2022` المخصصة برمز أساس Token-2022 (مع `TransferFeeConfig` اختياري). تتطلب جميع مسارات التهيئة الثلاثة تخرج CPMM.

رمز الاقتباس مستقل عن رمز الأساس وقد يكون بحد ذاته رمز Token-2022 — يقبل `InitializeV2` و `InitializeWithToken2022` أي برنامج في فتحة برنامج الاقتباس. لا يفعل `Initialize` المهمل: حساب برنامج الاقتباس الخاص به لا يزال مكتوباً إلى SPL Token، لذا يمكن فقط إطلاق إعداد رمز اقتباس Token-2022 من خلال الاثنين الآخرين. لا ينشئ LaunchLab رمز الاقتباس أبداً؛ إنه ما يسميه `GlobalConfig.quote_mint`. انظر [`global-config`](/ar/products/launchlab/global-config#token-2022-quote-mints) و [`reference/token-2022-support`](/ar/reference/token-2022-support#launchlab-quote-mints).

## الخزائن

كل من `base_vault` و `quote_vault` عبارة عن حسابات رموز مملوكة لـ LaunchLab `authority` PDA، تم إنشاء كل منها على البرنامج الذي يمتلك رمزه الخاص — لذا يمكن لإطلاق أن يكون لديه خزانة أساس قديمة وخزانة اقتباس Token-2022، أو أي مزيج آخر. يسجل `token_program_flag` أي منها. يتم تخزين العناوين على `PoolState` ويمكن أيضاً اشتقاقها:

```ts
const [baseVault] = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), baseMint.toBuffer()],
  LAUNCHLAB_PROGRAM_ID,
);
const [quoteVault] = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), quoteMint.toBuffer()],
  LAUNCHLAB_PROGRAM_ID,
);
```

(تحقق من بادئات البذور الدقيقة من بنية حسابات `Initialize` في المصدر قبل الاعتماد على الاشتقاق في الإنتاج.)

## خزائن الرسوم

يجمع PDA اثنان الرسوم عبر الإطلاقات:

- **خزانة رسوم المنشئ** — PDA عند البذور `[creator, quote_mint]`. كل إطلاق يكسب نفس رسوم المنشئ على نفس رمز الاقتباس يصب في نفس الخزانة. يقوم المنشئ بمسحها عبر `ClaimCreatorFee`.
- **خزانة رسوم المنصة** — PDA عند البذور `[platform_config, quote_mint]`. كل إطلاق يتم توجيهه عبر نفس المنصة التي تستخدم نفس رمز الاقتباس يصب في نفس الخزانة. يقوم `platform_fee_wallet` للمنصة بمسحها عبر `ClaimPlatformFeeFromVault`. هناك أيضاً متغير مسح لكل إطلاق (`ClaimPlatformFee`) يسحب من `quote_vault` للإطلاق مباشرة دون المرور عبر الخزانة المجمعة.

يتم إنشاء كلاهما بكسل على أول مبادلة تتراكم رسوماً، على أي برنامج يمتلك رمز الاقتباس. ينتهي المطاف بمنشئ أو منصة تشغل إطلاقات ضد رمز اقتباس قديم و Token-2022 بخزانة واحدة لكل رمز، كما كان الحال بالفعل لرمزين قديمين مختلفين.

نمط الخزانة المجمعة هو ما يسمح لمنشئ أو منصة عالية الحجم بإطفاء تكلفة الإيجار لتراكم الرسوم عبر العديد من الإطلاقات.

## خزانة الاقتباس ↔ `real_quote`

يجب أن تبقى `quote_vault.balance` و `PoolState.real_quote` متزامنة. يمكنهما الانجراف بحد أقصى مجموع عدادات الرسوم الثلاثة المعلقة (`quote_protocol_fee`، `platform_fee`، `migrate_fee`)، التي تجلس في الخزانة لكنها تنتمي إلى عدادات الرسوم وليس احتياطي المنحنى. تستخدم رياضيات المنحنى دائماً `real_quote`، أبداً رصيد الخزانة الخام.

ثابت ما قبل التخرج:

```
quote_vault.balance ==
   real_quote
 + quote_protocol_fee
 + platform_fee
 + migrate_fee
```

## انتقالات حساب دورة الحياة

| الحدث | الحالة | `real_base` | `real_quote` | مجموعة ما بعد التخرج |
|-------|--------|-------------|--------------|----------------------|
| `Initialize` | Funding | 0 | 0 | — |
| `BuyExactIn` / `BuyExactOut` | Funding | `+base_out` | `+quote_in_after_fee` | — |
| `SellExactIn` / `SellExactOut` | Funding | `−base_in` | `−quote_out_before_fee` | — |
| `MigrateToCpswap` (أو `MigrateToAmm` القديم) | Funding → Migrate → Migrated | (مجمد) | (مجمد) | تم الإنشاء؛ يتم تقسيم LP من CPMM إلى LP مقفول مملوك للمنصة و LP محترق |
| `ClaimCreatorFee` / `ClaimPlatformFee*` | أي | — | — | يستنزف خزانة رسوم |
| `CreateVestingAccount` | Funding | — | — | يرفع `vesting_schedule.allocated_share_amount` |
| `ClaimVestedToken` | Migrated فقط | — | — | يستنزف `base_vault` |

## أين تذهب بعد ذلك

- [`products/launchlab/bonding-curve`](/ar/products/launchlab/bonding-curve) — الرياضيات خلف `real_base` ↔ `real_quote`.
- [`products/launchlab/instructions`](/ar/products/launchlab/instructions) — قوائم الحسابات لكل تعليمة.
- [`products/launchlab/global-config`](/ar/products/launchlab/global-config) — الربط على مستوى البروتوكول.
- [`products/launchlab/platform-config`](/ar/products/launchlab/platform-config) — طبقة المنصة الإضافية.
- [`products/launchlab/vesting`](/ar/products/launchlab/vesting) — ميكانيكا العرض المقفول.
- [`products/cpmm/accounts`](/ar/products/cpmm/accounts) — كيف يبدو `cpmm_pool_state` بعد التخرج.

المصادر:

- `raydium-launch/programs/launchpad/src/states/pool.rs` — `PoolState`, `PoolStatus`, `VestingSchedule`, `AmmCreatorFeeOn`.
- `raydium-launch/programs/launchpad/src/lib.rs` — ثوابت بذور PDA (`AUTH_SEED`, `CREATOR_FEE_VAULT_AUTH_SEED`, `PLATFORM_FEE_VAULT_AUTH_SEED`).
- [وحدة Raydium SDK v2 `launchpad`](https://github.com/raydium-io/raydium-sdk-V2).
