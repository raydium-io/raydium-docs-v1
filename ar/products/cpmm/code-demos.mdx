---
title: "أمثلة كود CPMM"
description: "أمثلة TypeScript شاملة لكل عملية CPMM — إنشاء مجموعة، مبادلة، إيداع، سحب، جمع الرسوم — بالإضافة إلى هيكل عظمي بسيط لـ Rust CPI."
---

<Info>
  **هذه الصفحة مُترجَمة آليًا بواسطة الذكاء الاصطناعي. النسخة الإنجليزية هي المرجع المعتمد.**

  [عرض النسخة الإنجليزية →](/products/cpmm/code-demos)
</Info>

<Info>
  **لافتة الإصدار.** جميع أمثلة TypeScript تستهدف `@raydium-io/raydium-sdk-v2@0.2.64-alpha` على شبكة Solana mainnet-beta، تم التحقق منها في أبريل 2026. هيكل عظمي Rust CPI يستهدف `raydium-cp-swap` على فرع `master`، Anchor `0.30.x`. يتم سحب معرفات البرامج عبر ثوابت من [`reference/program-addresses`](/ar/reference/program-addresses).
</Info>

## المتطلبات الأساسية

```bash
npm install @raydium-io/raydium-sdk-v2 @solana/web3.js @solana/spl-token bn.js decimal.js
```

كل مثال على هذه الصفحة يعكس ملف في [`raydium-sdk-V2-demo/src/cpmm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/cpmm)؛ رابط GitHub يقع بجانب كل قسم. يتبع التمهيد ملف `config.ts.template` من مستودع العرض التوضيحي ([المصدر](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/config.ts.template)):

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);

const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});
```

مثيل `Raydium` هو واجهة SDK — كل مثال أدناه يستخدمه. يجلب قوائم الرموز وتكوينات الرسوم بكسل من `api-v3.raydium.io`؛ يمكنك تعيينها ببيانات خاصة بك في البيئات غير المتصلة بالإنترنت.

## إنشاء مجموعة CPMM

المصدر: [`src/cpmm/createCpmmPool.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/createCpmmPool.ts)

```ts
import { PublicKey } from "@solana/web3.js";
import BN from "bn.js";
import { getCpmmPdas, CREATE_CPMM_POOL_PROGRAM, CREATE_CPMM_POOL_FEE_ACC }
  from "@raydium-io/raydium-sdk-v2";

const mintA = new PublicKey("So11111111111111111111111111111111111111112"); // wSOL
const mintB = new PublicKey("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"); // USDC

// 1. اختر تكوين رسوم. index=0 هو المستوى 0.25%.
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0)!;

// 2. اسحب بيانات وصف الرمز حتى يتمكن SDK من التعامل مع امتدادات Token-2022.
const mintAInfo = await raydium.token.getTokenInfo(mintA);
const mintBInfo = await raydium.token.getTokenInfo(mintB);

// 3. بناء المعاملة.
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA:           mintAInfo,
  mintB:           mintBInfo,
  mintAAmount:     new BN(1_000_000_000),   // 1 SOL (بافتراض 9 منازل عشرية)
  mintBAmount:     new BN(   160_000_000),  // 160 USDC (عند 160/SOL)
  startTime:       new BN(0),               // افتح فوراً
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("تم إنشاء المجموعة في", extInfo.address.poolId.toBase58());
console.log("Tx:", txId);
```

بعض الأشياء التي يتعامل معها SDK بصمت:

- ترتيب الرموز في ترتيب token0/token1 قبل اشتقاق PDA.
- دفع `create_pool_fee` لمرة واحدة إلى `poolFeeAccount`.
- إنشاء حسابات الرموز المرتبطة بالمتصل إذا كانت مفقودة.
- اختيار برنامج الرمز الصحيح (SPL Token مقابل Token-2022) لكل جانب.

بعد التأكيد، يمكنك جلب حالة المجموعة المباشرة باستخدام:

```ts
const { poolKeys, poolInfo, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(
  extInfo.address.poolId,
);
```

## مبادلة (إدخال أساسي)

المصدر: [`src/cpmm/swap.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swap.ts)

```ts
import { CurveCalculator } from "@raydium-io/raydium-sdk-v2";

const poolId = new PublicKey("<POOL_ID>");

// 1. حمّل حالة المجموعة الحالية مباشرة من RPC (وليس من API).
const { poolInfo, poolKeys, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(poolId);

const inputMint  = new PublicKey(poolInfo.mintA.address); // مبادلة A → B
const amountIn   = new BN(100_000_000);                   // 0.1 SOL
const slippage   = 0.005;                                 // 0.5%

// 2. احصل على عرض سعر محلي. يعكس CurveCalculator الخاص بـ SDK الرياضيات على السلسلة،
//    بما في ذلك رسوم نقل Token-2022 على كلا الجانبين.
const baseIn = inputMint.equals(new PublicKey(poolInfo.mintA.address));
const swapResult = CurveCalculator.swap(
  amountIn,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
);
const minimumAmountOut =
  swapResult.destinationAmountSwapped.muln(1 - slippage * 100).divn(100);

// 3. بناء وإرسال.
const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: amountIn,
  swapResult,
  slippage,
  baseIn,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("معاملة المبادلة:", txId);
```

ملاحظة: SDK **دائماً** يعيد جلب حالة المجموعة من RPC داخل `getPoolInfoFromRpc`. لا تحصل على عرض سعر من `api-v3.raydium.io` لمعاملة أنت على وشك التوقيع عليها — عرض سعر قديم بمقدار كتلة واحدة يمكن أن ينزلق إلى `ExceededSlippage` في وقت الهبوط.

## مبادلة (إخراج أساسي)

المصدر: [`src/cpmm/swapBaseOut.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swapBaseOut.ts)

```ts
const amountOutWanted = new BN(15_000_000);        // 15 USDC
const slippage        = 0.005;

const baseIn = false; // B هو الإدخال، A هو الإخراج؟ يعتمد على اتجاهك
const swapResult = CurveCalculator.swapBaseOutput(
  amountOutWanted,
  rpcData.baseReserve,
  rpcData.quoteReserve,
  rpcData.configInfo!.tradeFeeRate,
);
const maxAmountIn = swapResult.sourceAmountSwapped.muln(1 + slippage * 100).divn(100);

const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: maxAmountIn,
  fixedOut:    true,
  amountOut:   amountOutWanted,
  baseIn,
  slippage,
  txVersion:   TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## إيداع السيولة

المصدر: [`src/cpmm/deposit.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/deposit.ts)

```ts
const lpAmount = new BN(100_000);           // مبلغ LP المطلوب
const slippage = 0.01;

const { execute } = await raydium.cpmm.addLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  slippage,
  baseIn: true,           // احصل على عرض سعر من جانب mintA
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

يحول SDK `lpAmount` إلى `needed_token_0` و `needed_token_1` باستخدام احتياطيات المجموعة الحالية، ينفخ كل واحد بـ `1 + slippage` لحجج `maximum_*` للتعليمات، وينشئ إنشاءات ATA إذا لزم الأمر.

## سحب السيولة

المصدر: [`src/cpmm/withdraw.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/withdraw.ts)

```ts
const lpAmount = new BN(100_000);           // LP للحرق
const slippage = 0.01;

const { execute } = await raydium.cpmm.withdrawLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## جمع رسوم البروتوكول والصندوق والمنشئ

المصدر: [`src/cpmm/collectCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectCreatorFee.ts)، [`src/cpmm/collectAllCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectAllCreatorFee.ts)

يبقى جمع البروتوكول والصندوق محمياً بالسلطة. رسوم المنشئ لها مساران: `CollectCreatorFee` الموقع من المنشئ الموجود، و `CollectCreatorFeePermissionless` الإضافي، الذي يمكن لأي دافع استدعاؤه لكن الذي يمكنه نقل فقط إلى ATAs الكنسية للمنشئ. يكشف SDK المثبت عن منشئي الخام الموجودين:

```ts
import {
  makeCollectProtocolFeeInstruction,
  makeCollectFundFeeInstruction,
  makeCollectCreatorFeeInstruction,
} from "@raydium-io/raydium-sdk-v2";

// تم تعيين PDAs والسلطة عند إنشاء المجموعة؛ انظر reference/program-addresses
// للبذور الكنسية. يكشف SDK عن مساعدين إذا كنت تفضل.
```

<Warning>
  `@raydium-io/raydium-sdk-v2@0.2.64-alpha` لم يكشف بعد عن منشئ لـ `CollectCreatorFeePermissionless`. حتى يضيف إصدار SDK جديد، قم بالبناء من IDL CPMM المحدث أو استدعِ البرنامج مباشرة. لا تعيد استخدام `makeCollectCreatorFeeInstruction`: التعليمات الجديدة لها مميز مختلف وتستبدل موقّع المنشئ / مواضع `amm_config` بحسابات `payer` و `creator` غير الموقعة منفصلة.
</Warning>

خارج السلسلة، يمكنك قراءة الرسوم المتراكمة مباشرة من `PoolState`:

```ts
const pool = await raydium.cpmm.getRpcPoolInfo(poolId);
console.log("رسم البروتوكول المتراكم token0:", pool.protocolFeesToken0.toString());
console.log("رسم البروتوكول المتراكم token1:", pool.protocolFeesToken1.toString());
```

## هيكل عظمي Rust CPI

إذا كنت تريد استدعاء CPMM من برنامج Anchor الخاص بك — على سبيل المثال، خزينة تقوم بمبادلة نيابة عن المودعين — يبدو سياق CPI كما يلي. يتبع ترتيب الحساب [`products/cpmm/instructions`](/ar/products/cpmm/instructions).

```rust
// Cargo.toml
// raydium-cp-swap = { git = "https://github.com/raydium-io/raydium-cp-swap" }
// anchor-lang      = "=1.0.2"   // يجب أن يطابق ما يثبته raydium-cp-swap
// anchor-spl       = "=1.0.2"

use anchor_lang::prelude::*;
use anchor_spl::token_interface::{TokenAccount, TokenInterface, Mint};
use raydium_cp_swap::cpi::accounts::Swap;
use raydium_cp_swap::cpi;
use raydium_cp_swap::program::RaydiumCpSwap;

#[derive(Accounts)]
pub struct ProxySwap<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,

    /// CHECK: تم التحقق من صحته بواسطة برنامج CPMM
    pub authority:     UncheckedAccount<'info>,
    /// CHECK:
    pub amm_config:    UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub pool_state:    UncheckedAccount<'info>,

    #[account(mut)]
    pub input_token_account:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_token_account: InterfaceAccount<'info, TokenAccount>,

    #[account(mut)]
    pub input_vault:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_vault: InterfaceAccount<'info, TokenAccount>,

    pub input_token_program:  Interface<'info, TokenInterface>,
    pub output_token_program: Interface<'info, TokenInterface>,

    pub input_token_mint:  InterfaceAccount<'info, Mint>,
    pub output_token_mint: InterfaceAccount<'info, Mint>,

    #[account(mut)]
    /// CHECK: حلقة مخزن مؤقت
    pub observation_state: UncheckedAccount<'info>,

    pub cpmm_program: Program<'info, RaydiumCpSwap>,
}

pub fn proxy_swap_base_input(
    ctx: Context<ProxySwap>,
    amount_in: u64,
    minimum_amount_out: u64,
) -> Result<()> {
    let cpi_accounts = Swap {
        payer:                ctx.accounts.payer.to_account_info(),
        authority:            ctx.accounts.authority.to_account_info(),
        amm_config:           ctx.accounts.amm_config.to_account_info(),
        pool_state:           ctx.accounts.pool_state.to_account_info(),
        input_token_account:  ctx.accounts.input_token_account.to_account_info(),
        output_token_account: ctx.accounts.output_token_account.to_account_info(),
        input_vault:          ctx.accounts.input_vault.to_account_info(),
        output_vault:         ctx.accounts.output_vault.to_account_info(),
        input_token_program:  ctx.accounts.input_token_program.to_account_info(),
        output_token_program: ctx.accounts.output_token_program.to_account_info(),
        input_token_mint:     ctx.accounts.input_token_mint.to_account_info(),
        output_token_mint:    ctx.accounts.output_token_mint.to_account_info(),
        observation_state:    ctx.accounts.observation_state.to_account_info(),
    };
    // يأخذ Anchor 1.0 Pubkey للبرنامج هنا، وليس AccountInfo.
    let cpi_ctx = CpiContext::new(
        *ctx.accounts.cpmm_program.key,
        cpi_accounts,
    );
    cpi::swap_base_input(cpi_ctx, amount_in, minimum_amount_out)
}
```

إذا كان CPI الخاص بك يوقع كـ PDA (على سبيل المثال، تدير خزينة نيابة عن المودعين)، استبدل `CpiContext::new` بـ `CpiContext::new_with_signer` وأمرر بذورك.

## الأخطاء الشائعة

قائمة تحقق قصيرة قبل فتح تذكرة دعم:

- **الرموز المرتبة.** إذا كان `poolState` PDA المشتق الخاص بك لا يطابق المجموعة على السلسلة، فربما نسيت ترتيب الرموز.
- **عرض سعر API قديم.** لا تمرر أبداً قيمة احتياطي من `api-v3.raydium.io` إلى `CurveCalculator.swap`. جلب من RPC.
- **برنامج رمز خاطئ.** خزينة رمز Token-2022 مملوكة ببرنامج Token-2022، وليس بـ SPL Token. استخدم دائماً حقول `token_0_program` / `token_1_program` للمجموعة.
- **الانزلاق تحت المقسوم عليه لرموز رسوم النقل.** إذا كان أي جانب من المجموعة رمز نقل رسوم Token-2022، يجب أن يكون `minimum_amount_out` الخاص بك مقسوماً عليه بما يتلقاه المستخدم فعلياً، وليس بما تُرسله الخزينة.
- **`NotApproved` على مبادلة.** تحقق من `PoolState.status` — قد يكون المسؤول قد أوقف المبادلات على تلك المجموعة. انظر [`products/cpmm/instructions`](/ar/products/cpmm/instructions) لقناع الحالة.

## أين تذهب بعد ذلك

- [`sdk-api/typescript-sdk`](/ar/sdk-api/typescript-sdk) — مرجع SDK الكامل.
- [`sdk-api/rest-api`](/ar/sdk-api/rest-api) — نقاط نهاية عرض السعر وبيانات وصف المجموعة.
- [`user-flows/create-cpmm-pool`](/ar/user-flows/create-cpmm-pool) — شرح غير الكود لنفس التدفق.
- [`integration-guides/aggregator`](/ar/integration-guides/aggregator) — إذا كنت توجه CPMM كجزء من مسار متعدد القفزات.

المصادر:

- [Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`](https://github.com/raydium-io/raydium-sdk-V2)
- [أمثلة Raydium SDK v2 — `raydium-sdk-v2-demo`](https://github.com/raydium-io/raydium-sdk-V2-demo)
- [`raydium-io/raydium-cp-swap`](https://github.com/raydium-io/raydium-cp-swap)
