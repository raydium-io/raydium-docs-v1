---
title: "Instrucciones CLMM"
description: "Referencia completa de instrucciones CLMM: ciclo de vida del pool, gestión de posiciones, swap y flujos de recompensas."
---

<Info>
  **Esta página fue traducida automáticamente por IA. La versión en inglés es la fuente autorizada.**

  [Ver versión en inglés →](/products/clmm/instructions)
</Info>

Esta página se complementa con [`products/clmm/accounts`](/es/products/clmm/accounts) (qué son las cuentas) y [`products/clmm/math`](/es/products/clmm/math) (qué es la matemática). Es autorizada para argumentos y orden de cuentas; los diseños de bytes específicos provienen del IDL.

## Inventario de instrucciones

| Grupo | Instrucción | Notas |
|-------|-------------|-------|
| Admin | `CreateAmmConfig` | Define un nuevo nivel de comisión. |
| Admin | `UpdateAmmConfig` | Cambia tasas en un nivel existente. |
| Admin | `UpdatePoolStatus` | Pausa/reanuda operaciones en un pool. |
| Admin | `CreateSupportMintAssociated` | Permite la configuración de extensión de mint Token-2022 para usar en pools CLMM. |
| Admin | `CreateOperationAccount` | Inicializa la cuenta de operación a nivel de programa (una sola vez). |
| Admin | `UpdateOperationAccount` | Modifica la lista blanca de la cuenta de operación. |
| Admin | `CreateDynamicFeeConfig` | Crea un conjunto de parámetros de tarifa dinámica reutilizable bajo un índice u16. |
| Admin | `UpdateDynamicFeeConfig` | Modifica un `DynamicFeeConfig` existente. Los pools que ya lo capturaron no se ven afectados. |
| Admin | `CreatePermissionPda` | Otorga a una autoridad el derecho de llamar a `CreatePermissionedPool` inicializando su PDA `Permission`. |
| Admin | `ClosePermissionPda` | Revoca un permiso previamente otorgado cerrando su PDA `Permission`; el alquiler se reembolsa al llamador. |
| Pool | `CreatePool` | Inicializa un pool CLMM vinculado a un `AmmConfig`. Ruta de comisión estándar, FromInput. Coexiste con `CreateCustomizablePool`. |
| Pool | `CreateCustomizablePool` | Recomendado para nuevos pools. Misma forma que `CreatePool` más `collect_fee_on` y una bandera `enable_dynamic_fee` opcional. |
| Pool | `CreatePermissionedPool` | Crea un pool **adicional** para un par que ya tiene uno, usando un `seed_index` proporcionado por el cliente plegado en el PDA del pool. El pagador debe tener un PDA `Permission`. |
| Posición | `OpenPosition` / `OpenPositionV2` / `OpenPositionWithToken22Nft` | Acuña un NFT de posición. `OpenPositionV2` reemplaza V1 (diseño de cuenta más nuevo con ranura de extensión de mapa de bits); `OpenPositionWithToken22Nft` emite el NFT de posición como Token-2022 en lugar de SPL Token. El código nuevo debe usar V2 o la variante Token-2022. |
| Posición | `IncreaseLiquidity` / `IncreaseLiquidityV2` | Añade liquidez a una posición existente. |
| Posición | `DecreaseLiquidity` / `DecreaseLiquidityV2` | Elimina liquidez; cobra comisiones adeudadas. |
| Posición | `ClosePosition` | Quema el NFT y cierra `PersonalPositionState`. |
| Posición | `CloseProtocolPosition` | Barrido solo para admin de PDAs `ProtocolPositionState` heredadas. El programa actual ya no crea ni lee `ProtocolPositionState` — esta instrucción existe únicamente para recuperar alquiler en cuentas creadas por versiones anteriores del programa. |
| Swap | `Swap` / `SwapV2` | Swap de liquidez constante. Ambas variantes aplican tarifa dinámica, enrutamiento de tarifa unilateral y coincidencia de órdenes limitadas; la única diferencia es que `SwapV2` acepta mints Token-2022 (la variante V1 requiere que ambas bóvedas sean SPL Token clásico). |
| Swap | `SwapRouterBaseIn` | Multi-salto, usado por el enrutador. |
| Orden limitada | `OpenLimitOrder` | Coloca una orden de venta en un tick. Los tokens no completados se sientan en el tick; el motor de coincidencia los completa cuando el precio cruza. |
| Orden limitada | `IncreaseLimitOrder` | Añade a una orden abierta existente. |
| Orden limitada | `DecreaseLimitOrder` | Reduce o cancela una orden abierta; paga el resto no completado más cualquier salida ya liquidada. |
| Orden limitada | `SettleLimitOrder` | Empuja tokens de salida completados al propietario de la orden. Puede ser llamado por el propietario o por el operador operacional. |
| Orden limitada | `CloseLimitOrder` | Cierra una cuenta de orden completamente consumida. El alquiler siempre se devuelve al `owner` de la orden. Puede ser llamado por el propietario o el operador. |
| Comisiones | `CollectProtocolFee` | Barrido de admin de comisiones de protocolo. |
| Comisiones | `CollectFundFee` | Barrido de admin de comisiones de fondo. |
| Recompensas | `InitializeReward` | Adjunta un nuevo flujo de recompensas a un pool. |
| Recompensas | `SetRewardParams` | Cambia la tasa de emisión/fin de una recompensa existente. |
| Recompensas | `UpdateRewardInfos` | Liquida el crecimiento de recompensas hasta ahora (llamado por cualquier swap / cambio de posición). |
| Recompensas | `TransferRewardOwner` | Transfiere la autoridad que puede establecer o financiar un flujo de recompensas. |
| Recompensas | `CollectRemainingRewards` | Después del `end_time` de un flujo de recompensas, barre cualquier token no asignado de vuelta al financiador. |
| Utilidad | `InitTickArray` | Inicializa una cuenta de matriz de tick (a menudo agrupada con `OpenPosition`). |

La mayoría de instrucciones solo para admin (`CreateAmmConfig`, `UpdateAmmConfig`, `UpdatePoolStatus`, `CreateSupportMintAssociated`, `CreateOperationAccount`, `UpdateOperationAccount`, `CloseProtocolPosition`) están controladas por la clave `admin` codificada del programa. `CreatePermissionPda` / `ClosePermissionPda` aceptan la clave `admin` **o** una clave `permission_pda_admin` dedicada. Las instrucciones de admin de flujo de recompensas (`TransferRewardOwner`, `CollectRemainingRewards`) están controladas por el financiador de recompensas, no por el admin del programa.

El sufijo V2 significa "soporta Token-2022 en bóvedas / NFT, requiere ranura de extensión de mapa de bits". El SDK elige V2 por defecto para nuevos pools.

## `CreatePool`

**Argumentos**

```
sqrt_price_x64: u128           // precio inicial
open_time:      u64            // swaps rechazados antes de este tiempo
```

**Cuentas (abreviadas)**

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| 1 | `pool_creator` | W | S | |
| 2 | `amm_config` |   |   | Nivel de comisión elegido. |
| 3 | `pool_state` | W |   | `init` aquí. |
| 4 | `token_mint_0` |   |   | Ordenado. |
| 5 | `token_mint_1` |   |   | |
| 6 | `token_vault_0` | W |   | `init` aquí, propiedad del PDA de autoridad del pool. |
| 7 | `token_vault_1` | W |   | |
| 8 | `observation_state` | W |   | `init` aquí. |
| 9 | `tick_array_bitmap_extension` | W |   | `init` aquí (V2). |
| 10 | `token_program` |   |   | |
| 11 | `token_program_2022` |   |   | |
| 12 | `system_program`, `rent` |   |   | |

**Precondiciones**

- `token_mint_0 < token_mint_1` por orden de bytes.
- `amm_config.disable_create_pool == false`.
- Los mints no son rechazados por la lista blanca de extensión Token-2022.

**Postcondiciones**

- `pool_state.sqrt_price_x64 = sqrt_price_x64`, `tick_current = floor(log_{1.0001}(price))`.
- `pool_state.liquidity = 0` (sin posiciones aún).
- `pool_state.fee_on = FromInput` (predeterminado heredado).
- `pool_state.dynamic_fee_info` está en cero (tarifa dinámica deshabilitada).

## `CreateCustomizablePool`

Recomendado para nuevos pools. Mismo efecto que `CreatePool` más modo de recopilación de comisiones por pool y una opción de tarifa dinámica opcional.

**Argumentos**

```rust
pub struct CreateCustomizableParams {
    pub sqrt_price_x64:    u128,
    pub collect_fee_on:    CollectFeeOn,   // FromInput | Token0Only | Token1Only
    pub enable_dynamic_fee: bool,
}
```

**Cuentas (abreviadas)** — igual que `CreatePool` más, cuando `enable_dynamic_fee = true`:

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| N | `dynamic_fee_config` |   |   | La configuración compartida de la que capturar. Debe existir ya. |

**Precondiciones** — igual que `CreatePool`. Si `enable_dynamic_fee = false`, `dynamic_fee_config` se ignora.

**Postcondiciones**

- `pool_state.fee_on` establecido a la variante `CollectFeeOn` elegida.
- Si la tarifa dinámica fue habilitada: `pool_state.dynamic_fee_info` se inicializa desde el `DynamicFeeConfig` suministrado (cinco parámetros de calibración copiados; campos de estado en cero).
- De lo contrario: `pool_state.dynamic_fee_info` está en cero (= tarifa dinámica inactiva para siempre en este pool).

`fee_on` y el bit de habilitación de tarifa dinámica se establecen **solo** en la creación del pool. No hay actualización in situ — los pools creados a través del `CreatePool` heredado no pueden obtener retroactivamente tarifa dinámica o comisión unilateral. Las nuevas implementaciones deben usar esta instrucción por defecto.

## `CreatePermissionedPool`

Tanto `CreatePool` como `CreateCustomizablePool` derivan el PDA del pool de `["pool", amm_config, token_mint_0, token_mint_1]`, por lo que hay exactamente **un** dirección de pool canónica por triple `(config, mint0, mint1)` — un segundo `init` en las mismas semillas falla. `CreatePermissionedPool` levanta esa restricción plegando un `seed_index: u16` proporcionado por el cliente en las semillas del PDA del pool, permitiendo **múltiples pools para el mismo par y nivel de comisión** — cada uno en su propia dirección. Debido a que una dirección de pool arbitraria es una capacidad privilegiada, el pagador debe tener un PDA `Permission` que lo autorice.

Todo lo demás sobre el pool es idéntico a `CreateCustomizablePool`: toma los mismos `CreateCustomizableParams` y soporta comisión unilateral y la opción de tarifa dinámica.

**Argumentos**

```rust
customizable_params: CreateCustomizableParams   // mismo struct que CreateCustomizablePool
seed_index:          u16                          // debe ser distinto de cero; plegado en las semillas del PDA del pool
```

**Cuentas (abreviadas)** — igual que `CreateCustomizablePool` más, al frente:

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| 1 | `payer` | W | S | Paga alquiler. Debe ser la autoridad del PDA `permission` abajo. |
| 2 | `pool_creator` |   |   | Registrado como `pool_state.owner`. Puede diferir de `payer`. |
| 3 | `permission` |   |   | PDA `["permission", payer]`. Anchor rechaza la llamada si no existe / no es propiedad del programa. |
| … | (`amm_config`, `pool_state`, mints, bóvedas, `observation_state`, `tick_array_bitmap`, programas de token/sistema) | | | Como en `CreateCustomizablePool`. |

El PDA `pool_state` se deriva de `["pool", amm_config, token_mint_0, token_mint_1, seed_index.to_le_bytes()]`.

**Precondiciones**

- `seed_index != 0`. Un `seed_index` de `0` está reservado para pools heredados y se rechaza aquí; el componente de semilla `[0, 0]` es lo que hace que una dirección de pool heredada se colapse a la forma de cuatro semillas clásica.
- El PDA `permission` para `payer` existe (creado por un admin a través de `CreatePermissionPda`).
- Mismas reglas de mint / lista blanca que `CreatePool`.

**Postcondiciones**

- Un nuevo `pool_state` existe en la dirección derivada de `seed_index`, con `pool_state.seed_index = seed_index`.
- Todo otro post-estado coincide con `CreateCustomizablePool` (modo de comisión, tarifa dinámica opcional).

<Note>
  Esta instrucción **no** amplía el acceso general a la creación de pools — la creación sin permisos continúa a través de `CreatePool` / `CreateCustomizablePool`, que permanecen como un pool por par. `CreatePermissionedPool` existe para el caso específico donde un operador en la lista blanca necesita varios pools para el mismo par (por ejemplo, precios iniciales diferentes o cohortes de lanzamiento) y tiene un PDA `Permission` otorgado por el admin.
</Note>

## `OpenPositionV2` / `OpenPositionWithToken22Nft`

Crea una nueva posición dentro de un pool existente.

**Argumentos**

```
tick_lower_index: i32
tick_upper_index: i32
tick_array_lower_start_index: i32
tick_array_upper_start_index: i32
liquidity:  u128              // L deseada (o 0 para usar montos abajo)
amount_0_max: u64
amount_1_max: u64
with_metadata: bool           // escribe metadatos de NFT (Metaplex)
base_flag: Option<bool>       // true = ajusta a amount0; false = ajusta a amount1
```

**Cuentas (abreviadas)**

| # | Nombre | W | S |
|---|--------|---|---|
| 1 | `payer` | W | S |
| 2 | `position_nft_owner` |   |   |
| 3 | `position_nft_mint` | W | S (keypair) |
| 4 | `position_nft_account` | W |   | ATA del propietario para el NFT. |
| 5 | `metadata_account` | W |   | Metaplex (opcional, si `with_metadata`). |
| 6 | `pool_state` | W |   |
| 7 | `protocol_position` |   |   |   |
| 8 | `tick_array_lower` | W |   | Creado si no está inicializado. |
| 9 | `tick_array_upper` | W |   | Igual. |
| 10 | `personal_position` | W |   | Creado aquí. |
| 11 | `token_account_0`, `token_account_1` | W |   | ATAs de fuente del usuario. |
| 12 | `token_vault_0`, `token_vault_1` | W |   | |
| 13 | `rent`, `system_program`, `token_program` |   |   | |
| 14 | `associated_token_program` |   |   | |
| 15 | `metadata_program` |   |   | Opcional. |
| 16 | `token_program_2022` |   |   | V2. |
| 17 | `vault_0_mint`, `vault_1_mint` |   |   | V2. |
| 18 | `tick_array_bitmap_extension` | W |   | V2 (si se toca). |

**Matemática** — ver [`products/clmm/math`](/es/products/clmm/math). Dado `base_flag`, el programa resuelve `liquidity` o `(amount_0_max, amount_1_max)` en la `L` real y los montos de token reales consumidos.

**Precondiciones**

- `tick_lower < tick_upper`, ambos múltiplos de `pool.tick_spacing`, dentro de `[MIN_TICK, MAX_TICK]`.
- Matrices de tick requeridas pasadas e inicializadas (o creadas aquí a través de `InitTickArray` CPI en la transacción).
- El usuario tiene al menos `amount_0_max` y `amount_1_max` en las ATAs de fuente.

**Postcondiciones**

- `personal_position` existe, `liquidity` establecida, `fee_growth_inside_last` capturada.
- Entradas de matriz de tick en `tick_lower` y `tick_upper` actualizadas (`liquidity_gross += L`, `liquidity_net ± L`, instantáneas de crecimiento de comisión mantenidas).
- `pool_state.liquidity += L` si la posición está en rango (`tick_lower ≤ tick_current < tick_upper`).

**Errores comunes** — `InvalidTickIndex`, `NotApproved`, `ZeroAmountSpecified`, `TransactionTooLarge` (si hay demasiadas matrices de tick).

## `IncreaseLiquidityV2`

Añade liquidez a una posición ya abierta.

**Argumentos**

```
liquidity: u128
amount_0_max: u64
amount_1_max: u64
base_flag: Option<bool>
```

**Cuentas** — como `OpenPosition` menos el mint del NFT (la posición ya existe; el NFT se pasa como la ATA del propietario que contiene 1 token).

**Efecto**

- Transfiere `amount_0_actual` / `amount_1_actual` del usuario → bóvedas.
- Incrementa `personal_position.liquidity` y `pool_state.liquidity` (si está en rango), y el `liquidity_gross` / `liquidity_net` del tick de punto final en consecuencia.
- **Cobra comisiones y recompensas adeudadas desde el último toque** y las acredita a `tokens_fees_owed_{0,1}` / `reward_amount_owed`. Esos se pagan solo en `DecreaseLiquidity` o `CollectReward`, no en aumento.

## `DecreaseLiquidityV2`

Elimina liquidez de una posición.

**Argumentos**

```
liquidity: u128
amount_0_min: u64
amount_1_min: u64
```

**Cuentas** — misma forma que `IncreaseLiquidity`.

**Efecto**

- Calcula `(amount_0, amount_1)` para la `L` eliminada dado el `sqrt_price_x64` actual.
- Liquida comisiones/recompensas acumuladas desde el último toque, igual que `IncreaseLiquidity`.
- Transfiere `amount_0 + fees_owed_0` y `amount_1 + fees_owed_1` fuera de las bóvedas al usuario.
- Decrementa contadores de liquidez; si el nuevo `personal_position.liquidity == 0`, la posición es elegible para `ClosePosition`.

**Slippage** — `amount_0_min` y `amount_1_min` son los mínimos que el usuario acepta netos de comisiones de transferencia Token-2022 en el lado de salida.

## `ClosePosition`

Quema el NFT de posición y cierra `PersonalPositionState`.

**Precondiciones**

- `personal_position.liquidity == 0`.
- `tokens_fees_owed_{0,1} == 0`.
- Todos los contadores de recompensas `reward_amount_owed == 0`.

(Es decir, cobra todo y disminuye a cero primero.)

**Efecto**

- Quema el NFT.
- Cierra la cuenta de mint del NFT y la cuenta `personal_position`, reembolsando alquiler al `payer`.

## `SwapV2`

Camina la curva de liquidez; entrada exacta *o* salida exacta dependiendo de `is_base_input`.

**Argumentos**

```
amount: u64                  // entrada si is_base_input=true, salida de lo contrario
other_amount_threshold: u64  // mín. salida o máx. entrada
sqrt_price_limit_x64: u128   // límite duro; 0 ⇒ sin límite
is_base_input: bool
```

**Cuentas (abreviadas)**

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| 1 | `payer` |   | S | |
| 2 | `amm_config` |   |   | |
| 3 | `pool_state` | W |   | |
| 4 | `input_token_account` | W |   | |
| 5 | `output_token_account` | W |   | |
| 6 | `input_vault` | W |   | |
| 7 | `output_vault` | W |   | |
| 8 | `observation_state` | W |   | |
| 9 | `token_program` |   |   | |
| 10 | `token_program_2022` |   |   | V2. |
| 11 | `memo_program` |   |   | V2 (requerido para algunas rutas Token-2022). |
| 12 | `input_vault_mint`, `output_vault_mint` |   |   | V2. |
| 13 | `tick_array_bitmap_extension` (opcional) | W |   | Si el swap entra en la extensión. |
| 14+ | `tick_array` (restantes) | W |   | Suficientes matrices para abarcar el rango esperado del recorrido. |

Los llamadores pasan una lista clasificada de matrices de tick que cubren el recorrido esperado del swap; el programa usa tantas como necesita. El SDK calcula esta lista a través de `PoolUtils.computeAmountOutFormat` o el punto final de cotización de la API.

**Precondiciones**

- `pool_state.status` permite swap.
- `now >= open_time`.
- `sqrt_price_limit_x64` está en el lado correcto de `sqrt_price_x64` para la dirección.

**Errores comunes** — `ExceededSlippage`, `SqrtPriceLimitOverflow`, `TickArrayNotFound`, `LiquidityInsufficient`.

**Lo que `SwapV2` hace internamente que los llamadores deben saber** (lanzamiento posterior a 2025):

1. **Recargo de tarifa dinámica** — si `pool.dynamic_fee_info` es distinto de cero, el programa actualiza el acumulador de volatilidad usando la distancia de tick recorrida desde el último swap (con las reglas de filtro/decaimiento de [`products/clmm/fees`](/es/products/clmm/fees)) y añade un `dynamic_fee_component` encima de `AmmConfig.trade_fee_rate`. La tarifa total está limitada al 10% (`MAX_FEE_RATE_NUMERATOR / 1_000_000`).
2. **Coincidencia de órdenes limitadas** — cuando el recorrido de precio cruza un tick que contiene órdenes limitadas abiertas, el programa primero completa la liquidez de orden limitada disponible en ese tick (FIFO por `order_phase`), luego procede a lo largo de la curva de liquidez de LP. Los montos completados actualizan `tick.unfilled_ratio_x64` y `tick.part_filled_orders_remaining` para liquidación posterior; las órdenes en sí permanecen sin gastar hasta que su propietario llama a `SettleLimitOrder`.
3. **Enrutamiento de tarifa unilateral** — cuando `pool.fee_on = Token0Only` o `Token1Only`, el paso de swap aún calcula el mismo comercio entrada-salida; la tarifa se enruta entonces al lado configurado. Para direcciones donde el lado de tarifa configurado es la salida, la tarifa se deduce de la salida del swap (el usuario recibe `out − fee`); para direcciones donde es la entrada, el comportamiento coincide con `FromInput`. Ver `is_fee_on_input(zero_for_one)` e `is_fee_on_token0(zero_for_one)` en `PoolState`.

`Swap` (V1) implementa la misma tarifa dinámica, enrutamiento de tarifa unilateral y coincidencia de órdenes limitadas que `SwapV2`; la única característica que le falta es soporte Token-2022 — ambas bóvedas deben ser SPL Token clásico. Los pools con cualquier mint Token-2022 deben ser intercambiados a través de `SwapV2`. El agregador y el SDK ya prefieren V2 para cada rama CLMM, por lo que los llamadores no tienen que ramificarse en el tipo de mint.

## `OpenLimitOrder`

Coloca una orden de venta en un tick específico. La orden se sienta en una cohorte FIFO por tick y se completa cuando el precio cruza.

**Argumentos**

```
nonce_index:    u8       // índice de cuenta nonce elegido por el usuario (0..255 por billetera)
zero_for_one:   bool     // true: vende token0 por token1; false: vende token1 por token0
tick_index:     i32      // debe ser múltiplo de pool.tick_spacing
amount:         u64      // monto de token de entrada
```

**Cuentas (abreviadas)**

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| 1 | `payer` | W | S | Propietario de la orden; paga alquiler. |
| 2 | `pool_state` | W |   | |
| 3 | `tick_array` | W |   | La matriz de tick que contiene `tick_index`. |
| 4 | `limit_order_nonce` | W |   | PDA. `init_if_needed` — creada en la primera orden del usuario bajo este `nonce_index`. |
| 5 | `limit_order` | W |   | PDA. `init` aquí. |
| 6 | `input_token_account` | W |   | ATA de entrada del usuario. Verificada por `frozen`. |
| 7 | `output_token_account` | W |   | ATA de **salida** del usuario. No debitada al abrir — requerida solo para que el programa confirme que no está `frozen` (es decir, el relleno puede entregarse después). |
| 8 | `input_vault` | W |   | Bóveda de entrada del pool. |
| 9 | `output_vault` | W |   | Bóveda de salida del pool. Identifica el mint de salida para las verificaciones `output_token_account` / `output_vault_mint`. |
| 10 | `input_vault_mint` |   |   | Manejo de comisión Token-2022. |
| 11 | `output_vault_mint` |   |   | Mint del lado de salida (emparejamiento de verificación congelada). |
| 12 | `input_token_program` |   |   | SPL o Token-2022. |
| 13 | `system_program`, `rent` |   |   | |

<Note>
  **Cambio de lista de cuentas (lanzamiento 2026-07).** `OpenLimitOrder` ahora también toma las cuentas del **lado de salida** — `output_token_account`, `output_vault` y `output_vault_mint` — además del lado de entrada. Se usan solo para validación: el programa rechaza la orden si la cuenta de token de entrada **o** salida del propietario está congelada. Esto garantiza que un relleno pueda ser liquidado a la ATA de salida del propietario, lo que importa para mints Token-2022 de lista blanca / congelados por defecto (por ejemplo, tokens permisionados) donde una cuenta puede no estar aún descongelada. Los clientes construidos contra la lista de cuentas unilateral más antigua deben añadir las tres cuentas de salida.
</Note>

**Precondiciones**

- Ni `input_token_account` ni `output_token_account` están congeladas (de lo contrario `NotApproved`).
- `pool_state.status` permite tanto swap (bit 4) como operaciones de orden limitada (bit 5) (de lo contrario `NotApproved`).
- `tick_index % pool.tick_spacing == 0` y dentro de `[MIN_TICK, MAX_TICK]`.
- `tick_index` está en el **lado correcto de `pool.tick_current`** para la dirección elegida (vender token0 → tick debe estar arriba del actual, y viceversa). Vender en un tick ya cruzado sería coincidido inmediatamente y se rechaza.

**Postcondiciones**

- `limit_order` existe, capturando `tick.order_phase` y `tick.unfilled_ratio_x64` en el momento de apertura.
- `tick.orders_amount += amount` (en la cohorte actual).
- `limit_order_nonce.order_nonce += 1`.
- `OpenLimitOrderEvent` emitido.

**Errores comunes** — `NotApproved` (cuenta de token de entrada o salida congelada, o el pool tiene swap / orden limitada deshabilitada), `InvalidLimitOrderAmount` (cero o por debajo del mínimo del pool), `InvalidTickIndex` (fuera de `[MIN_TICK, MAX_TICK]`, o en el lado incorrecto de `tick_current` para la dirección elegida), `TickAndSpacingNotMatch` (`tick_index % pool.tick_spacing != 0`), `OrderPhaseSaturated`.

## `IncreaseLimitOrder`

Añade a una orden abierta existente. Solo puede ser llamada por el `owner` de la orden.

**Argumentos**

```
amount: u64    // monto de token de entrada adicional
```

**Cuentas** — como `OpenLimitOrder` menos la cuenta nonce; el PDA `limit_order` se pasa directamente.

**Precondiciones**

- `limit_order.owner == signer`.
- La orden aún está en la misma cohorte (`tick.order_phase == limit_order.order_phase`). Si la cohorte ya ha comenzado a completarse, la orden está parcialmente liquidada — el llamador debe llamar a `DecreaseLimitOrder` o `SettleLimitOrder` primero para avanzar.

**Efecto**

- Transfiere `amount` de la ATA del propietario a `input_vault`.
- `limit_order.total_amount += amount`; `tick.orders_amount += amount`.

## `DecreaseLimitOrder`

Reduce o cancela completamente una orden abierta. Paga el resto no completado de vuelta al propietario, más cualquier salida ya liquidada por rellenos parciales anteriores.

**Argumentos**

```
amount:     u64    // monto de token de entrada a retirar (máx. = resto no completado)
amount_min: u64    // piso de slippage en la retirada del lado de entrada
```

**Cuentas** — ambos lados de token de entrada y salida:

| # | Nombre | W | S |
|---|--------|---|---|
| 1 | `owner` |   | S |
| 2 | `pool_state` | W |   |
| 3 | `tick_array` | W |   |
| 4 | `limit_order` | W |   |
| 5 | `input_token_account` | W |   |
| 6 | `output_token_account` | W |   |
| 7 | `input_vault` | W |   |
| 8 | `output_vault` | W |   |
| 9 | `input_vault_mint`, `output_vault_mint` |   |   |
| 10 | `token_program`, `token_program_2022` |   |   |

**Efecto**

- Recalcula el monto completado de la orden desde el `unfilled_ratio_x64` de la cohorte desde la apertura.
- Envía salida completada a `output_token_account`.
- Envía `amount` de entrada no completada de vuelta a `input_token_account`.
- Actualiza `limit_order` en consecuencia. Si el nuevo resto no completado es cero, el programa cierra la cuenta y reembolsa alquiler a `owner`.

## `SettleLimitOrder`

Empuja tokens de salida completados al propietario sin cambiar el resto no completado de la orden. Útil cuando los operadores `auto_withdraw` quieren pagar gradualmente rellenos parciales de larga duración.

**Llamador** — ya sea el `owner` de la orden, o el `limit_order_admin` del programa (una billetera caliente operacional fuera de cadena que ejecuta un bucle de operador automatizado). El operador no tiene otra autoridad — no puede mover fondos de usuario fuera de empujar salida completada a la ATA `owner` de la orden.

**Cuentas**

| # | Nombre | W | S |
|---|--------|---|---|
| 1 | `signer` |   | S | propietario *o* `limit_order_admin` |
| 2 | `pool_state` |   |   | |
| 3 | `tick_array` |   |   | |
| 4 | `limit_order` | W |   | |
| 5 | `output_token_account` | W |   | ATA de salida del propietario. |
| 6 | `output_vault` | W |   | Bóveda de salida del pool. |
| 7 | `output_vault_mint` |   |   | |
| 8 | `output_token_program` |   |   | |

**Efecto**

- Calcula la salida acumulada adeudada usando `(limit_order.unfilled_ratio_x64, tick.unfilled_ratio_x64)`.
- Transfiere el delta a `output_token_account`.
- Actualiza `limit_order.settled_output`.
- **No** cierra la orden; aún está abierta contra cualquier entrada restante.

## `CloseLimitOrder`

Cierra una cuenta de orden completamente consumida. El alquiler siempre se devuelve a `limit_order.owner` independientemente de quién firme.

**Llamador** — ya sea `owner` o `limit_order_admin`.

**Precondiciones**

- La orden tiene cero resto no completado (ya sea `amount == total_amount` fue completado y liquidado, o el propietario disminuyó previamente la orden a cero y olvidó cerrar).

**Efecto**

- Cierra `limit_order`; el alquiler se envía a `limit_order.owner`.

## `CreateDynamicFeeConfig` (admin)

Crea un conjunto de parámetros reutilizable bajo un índice u16.

**Argumentos**

```
index:                       u16
filter_period:               u16   // segundos; por ejemplo, 30
decay_period:                u16   // segundos; por ejemplo, 600. Debe ser > filter_period
reduction_factor:            u16   // 1..10_000; por ejemplo, 5_000 = 50% retención por ventana de decaimiento
dynamic_fee_control:         u32   // 1..100_000; ganancia en la curva volatilidad-a-tarifa
max_volatility_accumulator:  u32   // techo
```

**Cuentas**

| # | Nombre | W | S | Notas |
|---|--------|---|---|-------|
| 1 | `owner` | W | S | Clave de admin codificada. |
| 2 | `dynamic_fee_config` | W |   | PDA, `init` aquí. |
| 3 | `system_program` |   |   | |

**Errores comunes** — `InvalidDynamicFeeConfigParams` si `decay_period <= filter_period` o cualquier campo con valor 0 está fuera de límites.

## `UpdateDynamicFeeConfig` (admin)

Modifica un `DynamicFeeConfig` existente. Los pools que ya capturaron la configuración en el momento de creación **no** se actualizan retroactivamente; solo los pools recién creados que hagan referencia a esta configuración recogerán los nuevos valores.

**Argumentos** — los mismos cinco campos de calibración que `CreateDynamicFeeConfig` (`filter_period`, `decay_period`, `reduction_factor`, `dynamic_fee_control`, `max_volatility_accumulator`); `index` se fija en la creación y no se repasa aquí.

## `CollectProtocolFee` / `CollectFundFee`

Forma idéntica a `CollectProtocolFee` / `CollectFundFee` de CPMM. El firmante debe coincidir con `AmmConfig.owner` / `AmmConfig.fund_owner`. Barre comisiones de protocolo/fondo acumuladas de las bóvedas del pool a un destinatario, pone a cero los campos correspondientes `PoolState.protocol_fees_*` / `fund_fees_*`.

## `InitializeReward`

Añade un nuevo flujo de recompensas a un pool. Hasta 3 flujos pueden estar activos a la vez.

**Argumentos**

```
open_time:     u64
end_time:      u64
emissions_per_second_x64: u128   // Q64.64
```

**Cuentas**

| # | Nombre | W | S |
|---|--------|---|---|
| 1 | `reward_funder` | W | S |
| 2 | `funder_token_account` | W |   |
| 3 | `amm_config` |   |   |
| 4 | `pool_state` | W |   |
| 5 | `operation_state` |   |   | PDA de estado de operación CLMM que controla la creación de recompensas. |
| 6 | `reward_token_mint` |   |   |
| 7 | `reward_token_vault` | W |   | `init` aquí. |
| 8 | `reward_token_program` |   |   |
| 9 | `system_program`, `rent` |   |   |

**Precondiciones**

- Menos de 3 flujos actualmente activos en el pool.
- El financiador deposita `total_emission = emissions_per_second × (end_time − open_time)` de token de recompensa en la bóveda como parte de esta instrucción.
- Mint de recompensa en la lista blanca por `operation_state`.

## `SetRewardParams`

Extiende, recarga o cambia la tasa de emisión en un flujo de recompensas existente. Típicamente llamado por un creador de pool o el multisig de Raydium. Las restricciones viven en cadena: generalmente puedes extender `end_time` o aumentar emisiones, no reducirlas retroactivamente. Verifica la lista de propietarios de `operation_state`.

## `UpdateRewardInfos`

Pura contabilidad — liquida `reward_growth_global_x64` al tiempo actual multiplicando `emissions_per_second × Δt / liquidity`. Llamado internamente por cada instrucción que toca liquidez. Expuesto como instrucción independiente porque actores externos (UIs, cranks) a veces quieren activarlo.

## `CollectReward`

El propietario de la posición reclama tokens de recompensa adeudados.

**Cuentas**

| # | Nombre | W | S |
|---|--------|---|---|
| 1 | `nft_owner` |   | S |
| 2 | `nft_account` |   |   | ATA del propietario que contiene el NFT de posición. |
| 3 | `personal_position` | W |   |
| 4 | `pool_state` | W |   |
| 5 | `protocol_position` |   |   |   |
| 6 | `reward_token_vault` | W |   |
| 7 | `recipient_token_account` | W |   |
| 8 | `token_program` |   |   |
| 9 | `token_program_2022` |   |   |

**Efecto**

- Liquida crecimiento de recompensas (mismo patrón que comisiones).
- Transfiere el monto adeudado a la ATA del destinatario, pone a cero `reward_amount_owed[i]`.

## Matriz de cambio de estado

| Instrucción | `pool.liquidity` | `pool.fee_growth_global` | `pool.reward_growth_global` | `personal_position.liquidity` | Matriz de tick |
|-------------|------------------|---------------------------|------------------------------|--------------------------------|------------|
| `CreatePool` | 0 | 0 | — | — | — |
| `OpenPosition` | + si está en rango | — | — | nueva | añade liquidity_gross/net |
| `IncreaseLiquidity` | + si está en rango | liquida adeudado | liquida adeudado | + | ajusta |
| `DecreaseLiquidity` | − si está en rango | liquida adeudado | liquida adeudado | − | ajusta |
| `ClosePosition` | — | — | — | destruida | — |
| `SwapV2` | ± en cruces | + | — | — | cruza y voltea fuera; coincide cohortes de orden limitada |
| `OpenLimitOrder` | — | — | — | — | `orders_amount += amount` en el tick objetivo |
| `IncreaseLimitOrder` | — | — | — | — | `orders_amount += amount` |
| `DecreaseLimitOrder` | — | — | — | — | `orders_amount -=`, puede cerrar cohorte |
| `SettleLimitOrder` | — | — | — | — | — (solo lectura en tick) |
| `CloseLimitOrder` | — | — | — | — | — |
| `CreateCustomizablePool` | 0 | 0 | — | — | — |
| `UpdateRewardInfos` | — | — | + | — | — |
| `CollectReward` | — | — | liquida adeudado | — | — |

## Dónde ir a continuación

- [`products/clmm/code-demos`](/es/products/clmm/code-demos) — ejemplos TypeScript ejecutables.
- [`products/clmm/fees`](/es/products/clmm/fees) — detalles sobre acumulación de comisiones y recompensas.
- [`reference/error-codes`](/es/reference/error-codes) — tabla de errores Anchor CLMM completa.

Fuentes:

- [`raydium-io/raydium-clmm` — `programs/amm/src/instructions`](https://github.com/raydium-io/raydium-clmm)
- Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`
