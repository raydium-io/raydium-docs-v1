---
title: "Direcciones de programas"
description: "IDs de programas canónicos de Raydium y PDAs compartidas en Solana mainnet-beta y devnet. Todas las demás páginas enlazan aquí en lugar de codificar direcciones."
---

<Info>
  **Esta página fue traducida automáticamente por IA. La versión en inglés es la fuente autorizada.**

  [Ver versión en inglés →](/reference/program-addresses)
</Info>

<Info>
  **Fuente de verdad.** Esta página es el único lugar en la documentación que lista direcciones de programas textualmente. Las demás páginas enlazan aquí. Si un ID cambia, actualízalo **solo aquí** y todas las referencias en el sitio permanecerán consistentes.

  Siempre verifica los valores contra la API en vivo (`https://api-v3.raydium.io/main/info`) antes de firmar transacciones reales.
</Info>

## Mainnet-beta

<Tip>
  Regla de oro: si un ID de programa en la red no coincide con la tabla siguiente, **no firmes la transacción.** Un ID de programa que no coincide es la forma más fácil de perder fondos en Solana.
</Tip>

### Programas en cadena

| Programa | ID del programa | Fuente |
|---------|------------|--------|
| **AMM v4** (AMM híbrido + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (AMM estándar) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (Liquidez concentrada) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (curva tipo StableSwap) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | fuente no disponible públicamente |
| **Farm v3** (staking RAY heredado) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | fuente no disponible públicamente |
| **Farm v5** (granjas de ecosistema heredadas) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | fuente no disponible públicamente |
| **Farm v6** (granjas de ecosistema actuales) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | fuente no disponible públicamente |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | fuente no disponible públicamente |
| **AMM Routing** (enrutador en cadena de Raydium) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | fuente no disponible públicamente |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | fuente no disponible públicamente |

Notas:

- AMM v4 y Farm v3/v5 se **retienen para pools y posiciones existentes**. La interfaz de usuario de Raydium y el SDK enrutan la creación de nuevos pools y nuevas granjas de ecosistema a CPMM, CLMM y Farm v6 respectivamente — consulta [`protocol-overview/versions-and-migration`](/es/protocol-overview/versions-and-migration).
- El ID de Farm v3 anterior también funciona como el programa de staking de activo único \$RAY. El comportamiento es idéntico a Farm v3 para staking de LP.
- **Stable AMM** es un programa separado que el programa AMM Routing puede dirigirse junto con AMM v4, CPMM y CLMM. La liquidez es escasa en comparación con los otros tres programas y el SDK no expone una API de primera clase para él; los integradores que enrutan a través de él generalmente lo hacen a través del enrutador.
- **Disponibilidad del código fuente.** De los programas en cadena anteriores, solo `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM) y `raydium-clmm` (CLMM) se distribuyen con repositorios de código fuente público bajo [`github.com/raydium-io`](https://github.com/raydium-io). Stable AMM, LaunchLab, AMM Routing, Burn & Earn / LP Lock y los programas Farm no están disponibles públicamente — verifica contra la API en vivo, el bytecode en cadena y los IDLs publicados en [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl) en su lugar.

### Autoridad de administrador compartida

Todos los programas basados en Anchor (CLMM, CPMM, LaunchLab, Lock) comparten una única `Pubkey` de administrador codificada para control de acceso a nivel de instrucción en rutas de administrador (como `CreateAmmConfig` o `UpdatePoolStatus`):

| Cluster | Administrador |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

Las autoridades operacionales a nivel de cuenta (p. ej. `protocol_owner`, `fund_owner` en `AmmConfig` de CPMM/CLMM, o las billeteras de migración en `GlobalConfig` de LaunchLab) se almacenan en cadena y pueden diferir del administrador del programa. Léelas directamente de la cuenta de configuración relevante antes de enviar transacciones de alto riesgo.

### Autoridad creadora de PDA de permisos de CPMM

`CreatePermissionPda` acepta el administrador del programa compartido anterior o un propietario dedicado. `ClosePermissionPda` permanece restringido al administrador del programa compartido.

| Cluster | Propietario dedicado de `CreatePermissionPda` |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### Creador de configuración delegado de LaunchLab

`CreateConfig` acepta el administrador del programa compartido o la autoridad delegada siguiente. El rol delegado puede crear un nuevo `GlobalConfig`; no puede llamar a la ruta `UpdateConfig` de solo administrador para una cuenta existente.

| Cluster | Autoridad delegada de `CreateConfig` |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### Autoridades de congelación de emisor restringido de CLMM

Cuando cualquiera de los mints de bóveda en un pool de CLMM tiene una de las autoridades de congelación siguientes, las nuevas posiciones abiertas a través de una ruta V2 tienen su cuenta de token NFT de posición congelada. La entrada inicial es la autoridad utilizada por la detección de activos Superstate de CLMM. Estas son **autoridades de emisor de activo subyacente utilizadas solo como claves de coincidencia**. El PDA del pool de CLMM, no la clave del emisor, se convierte en autoridad de congelación para el mint de NFT de posición.

| Cluster | Autoridad de congelación de emisor restringido |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

La lista está codificada en el programa CLMM. Agregar o eliminar una autoridad requiere una actualización del programa.

### Convenciones de configuración compartida / PDA

Varios programas de Raydium exponen **cuentas de configuración** cuyas claves públicas son estables y se enumeran en la API pública. Prefiere la búsqueda de API sobre codificar:

```
# Configuraciones de tarifa CPMM (devuelve un array de {id, index, tradeFeeRate, ...})
GET https://api-v3.raydium.io/main/cpmm-config

# Configuraciones de tarifa CLMM
GET https://api-v3.raydium.io/main/clmm-config
```

Parámetros de tarifa de `AmmConfig` de CPMM predeterminado índice `0` (pool estándar 0.25%), como referencia:

| Campo | Valor | Significado |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 0.25% del volumen de operaciones |
| `protocol_fee_rate` | `120000` | 12% **de la tarifa de operación** (no volumen) enrutado al protocolo |
| `fund_fee_rate` | `40000` | 4% **de la tarifa de operación** enrutado al multisig del fondo |
| `creator_fee_rate` | `500` | 0.05% del volumen de operaciones al creador del pool (opcional) |

Consulta [`products/cpmm/fees`](/es/products/cpmm/fees) para ver cómo se componen las divisiones, y [`reference/fee-comparison`](/es/reference/fee-comparison) para la matriz entre productos.

### Semillas de PDA

Las semillas siguientes son **canónicas** y se utilizan tanto por el SDK como por CPIs en cadena. Siempre calcula PDAs; no codifiques direcciones derivadas.

```ts
// CPMM — todas las semillas son cadenas ASCII estáticas a menos que se indique lo contrario.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` están **ordenados por orden de bytes de clave pública** (token0 < token1) antes de hacer hash. Hacerlo incorrectamente produce un PDA válido para un pool inexistente.

Las semillas equivalentes de CLMM siguen el mismo estilo; consulta [`products/clmm/accounts`](/es/products/clmm/accounts).

## Devnet

| Programa | ID del programa |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM Routing** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DLockwT7X7sxtLmGH9g5kmfcjaBtncdbUmi738m5bvQC` |
| **Farm v3 / v5 / v6** | No se publica de forma confiable para devnet — confirma a través de la API en vivo (`https://api-v3-devnet.raydium.io/main/info`) antes de usar. |

Base de API REST de Devnet: `https://api-v3-devnet.raydium.io/` (misma forma de ruta que mainnet).

## Cómo verificar una dirección en cadena

1. **Solana Explorer.** Pega la dirección en [explorer.solana.com](https://explorer.solana.com) y confirma que está marcada como `Program` con una autoridad de actualización actual. Mainnet-beta debe mostrar implementaciones firmadas por la autoridad de actualización de Raydium.
2. **CLI.** Usa `solana program show <PROGRAM_ID> -u mainnet-beta` para inspeccionar slot de implementación, cargador BPF, autoridad de actualización y longitud de datos. Registra estos en tu runbook.
3. **Adjunto de IDL.** Consulta el IDL en cadena con `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet`. El campo `address` del IDL debe coincidir. Los mismos IDLs se replican en [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) — compara el IDL en cadena contra el repositorio antes de confiar en él.
4. **Autoridades de configuración/administrador.** Para cuentas de configuración CPMM/CLMM, lee el campo `owner`/`protocol_owner` y confirma que coincide con el multisig actual de Raydium publicado en [`security/admin-and-multisig`](/es/security/admin-and-multisig).

Si cualquiera de las cuatro comprobaciones anteriores no está de acuerdo con esta página, trata esta página como incorrecta y abre un problema antes de escribir código nuevo contra las direcciones.

## Actualizar esta página

- Los cambios en los IDs de programas son críticos para la seguridad. **No** envíes un cambio de ID de programa sin (a) enlazar al anuncio de Raydium, (b) agregar una entrada en [`reference/changelog`](/es/reference/changelog), y (c) ejecutar una verificación de enlaces sobre la documentación para confirmar que ninguna página aún hace referencia al valor anterior.
- Las deprecaciones permanecen en la tabla con una nota de estado en lugar de ser eliminadas — los pools existentes aún se resuelven a través del programa anterior.

Fuentes:

- API en vivo: `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
