---
title: "Alamat program"
description: "ID program Raydium kanonik dan PDA bersama di Solana mainnet-beta dan devnet. Setiap halaman lain menautkan ke sini daripada hardcoding alamat."
---

<Info>
  **Halaman ini diterjemahkan secara otomatis oleh AI. Versi bahasa Inggris adalah acuan resmi.**

  [Lihat versi bahasa Inggris →](/reference/program-addresses)
</Info>

<Info>
  **Sumber kebenaran.** Halaman ini adalah satu-satunya tempat dalam dokumentasi yang mencantumkan alamat program secara harfiah. Halaman lain menautkan ke sini. Jika ID berubah, perbarui **hanya di sini** dan setiap referensi di situs tetap konsisten.

  Selalu silang-periksa nilai terhadap API langsung (`https://api-v3.raydium.io/main/info`) sebelum menandatangani transaksi nyata.
</Info>

## Mainnet-beta

<Tip>
  Aturan praktis: jika ID program di lapangan tidak cocok dengan tabel di bawah, **jangan tandatangani transaksi.** ID program yang tidak cocok adalah cara paling mudah untuk kehilangan dana di Solana.
</Tip>

### Program on-chain

| Program | ID Program | Sumber |
|---------|------------|--------|
| **AMM v4** (Hybrid AMM + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (Standard AMM) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (Concentrated Liquidity) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (kurva gaya StableSwap) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | sumber tidak tersedia untuk publik |
| **Farm v3** (staking RAY warisan) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | sumber tidak tersedia untuk publik |
| **Farm v5** (farm ekosistem warisan) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | sumber tidak tersedia untuk publik |
| **Farm v6** (farm ekosistem saat ini) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | sumber tidak tersedia untuk publik |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | sumber tidak tersedia untuk publik |
| **AMM Routing** (router on-chain Raydium) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | sumber tidak tersedia untuk publik |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | sumber tidak tersedia untuk publik |

Catatan:

- AMM v4 dan Farm v3/v5 **dipertahankan untuk pool dan posisi yang ada**. UI Raydium dan SDK mengarahkan pembuatan pool baru dan farm ekosistem baru ke CPMM, CLMM, dan Farm v6 masing-masing — lihat [`protocol-overview/versions-and-migration`](/id/protocol-overview/versions-and-migration).
- ID Farm v3 di atas juga berfungsi sebagai program staking aset tunggal \$RAY. Perilakunya identik dengan Farm v3 untuk staking LP.
- **Stable AMM** adalah program terpisah yang dapat ditargetkan oleh program AMM Routing bersama dengan AMM v4, CPMM, dan CLMM. Likuiditas tipis dibandingkan dengan tiga program lainnya dan SDK tidak mengekspos API kelas satu untuk itu; integrator yang merutekan melaluinya umumnya melakukannya melalui router.
- **Ketersediaan kode sumber.** Dari program on-chain di atas, hanya `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM), dan `raydium-clmm` (CLMM) yang dilengkapi dengan repositori sumber publik di bawah [`github.com/raydium-io`](https://github.com/raydium-io). Stable AMM, LaunchLab, AMM Routing, Burn & Earn / LP Lock, dan program Farm tidak tersedia untuk publik — verifikasi terhadap API langsung, bytecode on-chain, dan IDL yang dipublikasikan di [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl) sebagai gantinya.

### Otoritas admin bersama

Setiap program Raydium — termasuk AMM v4 — berbagi satu `Pubkey` admin yang dikodekan keras untuk kontrol akses tingkat instruksi ke jalur admin. Ini adalah `config_feature::amm_owner::ID` di AMM v4 (gating `SetParams`, `CreateConfigAccount` dan `UpdateConfigAccount`, dan digunakan sebagai `pnl_owner` default) dan `admin::ID` di program Anchor — CLMM, CPMM, LaunchLab, Lock — gating jalur seperti `CreateAmmConfig` atau `UpdatePoolStatus`:

| Cluster | Admin |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

Otoritas operasional tingkat akun (misalnya `protocol_owner`, `fund_owner` pada `AmmConfig` CPMM/CLMM, atau dompet migrasi pada `GlobalConfig` LaunchLab) disimpan on-chain dan mungkin berbeda dari admin program. Bacalah langsung dari akun konfigurasi yang relevan sebelum mengirim transaksi berisiko tinggi.

### Otoritas pembuat permission-PDA CPMM

`CreatePermissionPda` menerima baik admin program bersama di atas atau pemilik khusus. Sejak upgrade CPMM 2026-09, `ClosePermissionPda` menerima dua penandatangan yang sama; sebelumnya hanya admin.

| Cluster | Pemilik `CreatePermissionPda` khusus |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### Otoritas registri support-mint

`CreateSupportMintAssociated` dan `CloseSupportMintAssociated` menerima baik admin program bersama atau pemilik khusus program di bawah. Mendaftarkan mint mengakuinya melewati daftar allow-list ekstensi Token-2022 program itu — lihat [`reference/token-2022-support`](/id/reference/token-2022-support#the-one-bypass-the-per-mint-registry). CPMM dan CLMM masing-masing menyimpan PDA registri mereka sendiri dan otoritas mereka sendiri; tidak ada kunci yang bekerja di program lain.

| Program | Cluster | Pemilik support-mint khusus |
|---------|---------|------------------------------|
| CPMM | mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| CPMM | devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |
| CLMM | mainnet-beta | `RayVyjyJQz9vAi126A4sGexKnSU1XeZaHTRcM1mZMPY` |
| CLMM | devnet | `rayf3nEbb3bnfN6RDGFpqPbjc5uUa3tRUzu6UVYrRx5` |

### Dompet fee-owner CPMM

Sejak upgrade CPMM 2026-09, `CreateAmmConfig` menulis kunci tetap ini ke dalam `protocol_owner` dan `fund_owner` konfigurasi baru daripada menyalin kunci penandatangan admin. `CollectProtocolFee` dan `CollectFundFee` menerima kunci yang disimpan pada konfigurasi **atau** admin program bersama (`|| crate::admin::ID`), jadi admin tetap menjadi penandatangan yang diterima — yang berubah hanya di mana hasil masuk secara default.

| Cluster | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  Ini adalah nilai yang *ditulis program ke dalam konfigurasi baru*, bukan deskripsi yang sudah ada. Akun `AmmConfig` yang dibuat sebelum upgrade menyimpan apa pun yang disimpan di dalamnya. Selalu baca `protocol_owner` / `fund_owner` dari akun sebelum membangun transaksi koleksi.
</Warning>

### Dompet koleksi biaya protokol

Di mana bagian sisi protokol dari biaya perdagangan setiap program terakumulasi. Ini adalah dompet pemegang token biasa, jadi satu alamat membawa set mint SPL campuran (wSOL dan USDC dari pool SOL/USDC, wSOL dan RAY dari pool SOL/RAY, dan seterusnya).

| Program | Alamat koleksi | Disimpan sebagai |
|---------|--------------------|-----------|
| CLMM | `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416` | `AmmConfig.owner` pada semua 21 konfigurasi |
| CPMM | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `AmmConfig.protocol_owner` (lihat [Dompet fee-owner CPMM](#dompet-fee-owner-cpmm)) |
| AMM v4 | `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF` | `AmmConfig.pnl_owner` pada PDA konfigurasi tunggal |

Bagian protokol Stable AMM terakumulasi dalam vault sebagai `out_put.need_take_pnl_*` dan disapu oleh `WithdrawPnl` daripada duduk di dompet berdiri. Untuk CPMM, baca `protocol_owner` dari `AmmConfig` spesifik sebelum membangun transaksi koleksi — konfigurasi yang dibuat sebelum upgrade 2026-09 mungkin menyimpan kunci yang berbeda.

### Dompet koleksi lamport berlebih

Masing-masing dari tiga program yang diupgrade pada 2026-09-09 mendapatkan instruksi admin yang mengembalikan lamport yang dipegang di atas minimum bebas sewa pada akun yang dikendalikan program, mengikuti [pengurangan sewa SIMD-0437](/solana-fundamentals/rent-and-reclaimable-rent). Setiap program mengkodekan dompet tujuan sendiri, dan masing-masing adalah kunci khusus — bukan admin program bersama.

| Program | Instruksi | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM dan LaunchLab juga menerima admin program bersama sebagai penandatangan. AMM v4 menerima **hanya** dompet di atas — tidak ada fallback admin. AMM v4 juga mengompilasi kunci testnet terpisah (`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`) di bawah fitur `testnet` nya.

Instruksi ini hanya memindahkan lamport; mereka tidak pernah menyentuh saldo token, menutup akun, atau mengubah status pool. Lihat [`products/amm-v4/instructions`](/id/products/amm-v4/instructions#withdrawexcesslamports), [`products/cpmm/instructions`](/id/products/cpmm/instructions#collectexcesslamports), dan [`products/launchlab/instructions`](/id/products/launchlab/instructions#collectexcesslamports).

### Pembuat konfigurasi delegasi LaunchLab

`CreateConfig` menerima baik admin program bersama atau otoritas delegasi di bawah. Peran delegasi dapat membuat `GlobalConfig` baru; itu tidak dapat memanggil jalur `UpdateConfig` khusus admin untuk akun yang ada.

| Cluster | Otoritas `CreateConfig` delegasi |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### Otoritas freeze issuer terbatas CLMM

Ketika salah satu vault mint dalam pool CLMM memiliki salah satu otoritas freeze di bawah, posisi baru yang dibuka melalui jalur V2 memiliki akun token NFT posisi mereka dibekukan. Daftarnya adalah `frozen_position_nft_authorities::IDS` dalam program CLMM, dan entri tunggalnya adalah otoritas issuer yang deteksi Superstate yang dihapus CLMM juga cocok. Ini adalah **otoritas issuer aset dasar yang digunakan hanya sebagai kunci kecocokan**. PDA pool CLMM, bukan kunci issuer, menjadi otoritas freeze untuk mint NFT posisi.

| Cluster | Otoritas freeze issuer terbatas |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

Daftarnya dikodekan dalam program CLMM. Menambah atau menghapus otoritas memerlukan upgrade program.

### Konvensi konfigurasi bersama / PDA

Beberapa program Raydium mengekspos **akun konfigurasi** yang kunci publiknya stabil dan tercantum di API publik. Lebih suka pencarian API daripada hardcoding:

```
# Konfigurasi biaya CPMM (mengembalikan array {id, index, tradeFeeRate, ...})
GET https://api-v3.raydium.io/main/cpmm-config

# Konfigurasi biaya CLMM
GET https://api-v3.raydium.io/main/clmm-config
```

### Penerima biaya pembuatan pool

Baik CPMM maupun AMM v4 memvalidasi tujuan biaya pembuatan pool mereka **dengan alamat yang tepat**, jadi harus sudah ada di cluster apa pun yang Anda uji. CLMM tidak memiliki biaya pembuatan pool terpisah.

| Program | Konstanta | mainnet-beta | devnet |
|---------|----------|--------------|--------|
| CPMM | `create_pool_fee_reveiver::ID` (typo ada di sumber) | `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` | `3oE58BKVt8KuYkGxx8zBojugnymWmBiyafWgMrnb6eYy` |
| AMM v4 | `config_feature::create_pool_fee_address::ID` | `7YttLkHDoNj9wyDur5pM1ejNaAvT9X4eqaYcHQqtj2G5` | `9y8ENuuZ3b19quffx9hQvRVygG5ky6snHfRvGpuSfeJy` |

AMM v4 juga mengompilasi `3TRTX4dXUpp2eqxi3tvQDFYUV7SdDJjcPE3Y4mbtftaX` di bawah fitur `testnet` nya. CPMM menolak ketidakcocokan dengan `ConstraintAddress` Anchor (`2012`); AMM v4 dengan `InvalidFee`.

### Penjaga limit-order CLMM

`SettleLimitOrder` dan `CloseLimitOrder` menerima baik `owner` pesanan sendiri atau kunci penjaga tingkat program di bawah. Penjaga tidak dapat membuka, menambah atau mengurangi pesanan, dan output selalu mendarat di akun pemilik pesanan — lihat [`security/admin-and-multisig`](/id/security/admin-and-multisig).

| Cluster | `limit_order_admin` |
|---------|---------------------|
| mainnet-beta | `Ray8HHtixhL9zvnokMyELCVGp622PDPJj96zcVC9RWp` |
| devnet | `DRaypkxM96mjYYnqMmuYjsSL3stHxLf1CvYuTcqxUaav` |

Parameter biaya `AmmConfig` default CPMM indeks `0` (pool 0,25% standar), untuk referensi:

| Bidang | Nilai | Makna |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 0,25% dari volume perdagangan |
| `protocol_fee_rate` | `120000` | 12% **dari biaya perdagangan** (bukan volume) yang dialihkan ke protokol |
| `fund_fee_rate` | `40000` | 4% **dari biaya perdagangan** yang dialihkan ke multisig dana |
| `creator_fee_rate` | `500` | 0,05% dari volume perdagangan ke pembuat pool (opsional) |

Lihat [`products/cpmm/fees`](/id/products/cpmm/fees) untuk cara komposisi split, dan [`reference/fee-comparison`](/id/reference/fee-comparison) untuk matriks lintas produk.

### Benih PDA

Benih di bawah adalah **kanonik** dan digunakan oleh SDK dan CPI on-chain. Selalu hitung PDA; jangan hardcode alamat turunan.

```ts
// CPMM — semua benih adalah string ASCII statis kecuali dicatat.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` **diurutkan berdasarkan urutan byte kunci publik** (token0 < token1) sebelum hashing. Melakukan ini dengan salah menghasilkan PDA yang valid untuk pool yang tidak ada.

Benih CLMM yang setara mengikuti gaya yang sama; lihat [`products/clmm/accounts`](/id/products/clmm/accounts).

## Devnet

| Program | ID Program |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM Routing** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DRay25Usp3YJAi7beckgpGUC7mGJ2cR1AVPxhYfwVCUX` |
| **Farm v3 / v5 / v6** | Tidak dapat diandalkan dipublikasikan untuk devnet — konfirmasi melalui API langsung (`https://api-v3-devnet.raydium.io/main/info`) sebelum digunakan. |

Basis REST API devnet: `https://api-v3-devnet.raydium.io/` (bentuk rute yang sama seperti mainnet).

## Otoritas upgrade

Setiap program Raydium di mainnet-beta dikerahkan di bawah BPF Upgradeable Loader dengan **otoritas upgrade yang sama** — vault multisig upgrade program Squads:

| | Nilai |
|---|---|
| Otoritas upgrade (semua program) | `FytDrVzDybM1TwFQPGb8qaxZR7dBCzNeqT3vtQsceZQK` |
| Akun konfigurasi multisig Squads | `tr8rgazUrZzgdkfc6Q622nVJHMMzh29trdBE2uBHb4u` |
| Program Squads | `SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf` (v4) |

Diverifikasi on-chain 2026-09-09 untuk AMM v4, CPMM, CLMM, Stable AMM, Farm v6, LaunchLab, AMM Routing dan Lock — kedelapannya melaporkan otoritas itu, dan tidak ada yang tidak dapat diubah. Periksa ulang dengan `solana program show <PROGRAM_ID> -u mainnet-beta` sebelum penerapan berisiko tinggi; lihat [`security/admin-and-multisig`](/id/security/admin-and-multisig) untuk ambang multisig dan praktik penandatanganan.

## Akun konfigurasi tingkat program

| Program | Akun konfigurasi | Isi |
|---------|----------------|----------|
| AMM v4 | `9DCxsMizn3H1hprZ7xWe6LDzeUeZBksYFpBWBtSf1PQX` | PDA `AmmConfig` tunggal pada benih `["amm_config_account_seed"]`. Menyimpan `pnl_owner` = `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF`, `cancel_owner` = `RAYdmrNjdRSu7ErPv9s2BqDtRoFa1QECn7UJYaC2tR3`, dan `create_pool_fee` = `150000000` (0,15 SOL). |
| CPMM | 21 akun `AmmConfig` (indeks 0–20) | Indeks 0 adalah `D4FPEruKEHrG5TenZ2mpDGEfu1iUvTiqBxvpU8HLBvC2`. Lihat [`reference/fee-comparison`](/id/reference/fee-comparison) untuk set tingkat. |
| CLMM | 21 akun `AmmConfig` (indeks 0–20) | Indeks 1 (0,25%, jarak tick 60) adalah `E64NGkDLLCdQ2yFNPcavaKptrEgmiQaNykUuLC1Qgwyp`. `owner` = `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416`, `fund_owner` = `FundHfY8oo8J9KYGyfXFFuQCHe7Z1VBNmsj84eMcdYs4` pada semua 21. |

Semua nilai dibaca dari mainnet-beta pada 2026-09-09. Tingkat biaya pada akun ini dapat diubah admin — bacalah secara langsung daripada caching.

## Cara memverifikasi alamat on-chain

1. **Solana Explorer.** Tempel alamat ke dalam [explorer.solana.com](https://explorer.solana.com) dan konfirmasi itu ditandai `Program` dengan otoritas upgrade saat ini. Mainnet-beta harus menunjukkan deploy yang ditandatangani oleh otoritas upgrade Raydium.
2. **CLI.** Gunakan `solana program show <PROGRAM_ID> -u mainnet-beta` untuk memeriksa slot deploy, pemuat BPF, otoritas upgrade, dan panjang data. Catat ini dalam runbook Anda.
3. **Lampiran IDL.** `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet` bekerja untuk **CLMM, LaunchLab dan Lock**, yang masih menyimpan akun `anchor:idl` warisan yang dimiliki oleh program itu sendiri. Itu **tidak** bekerja untuk CPMM: IDL on-chain CPMM pindah ke program Program Metadata (`ProgM6JCCvbYkfKqJYHePx4xxSUSqJp7rh8Lyv7nk7S`) dalam upgrade 2026-09, jadi ambil dari sana sebagai gantinya. AMM v4, Stable AMM dan Farm tidak memiliki IDL on-chain. IDL dicerminkan di [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) — diff IDL on-chain terhadap repo sebelum mempercayainya.
4. **Otoritas konfigurasi/admin.** Untuk akun konfigurasi CPMM/CLMM, baca bidang `owner`/`protocol_owner` dan konfirmasi itu cocok dengan multisig Raydium saat ini yang dipublikasikan di [`security/admin-and-multisig`](/id/security/admin-and-multisig).

Jika salah satu dari empat pemeriksaan di atas tidak setuju dengan halaman ini, perlakukan halaman ini sebagai salah dan buka masalah sebelum menulis kode baru terhadap alamat.

## Memperbarui halaman ini

- Perubahan ID program sangat penting untuk keselamatan. **Jangan** kirim perubahan ID program tanpa (a) menautkan ke pengumuman Raydium, (b) menambahkan entri di [`reference/changelog`](/id/reference/changelog), dan (c) menjalankan pemeriksaan tautan atas dokumentasi untuk mengonfirmasi tidak ada halaman yang masih mereferensikan nilai lama.
- Penghentian tetap dalam tabel dengan catatan status daripada dihapus — pool yang ada masih diselesaikan melalui program lama.

Sumber:

- API langsung: `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
