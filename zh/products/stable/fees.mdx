---
title: "Stable AMM 费用"
description: "一个低费用等级：0.02% 总交易费，88% LP / 12% 协议。与 AMM v4 相同的 Fees 结构，不同的数字。无基金费，无创建者费。"
---

<Info>
  **本页内容由 AI 自动翻译，所有内容以英文版本为准。**

  [查看英文版 →](/products/stable/fees)
</Info>

## 费用模型

Stable AMM 复用了 AMM v4 的 **`Fees` 结构**，但不复用 AMM v4 的**数字**。Stable 池收取的费用低得多，这正是你对为价格相近资产设计的池所期望的。每个池只有一个费用等级（在初始化时设置）；池无法重新配置为更高的等级。

| 字段 | 链上取值 | 含义 |
|-------|----------------|---------|
| `swap_fee_numerator / swap_fee_denominator` | `2 / 10_000` | **总交易费：输入量的 0.02%**。这是实际收取的唯一费用。 |
| `trade_fee_numerator / trade_fee_denominator` | `0 / 10_000` | **零。** 旧版字段，曾用于 OpenBook 订单定价；为保持布局兼容性而保留，已不再读取。 |
| `pnl_numerator / pnl_denominator` | `12 / 100` | **协议的费用分成：12%** — 即交易量的 `0.02% × 12% = 0.0024%`。 |
| `min_separate_numerator / min_separate_denominator` | `4 / 10_000` | 从总费用中拆分 PnL 时使用的最小分离阈值。 |

<Note>
  这些不是"默认值" — 它们就是**每一个**活跃主网 Stable 池上的取值。三个池（USDT/USDC `2EXiumdi14E9b8Fy62QcA5Uh6WdHS2b38wtSxp72Mibj`、`9DTY3rv8xRa3CnoPoWJCMcQUSY7kUHZAoFKNsBhx8DDz`、`9f4FtV6ikxUZr8fAjKSGNPPnUHJEwi4jNk8d79twbyFf`）都带有上面这套完全相同的值，而且随着 `SetParams` 在 2026-06-22 升级中被移除，已经不存在任何能改变它们的指令。通过从主网解码 `AmmInfo.fees` 验证。
</Note>

**没有基金费**，也**没有创建者费** — 这些是 AMM v4 之后的发明（CPMM/CLMM）。Stable AMM 早于这一惯例。

## 分成如何计算

每次交换时：

```
gross_fee = ceil(amount_in * (swap_fee_numerator / swap_fee_denominator))    // 0.02% of amount_in
pnl_portion = gross_fee * (pnl_numerator / pnl_denominator)                  // 12% of gross_fee
lp_portion = gross_fee − pnl_portion                                          // 88% of gross_fee
```

- **`lp_portion`** 留在金库中，增加 `k`，并在赎回时使 LP 受益。
- **`pnl_portion`** 增加 `AmmInfo.out_put.need_take_pnl_coin` 或 `need_take_pnl_pc`（取决于输入代币），并由 `WithdrawPnl` 清扫。

与 CPMM 相同的**不变量保留技巧**：PnL 金额物理上位于金库中，但从曲线数学中使用的"有效储备"中减去，因此移除它不会改变价格。

## OpenBook PnL（已停用）

在其早期，该池还在 OpenBook 上发布限价订单；当这些订单成交时，它赚取或损失做市商/接单者价差，在 `MonitorStep` 期间结算，并在 `out_put.total_pnl_{coin,pc}` 计数器中跟踪结果。**该路径已休眠多年** — 该池没有未平仓订单，因此唯一的费用累积是上述 0.0024% 的协议分成。`total_pnl_*` 计数器仍在布局中，但不再增加。[2026-06-22 升级](/zh/reference/changelog)移除了剩余的 OpenBook 代码（包括 `MonitorStep`）。CPMM 成为新池的默认选择，部分原因是这种与 OpenBook 的耦合；Stable AMM 与订单簿无关。

## 收集

Raydium 多签（或控制 `amm_admin` 的任何人）调用 `WithdrawPnl` 来清扫：

1. 将 `need_take_pnl_coin` 和 `need_take_pnl_pc` 从金库转移到管理员指定的账户。
2. 将计数器清零。

该操作不会移动曲线。LP 看不到价格变化。

<Warning>
  `WithdrawPnl` 在 2026-06-22 升级中进行了更改：它现在使用**固定的 10 账户、仅限管理员的布局，没有兼容性路径**（旧布局失败并出现 `InvalidTokenCoin` 等错误），当池的可用资金不足以覆盖 PnL 时，它直接返回 `TakePnlError` 而不是禁用池。请参阅 [`products/stable/instructions`](/zh/products/stable/instructions#withdrawpnl)。
</Warning>

## LP 费用赎回

没有专门的"收集费用"指令。LP 费用在金库中累积，增加储备。LP 通过 `Withdraw` 销毁 LP 来实现这些费用。LP 代币的价值随着储备增长而增长。

## 可视化：\$10,000 交易量的去向

在针对一个活跃 Stable 池、以 USDC 为主的 \$10,000 `Swap` 上：

```
Gross trade fee (0.02%):       $2.0000
  LP share   (0.0176%):         $1.7600  → stays in pool, raises k
  PnL share  (0.0024%):         $0.2400  → need_take_pnl_pc, swept by WithdrawPnl
User receives (minus curve):  $9,998.00
```

同样的 \$10,000 通过 AMM v4 池要支付 \$25.00，通过 CPMM `index=0` 池也是 \$25.00 —— **贵 12.5 倍**。这个差距正是该产品的意义：稳定币对的价格波动极小，所以 LP 只需要极少的库存风险补偿，而紧凑的曲线加上低费用正是让大额稳定币之间的互换保持竞争力的原因。

## 对比表

| | Stable AMM | AMM v4 | CPMM `index=0` |
|---|---|---|---|
| 交易费 | **0.02%** | 0.25% | 0.25% |
| LP | 0.0176% | 0.22% | 0.21% |
| 协议 | 0.0024% | 0.03% | 0.03% |
| 基金 | 无 | 无 | 0.01% |
| 创建者 | 无 | 无 | 默认为 0 |
| 可配置 | 否 | 否 | 是（按 `AmmConfig`） |

完整矩阵：[`reference/fee-comparison`](/zh/reference/fee-comparison)。

## 集成商注意事项

- **报价：** 始终从链上读取 `AmmInfo`；不要硬编码费用。由于 `SetParams` 现已移除，现有池上的费用参数实际上是固定的。
- **不要照搬 AMM v4 的费率。** `Fees` 结构与 AMM v4 共用，因此很容易把 AMM v4 的 `25 / 10_000` 抄进 Stable 的报价里。Stable 池收取的是 `2 / 10_000`。用 0.25% 给 Stable 池报价会把费用高估 12.5 倍。
- **曲线与费用：** 费用的应用与曲线是公式（AMM v4 中的 x·y=k）还是查找表（Stable）无关。两者都在触及曲线之前先从输入金额中扣除费用。
- **无奖励：** Stable 池不支持池上奖励发放。生态系统农场（Farm v3/v5/v6）在其他地方处理质押。

## 后续步骤

- [`products/stable/math`](/zh/products/stable/math) — 交换数学中的费用应用。
- [`products/stable/instructions`](/zh/products/stable/instructions) — `WithdrawPnl` 账户列表。
- [`products/amm-v4/fees`](/zh/products/amm-v4/fees) — OpenBook 路径的更深入费用推导。
- [`reference/fee-comparison`](/zh/reference/fee-comparison) — 所有产品的并排比较。

来源：

- `raydium-stable/program/src/state.rs`（`Fees` 结构体）
- 实时主网池上的链上 `AmmInfo.fees` 字段
