---
title: "部署 CPMM 池"
description: "即插即用的 Node 脚本，用于创建新的 CPMM 池。通过环境变量配置代币和初始流动性金额。"
---

<Info>
  **本页内容由 AI 自动翻译，所有内容以英文版本为准。**

  [查看英文版 →](/quick-start/deploy-cpmm-pool)
</Info>

<Info>
  **这个脚本的作用。** 为你指定的两个代币创建一个全新的 CPMM 池，选择 0.25% 费率等级，按照初始金额隐含的价格注入初始流动性，并输出新池的 ID 和交易签名。
</Info>

## 准备工作

确保你已阅读 [快速开始前置条件](/zh/quick-start)，并已配置 `RPC_URL`、`KEYPAIR` 和安装依赖。

你还需要 **用两种代币的初始金额为钱包充值**，加上足够的 SOL 来支付一次性的池创建费和账户租金（主网上总共约 0.19 SOL — 参见 [`reference/fee-comparison`](/zh/reference/fee-comparison#creation-costs) 了解租金/费用明细，或 [`ray/protocol-fees`](/zh/ray/protocol-fees#pool-creation-fees) 仅查看费用）。

**在 devnet 上运行。** 仅将 `cluster` 改为 `"devnet"` 是不够的。这个脚本固定了两个不遵循 `cluster` 的命名导入：`programId: CREATE_CPMM_POOL_PROGRAM` 和 `poolFeeAccount: CREATE_CPMM_POOL_FEE_ACC`，两者都是主网常量。从 SDK 导入 `DEVNET_PROGRAM_ID` 并改用 `DEVNET_PROGRAM_ID.CREATE_CPMM_POOL_PROGRAM` 和 `DEVNET_PROGRAM_ID.CREATE_CPMM_POOL_FEE_ACC` — 这与 [CLMM 脚本](/zh/quick-start/deploy-clmm-pool) 使用的模式相同。

## 脚本

保存为 `create-cpmm.mjs`：

```ts create-cpmm.mjs icon="js"
import { Connection, Keypair, PublicKey } from "@solana/web3.js";
import {
  Raydium,
  TxVersion,
  CREATE_CPMM_POOL_PROGRAM,
  CREATE_CPMM_POOL_FEE_ACC,
} from "@raydium-io/raydium-sdk-v2";
import BN from "bn.js";
import fs from "node:fs";

// ── Config from env ──────────────────────────────────────────────
const RPC_URL  = process.env.RPC_URL  ?? "https://api.mainnet-beta.solana.com";
const KEYPAIR  = process.env.KEYPAIR  ?? `${process.env.HOME}/.config/solana/id.json`;
const MINT_A   = process.env.MINT_A;   // required, base58
const MINT_B   = process.env.MINT_B;   // required, base58
const AMOUNT_A = process.env.AMOUNT_A; // required, integer raw units
const AMOUNT_B = process.env.AMOUNT_B; // required, integer raw units

if (!MINT_A || !MINT_B || !AMOUNT_A || !AMOUNT_B) {
  console.error("Set MINT_A, MINT_B, AMOUNT_A, AMOUNT_B env vars.");
  process.exit(1);
}

// ── Setup ────────────────────────────────────────────────────────
const connection = new Connection(RPC_URL, "confirmed");
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(KEYPAIR, "utf8"))),
);
const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});

// ── Pick the 0.25% fee tier ─────────────────────────────────────
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0);
if (!feeConfig) throw new Error("0.25% fee tier not found in CPMM configs.");

// ── Resolve mint metadata (Token-2022-aware) ────────────────────
const mintA = await raydium.token.getTokenInfo(new PublicKey(MINT_A));
const mintB = await raydium.token.getTokenInfo(new PublicKey(MINT_B));

// ── Build and execute ───────────────────────────────────────────
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA,
  mintB,
  mintAAmount:     new BN(AMOUNT_A),
  mintBAmount:     new BN(AMOUNT_B),
  startTime:       new BN(0), // open immediately
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
  computeBudgetConfig: {
    units: 600_000,
    microLamports: 100_000,
  },
});

const { txId } = await execute({ sendAndConfirm: true });

console.log(`Pool ID:  ${extInfo.address.poolId.toBase58()}`);
console.log(`LP mint:  ${extInfo.address.lpMint.toBase58()}`);
console.log(`Vault A:  ${extInfo.address.vaultA.toBase58()}`);
console.log(`Vault B:  ${extInfo.address.vaultB.toBase58()}`);
console.log(`Tx:       https://solscan.io/tx/${txId}`);
```

## 运行

示例：创建一个 SOL/USDC 池，初始流动性为 1 SOL 和 160 USDC：

```bash icon="terminal"
export MINT_A="So11111111111111111111111111111111111111112"   # wSOL
export MINT_B="EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"   # USDC
export AMOUNT_A="1000000000"   # 1 SOL (9 decimals)
export AMOUNT_B="160000000"    # 160 USDC (6 decimals)

node create-cpmm.mjs
```

预期输出：

```text
Pool ID:  HgC5...3kXb
LP mint:  4ZAS...9rkV
Vault A:  J9Mu...mP2k
Vault B:  AaJq...8wxx
Tx:       https://solscan.io/tx/5dQ...
```

## 刚才发生了什么

1. **`getCpmmConfigs`** 从 `api-v3.raydium.io` 拉取实时费率等级列表，并选择索引 0（0.25% 等级 — 参见 [`reference/fee-comparison`](/zh/reference/fee-comparison) 了解完整列表）。
2. **`getTokenInfo`** 解析每个代币的元数据，包括它由哪个代币程序管理。CPMM 同时接受 SPL Token 和 Token-2022 代币；SDK 会自动路由。
3. **`createPool`** 构建一个交易，该交易：
   - 将代币排序为规范顺序，
   - 推导池 PDA、金库、LP 代币和权限账户，
   - 向 `CREATE_CPMM_POOL_FEE_ACC` 支付一次性的 `create_pool_fee`，
   - 如果缺失则为调用者创建 ATA，
   - 用 `AMOUNT_A` 和 `AMOUNT_B` 注入金库。
4. **初始价格** 由初始金额比例决定：`price = AMOUNT_B / AMOUNT_A`（调整小数位后）。仔细选择这个值 — 机器人会在池开放后的几秒内套利任何错误定价。
5. **`startTime: new BN(0)`** 立即开放交易。要在向公众开放前暂存流动性，设置一个未来的 Unix 时间戳。

## 常见错误

- **`pool already exists`** — 此代币对在此费率等级已存在一个池。创建前先查询。
- **`insufficient funds`** — 你的钱包没有足够的 `MINT_A`、`MINT_B` 或 SOL（用于创建池费用 + 租金）。
- **`Token-2022 extension not supported`** — 你的某个代币使用了 CPMM 不支持的扩展。参见 [`reference/token-2022-support`](/zh/reference/token-2022-support)。

## 部署后

你可以立即对新池进行交换 — [从 CLI 交换](/zh/quick-start/swap-from-cli) 脚本直接接受你的新 `POOL_ID`。聚合器（Jupiter 等）会在几分钟内索引新池。

## 下一步

- [`products/cpmm/overview`](/zh/products/cpmm/overview) — CPMM 是什么以及何时选择它。
- [`user-flows/create-cpmm-pool`](/zh/user-flows/create-cpmm-pool) — 相同流程的截图版本，通过 Raydium UI。
- [`user-flows/choosing-a-pool-type`](/zh/user-flows/choosing-a-pool-type) — 你应该改用 CLMM 吗？
