---
title: "程序地址"
description: "Raydium 在 Solana mainnet-beta 和 devnet 上的规范程序 ID 和共享 PDA。文档中的其他页面都链接到这里，而不是硬编码地址。"
---

<Info>
  **本页内容由 AI 自动翻译，所有内容以英文版本为准。**

  [查看英文版 →](/reference/program-addresses)
</Info>

<Info>
  **真实信息来源。** 本页是文档中唯一逐字列出程序地址的地方。其他页面都链接到这里。如果某个 ID 发生变化，**仅在此处更新**，网站上的所有引用都会保持一致。

  在签署真实交易前，始终根据实时 API（`https://api-v3.raydium.io/main/info`）交叉验证这些值。
</Info>

## Mainnet-beta

<Tip>
  经验法则：如果野外的程序 ID 与下表不匹配，**不要签署该交易。** 程序 ID 不匹配是在 Solana 上损失资金的最简单方式。
</Tip>

### 链上程序

| 程序 | 程序 ID | 源代码 |
|---------|------------|--------|
| **AMM v4**（混合 AMM + OpenBook） | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM**（标准 AMM） | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM**（集中流动性） | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM**（StableSwap 风格曲线） | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | 源代码未公开 |
| **Farm v3**（遗留 RAY 质押） | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | 源代码未公开 |
| **Farm v5**（遗留生态系统农场） | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | 源代码未公开 |
| **Farm v6**（当前生态系统农场） | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | 源代码未公开 |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | 源代码未公开 |
| **AMM 路由**（Raydium 的链上路由器） | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | 源代码未公开 |
| **销毁与收益 / LP 锁定** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | 源代码未公开 |

注意：

- AMM v4 和 Farm v3/v5 **保留用于现有的池和头寸**。Raydium UI 和 SDK 将新池创建和新生态系统农场分别路由到 CPMM、CLMM 和 Farm v6 — 见 [`protocol-overview/versions-and-migration`](/zh/protocol-overview/versions-and-migration)。
- 上面的 Farm v3 ID 同时用作 \$RAY 单资产质押程序。对于 LP 质押，行为与 Farm v3 相同。
- **Stable AMM** 是一个独立程序，AMM 路由程序可以与 AMM v4、CPMM 和 CLMM 一起针对它。与其他三个程序相比，流动性较少，SDK 不为其公开第一类 API；通过它路由的集成商通常通过路由器进行。
- **源代码可用性。** 在上述链上程序中，只有 `raydium-amm`（AMM v4）、`raydium-cp-swap`（CPMM）和 `raydium-clmm`（CLMM）附带 [`github.com/raydium-io`](https://github.com/raydium-io) 下的公开源代码库。Stable AMM、LaunchLab、AMM 路由、销毁与收益 / LP 锁定和 Farm 程序不公开 — 改为根据实时 API、链上字节码和 [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl) 中发布的 IDL 验证它们。

### 共享管理员权限

每个 Raydium 程序（包括 AMM v4）都共享一个单一的硬编码管理员 `Pubkey`，用于指令级访问控制。在 AMM v4 中是 `config_feature::amm_owner::ID`（控制 `SetParams`、`CreateConfigAccount` 和 `UpdateConfigAccount`，并用作默认的 `pnl_owner`），在 Anchor 程序中是 `admin::ID` — CLMM、CPMM、LaunchLab、Lock — 控制 `CreateAmmConfig` 或 `UpdatePoolStatus` 等路径：

| 集群 | 管理员 |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

账户级操作权限（例如 CPMM/CLMM `AmmConfig` 上的 `protocol_owner`、`fund_owner`，或 LaunchLab `GlobalConfig` 上的迁移钱包）存储在链上，可能与程序管理员不同。在发送高风险交易前，直接从相关配置账户读取它们。

### CPMM 权限 PDA 创建者权限

`CreatePermissionPda` 接受上面的共享程序管理员或专用所有者。自 2026-09 CPMM 升级以来，`ClosePermissionPda` 接受相同的两个签名者；在此之前仅限管理员。

| 集群 | 专用 `CreatePermissionPda` 所有者 |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### 支持代币注册表权限

`CreateSupportMintAssociated` 和 `CloseSupportMintAssociated` 接受共享程序管理员或下面程序的专用所有者。注册代币允许它通过该程序的 Token-2022 扩展允许列表 — 见 [`reference/token-2022-support`](/zh/reference/token-2022-support#the-one-bypass-the-per-mint-registry)。CPMM 和 CLMM 各自维护自己的注册表 PDA 和自己的权限；两个密钥都不适用于另一个程序。

| 程序 | 集群 | 专用支持代币所有者 |
|---------|---------|------------------------------|
| CPMM | mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| CPMM | devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |
| CLMM | mainnet-beta | `RayVyjyJQz9vAi126A4sGexKnSU1XeZaHTRcM1mZMPY` |
| CLMM | devnet | `rayf3nEbb3bnfN6RDGFpqPbjc5uUa3tRUzu6UVYrRx5` |

### CPMM 费用所有者钱包

自 2026-09 CPMM 升级以来，`CreateAmmConfig` 将这些固定密钥写入新配置的 `protocol_owner` 和 `fund_owner`，而不是复制管理员签名者的密钥。`CollectProtocolFee` 和 `CollectFundFee` 接受存储在配置上的密钥**或**共享程序管理员（`|| crate::admin::ID`），因此管理员仍然是接受的签名者 — 改变的只是收益默认流向的位置。

| 集群 | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  这些是程序*写入新配置*的值，不是对现有配置的描述。升级前创建的 `AmmConfig` 账户保留存储在其上的任何内容。在构建收集交易前，始终从账户读取 `protocol_owner` / `fund_owner`。
</Warning>

### 协议费用收集钱包

每个程序的协议端交易费用份额累积的地方。这些是普通的代币持有钱包，因此单个地址持有混合的 SPL 代币集（来自 SOL/USDC 池的 wSOL 和 USDC、来自 SOL/RAY 池的 wSOL 和 RAY 等）。

| 程序 | 收集地址 | 存储为 |
|---------|--------------------|-----------|
| CLMM | `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416` | 所有 21 个配置上的 `AmmConfig.owner` |
| CPMM | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `AmmConfig.protocol_owner`（见 [CPMM 费用所有者钱包](#cpmm-费用所有者钱包)） |
| AMM v4 | `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF` | 单个配置 PDA 上的 `AmmConfig.pnl_owner` |

Stable AMM 的协议份额在金库中作为 `out_put.need_take_pnl_*` 累积，由 `WithdrawPnl` 扫除，而不是坐在常设钱包中。对于 CPMM，在构建收集交易前从特定 `AmmConfig` 读取 `protocol_owner` — 在 2026-09 升级前创建的配置可能持有不同的密钥。

### 超额 lamport 收集钱包

在 2026-09-09 升级的三个程序中，每个都获得了一个管理员指令，返回程序控制账户上超过租金豁免最低值的 lamport，遵循 [SIMD-0437 租金减少](/zh/solana-fundamentals/rent-and-reclaimable-rent)。每个程序硬编码自己的目标钱包，每个都是专用密钥 — 不是共享程序管理员。

| 程序 | 指令 | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM 和 LaunchLab 另外接受共享程序管理员作为签名者。AMM v4 **仅**接受上面的钱包 — 它没有管理员回退。AMM v4 还在其 `testnet` 功能下编译单独的 testnet 密钥（`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`）。

这些指令仅移动 lamport；它们从不触及代币余额、关闭账户或改变池状态。见 [`products/amm-v4/instructions`](/zh/products/amm-v4/instructions#withdrawexcesslamports)、[`products/cpmm/instructions`](/zh/products/cpmm/instructions#collectexcesslamports) 和 [`products/launchlab/instructions`](/zh/products/launchlab/instructions#collectexcesslamports)。

### LaunchLab 委托配置创建者

`CreateConfig` 接受共享程序管理员或下面的委托权限。委托角色可以创建新的 `GlobalConfig`；它不能为现有账户调用仅限管理员的 `UpdateConfig` 路径。

| 集群 | 委托 `CreateConfig` 权限 |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### CLMM 受限发行人冻结权限

当 CLMM 池中的任一金库代币具有下面的冻结权限之一时，通过 V2 路径打开的新头寸会冻结其头寸 NFT 代币账户。该列表是 CLMM 程序中的 `frozen_position_nft_authorities::IDS`，其单一条目是 CLMM 已删除的 Superstate 检测也匹配的发行人权限。这些是**仅用作匹配密钥的底层资产发行人权限**。CLMM 池 PDA（而不是发行人密钥）成为头寸 NFT 代币的冻结权限。

| 集群 | 受限发行人冻结权限 |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

该列表在 CLMM 程序中硬编码。添加或删除权限需要程序升级。

### 共享配置 / PDA 约定

几个 Raydium 程序公开**配置账户**，其公钥是稳定的并列在公开 API 上。优先使用 API 查询而不是硬编码：

```
# CPMM 费用配置（返回 {id, index, tradeFeeRate, ...} 数组）
GET https://api-v3.raydium.io/main/cpmm-config

# CLMM 费用配置
GET https://api-v3.raydium.io/main/clmm-config
```

### 池创建费用接收者

CPMM 和 AMM v4 都**按精确地址**验证其池创建费用目标，因此它必须已存在于你正在测试的任何集群上。CLMM 没有单独的池创建费用。

| 程序 | 常量 | mainnet-beta | devnet |
|---------|----------|--------------|--------|
| CPMM | `create_pool_fee_reveiver::ID`（源代码中有拼写错误） | `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` | `3oE58BKVt8KuYkGxx8zBojugnymWmBiyafWgMrnb6eYy` |
| AMM v4 | `config_feature::create_pool_fee_address::ID` | `7YttLkHDoNj9wyDur5pM1ejNaAvT9X4eqaYcHQqtj2G5` | `9y8ENuuZ3b19quffx9hQvRVygG5ky6snHfRvGpuSfeJy` |

AMM v4 还在其 `testnet` 功能下编译 `3TRTX4dXUpp2eqxi3tvQDFYUV7SdDJjcPE3Y4mbtftaX`。CPMM 用 Anchor 的 `ConstraintAddress`（`2012`）拒绝不匹配；AMM v4 用 `InvalidFee`。

### CLMM 限价单管理员

`SettleLimitOrder` 和 `CloseLimitOrder` 接受订单自己的 `owner` 或下面的程序范围管理员密钥。管理员不能打开、增加或减少订单，输出始终流向订单所有者的账户 — 见 [`security/admin-and-multisig`](/zh/security/admin-and-multisig)。

| 集群 | `limit_order_admin` |
|---------|---------------------|
| mainnet-beta | `Ray8HHtixhL9zvnokMyELCVGp622PDPJj96zcVC9RWp` |
| devnet | `DRaypkxM96mjYYnqMmuYjsSL3stHxLf1CvYuTcqxUaav` |

默认 CPMM `AmmConfig` 索引 `0`（标准 0.25% 池）费用参数，供参考：

| 字段 | 值 | 含义 |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 交易量的 0.25% |
| `protocol_fee_rate` | `120000` | 交易费用的 12%（不是交易量）路由到协议 |
| `fund_fee_rate` | `40000` | 交易费用的 4% 路由到基金多签 |
| `creator_fee_rate` | `500` | 交易量的 0.05% 给池创建者（可选） |

见 [`products/cpmm/fees`](/zh/products/cpmm/fees) 了解分割如何组成，[`reference/fee-comparison`](/zh/reference/fee-comparison) 了解跨产品矩阵。

### PDA 种子

下面的种子是**规范的**，由 SDK 和链上 CPI 使用。始终计算 PDA；不要硬编码派生地址。

```ts
// CPMM — 所有种子都是静态 ASCII 字符串，除非另有说明。
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` 在哈希前**按公钥字节顺序排序**（token0 < token1）。搞错这一点会为不存在的池产生有效的 PDA。

等效的 CLMM 种子遵循相同的风格；见 [`products/clmm/accounts`](/zh/products/clmm/accounts)。

## Devnet

| 程序 | 程序 ID |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM 路由** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **销毁与收益 / LP 锁定** | `DRay25Usp3YJAi7beckgpGUC7mGJ2cR1AVPxhYfwVCUX` |
| **Farm v3 / v5 / v6** | 未可靠地为 devnet 发布 — 在使用前通过实时 API（`https://api-v3-devnet.raydium.io/main/info`）确认。 |

Devnet REST API 基础：`https://api-v3-devnet.raydium.io/`（与 mainnet 相同的路由形状）。

## 升级权限

mainnet-beta 上的每个 Raydium 程序都在 BPF 可升级加载器下部署，具有**相同的**升级权限 — 程序升级 Squads 多签的金库：

| | 值 |
|---|---|
| 升级权限（所有程序） | `FytDrVzDybM1TwFQPGb8qaxZR7dBCzNeqT3vtQsceZQK` |
| Squads 多签配置账户 | `tr8rgazUrZzgdkfc6Q622nVJHMMzh29trdBE2uBHb4u` |
| Squads 程序 | `SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf`（v4） |

在 2026-09-09 为 AMM v4、CPMM、CLMM、Stable AMM、Farm v6、LaunchLab、AMM 路由和 Lock 验证链上 — 所有八个都报告该权限，且没有一个是不可变的。在高风险部署前用 `solana program show <PROGRAM_ID> -u mainnet-beta` 重新检查；见 [`security/admin-and-multisig`](/zh/security/admin-and-multisig) 了解多签的阈值和签名实践。

## 程序级配置账户

| 程序 | 配置账户 | 内容 |
|---------|----------------|----------|
| AMM v4 | `9DCxsMizn3H1hprZ7xWe6LDzeUeZBksYFpBWBtSf1PQX` | 种子为 `["amm_config_account_seed"]` 的单个 `AmmConfig` PDA。持有 `pnl_owner` = `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF`、`cancel_owner` = `RAYdmrNjdRSu7ErPv9s2BqDtRoFa1QECn7UJYaC2tR3` 和 `create_pool_fee` = `150000000`（0.15 SOL）。 |
| CPMM | 21 个 `AmmConfig` 账户（索引 0–20） | 索引 0 是 `D4FPEruKEHrG5TenZ2mpDGEfu1iUvTiqBxvpU8HLBvC2`。见 [`reference/fee-comparison`](/zh/reference/fee-comparison) 了解分级集。 |
| CLMM | 21 个 `AmmConfig` 账户（索引 0–20） | 索引 1（0.25%，tick 间距 60）是 `E64NGkDLLCdQ2yFNPcavaKptrEgmiQaNykUuLC1Qgwyp`。所有 21 个上的 `owner` = `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416`、`fund_owner` = `FundHfY8oo8J9KYGyfXFFuQCHe7Z1VBNmsj84eMcdYs4`。 |

所有值从 mainnet-beta 读取于 2026-09-09。这些账户上的费率是管理员可变的 — 实时读取而不是缓存。

## 如何在链上验证地址

1. **Solana Explorer。** 将地址粘贴到 [explorer.solana.com](https://explorer.solana.com) 并确认它标记为 `Program` 且具有当前升级权限。Mainnet-beta 应显示由 Raydium 升级权限签署的部署。
2. **CLI。** 使用 `solana program show <PROGRAM_ID> -u mainnet-beta` 检查部署槽、BPF 加载器、升级权限和数据长度。在你的运行手册中记录这些。
3. **IDL 附件。** `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet` 适用于 **CLMM、LaunchLab 和 Lock**，它们仍保留由程序本身拥有的遗留 `anchor:idl` 账户。它**不**适用于 CPMM：CPMM 的链上 IDL 在 2026-09 升级中移至程序元数据程序（`ProgM6JCCvbYkfKqJYHePx4xxSUSqJp7rh8Lyv7nk7S`），所以改为从那里获取。AMM v4、Stable AMM 和 Farm 没有链上 IDL。IDL 镜像在 [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) — 在信任链上 IDL 前将其与仓库进行对比。
4. **配置/管理员权限。** 对于 CPMM/CLMM 配置账户，读取 `owner`/`protocol_owner` 字段并确认它与 [`security/admin-and-multisig`](/zh/security/admin-and-multisig) 中发布的当前 Raydium 多签匹配。

如果上面的四项检查中的任何一项与本页不一致，将本页视为错误并在编写针对这些地址的新代码前开启一个问题。

## 更新本页

- 程序 ID 的更改是安全关键的。**不要**在没有 (a) 链接到 Raydium 公告、(b) 在 [`reference/changelog`](/zh/reference/changelog) 中添加条目和 (c) 对文档运行链接检查以确认没有页面仍引用旧值的情况下发布程序 ID 更改。
- 弃用保留在表中带有状态注释，而不是被删除 — 现有池仍通过旧程序解析。

来源：

- 实时 API：`https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
