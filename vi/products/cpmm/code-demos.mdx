---
title: "Các ví dụ mã CPMM"
description: "Các ví dụ TypeScript từ đầu đến cuối cho mọi hoạt động CPMM — tạo pool, swap, gửi, rút, thu phí — cộng với một bộ khung CPI Rust tối thiểu."
---

<Info>
  **Trang này được dịch tự động bằng AI. Phiên bản tiếng Anh là bản chính thức.**

  [Xem bản tiếng Anh →](/products/cpmm/code-demos)
</Info>

<Info>
  **Biểu ngữ phiên bản.** Tất cả các ví dụ TypeScript nhắm mục tiêu `@raydium-io/raydium-sdk-v2@0.2.64-alpha`; chúng được thực thi lần cuối cùng với `0.2.42-alpha` (2026-04) và chữ ký gọi của chúng được kiểm tra lại với nguồn `0.2.64-alpha` vào ngày 2026-09-09, trên Solana mainnet-beta. Bộ khung CPI Rust nhắm mục tiêu `raydium-cp-swap` trên nhánh `chore/upgrade-anchor`, Anchor `1.0.2` — `master` vẫn ghim `0.32.1`. ID chương trình được lấy thông qua các hằng số từ [`reference/program-addresses`](/vi/reference/program-addresses).
</Info>

## Điều kiện tiên quyết

```bash
npm install @raydium-io/raydium-sdk-v2 @solana/web3.js @solana/spl-token bn.js decimal.js
```

Mỗi ví dụ trên trang này phản ánh một tệp trong [`raydium-sdk-V2-demo/src/cpmm`](https://github.com/raydium-io/raydium-sdk-V2-demo/tree/master/src/cpmm); liên kết GitHub nằm bên cạnh mỗi phần. Bootstrap tuân theo `config.ts.template` của kho lưu trữ demo ([nguồn](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/config.ts.template)):

```ts
import { Connection, Keypair, clusterApiUrl } from "@solana/web3.js";
import { Raydium, TxVersion } from "@raydium-io/raydium-sdk-v2";
import fs from "node:fs";

const connection = new Connection(process.env.RPC_URL ?? clusterApiUrl("mainnet-beta"));
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(process.env.KEYPAIR!, "utf8"))),
);

const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});
```

Thực thể `Raydium` là giao diện của SDK — mỗi ví dụ dưới đây sử dụng nó. `Raydium.load` là không đồng bộ vì nó tải danh sách token theo mặc định; chuyển `disableLoadToken: true` để bỏ qua lần tìm nạp đó. Kiểm tra tính năng khả dụng đã bị bỏ qua trừ khi bạn chuyển `disableFeatureCheck: false`. Cấu hình phí được tìm nạp một cách lười biếng khi sử dụng lần đầu tiên thông qua `raydium.api.getCpmmConfigs()`.

## Tạo một pool CPMM

Nguồn: [`src/cpmm/createCpmmPool.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/createCpmmPool.ts)

```ts
import { PublicKey } from "@solana/web3.js";
import BN from "bn.js";
import { CREATE_CPMM_POOL_PROGRAM, CREATE_CPMM_POOL_FEE_ACC }
  from "@raydium-io/raydium-sdk-v2";
// (Nếu bạn cần mọi PDA pool cùng một lúc, trình trợ giúp được xuất là `getCreatePoolKeys`.
//  Không có `getCpmmPdas`.)

const mintA = new PublicKey("So11111111111111111111111111111111111111112"); // wSOL
const mintB = new PublicKey("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"); // USDC

// 1. Chọn một cấu hình phí. index=0 là tầng 0,25%.
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0)!;

// 2. Lấy siêu dữ liệu mint để SDK có thể xử lý các tiện ích mở rộng Token-2022.
const mintAInfo = await raydium.token.getTokenInfo(mintA);
const mintBInfo = await raydium.token.getTokenInfo(mintB);

// 3. Xây dựng giao dịch.
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA:           mintAInfo,
  mintB:           mintBInfo,
  mintAAmount:     new BN(1_000_000_000),   // 1 SOL (giả sử 9 chữ số thập phân)
  mintBAmount:     new BN(   160_000_000),  // 160 USDC (ở 160/SOL)
  startTime:       new BN(0),               // mở ngay lập tức
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Pool được tạo tại", extInfo.address.poolId.toBase58());
console.log("Tx:", txId);
```

Một vài điều mà SDK xử lý một cách im lặng:

- Sắp xếp các mint thành thứ tự token0/token1 trước khi lấy PDA.
- Trả `create_pool_fee` một lần cho `poolFeeAccount`.
- Tạo các tài khoản token liên kết của người gọi nếu bị thiếu.
- Chọn chương trình token phù hợp (SPL Token so với Token-2022) cho mỗi bên.

Sau khi xác nhận, bạn có thể tìm nạp trạng thái pool trực tiếp với:

```ts
// getPoolInfoFromRpc lấy một chuỗi base58 vị trí, không phải PublicKey và không phải một đối tượng.
const { poolKeys, poolInfo, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(
  extInfo.address.poolId.toBase58(),
);
```

## Swap (base-input)

Nguồn: [`src/cpmm/swap.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swap.ts)

```ts
import { CurveCalculator, FeeOn } from "@raydium-io/raydium-sdk-v2";

const POOL_ID = "<POOL_ID>";   // chuỗi base58

// 1. Tải trạng thái pool hiện tại trực tiếp từ RPC (không từ API).
const { poolInfo, poolKeys, rpcData } = await raydium.cpmm.getPoolInfoFromRpc(POOL_ID);

const inputMint  = poolInfo.mintA.address;                // swap A → B
const amountIn   = new BN(100_000_000);                   // 0,1 SOL
const slippage   = 0.005;                                 // 0,5%

// 2. Báo giá cục bộ. CurveCalculator phản ánh toán học trên chuỗi, bao gồm
//    phí chuyển Token-2022 ở cả hai bên. Tất cả tám đối số đều bắt buộc.
const baseIn = inputMint === poolInfo.mintA.address;
const swapResult = CurveCalculator.swapBaseInput(
  amountIn,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// Các trường swapResult là inputAmount / outputAmount / tradeFee / protocolFee /
// fundFee / creatorFee / newInputVaultAmount / newOutputVaultAmount.
// Không có destinationAmountSwapped hoặc sourceAmountSwapped.

// 3. Xây dựng và gửi. Chuyển `slippage` ở đây là cách áp dụng giới hạn —
//    không cần số học minimum-out thủ công.
const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: amountIn,
  swapResult,
  slippage,
  baseIn,
  txVersion: TxVersion.V0,
});

const { txId } = await execute({ sendAndConfirm: true });
console.log("Swap tx:", txId);
```

Lưu ý: SDK **luôn** tìm nạp lại trạng thái pool từ RPC bên trong `getPoolInfoFromRpc`. Không báo giá từ `api-v3.raydium.io` cho một giao dịch mà bạn sắp ký — một báo giá cũ một khối có thể trượt vào `ExceededSlippage` tại thời điểm hạ cánh.

## Swap (base-output)

Nguồn: [`src/cpmm/swapBaseOut.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/swapBaseOut.ts)

```ts
const amountOutWanted = new BN(15_000_000);        // 15 USDC
const slippage        = 0.005;

const baseIn = false; // false → mintB là bên đầu vào
const swapResult = CurveCalculator.swapBaseOutput(
  amountOutWanted,
  baseIn ? rpcData.baseReserve : rpcData.quoteReserve,
  baseIn ? rpcData.quoteReserve : rpcData.baseReserve,
  rpcData.configInfo!.tradeFeeRate,
  rpcData.configInfo!.creatorFeeRate,
  rpcData.configInfo!.protocolFeeRate,
  rpcData.configInfo!.fundFeeRate,
  rpcData.feeOn === FeeOn.BothToken || rpcData.feeOn === FeeOn.OnlyTokenB,
);
// swapResult.inputAmount là đầu vào cần thiết; không có sourceAmountSwapped.

const { execute } = await raydium.cpmm.swap({
  poolInfo,
  poolKeys,
  inputAmount: new BN(0),   // không sử dụng khi fixedOut — số tiền đến từ swapResult
  fixedOut:    true,
  swapResult,               // bắt buộc, và nó mang theo số tiền đầu ra mục tiêu
  baseIn,
  slippage,
  txVersion:   TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## Gửi thanh khoản

Nguồn: [`src/cpmm/deposit.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/deposit.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";
import Decimal from "decimal.js";

// addLiquidity được điều khiển bởi một số tiền INPUT, không phải một số tiền LP.
const uiInputAmount = "0.0001";
const inputAmount = new BN(
  new Decimal(uiInputAmount).mul(10 ** poolInfo.mintA.decimals).toFixed(0),
);
const slippage = new Percent(1, 100);   // một Percent, không phải một số

const { execute } = await raydium.cpmm.addLiquidity({
  poolInfo,
  poolKeys,
  inputAmount,
  baseIn: true,           // inputAmount được tính bằng mintA
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

SDK lấy số tiền được ghép nối từ dự trữ hiện tại của pool, tăng mỗi bên theo slippage cho các đối số `maximum_*` của hướng dẫn, và xây dựng các tạo ATA nếu cần thiết. `raydium.cpmm.computePairAmount({ ... })` trả về các số giống nhau nếu bạn muốn chúng cho xem trước giao diện người dùng.

## Rút thanh khoản

Nguồn: [`src/cpmm/withdraw.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/withdraw.ts)

```ts
import { Percent } from "@raydium-io/raydium-sdk-v2";

const lpAmount = new BN(100_000);           // LP để đốt
const slippage = new Percent(1, 100);       // một Percent, không phải một số

const { execute } = await raydium.cpmm.withdrawLiquidity({
  poolInfo,
  poolKeys,
  lpAmount,
  slippage,
  txVersion: TxVersion.V0,
});

await execute({ sendAndConfirm: true });
```

## Thu phí giao thức/quỹ/người tạo

Nguồn: [`src/cpmm/collectCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectCreatorFee.ts), [`src/cpmm/collectAllCreatorFee.ts`](https://github.com/raydium-io/raydium-sdk-V2-demo/blob/master/src/cpmm/collectAllCreatorFee.ts)

Thu phí giao thức và quỹ vẫn bị giới hạn bởi quyền. Phí người tạo có hai đường dẫn: `CollectCreatorFee` được ký bởi người tạo, và `CollectCreatorFeePermissionless` bổ sung, mà bất kỳ người trả tiền nào cũng có thể gọi nhưng chỉ có thể chuyển đến các ATA chính tắc của người tạo. `0.2.64-alpha` cung cấp các trình xây dựng cấp mô-đun cho cả hai đường dẫn người tạo:

```ts
// được ký bởi người tạo
const { execute } = await raydium.cpmm.collectCreatorFees({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// không cần phép — bất kỳ người trả tiền nào; tiền thu được đi đến các ATA chính tắc của người tạo
const { execute } = await raydium.cpmm.collectCreatorFeesPermissionless({
  poolInfo,
  poolKeys,
  txVersion: TxVersion.V0,
});

// nhiều pool cùng một lúc, được ký bởi người tạo
await raydium.cpmm.collectMultiCreatorFees({ poolInfoList, txVersion: TxVersion.V0 });
```

<Note>
  Hai hướng dẫn người tạo có **các bộ phân biệt và bố cục tài khoản khác nhau** — hướng dẫn
  không cần phép thay thế các vị trí người ký / `amm_config` của người tạo bằng một `payer`
  riêng biệt và một `creator` không ký. Sử dụng trình xây dựng phù hợp thay vì trao đổi tài khoản thủ công trên cái kia.

  Không có trình xây dựng phí giao thức hoặc quỹ trong SDK (`makeCollectProtocolFeeInstruction` và
  `makeCollectFundFeeInstruction` không tồn tại dưới bất kỳ tên nào) — đó là những đường dẫn quản trị. Các
  trình xây dựng hướng dẫn cấp thấp **thực sự** tồn tại là `collectCpFeeInstruction`,
  `makeCollectCreatorFeeInstruction` và `collectCreatorFeePermissionlessInInstruction`.
</Note>

Ngoài chuỗi, bạn có thể đọc phí tích lũy trực tiếp từ `PoolState`:

```ts
const pool = await raydium.cpmm.getRpcPoolInfo(POOL_ID);   // chuỗi base58
console.log("Phí giao thức tích lũy mintA:", pool.protocolFeesMintA.toString());
console.log("Phí giao thức tích lũy mintB:", pool.protocolFeesMintB.toString());
// Các trường CpmmPoolInfoLayout là protocolFeesMintA/B, fundFeesMintA/B và
// creatorFeesMintA/B — không có các trường ...Token0/...Token1.
```

## Bộ khung CPI Rust

Nếu bạn muốn gọi CPMM từ chương trình Anchor của riêng bạn — ví dụ, một kho lưu trữ thực hiện swap thay mặt cho những người gửi tiền của nó — ngữ cảnh CPI trông như thế này. Thứ tự tài khoản tuân theo [`products/cpmm/instructions`](/vi/products/cpmm/instructions).

```rust
// Cargo.toml — nhánh quan trọng: `master` vẫn ghim anchor-lang 0.32.1.
// raydium-cp-swap = { git = "https://github.com/raydium-io/raydium-cp-swap",
//                     branch = "chore/upgrade-anchor", features = ["cpi"] }
// anchor-lang      = "=1.0.2"   // phải khớp với những gì raydium-cp-swap ghim
// anchor-spl       = "=1.0.2"
//
// Tính năng `cpi` là bắt buộc — mô-đun `raydium_cp_swap::cpi` chỉ tồn tại
// dưới nó. Lưu ý rằng crate này không thể chia sẻ một chương trình với mẫu CLMM trong
// products/clmm/code-demos, ghim anchor-lang =0.32.1.

use anchor_lang::prelude::*;
use anchor_spl::token_interface::{TokenAccount, TokenInterface, Mint};
use raydium_cp_swap::cpi::accounts::Swap;
use raydium_cp_swap::cpi;
use raydium_cp_swap::program::RaydiumCpSwap;

#[derive(Accounts)]
pub struct ProxySwap<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,

    /// CHECK: được xác thực bởi chương trình CPMM
    pub authority:     UncheckedAccount<'info>,
    /// CHECK:
    pub amm_config:    UncheckedAccount<'info>,
    #[account(mut)]
    /// CHECK:
    pub pool_state:    UncheckedAccount<'info>,

    #[account(mut)]
    pub input_token_account:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_token_account: InterfaceAccount<'info, TokenAccount>,

    #[account(mut)]
    pub input_vault:  InterfaceAccount<'info, TokenAccount>,
    #[account(mut)]
    pub output_vault: InterfaceAccount<'info, TokenAccount>,

    pub input_token_program:  Interface<'info, TokenInterface>,
    pub output_token_program: Interface<'info, TokenInterface>,

    pub input_token_mint:  InterfaceAccount<'info, Mint>,
    pub output_token_mint: InterfaceAccount<'info, Mint>,

    #[account(mut)]
    /// CHECK: vòng đệm
    pub observation_state: UncheckedAccount<'info>,

    pub cpmm_program: Program<'info, RaydiumCpSwap>,
}

pub fn proxy_swap_base_input(
    ctx: Context<ProxySwap>,
    amount_in: u64,
    minimum_amount_out: u64,
) -> Result<()> {
    let cpi_accounts = Swap {
        payer:                ctx.accounts.payer.to_account_info(),
        authority:            ctx.accounts.authority.to_account_info(),
        amm_config:           ctx.accounts.amm_config.to_account_info(),
        pool_state:           ctx.accounts.pool_state.to_account_info(),
        input_token_account:  ctx.accounts.input_token_account.to_account_info(),
        output_token_account: ctx.accounts.output_token_account.to_account_info(),
        input_vault:          ctx.accounts.input_vault.to_account_info(),
        output_vault:         ctx.accounts.output_vault.to_account_info(),
        input_token_program:  ctx.accounts.input_token_program.to_account_info(),
        output_token_program: ctx.accounts.output_token_program.to_account_info(),
        input_token_mint:     ctx.accounts.input_token_mint.to_account_info(),
        output_token_mint:    ctx.accounts.output_token_mint.to_account_info(),
        observation_state:    ctx.accounts.observation_state.to_account_info(),
    };
    // Anchor 1.0 lấy Pubkey của chương trình ở đây, không phải AccountInfo của nó.
    let cpi_ctx = CpiContext::new(
        *ctx.accounts.cpmm_program.key,
        cpi_accounts,
    );
    cpi::swap_base_input(cpi_ctx, amount_in, minimum_amount_out)
}
```

Nếu CPI của bạn ký như một PDA (ví dụ, bạn quản lý một kho lưu trữ thay mặt cho những người gửi tiền), hãy hoán đổi `CpiContext::new` cho `CpiContext::new_with_signer` và chuyển các hạt giống của bạn.

## Những cạm bẫy phổ biến

Một danh sách kiểm tra ngắn trước khi mở một vé hỗ trợ:

- **Mint được sắp xếp.** Nếu PDA `poolState` được lấy của bạn không khớp với pool trên chuỗi, bạn có thể đã quên sắp xếp các mint.
- **Báo giá API cũ.** Không bao giờ chuyển một giá trị dự trữ từ `api-v3.raydium.io` vào `CurveCalculator.swap`. Tìm nạp từ RPC.
- **Chương trình token sai.** Kho lưu trữ của một mint Token-2022 được sở hữu bởi chương trình Token-2022, không phải bởi SPL Token. Luôn sử dụng các trường `token_0_program` / `token_1_program` của pool.
- **Slippage dưới mức cho các mint phí chuyển.** Nếu bất kỳ bên nào của pool là một mint phí chuyển Token-2022, `minimum_amount_out` của bạn phải được tính bằng những gì người dùng thực sự nhận được, không phải những gì kho lưu trữ gửi.
- **`NotApproved` trên một swap.** Kiểm tra `PoolState.status` — quản trị viên có thể đã tạm dừng swap trên pool đó. Xem [`products/cpmm/instructions`](/vi/products/cpmm/instructions) để biết mặt nạ trạng thái.

## Tiếp theo đi đâu

- [`sdk-api/typescript-sdk`](/vi/sdk-api/typescript-sdk) — tham chiếu SDK đầy đủ.
- [`sdk-api/rest-api`](/vi/sdk-api/rest-api) — các điểm cuối báo giá và siêu dữ liệu pool.
- [`user-flows/create-cpmm-pool`](/vi/user-flows/create-cpmm-pool) — hướng dẫn không mã của cùng một luồng.
- [`integration-guides/aggregator`](/vi/integration-guides/aggregator) — nếu bạn định tuyến CPMM như một phần của đường dẫn đa bước.

Nguồn:

- [Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`](https://github.com/raydium-io/raydium-sdk-V2)
- [Raydium SDK v2 demos — `raydium-sdk-v2-demo`](https://github.com/raydium-io/raydium-sdk-V2-demo)
- [`raydium-io/raydium-cp-swap`](https://github.com/raydium-io/raydium-cp-swap)
