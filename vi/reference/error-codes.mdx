---
title: "Mã lỗi"
description: "Các enum lỗi Anchor cho mọi chương trình on-chain của Raydium, kèm theo nguyên nhân và thông báo UX được khuyến nghị."
---

<Info>
  **Trang này được dịch tự động bằng AI. Phiên bản tiếng Anh là bản chính thức.**

  [Xem bản tiếng Anh →](/reference/error-codes)
</Info>

<Info>
  **Nguồn chính thức.** Các bảng dưới đây được tạo lại từ `error.rs` của mỗi chương trình trong các kho lưu trữ công khai của Raydium. Khi một chương trình được nâng cấp và thêm một biến thể mới, hãy chạy lại quá trình trích xuất (liên kết ở cuối mỗi bảng) và thêm vào bảng thay vì sắp xếp lại — các mã lỗi Anchor được gán theo **thứ tự nguồn**, không phải theo tên, vì vậy sắp xếp lại sẽ làm hỏng xử lý lỗi của người tích hợp.
</Info>

## Cách mã lỗi Anchor hoạt động

Anchor gán cho mỗi biến thể của enum `ErrorCode` của chương trình một mã số bắt đầu từ `6000`. Một giao dịch thất bại sẽ hiển thị:

- **Mã lỗi số** (ví dụ: `0x1771` = 6001) trong nhật ký giao dịch.
- **Tên lỗi** (ví dụ: `InvalidOwner`) từ IDL.
- **Chuỗi `#[msg(...)]`** mà Anchor phát ra trong `log_messages`.

Những người tích hợp nên khớp với **mã số**, không phải chuỗi thông báo (chuỗi có thể được diễn đạt lại mà không cần tăng phiên bản).

```ts
// Ví dụ: phát hiện lỗi slippage của CPMM và hiển thị thông báo UX sạch sẽ.
const CPMM_EXCEEDED_SLIPPAGE = 6005;

try {
  await sdk.cpmm.swap({ ... });
} catch (err: any) {
  const code = err?.error?.errorCode?.number ?? err?.code;
  if (code === CPMM_EXCEEDED_SLIPPAGE) {
    showToast("Price moved past your slippage tolerance. Increase slippage or retry.");
    return;
  }
  throw err;
}
```

## Lỗi CPMM (Standard AMM)

ID chương trình: xem [reference/program-addresses](/vi/reference/program-addresses#on-chain-programs). Nguồn: `raydium-cp-swap/programs/cp-swap/src/error.rs`.

| Mã | Biến thể | Chuỗi `#[msg]` | Nguyên nhân thường gặp | UX được khuyến nghị |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Người gọi không phải là cơ quan được cấu hình cho một hướng dẫn quản trị. | "Chỉ quản trị viên pool mới có thể thực hiện hành động này." |
| 6001 | `InvalidOwner` | Input account owner is not the program address | Một tài khoản được truyền vào được sở hữu bởi chương trình sai (thường là chương trình token sai hoặc PDA của chương trình sai). | "Nội bộ: không khớp chủ sở hữu tài khoản — làm mới và thử lại." |
| 6002 | `EmptySupply` | Input token account empty | Số dư tài khoản LP hoặc token bằng không tại một bước yêu cầu số tiền dương. | "Không có gì để rút." |
| 6003 | `InvalidInput` | InvalidInput | Đối số xấu chung (số tiền ngoài phạm vi, cờ sai). | "Đầu vào không hợp lệ — kiểm tra số tiền và thử lại." |
| 6004 | `IncorrectLpMint` | Address of the provided lp token mint is incorrect | Tài khoản mint LP được truyền vào không khớp với `pool_state.lp_mint`. | "Nội bộ: mint LP sai — làm mới dữ liệu pool." |
| 6005 | `ExceededSlippage` | Exceeds desired slippage limit | Giá thực thi tệ hơn `minAmountOut` / `maxAmountIn` của người dùng. | "Giá đã vượt quá dung sai slippage của bạn. Tăng slippage hoặc thử lại." |
| 6006 | `ZeroTradingTokens` | Given pool token amount results in zero trading tokens | Toán học gửi / rút làm tròn một bên thành không (vị trí quá nhỏ). | "Số tiền dưới mức tối thiểu cho pool này." |
| 6007 | `NotSupportMint` | Not support token_2022 mint extension | Pool gặp phải một phần mở rộng Token-2022 mà nó không thể xử lý an toàn (ví dụ: `TransferHook`, `DefaultAccountState=Frozen`). | "Token này có một phần mở rộng mà Raydium không hỗ trợ trong CPMM." |
| 6008 | `InvalidVault` | invaild vault | Tài khoản vault được truyền vào không khớp với tài khoản được ghi lại trong `pool_state`. | "Nội bộ: vault sai — làm mới và thử lại." |
| 6009 | `InitLpAmountTooLess` | Init lp amount is too less(Because 100 amount lp will be locked) | Khi khởi tạo pool, cung cấp LP được tính toán dưới mức khóa vĩnh viễn. | "Thanh khoản ban đầu quá nhỏ. Tăng tiền gửi." |
| 6010 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Số tiền sau chuyển được quan sát cho một mint phí Token-2022 không khớp với kỳ vọng được tính toán trước. | "Phí chuyển token thay đổi giữa giao dịch. Thử lại." |
| 6011 | `MathOverflow` | Math overflow | Một phép tính swap / gửi / phí trung gian bị tràn. | "Số tiền quá lớn cho pool này." |
| 6012 | `InsufficientVault` | Insufficient vault | Số dư vault của pool quá thấp để bao gồm đầu ra được yêu cầu. | "Không đủ thanh khoản trong pool cho kích thước này." |
| 6013 | `InvalidFeeModel` | Invalid fee model | Quản trị viên đặt một kết hợp tham số `AmmConfig` bị từ chối on-chain. | N/A — đường dẫn chỉ dành cho quản trị viên. |
| 6014 | `NoFeeCollect` | Fee is zero | Một hướng dẫn thu thập phí giao thức, quỹ hoặc người tạo không tìm thấy phí có thể thu thập. Đối với `CollectCreatorFeePermissionless`, cả hai bộ đếm người tạo đều bằng không. | "Không có gì để thu thập." Coi đó là một cuộc đua lành mạnh và dừng lại. |

Nguồn tái tạo: [github.com/raydium-io/raydium-cp-swap — error.rs](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs).

## Lỗi CLMM

ID chương trình: xem [reference/program-addresses](/vi/reference/program-addresses#on-chain-programs). Nguồn: `raydium-clmm/programs/amm/src/error.rs`.

| Mã | Biến thể | Chuỗi `#[msg]` | Nguyên nhân thường gặp | UX được khuyến nghị |
|------|---------|-----------------|---------------|----------------|
| 6000 | `NotApproved` | Not approved | Người gọi không phải là quản trị viên được cấu hình cho hướng dẫn này; **hoặc** (hướng dẫn quyền) người ký không phải là `admin` cũng không phải `permission_pda_admin`; **hoặc** (`OpenLimitOrder`) tài khoản token đầu vào/đầu ra bị đóng băng hoặc pool đã vô hiệu hóa swap / lệnh giới hạn; **hoặc** (`ClosePosition`, đường dẫn đóng băng) tài khoản còn lại được cung cấp không phải là `personal_position.pool_id`. | "Chỉ quản trị viên pool mới có thể thực hiện hành động này." / "Tài khoản token này bị đóng băng — hãy giải phóng nó trước khi đặt lệnh." / "Nội bộ: tài khoản vị trí sai." |
| 6001 | `InvalidUpdateConfigFlag` | invalid update amm config flag | Quản trị viên đã chuyển một giá trị `param` không được công nhận cho `update_amm_config`. | N/A — đường dẫn chỉ dành cho quản trị viên. |
| 6002 | `AccountLack` | Account lack | Tài khoản còn lại bắt buộc bị thiếu từ tx (thường là tick-array hoặc phần mở rộng oracle); hoặc `ClosePosition` đang đóng một NFT đóng băng mà không có pool làm tài khoản còn lại đầu tiên của nó. | "Nội bộ: tài khoản bị thiếu — làm mới dữ liệu pool hoặc cập nhật trình tạo đóng vị trí." |
| 6003 | `ClosePositionErr` | Remove liquidity, collect fees owed and reward then you can close position account | Đã cố gắng đóng một vị trí vẫn còn thanh khoản, phí chưa thu thập hoặc phần thưởng chưa thu thập. | "Rút tất cả thanh khoản và yêu cầu phí/phần thưởng trước khi đóng vị trí." |
| 6004 | `InvalidTickIndex` | Tick out of range | `tick_lower` hoặc `tick_upper` nằm ngoài `[-443636, 443636]`. | "Phạm vi giá ngoài giới hạn cho pool này." |
| 6005 | `TickInvalidOrder` | The lower tick must be below the upper tick | `tick_lower >= tick_upper`. | "Giá thấp hơn phải dưới giá cao hơn." |
| 6006 | `TickLowerOverflow` | The tick must be greater, or equal to the minimum tick(-443636) | Tick thấp hơn bị tràn dưới. | "Giá thấp hơn quá thấp." |
| 6007 | `TickUpperOverflow` | The tick must be lesser than, or equal to the maximum tick(443636) | Tick cao hơn bị tràn. | "Giá cao hơn quá cao." |
| 6008 | `TickAndSpacingNotMatch` | tick % tick_spacing must be zero | Tick được gửi không phải là bội số của `tick_spacing` của pool. | "Căn chỉnh giá đến mức tăng hợp lệ gần nhất." |
| 6009 | `InvalidTickArray` | Invalid tick array account | PDA sai được truyền cho một khe tick-array. | "Nội bộ: tick array sai — làm mới dữ liệu pool." |
| 6010 | `InvalidTickArrayBoundary` | Invalid tick array boundary | Lỗi off-by-one trong lập chỉ mục tick-array. | "Nội bộ: lỗi ranh giới tick-array." |
| 6011 | `SqrtPriceLimitOverflow` | Square root price limit overflow | `sqrt_price_limit` do người gọi cung cấp nằm ngoài phạm vi hợp lệ. | "Giới hạn giá ngoài phạm vi." |
| 6012 | `SqrtPriceX64` | sqrt_price_x64 out of range | Giá sqrt hiện tại của pool trôi ra khỏi phạm vi giữa swap. | "Thử lại swap." |
| 6013 | `LiquiditySubValueErr` | Liquidity sub delta L must be smaller than before | Vi phạm bất biến nội bộ khi giảm thanh khoản. | "Nội bộ: lỗi kế toán thanh khoản." |
| 6014 | `LiquidityAddValueErr` | Liquidity add delta L must be greater, or equal to before | Vi phạm bất biến nội bộ khi tăng thanh khoản. | "Nội bộ: lỗi kế toán thanh khoản." |
| 6015 | `ForbidBothZeroForSupplyLiquidity` | Both token amount must not be zero while supply liquidity | Gọi tăng thanh khoản với cả `amount_0_max` và `amount_1_max` bằng không. | "Cung cấp ít nhất một token." |
| 6016 | `LiquidityInsufficient` | Liquidity insufficient | Vị trí không có đủ thanh khoản để thỏa mãn việc rút tiền. | "Số tiền rút vượt quá thanh khoản vị trí." |
| 6017 | `PriceSlippageCheck` | Price slippage check | Giá thực thi không vượt qua bảo vệ slippage của người gọi. | "Giá đã vượt quá dung sai slippage của bạn. Tăng slippage hoặc thử lại." |
| 6018 | `TooLittleOutputReceived` | Too little output received | `SwapBaseInput`: số tiền ra dưới `other_amount_threshold`. | "Slippage vượt quá — đầu ra tối thiểu không được đáp ứng." |
| 6019 | `TooMuchInputPaid` | Too much input paid | `SwapBaseOutput`: số tiền vào trên `other_amount_threshold`. | "Slippage vượt quá — đầu vào tối đa vượt quá." |
| 6020 | `ZeroAmountSpecified` | Swap special amount can not be zero | Số tiền `amount` bằng không trên hướng dẫn swap. | "Nhập số tiền lớn hơn không." |
| 6021 | `InvalidInputPoolVault` | Input pool vault is invalid | Tài khoản input-vault của swap không khớp với vault được ghi lại của pool. | "Nội bộ: input vault sai — làm mới dữ liệu pool." |
| 6022 | `TooSmallInputOrOutputAmount` | Swap input or output amount is too small | Toán học swap làm tròn thành không, thường là bụi rất nhỏ. | "Số tiền quá nhỏ để swap trong pool này." |
| 6023 | `NotEnoughTickArrayAccount` | Not enough tick array account | Không đủ tài khoản tick-array còn lại được cung cấp cho phạm vi swap. | "Nội bộ: tick-arrays không đủ — làm mới dữ liệu pool." |
| 6024 | `InvalidFirstTickArrayAccount` | Invalid first tick array account | Tài khoản tick-array đầu tiên được truyền không bao gồm tick hiện tại. | "Nội bộ: tick array đầu tiên sai — làm mới dữ liệu pool." |
| 6025 | `InvalidRewardIndex` | Invalid reward index | `reward_index` nằm ngoài `[0, 2]`. | N/A — đường dẫn quản trị viên. |
| 6026 | `FullRewardInfo` | The init reward token reach to the max | Pool đã có số lượng tối đa (3) mint phần thưởng được cấu hình. | N/A — đường dẫn quản trị viên. |
| 6027 | `RewardTokenAlreadyInUse` | The init reward token already in use | Mint phần thưởng trùng lặp với một mint hiện có. | N/A — đường dẫn quản trị viên. |
| 6028 | `ExceptRewardMint` | The reward tokens must contain one of pool vault mint except the last reward | Trước khe 2, mint phần thưởng phải bằng một trong các mint vault của pool. | N/A — đường dẫn quản trị viên. |
| 6029 | `InvalidRewardInitParam` | Invalid reward init param | Thời gian bắt đầu/kết thúc phát hành hoặc tỷ lệ trên giây xấu. | N/A — đường dẫn quản trị viên. |
| 6030 | `InvalidRewardInputAccountNumber` | Invalid collect reward input account number | Số lượng tài khoản còn lại sai được truyền cho `CollectReward`. | "Nội bộ: số lượng tài khoản sai — làm mới và thử lại." |
| 6031 | `InvalidRewardPeriod` | Invalid reward period | Kỳ phát hành phần thưởng không hợp lệ (`end <= start`, thời lượng bằng không). | N/A — đường dẫn quản trị viên. |
| 6032 | `NotApproveUpdateRewardEmissions` | Modification of emissions is allowed within 72 hours from the end of the previous cycle | Quản trị viên đã cố gắng sửa đổi phát hành ngoài cửa sổ 72 giờ xung quanh ranh giới chu kỳ. | N/A — đường dẫn quản trị viên. |
| 6033 | `UnInitializedRewardInfo` | uninitialized reward info | Khe phần thưởng chưa được khởi tạo nhưng được tham chiếu. | N/A — đường dẫn quản trị viên. |
| 6034 | `NotSupportMint` | Not support token_2022 mint extension | CLMM gặp phải một phần mở rộng Token-2022 mà nó không thể xử lý. | "Token này có một phần mở rộng mà Raydium CLMM không hỗ trợ." |
| 6035 | `MissingTickArrayBitmapExtensionAccount` | Missing tickarray bitmap extension account | Swap vượt qua một phạm vi tick không được bao gồm bởi bitmap cơ sở; cần tài khoản bitmap mở rộng. | "Nội bộ: tick-array extension bị thiếu — làm mới dữ liệu pool." |
| 6036 | `InsufficientLiquidityForDirection` | Insufficient liquidity for this direction | Không có đủ thanh khoản theo hướng của swap. | "Thanh khoản không đủ cho kích thước swap này." |
| 6037 | `MaxTokenOverflow` | Max token overflow | Số tiền đầu vào/đầu ra vượt quá u64. | "Số tiền quá lớn cho pool này." |
| 6038 | `CalculateOverflow` | Calculate overflow | Tràn số học phí / thanh khoản. | "Số tiền quá lớn cho pool này." |
| 6039 | `TransferFeeCalculateNotMatch` | TransferFee calculate not match | Số tiền được quan sát phí chuyển Token-2022 không khớp với dự kiến. | "Phí chuyển token thay đổi giữa giao dịch. Thử lại." |
| 6040 | `OrderAlreadyFilled` | Order already fully filled, cannot modify | `IncreaseLimitOrder` / `DecreaseLimitOrder` được gọi trên một lệnh có phần chưa điền bằng không. | "Lệnh giới hạn này đã được điền — thanh toán để nhận đầu ra." |
| 6041 | `InvalidOrderPhase` | Invalid order phase | Đang thay đổi một lệnh có pha FIFO cohort không còn khớp với pha cohort hiện tại của tick. | "Không thể thực hiện hành động này ở trạng thái hiện tại của lệnh." |
| 6042 | `InvalidLimitOrderAmount` | Invalid limit order amount | Đầu vào lệnh dưới mức tối thiểu của pool (hoặc bằng không) tại thời gian mở / tăng / giảm. | "Kích thước lệnh dưới mức tối thiểu của pool." |
| 6043 | `OrderPhaseSaturated` | Tick order phase saturated | Bộ đếm `order_phase` của cohort trên tick đã bão hòa; không thể mở thêm lệnh ở tick đó cho đến khi các cohort hiện có thanh toán và cuộn lại. | "Quá nhiều lệnh hoạt động ở mức giá này; hãy thử một tick gần đó hoặc chờ các lệnh thanh toán." |
| 6044 | `InvalidDynamicFeeConfigParams` | Invalid dynamic fee config params | `CreateDynamicFeeConfig` / `UpdateDynamicFeeConfig` bị từ chối; hoặc `CreateCustomizablePool` đã bật phí động mà không có cấu hình hợp lệ. | N/A trên đường dẫn quản trị viên; "Cấu hình phí động không hợp lệ" trên đường dẫn người dùng. |
| 6045 | `InvalidFeeOn` | Invalid fee on which token (must be 0, 1, or 2) | `CreateCustomizablePool` đã chuyển một giá trị `collect_fee_on` ngoài `{0, 1, 2}`. | "Nội bộ: chế độ phí không hợp lệ." |
| 6046 | `ZeroSqrtPrice` | sqrt_price_x64 must be greater than 0 | `CreateCustomizablePool` (hoặc một đường dẫn khác chấp nhận giá sqrt ban đầu có thể tùy chỉnh) được gọi với `sqrt_price_x64 == 0`. | "Giá ban đầu phải lớn hơn không." |
| 6047 | `ZeroLiquidity` | liquidity must be greater than 0 | Một đường dẫn cung cấp thanh khoản được gọi với `liquidity == 0` và không có số tiền bù đắp. | "Số tiền thanh khoản phải lớn hơn không." |
| 6048 | `MissingBaseFlag` | base_flag is required when liquidity is zero | Một đường dẫn mở vị trí theo số tiền đã tính toán `liquidity == 0` và người gọi không cung cấp `base_flag` để phân biệt bên nào là cơ sở. | "Cung cấp thanh khoản khác không hoặc chỉ định token nào là cơ sở." |
| 6049 | `MissingMintAccount` | Mint account is required but not provided | Một đường dẫn nhận biết Token-2022 được gọi mà không có tài khoản mint đầu vào/đầu ra cần thiết để xác thực phần mở rộng và phí chuyển. | "Nội bộ: tài khoản mint bị thiếu — làm mới dữ liệu pool." |
| 6050 | `MissingTokenProgram2022` | Token-2022 program is required but not provided | Tương tự như trên đối với tài khoản chương trình SPL-Token-2022. | "Nội bộ: chương trình Token-2022 bị thiếu — làm mới và thử lại." |

> **Lưu ý về đánh số lại.** Enum `ErrorCode` của CLMM đã được đánh số lại trong bản phát hành này: năm biến thể kế thừa (`LOK`, `ZeroMintAmount`, `InvalidLiquidity`, `TransactionTooOld`, `InvalidRewardDesiredAmount`) và một số lỗi chính tả (`Liquitity`, `enought`, `emissiones`) đã bị xóa/sửa, và mười một biến thể mới đã được thêm vào. Vì Anchor đánh số lỗi theo thứ tự nguồn, mọi mã ở hoặc sau `6000` đã thay đổi so với các bản dựng trước phát hành. Các máy khách đã mã hóa cứng các mã số so với phiên bản trước cần ánh xạ lại.

Nguồn tái tạo: [github.com/raydium-io/raydium-clmm — error.rs](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs).

## Lỗi AMM v4, Farm v3 / v5 / v6, LaunchLab

Các chương trình này được ghi chép trong các chương tương ứng của chúng (xem [`products/amm-v4/instructions`](/vi/products/amm-v4/instructions), [`products/farm-staking/instructions`](/vi/products/farm-staking/instructions), [`products/launchlab/instructions`](/vi/products/launchlab/instructions)). Vì những chương trình này sử dụng một hỗn hợp các bề mặt lỗi Anchor và Solana thuần túy, các bảng lỗi của chúng nằm cạnh tham chiếu hướng dẫn thay vì ở đây. Các mã dưới đây được dành riêng bởi các chương:

| Chương trình | Phạm vi mã (chỉ Anchor) | Tham chiếu |
|---------|--------------------------|-----------|
| AMM v4 | Mã u32 tùy chỉnh, không phải kiểu Anchor | [`products/amm-v4/instructions`](/vi/products/amm-v4/instructions) |
| Farm v3 / v5 | Mã u32 tùy chỉnh | [`products/farm-staking/instructions`](/vi/products/farm-staking/instructions) |
| Farm v6 | 6000+ (Anchor) | [`products/farm-staking/instructions`](/vi/products/farm-staking/instructions) |
| LaunchLab | 6000+ (Anchor) | [`products/launchlab/instructions`](/vi/products/launchlab/instructions) |

Các bản phát hành LaunchLab gần đây giữ nguyên các mã `6000`–`6021`. Bản phát hành 2026-08-17 đã xóa các lỗi truy cập toàn cầu nền tảng trước đó và gán `6022` cho lỗi xác thực tài khoản cho phép; bản phát hành 2026-08-24 thêm `6023`; bản phát hành 2026-08-31 thêm `6024`–`6030`:

| Mã | Tên | Ý nghĩa |
|---|---|---|
| 6018 | `NotEnoughRemainingAccounts` | Một tài khoản còn lại bắt buộc bị thiếu. Điều này bao gồm `PlatformAllowConfig` bắt buộc, `PlatformCurveRule` bắt buộc và bộ tài khoản di chuyển CPMM mở rộng. |
| 6020 | `CurveParamIsNotExist` | Đã loại bỏ. Được giữ lại như một phần giữ chỗ để các mã sau nó không thay đổi; bản phát hành 2026-08-31 đã xóa người gọi cuối cùng của nó. |
| 6022 | `InvalidPlatformAllowConfig` | Tài khoản cho phép được cung cấp không mã hóa cặp cấu hình nền tảng và toàn cầu dự kiến. |
| 6023 | `CalculateOverflow` | Tính toán phí chuyển Token-2022 hoặc phí chuyển ngược không thể được biểu diễn. Có thể tiếp cận trên các đường dẫn giao dịch chính xác khi phí mint trích dẫn không thể được đảo ngược (ví dụ: mint phí 100%). |
| 6024 | `InvalidPlatformCurveRule` | Tài khoản quy tắc được cung cấp không phải là PDA của cặp nền tảng và cấu hình được khởi chạy dưới. |
| 6025 | `CurveParamNotMatchPlatformRule` | Các tham số khởi chạy không thỏa mãn bất kỳ nhóm kiểm tra nào của quy tắc nền tảng. Lỗi duy nhất mà người tạo thực sự có thể gặp — hiển thị nó dưới dạng "hình dạng khởi chạy này không được phép trên nền tảng này". |
| 6026 | `CurveRuleGroupNotExist` | `RemovePlatformCurveRule` được gọi với `group_id` mà quy tắc không giữ. |
| 6027 | `CurveRuleGroupsExceeded` | Một quy tắc không thể giữ nhiều hơn 10 nhóm kiểm tra. |
| 6028 | `InvalidCurveRuleConstraint` | Một ràng buộc có trường hoặc toán tử không xác định, một nhóm có nhiều hơn 25 ràng buộc, hoặc cặp `(field, op)` giống nhau xuất hiện hai lần trong một nhóm. |
| 6029 | `InvalidCurveRuleAuthority` | Người ký không phải là `PlatformConfig.curve_rule_manager` cũng không phải là quản trị viên nền tảng. |
| 6030 | `CurveRuleFieldNotSupportedByCurve` | Một ràng buộc đọc `total_base_sell` trên cấu hình có đường cong của nó — các đường cong giá cố định và giá tuyến tính. Được nâng lên khi quy tắc được viết, không phải khi khởi chạy chạy. |

Các biến thể `PlatformGlobalAccessDenied` (`6022`) và `InvalidPlatformGlobalAccess` (`6023`) cũ đã bị xóa. Không giải mã `6022` bằng IDL trước đó, và lưu ý rằng `6023` — được giải phóng bởi bản phát hành 2026-08-17 — được sử dụng lại bởi bản phát hành 2026-08-24 cho một lỗi không liên quan, vì vậy nó không được giải mã bằng IDL từ bất kỳ phiên bản trước nào.

`6020` được giữ lại cố ý. Anchor đánh số các biến thể theo vị trí, vì vậy xóa nó sẽ dịch chuyển mọi mã sau nó và phá vỡ mọi máy khách giải mã `6021`–`6030`.

## Ánh xạ lỗi SDK cho lỗi chương trình

SDK TypeScript chính thức bao bọc các lỗi on-chain thành `SendTransactionError` và, đối với các chương trình Anchor, `AnchorError`:

```ts
import { AnchorError } from "@coral-xyz/anchor";

try {
  await sdk.cpmm.swap({ ... });
} catch (err) {
  if (err instanceof AnchorError) {
    console.log(err.error.errorCode.number, err.error.errorCode.code, err.error.errorMessage);
    // 6005 ExceededSlippage "Exceeds desired slippage limit"
  } else {
    // Raw SendTransactionError — inspect err.logs for the "custom program error: 0x..." line.
  }
}
```

Nếu bạn không sử dụng máy khách Anchor, hãy phân tích nhật ký giao dịch:

```
Program <ProgramID> invoke [1]
Program log: AnchorError caused by account: pool_state. Error Code: ExceededSlippage. Error Number: 6005. Error Message: Exceeds desired slippage limit.
```

Mẫu `Error Number: (\d+)` ổn định trên các phiên bản Anchor và an toàn để khớp.

## Tái tạo các bảng này

Khi một chương trình được nâng cấp và thêm một lỗi mới, hãy trích xuất lại từ nguồn:

```bash
# Clone and grep the error enum in order.
git clone --depth=1 https://github.com/raydium-io/raydium-cp-swap
awk '/#\[msg\(/{ gsub(/^[ \t]*#\[msg\("|"\)\][ \t]*/,""); m=$0; getline; gsub(/,/,""); gsub(/^[ \t]+/,""); print "6000+++\t" $0 "\t" m }' \
  raydium-cp-swap/programs/cp-swap/src/error.rs
# Replace 6000+++ with monotonically-increasing 6000,6001,... Append new rows to the table above.
```

Luôn cập nhật [`reference/changelog`](/vi/reference/changelog) khi thêm một biến thể mới, để những người tích hợp nâng cấp SDK biết cần làm mới trình xử lý lỗi của họ.

Nguồn:

- [raydium-cp-swap error.rs (mainnet CPMM)](https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/error.rs)
- [raydium-clmm error.rs (mainnet CLMM)](https://github.com/raydium-io/raydium-clmm/blob/master/programs/amm/src/error.rs)
