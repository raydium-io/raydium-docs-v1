---
title: "2026-08-17 — CPMM: gebührenfreie Creator-Fee-Erfassung"
description: "CPMM fügt eine zielgerichtete Anweisung hinzu, die es jedem Zahler ermöglicht, Creator-Gebühren zum kanonischen ATA des Pool-Erstellers zu übertragen, und delegiert die Permission-PDA-Erstellung ohne Widerrufsdelegation."
---

<Info>
  **Diese Seite wurde mit KI automatisch übersetzt. Maßgeblich ist stets die englische Version.**

  [Englische Version ansehen →](/reference/changelog/2026-08-17-cpmm-permissionless-creator-fee-collection)
</Info>

<Info>
  Ein Dokumentations-Changelog-Eintrag für ein bevorstehendes CPMM-Programm-Update. Für den Index aller Updates siehe [`reference/changelog`](/de/reference/changelog). Für die historische Zeitleiste des Protokolls siehe [`introduction/history-and-milestones`](/de/introduction/history-and-milestones).
</Info>

Dieses Release fügt einen gebührenfreien Betriebspfad für die Creator-Fee-Erfassung hinzu. Jeder kann zahlen, um die Übertragung auszulösen, aber das Programm legt den Empfänger-Owner auf `PoolState.pool_creator` fest und setzt beide Ziele auf die kanonischen Associated Token Accounts des Erstellers. Die bestehende, vom Creator signierte Anweisung bleibt unverändert.

Es ermöglicht auch einer dedizierten Autorität, CPMM-`Permission`-PDAs zu erstellen. Diese Delegierung ist absichtlich asymmetrisch: Die dedizierte Autorität kann Zugriff auf `InitializeWithPermission` gewähren, während nur der Programm-Admin einen Zugriff mit `ClosePermissionPda` widerrufen kann.

## TL;DR für Integratoren

- **Neue additive Anweisung: `CollectCreatorFeePermissionless`.** Jeder Unterzeichner kann der `payer` sein. Der eingetragene Pool-Ersteller signiert nicht.
- **Der Aufrufer kann Gelder nicht umleiten.** `creator` muss gleich `pool_state.pool_creator` sein. `creator_token_0` und `creator_token_1` müssen die kanonischen ATAs dieses Erstellers für die Pool-Vault-Mints und ihre jeweiligen Token-Programme sein.
- **Der Zahler deckt fehlende ATAs ab.** Beide Empfängerkonten verwenden `init_if_needed`; der Zahler finanziert die Miete, wenn eines der Konten nicht existiert.
- **Erfassung ist alles oder nichts.** Die Anweisung überträgt beide vollständig aufgelaufenen Creator-Fee-Zähler, setzt beide auf Null und aktualisiert `recent_epoch`. Sie gibt den bestehenden `NoFeeCollect`-Fehler zurück, wenn beide Zähler Null sind.
- **Bestehende Integrationen bleiben kompatibel.** `CollectCreatorFee` wird nicht geändert oder entfernt. Es gibt keine `PoolState`-, `AmmConfig`-, `Permission`-, Event- oder Error-Code-Layout-Änderungen.
- **Permission-Erstellung hat einen zweiten Owner.** `CreatePermissionPda` akzeptiert den Programm-Admin oder einen dedizierten Create-Permission-PDA-Owner. `ClosePermissionPda` bleibt nur für Admins.

## Neue Anweisungskonten

`CollectCreatorFeePermissionless` nimmt 14 Konten in dieser Reihenfolge:

| # | Konto | Einschränkung oder Rolle |
|---|---------|--------------------|
| 1 | `payer` | Beschreibbar, Unterzeichner; finanziert fehlende Creator-ATAs. |
| 2 | `creator` | Muss gleich `pool_state.pool_creator` sein; signiert nicht. |
| 3 | `authority` | CPMM globale Vault-Autorität PDA. |
| 4 | `pool_state` | Beschreibbar; enthält die Creator-Fee-Zähler. |
| 5–6 | `token_0_vault`, `token_1_vault` | Beschreibbar; müssen den in dem Pool gespeicherten Vaults entsprechen. |
| 7–8 | `vault_0_mint`, `vault_1_mint` | Müssen dem entsprechenden Vault-Mint entsprechen. |
| 9–10 | `creator_token_0`, `creator_token_1` | Beschreibbare kanonische Creator-ATAs; werden bei Bedarf initialisiert. |
| 11–12 | `token_0_program`, `token_1_program` | SPL Token oder Token-2022 für jeden Mint. |
| 13 | `associated_token_program` | Erstellt einen fehlenden Creator-ATA. |
| 14 | `system_program` | Unterstützt die vom Zahler finanzierte Kontoerstellung. |

Im Gegensatz zu `CollectCreatorFee` hat das neue Layout separate `payer`- und nicht signierende `creator`-Konten und nimmt nicht `amm_config`. Es erfordert daher seinen eigenen Diskriminator und Builder.

## SDK- und IDL-Auswirkungen

Der Programm-Source-Branch enthält kein committiertes SDK- oder IDL-Update. Integratoren müssen die bereitgestellte CPMM-IDL aktualisieren oder einen Client-Release verwenden, der `CollectCreatorFeePermissionless` explizit unterstützt. Der in der Dokumentation angeheftete `@raydium-io/raydium-sdk-v2@0.2.42-alpha`-Builder-Satz deckt nur den ursprünglichen vom Creator signierten Pfad ab.

## Sicherheitsgrenze

Gebührenfreie Invokation bedeutet nicht gebührenfreie Verwahrung. Der Aufrufer kann wählen, wann die Erfassung stattfindet, und kann SOL ausgeben, um fehlende Creator-ATAs zu erstellen. Der Aufrufer kann den Begünstigten nicht wählen, beliebige Token-Konten ersetzen, einen Teilbetrag anfordern oder die Creator-Fee-Konfiguration des Pools ändern.

Die neue Permission-PDA-Creator-Rolle kann einen Zugriff für jede `permission_authority` gewähren und zahlt die Miete des neuen Kontos. Sie kann diese PDA unter dieser Änderung nicht schließen. Behandeln Sie sie als delegierte Zugriffsberechtigung, nicht als vollständiger Permission-Administrator.

## Migrationsnoten

- Behalten Sie bestehende `CollectCreatorFee`-Transaktionen unverändert bei.
- Fügen Sie die gebührenfreie Anweisung nur nach der Ziel-CPMM-Bereitstellung und dem Client-IDL hinzu, die sie enthalten.
- Automatisierung kann Pools mit nicht-Null `creator_fees_token_0` oder `creator_fees_token_1` scannen und die Erfassung ohne Verwahrung von Creator-Schlüsseln auslösen.
- Behandeln Sie `NoFeeCollect` als gutartiges Rennen, wenn ein anderer Aufrufer die Zähler zuerst leert.
- Übergeben Sie keine beliebigen Empfänger-Token-Konten; leiten Sie die Creator-ATAs von `pool_state.pool_creator`, jedem Vault-Mint und dem entsprechenden Token-Programm ab.

## Aktualisierte Seiten

- `products/cpmm/overview` — Creator-Fee-Erfassungs-Update und Ziel-Sicherheits-Zusammenfassung.
- `products/cpmm/instructions` — neue Anweisungs-Zusammenfassung, vollständige Kontoliste, Effekte und State-Change-Matrix.
- `products/cpmm/accounts` — Creator-Fee-Zähler-Erfassungspfade und delegierte Permission-PDA-Erstellung.
- `products/cpmm/fees` — gebührenfreie Erfassungsmechanik und Betriebsfluss.
- `products/cpmm/code-demos` — SDK-Verfügbarkeitswarnung und Client-Migrations-Grenze.
- `products/launchlab/platform-config` — klargestellt, dass der eingetragene CPMM-Creator der Begünstigte bleibt, auch wenn ein anderer Zahler die Erfassung auslöst.
- `protocol-overview/shared-infrastructure` — Autorität-gesteuerte und zielgerichtete Erfassungspfade.
- `security/admin-and-multisig` — delegierte Zugriffsberechtigung und gebührenfreie Collector-Grenzen.
- `reference/program-addresses` — kanonische dedizierte `CreatePermissionPda`-Owner-Adressen.
- `reference/error-codes` — erweiterter `NoFeeCollect` zur Abdeckung der Creator-Fee-Erfassung.

**Verifiziert 2026-08-13 gegen**:

- [`raydium-cp-swap` Commit `244e124` — *Feat/permissionless collect creator fee (#76)*](https://github.com/raydium-io/raydium-cp-swap/commit/244e1241f3c8d90eb93f176dfbc35f2605ec5a5c), das Merge des `feat/permissionless-collect-creator-fee`-Branches, gegen den dieser Eintrag geschrieben wurde. Der Branch selbst wurde nach dem Merge gelöscht.
- Commits [`9585ea1`](https://github.com/raydium-io/raydium-cp-swap/commit/9585ea1efeb27be7be7eab1ebc033449f9c793b4) und [`d99d542`](https://github.com/raydium-io/raydium-cp-swap/commit/d99d5420c65b973af05d4efa009a2a9d060461f9), verglichen mit `master` bei [`78f254e`](https://github.com/raydium-io/raydium-cp-swap/commit/78f254e1023751e706df7dc15c453fc3e046697c).

<Warning>
  Diese Verifizierung deckt den Source-Branch ab, nicht eine Mainnet-Beta-Bereitstellung. Bestätigen Sie das bereitgestellte Programm und die veröffentlichte IDL, bevor Sie den neuen Pfad in der Produktion aktivieren.
</Warning>
