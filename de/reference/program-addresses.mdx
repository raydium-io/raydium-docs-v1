---
title: "Programmadressen"
description: "Kanonische Raydium-Programm-IDs und gemeinsame PDAs auf Solana mainnet-beta und devnet. Jede andere Seite verlinkt hierher, statt Adressen hartcodiert zu haben."
---

<Info>
  **Diese Seite wurde mit KI automatisch übersetzt. Maßgeblich ist stets die englische Version.**

  [Englische Version ansehen →](/reference/program-addresses)
</Info>

<Info>
  **Maßgebliche Quelle.** Diese Seite ist der einzige Ort in der Dokumentation, an dem Programmadressen wörtlich aufgelistet sind. Andere Seiten verlinken hierher. Falls sich eine ID ändert, aktualisieren Sie sie **nur hier**, und alle Verweise auf der Website bleiben konsistent.

  Überprüfen Sie die Werte immer gegen die Live-API (`https://api-v3.raydium.io/main/info`), bevor Sie echte Transaktionen signieren.
</Info>

## Mainnet-beta

<Tip>
  Faustregel: Falls eine Programm-ID in der Praxis nicht mit der Tabelle unten übereinstimmt, **signieren Sie die Transaktion nicht.** Eine nicht übereinstimmende Programm-ID ist der einfachste Weg, Gelder auf Solana zu verlieren.
</Tip>

### On-Chain-Programme

| Programm | Programm-ID | Quelle |
|---------|------------|--------|
| **AMM v4** (Hybrid AMM + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (Standard AMM) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (Konzentrierte Liquidität) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (StableSwap-ähnliche Kurve) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | Quelle nicht öffentlich verfügbar |
| **Farm v3** (Legacy RAY Staking) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | Quelle nicht öffentlich verfügbar |
| **Farm v5** (Legacy Ökosystem-Farmen) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | Quelle nicht öffentlich verfügbar |
| **Farm v6** (Aktuelle Ökosystem-Farmen) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | Quelle nicht öffentlich verfügbar |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | Quelle nicht öffentlich verfügbar |
| **AMM Routing** (Raydiums On-Chain-Router) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | Quelle nicht öffentlich verfügbar |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | Quelle nicht öffentlich verfügbar |

Hinweise:

- AMM v4 und Farm v3/v5 werden **für bestehende Pools und Positionen beibehalten**. Die Raydium-UI und das SDK leiten die Erstellung neuer Pools und neue Ökosystem-Farmen an CPMM, CLMM bzw. Farm v6 weiter – siehe [`protocol-overview/versions-and-migration`](/de/protocol-overview/versions-and-migration).
- Die Farm v3 ID oben dient auch als \$RAY Single-Asset-Staking-Programm. Das Verhalten ist identisch mit Farm v3 für LP-Staking.
- **Stable AMM** ist ein separates Programm, das das AMM Routing-Programm neben AMM v4, CPMM und CLMM ansteuern kann. Die Liquidität ist im Vergleich zu den anderen drei Programmen dünn, und das SDK stellt keine First-Class-API dafür bereit; Integratoren, die es durchleiten, tun dies in der Regel über den Router.
- **Verfügbarkeit des Quellcodes.** Von den On-Chain-Programmen oben werden nur `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM) und `raydium-clmm` (CLMM) mit öffentlichen Quellrepositorys unter [`github.com/raydium-io`](https://github.com/raydium-io) ausgeliefert. Stable AMM, LaunchLab, AMM Routing, Burn & Earn / LP Lock und die Farm-Programme sind nicht öffentlich verfügbar – überprüfen Sie sie stattdessen gegen die Live-API, den On-Chain-Bytecode und die veröffentlichten IDLs in [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl).

### Gemeinsame Admin-Autorität

Jedes Raydium-Programm – einschließlich AMM v4 – teilt sich einen einzigen hartcodierten Admin-`Pubkey` für Zugriffskontrolle auf Anweisungsebene zu Admin-Pfaden. Es ist `config_feature::amm_owner::ID` in AMM v4 (gating `SetParams`, `CreateConfigAccount` und `UpdateConfigAccount`, und als Standard-`pnl_owner` verwendet) und `admin::ID` in den Anchor-Programmen – CLMM, CPMM, LaunchLab, Lock – gating Pfade wie `CreateAmmConfig` oder `UpdatePoolStatus`:

| Cluster | Admin |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

Betriebliche Autoritäten auf Kontoebene (z. B. `protocol_owner`, `fund_owner` auf CPMM/CLMM `AmmConfig`, oder die Migrations-Wallets auf LaunchLab `GlobalConfig`) werden On-Chain gespeichert und können sich vom Programm-Admin unterscheiden. Lesen Sie sie direkt vom relevanten Config-Konto, bevor Sie hochriskante Transaktionen senden.

### CPMM Permission-PDA Creator Authority

`CreatePermissionPda` akzeptiert entweder den gemeinsamen Programm-Admin oben oder einen dedizierten Owner. Seit dem CPMM-Upgrade 2026-09 akzeptiert `ClosePermissionPda` die gleichen zwei Unterzeichner; davor war es nur Admin.

| Cluster | Dedizierter `CreatePermissionPda` Owner |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### Support-Mint Registry Autoritäten

`CreateSupportMintAssociated` und `CloseSupportMintAssociated` akzeptieren entweder den gemeinsamen Programm-Admin oder den dedizierten Owner des Programms unten. Das Registrieren eines Mint erlaubt ihm, die Token-2022-Erweiterungs-Allowlist des Programms zu passieren – siehe [`reference/token-2022-support`](/de/reference/token-2022-support#the-one-bypass-the-per-mint-registry). CPMM und CLMM führen jeweils ihre eigene Registry-PDA und ihre eigene Autorität; keiner der Schlüssel funktioniert auf dem anderen Programm.

| Programm | Cluster | Dedizierter Support-Mint Owner |
|---------|---------|------------------------------|
| CPMM | mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| CPMM | devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |
| CLMM | mainnet-beta | `RayVyjyJQz9vAi126A4sGexKnSU1XeZaHTRcM1mZMPY` |
| CLMM | devnet | `rayf3nEbb3bnfN6RDGFpqPbjc5uUa3tRUzu6UVYrRx5` |

### CPMM Fee-Owner Wallets

Seit dem CPMM-Upgrade 2026-09 schreibt `CreateAmmConfig` diese festen Schlüssel in die `protocol_owner` und `fund_owner` der neuen Config, statt den Schlüssel des Admin-Unterzeichners zu kopieren. `CollectProtocolFee` und `CollectFundFee` akzeptieren den auf der Config gespeicherten Schlüssel **oder** den gemeinsamen Programm-Admin (`|| crate::admin::ID`), sodass der Admin ein akzeptierter Unterzeichner bleibt – was sich geändert hat, ist nur, wohin die Einnahmen standardmäßig gehen.

| Cluster | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  Dies sind die Werte, die das Programm *in neue Configs schreibt*, keine Beschreibung bestehender. `AmmConfig`-Konten, die vor dem Upgrade erstellt wurden, behalten das, was auf ihnen gespeichert ist. Lesen Sie immer `protocol_owner` / `fund_owner` vom Konto, bevor Sie eine Sammel-Transaktion erstellen.
</Warning>

### Protokoll-Gebühren-Sammelwallets

Wo sich der Protokoll-Anteil der Handelsgebühren jedes Programms ansammelt. Dies sind gewöhnliche Token-Holding-Wallets, daher trägt eine einzelne Adresse eine gemischte Menge von SPL-Mints (wSOL und USDC von SOL/USDC-Pools, wSOL und RAY von SOL/RAY-Pools usw.).

| Programm | Sammeladresse | Gespeichert als |
|---------|--------------------|-----------|
| CLMM | `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416` | `AmmConfig.owner` auf allen 21 Configs |
| CPMM | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `AmmConfig.protocol_owner` (siehe [CPMM Fee-Owner Wallets](#cpmm-fee-owner-wallets)) |
| AMM v4 | `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF` | `AmmConfig.pnl_owner` auf der einzelnen Config-PDA |

Der Protokoll-Anteil von Stable AMM sammelt sich im Tresor als `out_put.need_take_pnl_*` an und wird durch `WithdrawPnl` geleert, statt in einer stehenden Wallet zu sitzen. Für CPMM lesen Sie `protocol_owner` von der spezifischen `AmmConfig`, bevor Sie eine Sammel-Transaktion erstellen – Configs, die vor dem Upgrade 2026-09 erstellt wurden, können einen anderen Schlüssel enthalten.

### Excess-Lamports Sammelwallets

Jedes der drei Programme, die am 2026-09-09 aktualisiert wurden, erhielt eine Admin-Anweisung, die Lamports zurückgibt, die über dem Rent-Exempt-Minimum auf Programm-kontrollierten Konten gehalten werden, gemäß [SIMD-0437 Rent-Reduktion](/de/solana-fundamentals/rent-and-reclaimable-rent). Jedes Programm hartcodiert sein eigenes Ziel-Wallet, und jedes ist ein dedizierter Schlüssel – nicht der gemeinsame Programm-Admin.

| Programm | Anweisung | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM und LaunchLab akzeptieren zusätzlich den gemeinsamen Programm-Admin als Unterzeichner. AMM v4 akzeptiert **nur** das Wallet oben – es hat kein Admin-Fallback. AMM v4 kompiliert auch einen separaten Testnet-Schlüssel (`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`) unter seinem `testnet`-Feature.

Diese Anweisungen verschieben nur Lamports; sie berühren niemals einen Token-Saldo, schließen ein Konto oder ändern den Pool-Status. Siehe [`products/amm-v4/instructions`](/de/products/amm-v4/instructions#withdrawexcesslamports), [`products/cpmm/instructions`](/de/products/cpmm/instructions#collectexcesslamports) und [`products/launchlab/instructions`](/de/products/launchlab/instructions#collectexcesslamports).

### LaunchLab delegierte Config Creator

`CreateConfig` akzeptiert entweder den gemeinsamen Programm-Admin oder die delegierte Autorität unten. Die delegierte Rolle kann eine neue `GlobalConfig` erstellen; sie kann den Admin-Only-Pfad `UpdateConfig` für ein bestehendes Konto nicht aufrufen.

| Cluster | Delegierte `CreateConfig` Autorität |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### CLMM Restricted-Issuer Freeze Authorities

Wenn einer der Vault-Mints in einem CLMM-Pool eine der Freeze-Autoritäten unten hat, haben neue Positionen, die über einen V2-Pfad geöffnet werden, ihr Position-NFT-Token-Konto eingefroren. Die Liste ist `frozen_position_nft_authorities::IDS` im CLMM-Programm, und sein einziger Eintrag ist die Issuer-Autorität, die auch die seit langem entfernte Superstate-Erkennung von CLMM abgeglichen hat. Dies sind **Underlying-Asset-Issuer-Autoritäten, die nur als Match-Schlüssel verwendet werden**. Die CLMM-Pool-PDA, nicht der Issuer-Schlüssel, wird Freeze-Autorität für den Position-NFT-Mint.

| Cluster | Restricted Issuer Freeze Authority |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

Die Liste ist im CLMM-Programm hartcodiert. Das Hinzufügen oder Entfernen einer Autorität erfordert ein Programm-Upgrade.

### Gemeinsame Config / PDA Konventionen

Mehrere Raydium-Programme stellen **Config-Konten** bereit, deren öffentliche Schlüssel stabil sind und auf der öffentlichen API aufgelistet sind. Bevorzugen Sie die API-Suche gegenüber dem Hartcodieren:

```
# CPMM Fee-Configs (gibt ein Array von {id, index, tradeFeeRate, ...} zurück)
GET https://api-v3.raydium.io/main/cpmm-config

# CLMM Fee-Configs
GET https://api-v3.raydium.io/main/clmm-config
```

### Pool-Erstellungs-Gebühren-Empfänger

Sowohl CPMM als auch AMM v4 validieren ihr Pool-Erstellungs-Gebühren-Ziel **nach exakter Adresse**, daher muss es bereits auf dem Cluster vorhanden sein, gegen den Sie testen. CLMM hat keine separate Pool-Erstellungs-Gebühr.

| Programm | Konstante | mainnet-beta | devnet |
|---------|----------|--------------|--------|
| CPMM | `create_pool_fee_reveiver::ID` (der Tippfehler ist in der Quelle) | `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` | `3oE58BKVt8KuYkGxx8zBojugnymWmBiyafWgMrnb6eYy` |
| AMM v4 | `config_feature::create_pool_fee_address::ID` | `7YttLkHDoNj9wyDur5pM1ejNaAvT9X4eqaYcHQqtj2G5` | `9y8ENuuZ3b19quffx9hQvRVygG5ky6snHfRvGpuSfeJy` |

AMM v4 kompiliert auch `3TRTX4dXUpp2eqxi3tvQDFYUV7SdDJjcPE3Y4mbtftaX` unter seinem `testnet`-Feature. CPMM lehnt eine Nichtübereinstimmung mit Anchors `ConstraintAddress` (`2012`) ab; AMM v4 mit `InvalidFee`.

### CLMM Limit-Order Keeper

`SettleLimitOrder` und `CloseLimitOrder` akzeptieren entweder den `owner` der Order selbst oder den Programm-weiten Keeper-Schlüssel unten. Der Keeper kann Orders nicht öffnen, erhöhen oder verringern, und die Ausgabe landet immer auf dem Konto des Order-Owners – siehe [`security/admin-and-multisig`](/de/security/admin-and-multisig).

| Cluster | `limit_order_admin` |
|---------|---------------------|
| mainnet-beta | `Ray8HHtixhL9zvnokMyELCVGp622PDPJj96zcVC9RWp` |
| devnet | `DRaypkxM96mjYYnqMmuYjsSL3stHxLf1CvYuTcqxUaav` |

Standard-CPMM `AmmConfig` Index `0` (Standard 0,25% Pool) Fee-Parameter, zur Referenz:

| Feld | Wert | Bedeutung |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 0,25% des Handelsvolumens |
| `protocol_fee_rate` | `120000` | 12% **der Handelsgebühr** (nicht des Volumens) an das Protokoll |
| `fund_fee_rate` | `40000` | 4% **der Handelsgebühr** an das Fund-Multisig |
| `creator_fee_rate` | `500` | 0,05% des Handelsvolumens an den Pool-Creator (optional) |

Siehe [`products/cpmm/fees`](/de/products/cpmm/fees) für die Zusammensetzung der Aufteilungen und [`reference/fee-comparison`](/de/reference/fee-comparison) für die Cross-Product-Matrix.

### PDA Seeds

Die Seeds unten sind **kanonisch** und werden sowohl vom SDK als auch von On-Chain-CPIs verwendet. Berechnen Sie immer PDAs; codieren Sie abgeleitete Adressen nicht hart.

```ts
// CPMM — alle Seeds sind statische ASCII-Strings, sofern nicht anders angegeben.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` sind **nach öffentlichem Schlüssel-Byte-Reihenfolge sortiert** (token0 < token1), bevor sie gehasht werden. Dies falsch zu machen ergibt eine gültige PDA für einen nicht existierenden Pool.

Die äquivalenten CLMM-Seeds folgen dem gleichen Stil; siehe [`products/clmm/accounts`](/de/products/clmm/accounts).

## Devnet

| Programm | Programm-ID |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM Routing** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DRay25Usp3YJAi7beckgpGUC7mGJ2cR1AVPxhYfwVCUX` |
| **Farm v3 / v5 / v6** | Nicht zuverlässig für devnet veröffentlicht – bestätigen Sie über die Live-API (`https://api-v3-devnet.raydium.io/main/info`) vor der Verwendung. |

Devnet REST API Base: `https://api-v3-devnet.raydium.io/` (gleiche Route-Form wie mainnet).

## Upgrade-Autorität

Jedes Raydium-Programm auf mainnet-beta wird unter dem BPF Upgradeable Loader mit der **gleichen** Upgrade-Autorität bereitgestellt – dem Programm-Upgrade Squads Multisig Vault:

| | Wert |
|---|---|
| Upgrade-Autorität (alle Programme) | `FytDrVzDybM1TwFQPGb8qaxZR7dBCzNeqT3vtQsceZQK` |
| Squads Multisig Config-Konto | `tr8rgazUrZzgdkfc6Q622nVJHMMzh29trdBE2uBHb4u` |
| Squads Programm | `SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf` (v4) |

Verifiziert On-Chain am 2026-09-09 für AMM v4, CPMM, CLMM, Stable AMM, Farm v6, LaunchLab, AMM Routing und Lock – alle acht berichten diese Autorität, und keine ist unveränderlich. Überprüfen Sie erneut mit `solana program show <PROGRAM_ID> -u mainnet-beta`, bevor Sie eine hochriskante Bereitstellung durchführen; siehe [`security/admin-and-multisig`](/de/security/admin-and-multisig) für den Schwellenwert des Multisigs und die Signierungspraxis.

## Programm-Level Config-Konten

| Programm | Config-Konto | Inhalt |
|---------|----------------|----------|
| AMM v4 | `9DCxsMizn3H1hprZ7xWe6LDzeUeZBksYFpBWBtSf1PQX` | Die einzelne `AmmConfig` PDA bei Seed `["amm_config_account_seed"]`. Enthält `pnl_owner` = `PNLCQcVCD26aC7ZWgRyr5ptfaR7bBrWdTFgRWwu2tvF`, `cancel_owner` = `RAYdmrNjdRSu7ErPv9s2BqDtRoFa1QECn7UJYaC2tR3` und `create_pool_fee` = `150000000` (0,15 SOL). |
| CPMM | 21 `AmmConfig` Konten (Index 0–20) | Index 0 ist `D4FPEruKEHrG5TenZ2mpDGEfu1iUvTiqBxvpU8HLBvC2`. Siehe [`reference/fee-comparison`](/de/reference/fee-comparison) für die Tier-Menge. |
| CLMM | 21 `AmmConfig` Konten (Index 0–20) | Index 1 (0,25%, Tick-Abstand 60) ist `E64NGkDLLCdQ2yFNPcavaKptrEgmiQaNykUuLC1Qgwyp`. `owner` = `projjosVCPQH49d5em7VYS7fJZzaqKixqKtus7yk416`, `fund_owner` = `FundHfY8oo8J9KYGyfXFFuQCHe7Z1VBNmsj84eMcdYs4` auf allen 21. |

Alle Werte von mainnet-beta am 2026-09-09 gelesen. Gebührensätze auf diesen Konten sind Admin-veränderlich – lesen Sie sie live, statt sie zu cachen.

## Wie man eine Adresse On-Chain verifiziert

1. **Solana Explorer.** Fügen Sie die Adresse in [explorer.solana.com](https://explorer.solana.com) ein und bestätigen Sie, dass sie als `Program` mit einer aktuellen Upgrade-Autorität markiert ist. Mainnet-beta sollte Bereitstellungen anzeigen, die von Raydiums Upgrade-Autorität signiert sind.
2. **CLI.** Verwenden Sie `solana program show <PROGRAM_ID> -u mainnet-beta`, um Deploy-Slot, BPF-Loader, Upgrade-Autorität und Datenlänge zu überprüfen. Notieren Sie diese in Ihrem Runbook.
3. **IDL-Anhang.** `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet` funktioniert für **CLMM, LaunchLab und Lock**, die noch ein Legacy-`anchor:idl`-Konto behalten, das vom Programm selbst besessen wird. Es funktioniert **nicht** für CPMM: CPMMs On-Chain-IDL wurde im Upgrade 2026-09 zum Program Metadata-Programm (`ProgM6JCCvbYkfKqJYHePx4xxSUSqJp7rh8Lyv7nk7S`) verschoben, daher rufen Sie es stattdessen von dort ab. AMM v4, Stable AMM und Farm haben keine On-Chain-IDL. Die IDLs werden unter [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) gespiegelt – vergleichen Sie die On-Chain-IDL mit dem Repo, bevor Sie ihr vertrauen.
4. **Config/Admin-Autoritäten.** Für CPMM/CLMM Config-Konten lesen Sie das Feld `owner`/`protocol_owner` und bestätigen Sie, dass es dem aktuellen Raydium-Multisig entspricht, das in [`security/admin-and-multisig`](/de/security/admin-and-multisig) veröffentlicht ist.

Falls einer der vier Überprüfungen oben mit dieser Seite nicht übereinstimmt, behandeln Sie diese Seite als falsch und öffnen Sie ein Issue, bevor Sie neuen Code gegen die Adressen schreiben.

## Diese Seite aktualisieren

- Änderungen an Programm-IDs sind sicherheitskritisch. Versenden Sie eine Programm-ID-Änderung **nicht**, ohne (a) auf die Raydium-Ankündigung zu verlinken, (b) einen Eintrag in [`reference/changelog`](/de/reference/changelog) hinzuzufügen und (c) eine Link-Überprüfung über die Docs auszuführen, um zu bestätigen, dass keine Seite noch auf den alten Wert verweist.
- Veraltete Einträge bleiben in der Tabelle mit einer Statusnotiz, statt gelöscht zu werden – bestehende Pools werden immer noch über das alte Programm aufgelöst.

Quellen:

- Live API: `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
