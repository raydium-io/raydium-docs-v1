---
title: "Programmadressen"
description: "Kanonische Raydium-Programm-IDs und gemeinsame PDAs auf Solana mainnet-beta und devnet. Jede andere Seite verlinkt hierher, statt Adressen hardcodiert zu verwenden."
---

<Info>
  **Diese Seite wurde mit KI automatisch übersetzt. Maßgeblich ist stets die englische Version.**

  [Englische Version ansehen →](/reference/program-addresses)
</Info>

<Info>
  **Maßgebliche Quelle.** Diese Seite ist der einzige Ort in der Dokumentation, an dem Programmadressen wörtlich aufgelistet sind. Andere Seiten verlinken hierher. Falls sich eine ID ändert, aktualisieren Sie sie **nur hier**, und alle Verweise auf der Website bleiben konsistent.

  Überprüfen Sie die Werte immer gegen die Live-API (`https://api-v3.raydium.io/main/info`), bevor Sie echte Transaktionen signieren.
</Info>

## Mainnet-beta

<Tip>
  Faustregel: Falls eine Programm-ID in der Praxis nicht mit der Tabelle unten übereinstimmt, **signieren Sie die Transaktion nicht.** Eine nicht übereinstimmende Programm-ID ist der einfachste Weg, um Gelder auf Solana zu verlieren.
</Tip>

### On-Chain-Programme

| Programm | Programm-ID | Quelle |
|---------|------------|--------|
| **AMM v4** (Hybrid AMM + OpenBook) | `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` | [raydium-amm](https://github.com/raydium-io/raydium-amm) |
| **CPMM** (Standard AMM) | `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` | [raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap) |
| **CLMM** (Konzentrierte Liquidität) | `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` | [raydium-clmm](https://github.com/raydium-io/raydium-clmm) |
| **Stable AMM** (StableSwap-ähnliche Kurve) | `5quBtoiQqxF9Jv6KYKctB59NT3gtJD2Y65kdnB1Uev3h` | Quelle nicht öffentlich verfügbar |
| **Farm v3** (Legacy RAY Staking) | `EhhTKczWMGQt46ynNeRX1WfeagwwJd7ufHvCDjRxjo5Q` | Quelle nicht öffentlich verfügbar |
| **Farm v5** (Legacy Ökosystem-Farmen) | `9KEPoZmtHUrBbhWN1v1KWLMkkvwY6WLtAVUCPRtRjP4z` | Quelle nicht öffentlich verfügbar |
| **Farm v6** (aktuelle Ökosystem-Farmen) | `FarmqiPv5eAj3j1GMdMCMUGXqPUvmquZtMy86QH6rzhG` | Quelle nicht öffentlich verfügbar |
| **LaunchLab** | `LanMV9sAd7wArD4vJFi2qDdfnVhFxYSUg6eADduJ3uj` | Quelle nicht öffentlich verfügbar |
| **AMM Routing** (Raydiums On-Chain-Router) | `routeUGWgWzqBWFcrCfv8tritsqukccJPu3q5GPP3xS` | Quelle nicht öffentlich verfügbar |
| **Burn & Earn / LP Lock** | `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE` | Quelle nicht öffentlich verfügbar |

Hinweise:

- AMM v4 und Farm v3/v5 werden **für bestehende Pools und Positionen beibehalten**. Die Raydium-UI und das SDK leiten die Erstellung neuer Pools und neue Ökosystem-Farmen an CPMM, CLMM bzw. Farm v6 weiter – siehe [`protocol-overview/versions-and-migration`](/de/protocol-overview/versions-and-migration).
- Die Farm v3 ID oben dient auch als \$RAY Single-Asset-Staking-Programm. Das Verhalten ist identisch mit Farm v3 für LP-Staking.
- **Stable AMM** ist ein separates Programm, das das AMM Routing-Programm neben AMM v4, CPMM und CLMM ansteuern kann. Die Liquidität ist im Vergleich zu den anderen drei Programmen dünn, und das SDK stellt keine First-Class-API dafür bereit; Integratoren, die es durchleiten, tun dies in der Regel über den Router.
- **Quellcode-Verfügbarkeit.** Von den On-Chain-Programmen oben werden nur `raydium-amm` (AMM v4), `raydium-cp-swap` (CPMM) und `raydium-clmm` (CLMM) mit öffentlichen Quellrepositorys unter [`github.com/raydium-io`](https://github.com/raydium-io) ausgeliefert. Stable AMM, LaunchLab, AMM Routing, Burn & Earn / LP Lock und die Farm-Programme sind nicht öffentlich verfügbar – überprüfen Sie sie stattdessen gegen die Live-API, den On-Chain-Bytecode und die veröffentlichten IDLs in [`raydium-io/raydium-idl`](https://github.com/raydium-io/raydium-idl).

### Gemeinsame Admin-Autorität

Alle Anchor-basierten Programme (CLMM, CPMM, LaunchLab, Lock) teilen sich einen einzigen hardcodierten Admin-`Pubkey` für Zugriffskontrolle auf Anweisungsebene zu Admin-Pfaden (wie `CreateAmmConfig` oder `UpdatePoolStatus`):

| Cluster | Admin |
|---------|-------|
| mainnet-beta | `GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ` |
| devnet | `DRayqG9RXYi8WHgWEmRQGrUWRWbhjYWYkCRJDd6JBBak` |

Operationale Autoritäten auf Kontoebene (z. B. `protocol_owner`, `fund_owner` auf CPMM/CLMM `AmmConfig` oder die Migrations-Wallets auf LaunchLab `GlobalConfig`) werden On-Chain gespeichert und können vom Programm-Admin abweichen. Lesen Sie sie direkt vom relevanten Config-Konto, bevor Sie hochriskante Transaktionen senden.

### CPMM Permission-PDA Creator Authority

`CreatePermissionPda` akzeptiert entweder den gemeinsamen Programm-Admin oben oder einen dedizierten Owner. Seit dem CPMM-Upgrade 2026-09 akzeptiert `ClosePermissionPda` die gleichen zwei Unterzeichner; davor war es nur Admin.

| Cluster | Dedizierter `CreatePermissionPda` Owner |
|---------|----------------------------------------|
| mainnet-beta | `RayqjDRsNEFuPcDE4JpEScwJvwusmHYuNZ3MgET4D7U` |
| devnet | `DRayJkSKsijbcEqdooK4uUGcT6gjbEuwUh7V6Nmqct7M` |

### CPMM Support-Mint Registry Authority

`CreateSupportMintAssociated` und `CloseSupportMintAssociated` akzeptieren entweder den gemeinsamen Programm-Admin oder den dedizierten Owner unten. Die Registrierung eines Mint erlaubt ihm, die Token-2022-Erweiterungs-Allowlist von CPMM zu passieren – siehe [`reference/token-2022-support`](/de/reference/token-2022-support#the-one-bypass-the-per-mint-registry).

| Cluster | Dedizierter Support-Mint Owner |
|---------|------------------------------|
| mainnet-beta | `Rayv2LG4tFSMizZhMP8aSUYxDPjV8qJtx2NQY9RKYZy` |
| devnet | `DRaypyeDL6y1dUusMgwyeDM5JebjhsSi8aRXobKQ9DcQ` |

### CPMM Fee-Owner Wallets

Seit dem CPMM-Upgrade 2026-09 schreibt `CreateAmmConfig` diese festen Schlüssel in die `protocol_owner` und `fund_owner` der neuen Config, statt den Admin-Unterzeichner-Schlüssel zu kopieren. Sie sind die Unterzeichner, die `CollectProtocolFee` und `CollectFundFee` für jede Config akzeptieren, die ab diesem Upgrade erstellt wurde.

| Cluster | `protocol_fee_owner` | `fund_fee_owner` |
|---------|----------------------|------------------|
| mainnet-beta | `ProCXqRcXJjoUd1RNoo28bSizAA6EEqt9wURZYPDc5u` | `FUNDduJTA7XcckKHKfAoEnnhuSud2JUCUZv6opWEjrBU` |
| devnet | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` | `DRay33UmULQCeawH3dVpJfN3uqLj6Qtq4ymSRx2pAgGK` |

<Warning>
  Dies sind die Werte, die das Programm *in neue Configs schreibt*, keine Beschreibung bestehender. `AmmConfig`-Konten, die vor dem Upgrade erstellt wurden, behalten das, was auf ihnen gespeichert ist. Lesen Sie immer `protocol_owner` / `fund_owner` vom Konto, bevor Sie eine Sammel-Transaktion erstellen.
</Warning>

### Excess-Lamports Collection Wallets

Jedes der drei Programme, die am 2026-09-09 aktualisiert wurden, erhielt eine Admin-Anweisung, die Lamports zurückgibt, die über dem Rent-Exempt-Minimum auf Programm-kontrollierten Konten gehalten werden, gemäß [SIMD-0437 Rent Reduction](/solana-fundamentals/rent-and-reclaimable-rent). Jedes Programm hardcodiert sein eigenes Ziel-Wallet, und jedes ist ein dedizierter Schlüssel – nicht der gemeinsame Programm-Admin.

| Programm | Anweisung | mainnet-beta | devnet |
|---------|-------------|--------------|--------|
| AMM v4 | `WithdrawExcessLamports` | `Rayyxz4cjBTQo7XTT1fhVNqFWK21TjiWcrtPgC3czbL` | `DRaySTrJSn4nD9TF1dLZ7uAGNhUXV5kth6yudWTTx39A` |
| CPMM | `CollectExcessLamports` | `RayGkhY93thaTgCv98sx1pNLgBHhJDxWUeZXp4bjmnp` | `DRaydJNq54dSDHUqYCE3G8YySgaXfZucbh7dTXw9fBMs` |
| LaunchLab | `CollectExcessLamports` | `RayFqUZuGuG4Lt35YSEVX1TS12pKLbiqpB3fPzFG4Ag` | `DRayrZj22uaieAuGA8BnYUnK5zRMEiJM88A13dPrJDyB` |

CPMM und LaunchLab akzeptieren zusätzlich den gemeinsamen Programm-Admin als Unterzeichner. AMM v4 akzeptiert **nur** das Wallet oben – es hat kein Admin-Fallback. AMM v4 kompiliert auch einen separaten Testnet-Schlüssel (`75KWb5XcqPTgacQyNw9P5QU2HL3xpezEVcgsFCiJgTT`) unter seinem `testnet`-Feature.

Diese Anweisungen verschieben nur Lamports; sie berühren niemals einen Token-Saldo, schließen ein Konto oder ändern den Pool-Status. Siehe [`products/amm-v4/instructions`](/de/products/amm-v4/instructions#withdrawexcesslamports), [`products/cpmm/instructions`](/de/products/cpmm/instructions#collectexcesslamports) und [`products/launchlab/instructions`](/de/products/launchlab/instructions#collectexcesslamports).

### LaunchLab Delegated Config Creator

`CreateConfig` akzeptiert entweder den gemeinsamen Programm-Admin oder die delegierte Autorität unten. Die delegierte Rolle kann eine neue `GlobalConfig` erstellen; sie kann den Admin-Only-Pfad `UpdateConfig` für ein bestehendes Konto nicht aufrufen.

| Cluster | Delegierte `CreateConfig` Autorität |
|---------|------------------------------------|
| mainnet-beta | `RayUzntHM1dWZJyCnkQjHusUGhYyi6gpNf7t8srtty2` |
| devnet | `DRayDZimvpoExGSuPFHSD8ys1iUCeyr77WpWkUBHh7LB` |

### CLMM Restricted-Issuer Freeze Authorities

Wenn einer der Vault-Mints in einem CLMM-Pool eine der Freeze-Autoritäten unten hat, haben neue Positionen, die über einen V2-Pfad geöffnet werden, ihr Position-NFT-Token-Konto eingefroren. Die Liste ist `frozen_position_nft_authorities::IDS` im CLMM-Programm, und sein einziger Eintrag ist die Issuer-Autorität, die auch die seit langem entfernte Superstate-Erkennung von CLMM abgeglichen hat. Dies sind **Underlying-Asset-Issuer-Autoritäten, die nur als Match-Schlüssel verwendet werden**. Das CLMM-Pool-PDA, nicht der Issuer-Schlüssel, wird Freeze-Autorität für den Position-NFT-Mint.

| Cluster | Restricted Issuer Freeze Authority |
|---------|--------------------------------------|
| mainnet-beta | `2Yq4T3mPNfjtEyTxSbRjRKqLf1pwbTasuCQrWe6QpM7x` |
| devnet | `3TRuL3MFvzHaUfQAb6EsSAbQhWdhmYrKxEiViVkdQfXu` |

Die Liste ist im CLMM-Programm hardcodiert. Das Hinzufügen oder Entfernen einer Autorität erfordert ein Programm-Upgrade.

### Gemeinsame Config / PDA Konventionen

Mehrere Raydium-Programme stellen **Config-Konten** bereit, deren öffentliche Schlüssel stabil sind und auf der öffentlichen API aufgelistet sind. Bevorzugen Sie die API-Abfrage gegenüber dem Hardcoding:

```
# CPMM Fee Configs (gibt ein Array von {id, index, tradeFeeRate, ...} zurück)
GET https://api-v3.raydium.io/main/cpmm-config

# CLMM Fee Configs
GET https://api-v3.raydium.io/main/clmm-config
```

Der Pool-Erstellungs-Fee-Empfänger von CPMM ist ein festes Konto, nicht abgeleitet oder über die API nachgeschlagen: `create_pool_fee_reveiver::ID` in `raydium-cp-swap`'s `lib.rs`, derzeit `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8`. Die `initialize`-Anweisung von CPMM validiert es durch exakte Adresse, daher muss es bereits auf dem Cluster vorhanden sein, gegen den Sie testen.

Standard-CPMM `AmmConfig` Index `0` (Standard 0,25%-Pool) Fee-Parameter als Referenz:

| Feld | Wert | Bedeutung |
|-------|-------|---------|
| `trade_fee_rate` | `2500` | 0,25% des Handelsvolumens |
| `protocol_fee_rate` | `120000` | 12% **der Handelsgebühr** (nicht des Volumens) an das Protokoll |
| `fund_fee_rate` | `40000` | 4% **der Handelsgebühr** an den Fund Multisig |
| `creator_fee_rate` | `500` | 0,05% des Handelsvolumens an den Pool-Ersteller (optional) |

Siehe [`products/cpmm/fees`](/de/products/cpmm/fees) für die Zusammensetzung der Aufteilungen und [`reference/fee-comparison`](/de/reference/fee-comparison) für die produktübergreifende Matrix.

### PDA Seeds

Die Seeds unten sind **kanonisch** und werden sowohl vom SDK als auch von On-Chain CPIs verwendet. Berechnen Sie PDAs immer; hardcodieren Sie abgeleitete Adressen nicht.

```ts
// CPMM — alle Seeds sind statische ASCII-Strings, sofern nicht anders angegeben.
const [ammConfig]   = PublicKey.findProgramAddressSync(
  [Buffer.from("amm_config"), u16ToBytes(index)],
  CPMM_PROGRAM_ID,
);
const [authority]   = PublicKey.findProgramAddressSync(
  [Buffer.from("vault_and_lp_mint_auth_seed")],
  CPMM_PROGRAM_ID,
);
const [poolState]   = PublicKey.findProgramAddressSync(
  [Buffer.from("pool"), ammConfig.toBuffer(), token0Mint.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [lpMint]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_lp_mint"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault0]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token0Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [vault1]      = PublicKey.findProgramAddressSync(
  [Buffer.from("pool_vault"), poolState.toBuffer(), token1Mint.toBuffer()],
  CPMM_PROGRAM_ID,
);
const [observation] = PublicKey.findProgramAddressSync(
  [Buffer.from("observation"), poolState.toBuffer()],
  CPMM_PROGRAM_ID,
);
```

`token0Mint` / `token1Mint` werden **nach Byte-Reihenfolge des öffentlichen Schlüssels sortiert** (token0 < token1), bevor sie gehasht werden. Wenn Sie dies falsch machen, erhalten Sie ein gültiges PDA für einen nicht existierenden Pool.

Die äquivalenten CLMM Seeds folgen dem gleichen Stil; siehe [`products/clmm/accounts`](/de/products/clmm/accounts).

## Devnet

| Programm | Programm-ID |
|---------|------------|
| **AMM v4** | `DRaya7Kj3aMWQSy19kSjvmuwq9docCHofyP9kanQGaav` |
| **CPMM** | `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb` |
| **CLMM** | `DRayAUgENGQBKVaX8owNhgzkEDyoHTGVEGHVJT1E9pfH` |
| **Stable AMM** | `DRayDdXc1NZQ9C3hRWmoSf8zK4iapgMnjdNZWrfwsP8m` |
| **LaunchLab** | `DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6` |
| **AMM Routing** | `DRaybByLpbUL57LJARs3j8BitTxVfzBg351EaMr5UTCd` |
| **Burn & Earn / LP Lock** | `DLockwT7X7sxtLmGH9g5kmfcjaBtncdbUmi738m5bvQC` |
| **Farm v3 / v5 / v6** | Nicht zuverlässig für devnet veröffentlicht – bestätigen Sie über die Live-API (`https://api-v3-devnet.raydium.io/main/info`) vor der Verwendung. |

Devnet REST API Base: `https://api-v3-devnet.raydium.io/` (gleiche Route-Form wie Mainnet).

## Wie man eine Adresse On-Chain überprüft

1. **Solana Explorer.** Fügen Sie die Adresse in [explorer.solana.com](https://explorer.solana.com) ein und bestätigen Sie, dass sie als `Program` mit einer aktuellen Upgrade-Autorität markiert ist. Mainnet-beta sollte Deployments anzeigen, die von Raydiums Upgrade-Autorität signiert sind.
2. **CLI.** Verwenden Sie `solana program show <PROGRAM_ID> -u mainnet-beta`, um Deploy-Slot, BPF-Loader, Upgrade-Autorität und Datenlänge zu überprüfen. Notieren Sie diese in Ihrem Runbook.
3. **IDL-Anhang.** Fragen Sie die On-Chain-IDL mit `anchor idl fetch <PROGRAM_ID> --provider.cluster mainnet` ab. Das Feld `address` der IDL sollte übereinstimmen. Die gleichen IDLs werden unter [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl) gespiegelt – vergleichen Sie die On-Chain-IDL mit dem Repo, bevor Sie ihr vertrauen.
4. **Config/Admin-Autoritäten.** Für CPMM/CLMM Config-Konten lesen Sie das Feld `owner`/`protocol_owner` und bestätigen Sie, dass es dem aktuellen Raydium Multisig entspricht, das in [`security/admin-and-multisig`](/de/security/admin-and-multisig) veröffentlicht ist.

Falls einer der vier Überprüfungen oben mit dieser Seite nicht übereinstimmt, behandeln Sie diese Seite als falsch und öffnen Sie ein Issue, bevor Sie neuen Code gegen die Adressen schreiben.

## Diese Seite aktualisieren

- Änderungen an Programm-IDs sind sicherheitskritisch. Versenden Sie eine Programm-ID-Änderung **nicht**, ohne (a) auf die Raydium-Ankündigung zu verlinken, (b) einen Eintrag in [`reference/changelog`](/de/reference/changelog) hinzuzufügen und (c) eine Link-Überprüfung über die Docs auszuführen, um zu bestätigen, dass keine Seite noch auf den alten Wert verweist.
- Veraltete Einträge bleiben in der Tabelle mit einer Status-Notiz, statt gelöscht zu werden – bestehende Pools werden weiterhin über das alte Programm aufgelöst.

Quellen:

- Live API: `https://api-v3.raydium.io/main/info`
- [github.com/raydium-io/raydium-idl](https://github.com/raydium-io/raydium-idl)
- [github.com/raydium-io/raydium-cp-swap](https://github.com/raydium-io/raydium-cp-swap)
