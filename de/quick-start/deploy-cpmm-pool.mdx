---
title: "Einen CPMM-Pool bereitstellen"
description: "Copy-and-paste Node-Skript, das einen neuen CPMM-Pool erstellt. Konfigurieren Sie Mints und Seed-Beträge über Umgebungsvariablen."
---

<Info>
  **Diese Seite wurde mit KI automatisch übersetzt. Maßgeblich ist stets die englische Version.**

  [Englische Version ansehen →](/quick-start/deploy-cpmm-pool)
</Info>

<Info>
  **Was dies tut.** Erstellt einen brandneuen CPMM-Pool für zwei von Ihnen angegebene Mints, wählt die 0,25%-Gebührenebene, initialisiert die Liquidität zum durch die Seed-Beträge implizierten Preis und gibt die neue Pool-ID und die Transaktionssignatur aus.
</Info>

## Setup

Stellen Sie sicher, dass Sie die [Quick-Start-Voraussetzungen](/de/quick-start) gelesen haben und `RPC_URL`, `KEYPAIR` sowie die Abhängigkeiten installiert sind.

Sie müssen auch **das Wallet mit den Seed-Beträgen beider Mints finanzieren**, plus ausreichend SOL, um die einmalige Pool-Erstellungsgebühr und Account-Miete zu decken (~0,19 SOL insgesamt auf Mainnet — siehe [`reference/fee-comparison`](/de/reference/fee-comparison#creation-costs) für die Aufschlüsselung von Miete/Gebühr oder [`ray/protocol-fees`](/de/ray/protocol-fees#pool-creation-fees) nur für die Gebühr).

**Ausführung auf Devnet.** Das Umschalten von `cluster` auf `"devnet"` reicht allein nicht aus. Dieses Skript fixiert zwei benannte Importe, die nicht dem `cluster` folgen: `programId: CREATE_CPMM_POOL_PROGRAM` und `poolFeeAccount: CREATE_CPMM_POOL_FEE_ACC`, beide Mainnet-Konstanten. Importieren Sie stattdessen `DEVNET_PROGRAM_ID` aus dem SDK und verwenden Sie `DEVNET_PROGRAM_ID.CREATE_CPMM_POOL_PROGRAM` und `DEVNET_PROGRAM_ID.CREATE_CPMM_POOL_FEE_ACC` — das gleiche Muster, das das [CLMM-Skript](/de/quick-start/deploy-clmm-pool) verwendet.

## Das Skript

Speichern Sie als `create-cpmm.mjs`:

```ts create-cpmm.mjs icon="js"
import { Connection, Keypair, PublicKey } from "@solana/web3.js";
import {
  Raydium,
  TxVersion,
  CREATE_CPMM_POOL_PROGRAM,
  CREATE_CPMM_POOL_FEE_ACC,
} from "@raydium-io/raydium-sdk-v2";
import BN from "bn.js";
import fs from "node:fs";

// ── Config from env ──────────────────────────────────────────────
const RPC_URL  = process.env.RPC_URL  ?? "https://api.mainnet-beta.solana.com";
const KEYPAIR  = process.env.KEYPAIR  ?? `${process.env.HOME}/.config/solana/id.json`;
const MINT_A   = process.env.MINT_A;   // required, base58
const MINT_B   = process.env.MINT_B;   // required, base58
const AMOUNT_A = process.env.AMOUNT_A; // required, integer raw units
const AMOUNT_B = process.env.AMOUNT_B; // required, integer raw units

if (!MINT_A || !MINT_B || !AMOUNT_A || !AMOUNT_B) {
  console.error("Set MINT_A, MINT_B, AMOUNT_A, AMOUNT_B env vars.");
  process.exit(1);
}

// ── Setup ────────────────────────────────────────────────────────
const connection = new Connection(RPC_URL, "confirmed");
const owner = Keypair.fromSecretKey(
  new Uint8Array(JSON.parse(fs.readFileSync(KEYPAIR, "utf8"))),
);
const raydium = await Raydium.load({
  owner,
  connection,
  cluster: "mainnet",
  disableFeatureCheck: true,
  blockhashCommitment: "finalized",
});

// ── Pick the 0.25% fee tier ─────────────────────────────────────
const feeConfigs = await raydium.api.getCpmmConfigs();
const feeConfig  = feeConfigs.find((c) => c.index === 0);
if (!feeConfig) throw new Error("0.25% fee tier not found in CPMM configs.");

// ── Resolve mint metadata (Token-2022-aware) ────────────────────
const mintA = await raydium.token.getTokenInfo(new PublicKey(MINT_A));
const mintB = await raydium.token.getTokenInfo(new PublicKey(MINT_B));

// ── Build and execute ───────────────────────────────────────────
const { execute, extInfo } = await raydium.cpmm.createPool({
  programId:       CREATE_CPMM_POOL_PROGRAM,
  poolFeeAccount:  CREATE_CPMM_POOL_FEE_ACC,
  mintA,
  mintB,
  mintAAmount:     new BN(AMOUNT_A),
  mintBAmount:     new BN(AMOUNT_B),
  startTime:       new BN(0), // open immediately
  feeConfig,
  associatedOnly:  false,
  ownerInfo:       { useSOLBalance: true },
  txVersion:       TxVersion.V0,
  computeBudgetConfig: {
    units: 600_000,
    microLamports: 100_000,
  },
});

const { txId } = await execute({ sendAndConfirm: true });

console.log(`Pool ID:  ${extInfo.address.poolId.toBase58()}`);
console.log(`LP mint:  ${extInfo.address.lpMint.toBase58()}`);
console.log(`Vault A:  ${extInfo.address.vaultA.toBase58()}`);
console.log(`Vault B:  ${extInfo.address.vaultB.toBase58()}`);
console.log(`Tx:       https://solscan.io/tx/${txId}`);
```

## Ausführung

Beispiel: Erstellen Sie einen SOL/USDC-Pool mit 1 SOL und 160 USDC Seed:

```bash icon="terminal"
export MINT_A="So11111111111111111111111111111111111111112"   # wSOL
export MINT_B="EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"   # USDC
export AMOUNT_A="1000000000"   # 1 SOL (9 decimals)
export AMOUNT_B="160000000"    # 160 USDC (6 decimals)

node create-cpmm.mjs
```

Erwartete Ausgabe:

```text
Pool ID:  HgC5...3kXb
LP mint:  4ZAS...9rkV
Vault A:  J9Mu...mP2k
Vault B:  AaJq...8wxx
Tx:       https://solscan.io/tx/5dQ...
```

## Was gerade passiert ist

1. **`getCpmmConfigs`** hat die aktuelle Liste der Gebührenebenen von `api-v3.raydium.io` abgerufen und Index 0 ausgewählt (die 0,25%-Ebene — siehe [`reference/fee-comparison`](/de/reference/fee-comparison) für den vollständigen Satz).
2. **`getTokenInfo`** hat die Metadaten jedes Mints aufgelöst, einschließlich welches Token-Programm es besitzt. CPMM akzeptiert sowohl SPL Token als auch Token-2022 Mints; das SDK leitet automatisch weiter.
3. **`createPool`** hat eine Transaktion erstellt, die:
   - die Mints in kanonische Reihenfolge sortiert,
   - die Pool-PDA, Vaults, LP-Mint und Authority ableitet,
   - die einmalige `create_pool_fee` an `CREATE_CPMM_POOL_FEE_ACC` zahlt,
   - die ATAs des Aufrufers erstellt, falls fehlend,
   - die Vaults mit `AMOUNT_A` und `AMOUNT_B` initialisiert.
4. Der **Anfangspreis** wird durch das Seed-Verhältnis festgelegt: `price = AMOUNT_B / AMOUNT_A` nach Dezimalanpassung. Wählen Sie dies sorgfältig — Bots werden jede Fehlbewertung innerhalb von Sekunden nach dem Pool-Start arbitragieren.
5. **`startTime: new BN(0)`** öffnet den Handel sofort. Um Liquidität vor der Öffnung für die Öffentlichkeit bereitzustellen, setzen Sie einen zukünftigen Unix-Zeitstempel.

## Häufige Fehler

- **`pool already exists`** — Ein Pool existiert bereits für dieses Mint-Paar bei dieser Gebührenebene. Suchen Sie ihn auf, bevor Sie ihn erstellen.
- **`insufficient funds`** — Ihr Wallet hat nicht genug von `MINT_A`, `MINT_B` oder SOL (für die Pool-Erstellungsgebühr + Miete).
- **`Token-2022 extension not supported`** — Einer Ihrer Mints verwendet eine Erweiterung, die CPMM nicht akzeptiert. Siehe [`reference/token-2022-support`](/de/reference/token-2022-support).

## Nach der Bereitstellung

Sie können sofort gegen den neuen Pool tauschen — das Skript [Swap from CLI](/de/quick-start/swap-from-cli) akzeptiert Ihre neue `POOL_ID` direkt. Aggregatoren (Jupiter usw.) werden den neuen Pool innerhalb von Minuten indexieren.

## Nächste Schritte

- [`products/cpmm/overview`](/de/products/cpmm/overview) — was CPMM ist und wann Sie es wählen sollten.
- [`user-flows/create-cpmm-pool`](/de/user-flows/create-cpmm-pool) — der gleiche Ablauf mit Screenshots über die Raydium-Benutzeroberfläche.
- [`user-flows/choosing-a-pool-type`](/de/user-flows/choosing-a-pool-type) — hätten Sie stattdessen CLMM verwenden sollen?
