---
title: "CLMM-Anweisungen"
description: "Vollständige CLMM-Anweisungsreferenz: Pool-Lebenszyklus, Positionsverwaltung, Swap und Reward-Streams."
---

<Info>
  **Diese Seite wurde mit KI automatisch übersetzt. Maßgeblich ist stets die englische Version.**

  [Englische Version ansehen →](/products/clmm/instructions)
</Info>

Diese Seite ergänzt [`products/clmm/accounts`](/de/products/clmm/accounts) (was die Accounts sind) und [`products/clmm/math`](/de/products/clmm/math) (was die Mathematik ist). Sie ist maßgeblich für Argumente und Account-Reihenfolge; spezifische Byte-Layouts stammen aus dem IDL.

## Anweisungsübersicht

| Gruppe | Anweisung | Hinweise |
|--------|-----------|---------|
| Admin | `CreateAmmConfig` | Definiert eine neue Gebührenebene. |
| Admin | `UpdateAmmConfig` | Ändert Sätze auf einer bestehenden Ebene. |
| Admin | `UpdatePoolStatus` | Pausiert/setzt Operationen auf einem Pool fort. |
| Admin | `CreateSupportMintAssociated` | Erlaubt-listet eine Token-2022-Mint-Erweiterungskonfiguration zur Verwendung in CLMM-Pools. |
| Admin | `CreateOperationAccount` | Initialisiert das Programm-Operationskonto (einmalig). |
| Admin | `UpdateOperationAccount` | Ändert die Operationskonto-Whitelist. |
| Admin | `CreateDynamicFeeConfig` | Erstellt einen wiederverwendbaren dynamischen Gebührenparametersatz unter einem u16-Index. |
| Admin | `UpdateDynamicFeeConfig` | Ändert eine bestehende `DynamicFeeConfig`. Pools, die sie bereits bei der Erstellung erfasst haben, sind nicht betroffen. |
| Admin | `CreatePermissionPda` | Gewährt einer Autorität das Recht, `CreatePermissionedPool` aufzurufen, indem ihre `Permission` PDA initialisiert wird. |
| Admin | `ClosePermissionPda` | Widerruft eine zuvor gewährte Berechtigung durch Schließen ihrer `Permission` PDA; Miete wird an den Aufrufer zurückerstattet. |
| Pool | `CreatePool` | Initialisiert einen CLMM-Pool, der an eine `AmmConfig` gebunden ist. Standard, FromInput-Gebührenpfad. Koexistiert mit `CreateCustomizablePool`. |
| Pool | `CreateCustomizablePool` | Empfohlen für neue Pools. Gleiche Form wie `CreatePool` plus `collect_fee_on` und ein optionales `enable_dynamic_fee`-Flag. |
| Pool | `CreatePermissionedPool` | Erstellt einen **zusätzlichen** Pool für ein Paar, das bereits einen hat, unter Verwendung eines vom Client bereitgestellten `seed_index`, der in die Pool-PDA gefaltet wird. Der Zahler muss eine `Permission` PDA halten. |
| Position | `OpenPosition` / `OpenPositionV2` / `OpenPositionWithToken22Nft` | Prägt eine Positions-NFT. `OpenPositionV2` ersetzt V1 (neueres Account-Layout mit Bitmap-Erweiterungsslot); `OpenPositionWithToken22Nft` gibt die Positions-NFT als Token-2022 statt SPL Token aus. Neuer Code sollte V2 oder die Token-2022-Variante verwenden. |
| Position | `IncreaseLiquidity` / `IncreaseLiquidityV2` | Fügt Liquidität zu einer bestehenden Position hinzu. |
| Position | `DecreaseLiquidity` / `DecreaseLiquidityV2` | Entfernt Liquidität; erhebt fällige Gebühren. |
| Position | `ClosePosition` | Verbrennt die NFT und schließt die `PersonalPositionState`. |
| Position | `CloseProtocolPosition` | Nur für Admin – Sweep für Legacy-`ProtocolPositionState`-PDAs. Das aktuelle Programm erstellt oder liest `ProtocolPositionState` nicht mehr – diese Anweisung existiert nur, um Miete auf Konten zu beanspruchen, die von älteren Programmversionen erstellt wurden. |
| Swap | `Swap` / `SwapV2` | Konstante-Liquidität-Swap. Beide Varianten wenden dynamische Gebühren, einseitige Gebührenrouting und Limit-Order-Matching an; der einzige Unterschied ist, dass `SwapV2` Token-2022-Mints akzeptiert (die V1-Variante erfordert, dass beide Vaults klassisches SPL Token sind). |
| Swap | `SwapRouterBaseIn` | Multi-Hop, verwendet vom Router. |
| Limit-Order | `OpenLimitOrder` | Platziert eine Verkaufsorder bei einem Tick. Unerfüllte Token sitzen auf dem Tick; die Matching-Engine füllt sie, wenn der Preis kreuzt. |
| Limit-Order | `IncreaseLimitOrder` | Fügt zu einer bestehenden offenen Order hinzu. |
| Limit-Order | `DecreaseLimitOrder` | Reduziert oder storniert eine offene Order; zahlt den unerfüllten Rest plus bereits abgewickelte Ausgabe. |
| Limit-Order | `SettleLimitOrder` | Schiebt gefüllte Ausgabe-Token zum Order-Besitzer. Kann vom Besitzer oder vom operativen Keeper aufgerufen werden. |
| Limit-Order | `CloseLimitOrder` | Schließt ein vollständig verbrauchtes Order-Konto. Miete wird immer an den `owner` der Order zurückgegeben. Kann vom Besitzer oder Keeper aufgerufen werden. |
| Gebühren | `CollectProtocolFee` | Admin-Sweep von Protokollgebühren. |
| Gebühren | `CollectFundFee` | Admin-Sweep von Fondsgebühren. |
| Rewards | `InitializeReward` | Hängt einen neuen Reward-Stream an einen Pool an. |
| Rewards | `SetRewardParams` | Ändert die Emissionsrate/das Ende eines bestehenden Rewards. |
| Rewards | `UpdateRewardInfos` | Setzt Reward-Wachstum auf jetzt (wird von jedem Swap/Positionsänderung aufgerufen). |
| Rewards | `TransferRewardOwner` | Überträgt die Autorität, die einen Reward-Stream setzen oder auffüllen kann. |
| Rewards | `CollectRemainingRewards` | Nach der `end_time` eines Reward-Streams werden alle nicht zugeordneten Token zum Funder zurückgegeben. |
| Utility | `InitTickArray` | Initialisiert ein Tick-Array-Konto (oft mit `OpenPosition` gebündelt). |

Die meisten Admin-only-Anweisungen (`CreateAmmConfig`, `UpdateAmmConfig`, `UpdatePoolStatus`, `CreateSupportMintAssociated`, `CreateOperationAccount`, `UpdateOperationAccount`, `CloseProtocolPosition`) werden durch den hardcodierten `admin`-Public-Key des Programms gated. `CreatePermissionPda` / `ClosePermissionPda` akzeptieren entweder den `admin`-Public-Key **oder** einen dedizierten `permission_pda_admin`-Schlüssel. Reward-Stream-Admin-Anweisungen (`TransferRewardOwner`, `CollectRemainingRewards`) werden durch den Reward-Funder gated, nicht durch den Programm-Admin.

V2-Suffix bedeutet „unterstützt Token-2022 auf Vaults/NFT, erfordert Bitmap-Erweiterungsslot". Das SDK wählt V2 standardmäßig für neue Pools.

## `CreatePool`

**Argumente**

```
sqrt_price_x64: u128           // Anfangspreis
open_time:      u64            // Swaps werden vor dieser Zeit abgelehnt
```

**Accounts (gekürzt)**

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| 1 | `pool_creator` | W | S | |
| 2 | `amm_config` |   |   | Gewählte Gebührenebene. |
| 3 | `pool_state` | W |   | `init` hier. |
| 4 | `token_mint_0` |   |   | Sortiert. |
| 5 | `token_mint_1` |   |   | |
| 6 | `token_vault_0` | W |   | `init` hier, im Besitz der Pool-Autorität PDA. |
| 7 | `token_vault_1` | W |   | |
| 8 | `observation_state` | W |   | `init` hier. |
| 9 | `tick_array_bitmap_extension` | W |   | `init` hier (V2). |
| 10 | `token_program` |   |   | |
| 11 | `token_program_2022` |   |   | |
| 12 | `system_program`, `rent` |   |   | |

**Vorbedingungen**

- `token_mint_0 < token_mint_1` nach Byte-Reihenfolge.
- `amm_config.disable_create_pool == false`.
- Mints werden nicht durch die Token-2022-Erweiterungs-Allowlist abgelehnt.

**Nachbedingungen**

- `pool_state.sqrt_price_x64 = sqrt_price_x64`, `tick_current = floor(log_{1.0001}(price))`.
- `pool_state.liquidity = 0` (noch keine Positionen).
- `pool_state.fee_on = FromInput` (Legacy-Standard).
- `pool_state.dynamic_fee_info` ist auf Null gesetzt (dynamische Gebühr deaktiviert).

## `CreateCustomizablePool`

Empfohlen für neue Pools. Gleiche Wirkung wie `CreatePool` plus Pool-spezifischer Gebührenerfassungsmodus und ein optionales dynamisches Gebühren-Opt-in.

**Argumente**

```rust
pub struct CreateCustomizableParams {
    pub sqrt_price_x64:    u128,
    pub collect_fee_on:    CollectFeeOn,   // FromInput | Token0Only | Token1Only
    pub enable_dynamic_fee: bool,
}
```

**Accounts (gekürzt)** – gleich wie `CreatePool` plus, wenn `enable_dynamic_fee = true`:

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| N | `dynamic_fee_config` |   |   | Die gemeinsame Konfiguration zum Erfassen. Muss bereits existieren. |

**Vorbedingungen** – gleich wie `CreatePool`. Wenn `enable_dynamic_fee = false`, wird `dynamic_fee_config` ignoriert.

**Nachbedingungen**

- `pool_state.fee_on` auf die gewählte `CollectFeeOn`-Variante gesetzt.
- Wenn dynamische Gebühr aktiviert wurde: `pool_state.dynamic_fee_info` wird aus der bereitgestellten `DynamicFeeConfig` initialisiert (fünf Kalibrierungsparameter kopiert; Zustandsfelder auf Null gesetzt).
- Andernfalls: `pool_state.dynamic_fee_info` ist auf Null gesetzt (= dynamische Gebühr für immer inaktiv für diesen Pool).

`fee_on` und das dynamische Gebühren-Aktivierungsbit werden **nur** bei der Pool-Erstellung gesetzt. Es gibt kein direktes Upgrade – Pools, die über Legacy-`CreatePool` erstellt wurden, können nicht nachträglich dynamische Gebühren oder einseitige Gebühren erhalten. Neue Deployments sollten standardmäßig diese Anweisung verwenden.

## `CreatePermissionedPool`

Sowohl `CreatePool` als auch `CreateCustomizablePool` leiten die Pool-PDA von `["pool", amm_config, token_mint_0, token_mint_1]` ab, daher gibt es genau **eine** kanonische Pool-Adresse pro `(config, mint0, mint1)`-Triple – ein zweites `init` bei denselben Seeds schlägt fehl. `CreatePermissionedPool` hebt diese Einschränkung auf, indem ein vom Client bereitgestellter `seed_index: u16` in die Pool-PDA-Seeds gefaltet wird, was **mehrere Pools für dasselbe Paar und dieselbe Gebührenebene** ermöglicht – jeweils unter seiner eigenen Adresse. Da eine beliebige Pool-Adresse eine privilegierte Fähigkeit ist, muss der Zahler eine `Permission` PDA halten, die ihn autorisiert.

Alles andere am Pool ist identisch mit `CreateCustomizablePool`: Es nimmt die gleichen `CreateCustomizableParams` und unterstützt einseitige Gebühren und das dynamische Gebühren-Opt-in.

**Argumente**

```rust
customizable_params: CreateCustomizableParams   // gleiche Struktur wie CreateCustomizablePool
seed_index:          u16                          // muss ungleich Null sein; wird in die Pool-PDA-Seeds gefaltet
```

**Accounts (gekürzt)** – gleich wie `CreateCustomizablePool` plus, am Anfang:

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| 1 | `payer` | W | S | Zahlt Miete. Muss die Autorität der `permission` PDA unten sein. |
| 2 | `pool_creator` |   |   | Wird als `pool_state.owner` aufgezeichnet. Kann sich vom `payer` unterscheiden. |
| 3 | `permission` |   |   | PDA `["permission", payer]`. Anchor lehnt den Aufruf ab, wenn er nicht existiert/nicht vom Programm besessen wird. |
| … | (`amm_config`, `pool_state`, Mints, Vaults, `observation_state`, `tick_array_bitmap`, Token/System-Programme) | | | Wie in `CreateCustomizablePool`. |

Die `pool_state` PDA wird von `["pool", amm_config, token_mint_0, token_mint_1, seed_index.to_le_bytes()]` abgeleitet.

**Vorbedingungen**

- `seed_index != 0`. Ein `seed_index` von `0` ist für Legacy-Pools reserviert und wird hier abgelehnt; die `[0, 0]`-Seed-Komponente ist das, was eine Legacy-Pool-Adresse auf die klassische Vier-Seed-Form zusammenfallen lässt.
- Die `permission` PDA für `payer` existiert (erstellt von einem Admin über `CreatePermissionPda`).
- Gleiche Mint-/Allowlist-Regeln wie `CreatePool`.

**Nachbedingungen**

- Eine neue `pool_state` existiert unter der `seed_index`-abgeleiteten Adresse, mit `pool_state.seed_index = seed_index`.
- Alle anderen Post-State-Übereinstimmungen mit `CreateCustomizablePool` (Gebührenmodus, optionale dynamische Gebühr).

<Note>
  Diese Anweisung **erweitert nicht** den allgemeinen Pool-Erstellungszugriff – erlaubnislose Erstellung läuft weiterhin über `CreatePool` / `CreateCustomizablePool`, die weiterhin einen Pool pro Paar haben. `CreatePermissionedPool` existiert für den spezifischen Fall, in dem ein auf der Whitelist stehender Operator mehrere Pools für dasselbe Paar benötigt (z. B. unterschiedliche Anfangspreise oder Launch-Kohorten) und eine `Permission` PDA hält, die vom Admin gewährt wurde.
</Note>

## `OpenPositionV2` / `OpenPositionWithToken22Nft`

Erstellt eine neue Position in einem bestehenden Pool.

**Argumente**

```
tick_lower_index: i32
tick_upper_index: i32
tick_array_lower_start_index: i32
tick_array_upper_start_index: i32
liquidity:  u128              // gewünschtes L (oder 0, um Beträge unten zu verwenden)
amount_0_max: u64
amount_1_max: u64
with_metadata: bool           // schreibe NFT-Metadaten (Metaplex)
base_flag: Option<bool>       // true = passe zu amount0 an; false = passe zu amount1 an
```

**Accounts (gekürzt)**

| # | Name | W | S |
|---|------|---|---|
| 1 | `payer` | W | S |
| 2 | `position_nft_owner` |   |   |
| 3 | `position_nft_mint` | W | S (Keypair) |
| 4 | `position_nft_account` | W |   | Owner's ATA für die NFT. |
| 5 | `metadata_account` | W |   | Metaplex (optional, wenn `with_metadata`). |
| 6 | `pool_state` | W |   |
| 7 | `protocol_position` |   |   |   |
| 8 | `tick_array_lower` | W |   | Erstellt, wenn nicht initialisiert. |
| 9 | `tick_array_upper` | W |   | Gleich. |
| 10 | `personal_position` | W |   | Erstellt hier. |
| 11 | `token_account_0`, `token_account_1` | W |   | Benutzer-Quell-ATAs. |
| 12 | `token_vault_0`, `token_vault_1` | W |   | |
| 13 | `rent`, `system_program`, `token_program` |   |   | |
| 14 | `associated_token_program` |   |   | |
| 15 | `metadata_program` |   |   | Optional. |
| 16 | `token_program_2022` |   |   | V2. |
| 17 | `vault_0_mint`, `vault_1_mint` |   |   | V2. |
| 18 | `tick_array_bitmap_extension` | W |   | V2 (wenn berührt). |

**Mathematik** – siehe [`products/clmm/math`](/de/products/clmm/math). Gegeben `base_flag` löst das Programm entweder `liquidity` oder `(amount_0_max, amount_1_max)` in das tatsächliche `L` und die tatsächlich verbrauchten Token-Beträge auf.

**Vorbedingungen**

- `tick_lower < tick_upper`, beide Vielfache von `pool.tick_spacing`, innerhalb von `[MIN_TICK, MAX_TICK]`.
- Erforderliche Tick-Arrays übergeben und initialisiert (oder hier über `InitTickArray` CPI in der Transaktion erstellt).
- Benutzer hat mindestens `amount_0_max` und `amount_1_max` in den Quell-ATAs.

**Nachbedingungen**

- `personal_position` existiert, `liquidity` gesetzt, `fee_growth_inside_last` erfasst.
- Tick-Array-Einträge bei `tick_lower` und `tick_upper` aktualisiert (`liquidity_gross += L`, `liquidity_net ± L`, Gebührenwachstums-Snapshots gepflegt).
- `pool_state.liquidity += L`, wenn Position im Bereich ist (`tick_lower ≤ tick_current < tick_upper`).

**Häufige Fehler** – `InvalidTickIndex`, `NotApproved`, `ZeroAmountSpecified`, `TransactionTooLarge` (wenn zu viele Tick-Arrays).

## `IncreaseLiquidityV2`

Fügt Liquidität zu einer bereits offenen Position hinzu.

**Argumente**

```
liquidity: u128
amount_0_max: u64
amount_1_max: u64
base_flag: Option<bool>
```

**Accounts** – wie `OpenPosition` minus die NFT-Mint (Position existiert bereits; die NFT wird als Owner's ATA übergeben, die 1 Token hält).

**Effekt**

- Überträgt `amount_0_actual` / `amount_1_actual` von Benutzer → Vaults.
- Erhöht `personal_position.liquidity` und `pool_state.liquidity` (wenn im Bereich), und die Endpunkt-Tick `liquidity_gross` / `liquidity_net` entsprechend.
- **Erhebt fällige Gebühren und Rewards seit letztem Zugriff** und schreibt sie `tokens_fees_owed_{0,1}` / `reward_amount_owed` gut. Diese werden nur bei `DecreaseLiquidity` oder `CollectReward` ausgezahlt, nicht bei Erhöhung.

## `DecreaseLiquidityV2`

Entfernt Liquidität aus einer Position.

**Argumente**

```
liquidity: u128
amount_0_min: u64
amount_1_min: u64
```

**Accounts** – gleiche Form wie `IncreaseLiquidity`.

**Effekt**

- Berechnet `(amount_0, amount_1)` für das entfernte `L` gegeben aktuelles `sqrt_price_x64`.
- Setzt Gebühren/Rewards ab, die seit letztem Zugriff aufgelaufen sind, gleich wie `IncreaseLiquidity`.
- Überträgt `amount_0 + fees_owed_0` und `amount_1 + fees_owed_1` aus Vaults zum Benutzer.
- Verringert Liquiditätszähler; wenn das neue `personal_position.liquidity == 0`, ist die Position berechtigt für `ClosePosition`.

**Slippage** – `amount_0_min` und `amount_1_min` sind die Mindestwerte, die der Benutzer netto von Token-2022-Transfergebühren auf der Ausgabeseite akzeptiert.

## `ClosePosition`

Verbrennt die Positions-NFT und schließt `PersonalPositionState`.

**Vorbedingungen**

- `personal_position.liquidity == 0`.
- `tokens_fees_owed_{0,1} == 0`.
- Alle Reward-Zähler `reward_amount_owed == 0`.

(D. h. alles einsammeln und zuerst auf Null verringern.)

**Effekt**

- Verbrennt die NFT.
- Schließt das NFT-Mint-Konto und das `personal_position`-Konto, erstattet Miete an den `payer`.

## `SwapV2`

Geht die Liquiditätskurve entlang; exakte Eingabe *oder* exakte Ausgabe je nach `is_base_input`.

**Argumente**

```
amount: u64                  // Eingabe, wenn is_base_input=true, Ausgabe andernfalls
other_amount_threshold: u64  // Min-Ausgabe oder Max-Eingabe
sqrt_price_limit_x64: u128   // harte Grenze; 0 ⇒ unbegrenzt
is_base_input: bool
```

**Accounts (gekürzt)**

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| 1 | `payer` |   | S | |
| 2 | `amm_config` |   |   | |
| 3 | `pool_state` | W |   | |
| 4 | `input_token_account` | W |   | |
| 5 | `output_token_account` | W |   | |
| 6 | `input_vault` | W |   | |
| 7 | `output_vault` | W |   | |
| 8 | `observation_state` | W |   | |
| 9 | `token_program` |   |   | |
| 10 | `token_program_2022` |   |   | V2. |
| 11 | `memo_program` |   |   | V2 (erforderlich für einige Token-2022-Pfade). |
| 12 | `input_vault_mint`, `output_vault_mint` |   |   | V2. |
| 13 | `tick_array_bitmap_extension` (optional) | W |   | Wenn Swap in die Erweiterung geht. |
| 14+ | `tick_array` (Rest) | W |   | Genug Arrays, um den erwarteten Bereich des Walks zu spannen. |

Aufrufer übergeben eine geordnete Liste von Tick-Arrays, die den erwarteten Swap-Walk abdecken; das Programm verwendet so viele wie nötig. Das SDK berechnet diese Liste über `PoolUtils.computeAmountOutFormat` oder den Quote-Endpunkt der API.

**Vorbedingungen**

- `pool_state.status` erlaubt Swap.
- `now >= open_time`.
- `sqrt_price_limit_x64` ist auf der korrekten Seite von `sqrt_price_x64` für die Richtung.

**Häufige Fehler** – `ExceededSlippage`, `SqrtPriceLimitOverflow`, `TickArrayNotFound`, `LiquidityInsufficient`.

**Was `SwapV2` intern macht, das Aufrufer wissen sollten** (Post-2025-Release):

1. **Dynamische Gebührenzuschlag** – wenn `pool.dynamic_fee_info` ungleich Null ist, aktualisiert das Programm den Volatilitätsakkumulator unter Verwendung der seit dem letzten Swap durchquerten Tick-Distanz (mit den Filter-/Decay-Regeln aus [`products/clmm/fees`](/de/products/clmm/fees)) und addiert eine `dynamic_fee_component` auf `AmmConfig.trade_fee_rate`. Die Gesamtgebühr ist auf 10% begrenzt (`MAX_FEE_RATE_NUMERATOR / 1_000_000`).
2. **Limit-Order-Matching** – wenn der Preis-Walk einen Tick kreuzt, der offene Limit-Orders hält, füllt das Programm zuerst verfügbare Limit-Order-Liquidität bei diesem Tick (FIFO nach `order_phase`), dann geht es entlang der LP-Liquiditätskurve weiter. Gefüllte Beträge aktualisieren `tick.unfilled_ratio_x64` und `tick.part_filled_orders_remaining` für spätere Abwicklung; Orders selbst bleiben unausgegeben, bis ihr Besitzer `SettleLimitOrder` aufruft.
3. **Einseitiges Gebühren-Routing** – wenn `pool.fee_on = Token0Only` oder `Token1Only`, berechnet der Swap-Schritt immer noch die gleiche Input-Output-Trade; die Gebühr wird dann zur konfigurierten Seite geleitet. Für Richtungen, bei denen die konfigurierte Gebührenseite die Ausgabe ist, wird die Gebühr von der Swap-Ausgabe abgezogen (der Benutzer erhält `out − fee`); für Richtungen, bei denen sie die Eingabe ist, entspricht das Verhalten `FromInput`. Siehe `is_fee_on_input(zero_for_one)` und `is_fee_on_token0(zero_for_one)` auf `PoolState`.

`Swap` (V1) implementiert die gleiche dynamische Gebühr, einseitiges Gebühren-Routing und Limit-Order-Matching wie `SwapV2`; das einzige Feature, das es fehlt, ist Token-2022-Unterstützung – beide Vaults müssen klassisches SPL Token sein. Pools mit einem Token-2022-Mint müssen über `SwapV2` getauscht werden. Der Aggregator und das SDK bevorzugen bereits V2 für jeden CLMM-Leg, daher müssen Aufrufer nicht nach Mint-Typ verzweigen.

## `OpenLimitOrder`

Platziert eine Verkaufsorder bei einem bestimmten Tick. Die Order sitzt in einer Pro-Tick-FIFO-Kohorte und wird gefüllt, wenn der Preis vorbeigeht.

**Argumente**

```
nonce_index:    u8       // vom Benutzer gewählter Nonce-Konto-Index (0..255 pro Wallet)
zero_for_one:   bool     // true: verkaufe token0 für token1; false: verkaufe token1 für token0
tick_index:     i32      // muss ein Vielfaches von pool.tick_spacing sein
amount:         u64      // Input-Token-Betrag
```

**Accounts (gekürzt)**

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| 1 | `payer` | W | S | Order-Besitzer; zahlt Miete. |
| 2 | `pool_state` | W |   | |
| 3 | `tick_array` | W |   | Das Tick-Array, das `tick_index` enthält. |
| 4 | `limit_order_nonce` | W |   | PDA. `init_if_needed` – erstellt bei der ersten Order des Benutzers unter diesem `nonce_index`. |
| 5 | `limit_order` | W |   | PDA. `init` hier. |
| 6 | `input_token_account` | W |   | Benutzer-Input-ATA. Wird auf `frozen` überprüft. |
| 7 | `output_token_account` | W |   | Benutzer-**Output**-ATA. Wird bei Öffnung nicht belastet – erforderlich nur, damit das Programm bestätigen kann, dass es nicht `frozen` ist (d. h. die Füllung kann später geliefert werden). |
| 8 | `input_vault` | W |   | Pool-Input-Vault. |
| 9 | `output_vault` | W |   | Pool-Output-Vault. Identifiziert die Output-Mint für die `output_token_account` / `output_vault_mint`-Überprüfungen. |
| 10 | `input_vault_mint` |   |   | Token-2022-Gebührenbehandlung. |
| 11 | `output_vault_mint` |   |   | Mint der Output-Seite (Frozen-Check-Paarung). |
| 12 | `input_token_program` |   |   | SPL oder Token-2022. |
| 13 | `system_program`, `rent` |   |   | |

<Note>
  **Account-Listen-Änderung (2026-07-Release).** `OpenLimitOrder` nimmt jetzt auch die **Output-Seiten**-Accounts – `output_token_account`, `output_vault` und `output_vault_mint` – zusätzlich zur Input-Seite. Sie werden nur zur Validierung verwendet: Das Programm lehnt die Order ab, wenn das Input- **oder** Output-Token-Konto des Besitzers gefroren ist. Dies garantiert, dass eine Füllung tatsächlich zum Output-ATA des Besitzers abgewickelt werden kann, was für Allowlist-/Standard-Frozen-Token-2022-Mints (z. B. berechtigte Token) wichtig ist, bei denen ein Konto möglicherweise noch nicht aufgetaut ist. Clients, die gegen die ältere einseitige Account-Liste erstellt wurden, müssen die drei Output-Accounts hinzufügen.
</Note>

**Vorbedingungen**

- Weder `input_token_account` noch `output_token_account` ist gefroren (andernfalls `NotApproved`).
- `pool_state.status` erlaubt sowohl den Swap (Bit 4) als auch Limit-Order-Operationen (Bit 5) (andernfalls `NotApproved`).
- `tick_index % pool.tick_spacing == 0` und innerhalb von `[MIN_TICK, MAX_TICK]`.
- `tick_index` ist auf der **rechten Seite von `pool.tick_current`** für die gewählte Richtung (Verkauf von token0 → Tick muss über aktuell sein, und umgekehrt). Der Verkauf bei einem bereits gekreuzten Tick würde sofort abgeglichen und wird abgelehnt.

**Nachbedingungen**

- `limit_order` existiert, erfasst `tick.order_phase` und `tick.unfilled_ratio_x64` bei Öffnungszeit.
- `tick.orders_amount += amount` (in der aktuellen Kohorte).
- `limit_order_nonce.order_nonce += 1`.
- `OpenLimitOrderEvent` emittiert.

**Häufige Fehler** – `NotApproved` (Input- oder Output-Token-Konto gefroren, oder der Pool hat Swap/Limit-Order deaktiviert), `InvalidLimitOrderAmount` (Null oder unter dem Pool-Minimum), `InvalidTickIndex` (außerhalb von `[MIN_TICK, MAX_TICK]`, oder auf der falschen Seite von `tick_current` für die gewählte Richtung), `TickAndSpacingNotMatch` (`tick_index % pool.tick_spacing != 0`), `OrderPhaseSaturated`.

## `IncreaseLimitOrder`

Fügt zu einer bestehenden offenen Order hinzu. Kann nur vom `owner` der Order aufgerufen werden.

**Argumente**

```
amount: u64    // zusätzlicher Input-Token-Betrag
```

**Accounts** – wie `OpenLimitOrder` minus das Nonce-Konto; die `limit_order` PDA wird direkt übergeben.

**Vorbedingungen**

- `limit_order.owner == signer`.
- Die Order ist immer noch in der gleichen Kohorte (`tick.order_phase == limit_order.order_phase`). Wenn die Kohorte bereits mit dem Füllen begonnen hat, ist die Order teilweise abgewickelt – der Aufrufer sollte zuerst `DecreaseLimitOrder` oder `SettleLimitOrder` aufrufen, um voranzugehen.

**Effekt**

- Überträgt `amount` von Owner-ATA zu `input_vault`.
- `limit_order.total_amount += amount`; `tick.orders_amount += amount`.

## `DecreaseLimitOrder`

Reduziert oder storniert vollständig eine offene Order. Zahlt den unerfüllten Rest zurück zum Besitzer, plus jede bereits durch frühere Teilfüllungen abgewickelte Ausgabe.

**Argumente**

```
amount:     u64    // Input-Token-Betrag zum Abheben (max = unerfüllter Rest)
amount_min: u64    // Slippage-Untergrenze bei der Input-seitigen Abhebung
```

**Accounts** – sowohl Input- als auch Output-Token-Seiten:

| # | Name | W | S |
|---|------|---|---|
| 1 | `owner` |   | S |
| 2 | `pool_state` | W |   |
| 3 | `tick_array` | W |   |
| 4 | `limit_order` | W |   |
| 5 | `input_token_account` | W |   |
| 6 | `output_token_account` | W |   |
| 7 | `input_vault` | W |   |
| 8 | `output_vault` | W |   |
| 9 | `input_vault_mint`, `output_vault_mint` |   |   |
| 10 | `token_program`, `token_program_2022` |   |   |

**Effekt**

- Berechnet den gefüllten Betrag der Order aus der Kohorte `unfilled_ratio_x64` seit Öffnung neu.
- Sendet gefüllte Ausgabe zu `output_token_account`.
- Sendet `amount` der unerfüllten Eingabe zurück zu `input_token_account`.
- Aktualisiert `limit_order` entsprechend. Wenn der neue unerfüllte Rest Null ist, schließt das Programm das Konto und erstattet Miete an `owner`.

## `SettleLimitOrder`

Schiebt gefüllte Ausgabe-Token zum Besitzer, ohne den unerfüllten Rest der Order zu ändern. Nützlich, wenn `auto_withdraw`-Keeper lange laufende Teilfüllungen tropfenweise auszahlen möchten.

**Aufrufer** – entweder der `owner` der Order oder der `limit_order_admin` des Programms (ein Off-Chain-Operationshot-Wallet, das eine automatisierte Keeper-Schleife ausführt). Der Keeper hat keine andere Autorität – er kann keine Benutzer-Gelder außerhalb des Pushens gefüllter Ausgabe zum Order-Owner-ATA verschieben.

**Accounts**

| # | Name | W | S |
|---|------|---|---|
| 1 | `signer` |   | S | Owner *oder* `limit_order_admin` |
| 2 | `pool_state` |   |   | |
| 3 | `tick_array` |   |   | |
| 4 | `limit_order` | W |   | |
| 5 | `output_token_account` | W |   | Owner's Output-ATA. |
| 6 | `output_vault` | W |   | Pool-Output-Vault. |
| 7 | `output_vault_mint` |   |   | |
| 8 | `output_token_program` |   |   | |

**Effekt**

- Berechnet die kumulierte fällige Ausgabe unter Verwendung von `(limit_order.unfilled_ratio_x64, tick.unfilled_ratio_x64)`.
- Überträgt das Delta zu `output_token_account`.
- Aktualisiert `limit_order.settled_output`.
- Schließt die Order **nicht**; sie ist immer noch gegen jede verbleibende Eingabe offen.

## `CloseLimitOrder`

Schließt ein vollständig verbrauchtes Order-Konto. Miete wird immer an `limit_order.owner` zurückgegeben, unabhängig davon, wer signiert.

**Aufrufer** – entweder `owner` oder `limit_order_admin`.

**Vorbedingungen**

- Die Order hat einen unerfüllten Rest von Null (entweder `amount == total_amount` wurde gefüllt und abgewickelt, oder der Besitzer hat die Order zuvor auf Null verringert und vergessen zu schließen).

**Effekt**

- Schließt `limit_order`; Miete wird an `limit_order.owner` gesendet.

## `CreateDynamicFeeConfig` (Admin)

Erstellt einen wiederverwendbaren Parametersatz unter einem u16-Index.

**Argumente**

```
index:                       u16
filter_period:               u16   // Sekunden; z. B. 30
decay_period:                u16   // Sekunden; z. B. 600. Muss > filter_period sein
reduction_factor:            u16   // 1..10_000; z. B. 5_000 = 50% Beibehaltung pro Decay-Fenster
dynamic_fee_control:         u32   // 1..100_000; Verstärkung auf der Volatilitäts-zu-Gebühren-Kurve
max_volatility_accumulator:  u32   // Obergrenze
```

**Accounts**

| # | Name | W | S | Hinweise |
|---|------|---|---|---------|
| 1 | `owner` | W | S | Hardcodierter Admin-Public-Key. |
| 2 | `dynamic_fee_config` | W |   | PDA, `init` hier. |
| 3 | `system_program` |   |   | |

**Häufige Fehler** – `InvalidDynamicFeeConfigParams`, wenn `decay_period <= filter_period` oder ein Feld mit Wert Null außerhalb der Grenzen liegt.

## `UpdateDynamicFeeConfig` (Admin)

Ändert eine bestehende `DynamicFeeConfig`. Pools, die die Konfiguration bereits bei der Erstellung erfasst haben, werden **nicht** rückwirkend aktualisiert; nur neu erstellte Pools, die auf diese Konfiguration verweisen, werden die neuen Werte aufgreifen.

**Argumente** – gleich fünf Kalibrierungsfelder wie `CreateDynamicFeeConfig` (`filter_period`, `decay_period`, `reduction_factor`, `dynamic_fee_control`, `max_volatility_accumulator`); `index` ist bei der Erstellung festgelegt und wird hier nicht erneut übergeben.

## `CollectProtocolFee` / `CollectFundFee`

Identische Form zu CPMM's `CollectProtocolFee` / `CollectFundFee`. Unterzeichner muss `AmmConfig.owner` / `AmmConfig.fund_owner` entsprechen. Sammelt aufgelaufene Protokoll-/Fondsgebühren aus den Pool-Vaults zu einem Empfänger, setzt die entsprechenden `PoolState.protocol_fees_*` / `fund_fees_*`-Felder auf Null.

## `InitializeReward`

Fügt einen neuen Reward-Stream zu einem Pool hinzu. Bis zu 3 Streams können gleichzeitig aktiv sein.

**Argumente**

```
open_time:     u64
end_time:      u64
emissions_per_second_x64: u128   // Q64.64
```

**Accounts**

| # | Name | W | S |
|---|------|---|---|
| 1 | `reward_funder` | W | S |
| 2 | `funder_token_account` | W |   |
| 3 | `amm_config` |   |   |
| 4 | `pool_state` | W |   |
| 5 | `operation_state` |   |   | CLMM-Operationszustands-PDA, die Reward-Erstellung gatet. |
| 6 | `reward_token_mint` |   |   |
| 7 | `reward_token_vault` | W |   | `init` hier. |
| 8 | `reward_token_program` |   |   |
| 9 | `system_program`, `rent` |   |   |

**Vorbedingungen**

- Weniger als 3 Streams sind derzeit auf dem Pool aktiv.
- Funder zahlt `total_emission = emissions_per_second × (end_time − open_time)` Wert des Reward-Tokens in den Vault als Teil dieser Anweisung ein.
- Whitelisted-Reward-Mint pro `operation_state`.

## `SetRewardParams`

Erweitert, füllt auf oder ändert die Emissionsrate auf einem bestehenden Reward-Stream. Typischerweise aufgerufen von einem Pool-Ersteller oder dem Raydium-Multisig. Einschränkungen sind On-Chain: Sie können normalerweise `end_time` erweitern oder Emissionen erhöhen, nicht rückwirkend verringern. Überprüfen Sie die Owner-Liste von `operation_state`.

## `UpdateRewardInfos`

Reine Buchhaltung – setzt `reward_growth_global_x64` auf die aktuelle Zeit, indem `emissions_per_second × Δt / liquidity` multipliziert wird. Wird intern von jeder Liquiditäts-berührenden Anweisung aufgerufen. Wird als eigenständige Anweisung bereitgestellt, da externe Akteure (UIs, Cranks) sie manchmal auslösen möchten.

## `CollectReward`

Position-Besitzer fordert fällige Reward-Token an.

**Accounts**

| # | Name | W | S |
|---|------|---|---|
| 1 | `nft_owner` |   | S |
| 2 | `nft_account` |   |   | Owner's ATA, die die Positions-NFT hält. |
| 3 | `personal_position` | W |   |
| 4 | `pool_state` | W |   |
| 5 | `protocol_position` |   |   |   |
| 6 | `reward_token_vault` | W |   |
| 7 | `recipient_token_account` | W |   |
| 8 | `token_program` |   |   |
| 9 | `token_program_2022` |   |   |

**Effekt**

- Setzt Reward-Wachstum ab (gleiche Muster wie Gebühren).
- Überträgt den fälligen Betrag zur Empfänger-ATA, setzt `reward_amount_owed[i]` auf Null.

## Zustandsänderungsmatrix

| Anweisung | `pool.liquidity` | `pool.fee_growth_global` | `pool.reward_growth_global` | `personal_position.liquidity` | Tick-Array |
|-----------|------------------|---------------------------|------------------------------|--------------------------------|------------|
| `CreatePool` | 0 | 0 | — | — | — |
| `OpenPosition` | + wenn im Bereich | — | — | neu | füge liquidity_gross/net hinzu |
| `IncreaseLiquidity` | + wenn im Bereich | setze fällig ab | setze fällig ab | + | passe an |
| `DecreaseLiquidity` | − wenn im Bereich | setze fällig ab | setze fällig ab | − | passe an |
| `ClosePosition` | — | — | — | zerstört | — |
| `SwapV2` | ± bei Kreuzungen | + | — | — | kreuze & flip außerhalb; match Limit-Order-Kohorten |
| `OpenLimitOrder` | — | — | — | — | `orders_amount += amount` auf dem Ziel-Tick |
| `IncreaseLimitOrder` | — | — | — | — | `orders_amount += amount` |
| `DecreaseLimitOrder` | — | — | — | — | `orders_amount -=`, kann Kohorte schließen |
| `SettleLimitOrder` | — | — | — | — | — (schreibgeschützt auf Tick) |
| `CloseLimitOrder` | — | — | — | — | — |
| `CreateCustomizablePool` | 0 | 0 | — | — | — |
| `UpdateRewardInfos` | — | — | + | — | — |
| `CollectReward` | — | — | setze fällig ab | — | — |

## Nächste Schritte

- [`products/clmm/code-demos`](/de/products/clmm/code-demos) – ausführbare TypeScript-Beispiele.
- [`products/clmm/fees`](/de/products/clmm/fees) – Details zu Gebühren- und Reward-Abgrenzung.
- [`reference/error-codes`](/de/reference/error-codes) – vollständige CLMM-Anchor-Fehlertabelle.

Quellen:

- [`raydium-io/raydium-clmm` — `programs/amm/src/instructions`](https://github.com/raydium-io/raydium-clmm)
- Raydium SDK v2 — `@raydium-io/raydium-sdk-v2`
